#hidden
# aci:v1 id=f3c6d32 src=agent-context/session-summary.md
@kv sha256=a1daf0ff2470a1540852ec2584f5b4a0e35b03ff5cf7c0779ccc5c49b6be274e bytes=97175 lines=1529 title=세션-요약-PR-Search-구현-2026-08-25-후반
@sig agent-context/session-summary.md;refs/tags/;git/refs;10/11;CR/DEV;11/11;1/8;2/8;3/8;acme/b;acme/a;900/900;66/67;apps/web;4/4;7/7;4/8;25/200;close/reopen;Docker/WSL;5/8;agent-context/_handoff/context-index.md;W-001/W-008;6/8
@h1 세션 요약 — PR Search 구현 (2026-08-25 후반)
@h2 2026-09-11 — CR-077 M 넘버 도입 (문서 캐스케이드, 코드 변경 없음)
@h3 이 세션의 목표
@p "PR이 머지되는 순간 수집되는 웹훅 이벤트에 M 넘버를 붙일 수 있나"라는 질문에서 시작해, P4 회고 회의가 결정한 M 넘버를 SRS 체계에 반영하고 다음 착수 WP로 승격하는 것으로 끝났다.
@h3 결과
@path main = 2cd7c1c (PR #164 머지). 문서 21개, 495줄 추가, 39줄 삭제. 코드 변경 0건.
@path CR-077 등록 후 종료(closed). srs_final.md는 v2.22.
@path 신규 ID: FR-SEQ-008(채번)·FR-SEQ-009(PR 제목 표기), ADR-007 Clarification·ADR-022,
@path JOB-SEQ-004·JOB-SEQ-005, EVT-SEQ-004, API-SEQ-007, THR-046·THR-047,
@path 마이그레이션 025, WP-074·WP-075, DEV-576, OD-009(같은 판에서 닫힘).
@path 다음 착수는 WP-074다. 선행 WP(WP-021·WP-020·WP-008)가 전부 done이고 오픈 결정이 차단하지 않는다.
@h3 이 세션이 뒤집은 판단 — 다시 논쟁하지 마라
@path 처음에는 M 넘버를 만들지 말라고 권고했다. ADR-007이 이미 그 방식(머지 시각 정렬 / 수신 시점
@p 카운터)을 후보로 올렸다가 기각했고 merge_seq가 그 자리를 채우고 있기 때문이다. 그 뒤 사용자가 P4 회고 회의 결정을 가져오면서 권고를 접고 구현 계획으로 전환했다. 조직이 내린 결정이므로 다음 에이전트는 "merge_seq가 이미 있는데 왜 M 넘버를 만드나"를 다시 꺼내지 마라.
@p 다만 설계는 회의 문구를 그대로 옮기지 않았다. 회의는 "merged_at 순서"라고 했으나 M 넘버를 merge_seq 파생으로 정의했다 — 자세한 이유는 decisions.md 최상단.
@h3 작업 방식
@p 핵심 6종(변경 관리·SRS·ADR·데이터 모델·잡/이벤트·작업 패키지)은 직접 썼고, 파생 문서는 4갈래로 병렬 위임했다(요구사항 / 파생 UI / API·보안 / 딜리버리). 파일 소유를 겹치지 않게 배분해 충돌을 막았다. 위임 결과에서 오류 3건을 잡아 정정했다.
@h3 같은 체크아웃을 쓰던 다른 세션
@p pr-search-23 세션이 사내 pilot.3 웹 500 결함을 조사하며 한때 같은 디렉터리에서 브랜치를
@path 바꿨다가 워크트리로 나갔다. 그쪽은 CR-078·DEV-577 이후를 쓴다. 머지 사실과 main이
@p 앞섰다는 것을 전달했다.
@h2 2026-09-08 (2차) — 0.1.0-pilot.3 발행
@h3 이 세션의 목표
@p 결정자의 말로 "갱신된 커밋들을 기준으로 새로 업데이트된 피쳐를 내부망에 필요한 릴리즈 준비를 진행해줘"였다. 범위가 "빌드까지만" → "병합과 재빌드" → "이어서 발행해라" 순으로 확장됐다.
@h3 핵심 발견 넷
@b 발행을 마지막에 둔 것이 이 라운드의 값이다. immutable releases가 켜져 버전 이름이 한 번만 쓰이므로 결정자가 범위를 끊었고, 그 사이에 도착한 머지 후 리뷰가 P1을 잡았다. 바로 발행했다면 결함을 담은 번들이 잠긴 채 사내에 갔다.
@path 주석은 강제하지 않는다. 앞 라운드에서 DEV-553(첫 반입의 store_failed)을 고치며 처방을 .env.example의 주석으로 두었는데, require_env가 그 값을 세지 않아 새 설치는 빈 채로 모든 관문을 통과한다. 게이트의 존재가 아니라 위치가 대가를 정한다 — 이 저장소가 세 번째로 겪었다(DEV-524 → DEV-544 → DEV-556).
@path 내 시험이 틀린 계약을 굳히고 있었다. CR-066에서 쓴 시험이 같은 세션의 DEV-556 때문에 거짓이 됐다. 느슨하게 하지 않고 새 계약으로 갱신했다.
@b 발행한 뒤에도 P1이 둘 더 나왔다. 지금 잠긴 릴리스 안에 있고 미해결이다.
@h3 완료한 것
@path CR-068(DEV-555) — 파생 토큰 문서가 스스로와 어긋난 것. 근거만 현행화하고 결론은 남겼다
@path CR-069(DEV-556~558) — 파티션 수명 주체를 강제 경로로 · 절 참조 정정 · 제품 CSS 검사를 재귀로
@b 0.1.0-pilot.3 발행 — 태그 → 139e818, immutable: true. 값은 GitHub API로 재독해 원장 6.72.7에 적었다
@b 원장 6.72장 계열(후보 감사·번들 생성·리뷰 라운드·발행 사실), 7장 제한 하나 해소
@h3 반복해서 나타난 패턴 — 고침이 다음 구멍을 연다
@path DEV-553을 고치며 DEV-556을 만들었고, DEV-555를 고치며 DEV-557·DEV-558을 만들었다. 셋 다 머지 후 리뷰가 잡았다. 이 저장소가 반복해 기록해 온 형태이며, 고침 자체를 다시 감사하지 않으면 보이지 않는다.
@h3 검증
@risk 단위 1919 · 통합 1476 · 회귀 377 · a11y 358 · 대비 232쌍 · e2e 168통과 1실패(DEV-377, 단독 3회 통과로 귀속) · typecheck·lint 통과. 문서 검증기는 매 커밋 새 issue 0건. 변이 12종 중 1 ... 세 번 만들어 매번 사내 절차로 검사했고, ADMIN_DATABASE_URL을 비운 채 load가 거부되는 것을 실물로 확인했다.
@h3 현재 상태 한 줄
@p 발행은 됐으나 그 산출물에 미처리 P1이 둘 있다 — immutable이라 고칠 수 없으므로 작성 규칙 전달과 0.1.0-pilot.4 중 하나를 골라야 하고, 토큰 발급 전이라 지금이 가장 싸다.
@h2 2026-09-08 — 첫 사내 반입의 결과를 저장소에 반영
@h3 이 세션의 목표
@p 결정자가 2026-09-07에 실제로 사내망에 반입해 서비스를 올렸고, 그 보고를 저장소에 반영하는 것이 이 세션의 일이다. 반입 자체는 이 세션이 하지 않았다.
@h3 핵심 발견 넷
@b 인계 pack이 하루 뒤처져 있었다. sha256은 소스와 일치했으나 pack 생성 커밋(fcc7018)과 HEAD(268efa9) 사이에 세션이 하나 더 있었다. pack이 "다음 빈 ID"라고 예고한 번호가 전부 쓰인 뒤였다. pack이 신선한 것과 소스가 최신인 것은 다른 문제다.
@b 외부 검증이 전부 초록인 채로 결함 여섯이 사내에 갔다. 게이트가 느슨했던 것이 아니다 — 런북 7장이 그 자리들을 정직하게 NOT RUN으로 두고 있었고, 실행이 그 표를 채우자 여섯이 한꺼번에 나왔다. 실행하지 않으면 드러나지 않는 자리가 있다는 것이 이 라운드의 요지다.
@todo 개발 트리와 배포 트리는 다른 것을 한다. pnpm build가 초록이고 next start가 200이어도 pnpm deploy --prod 트리에서는 모든 SSR이 500이었다. 이 저장소에 그것을 재는 시험이 없었다.
@b 보고된 원인이 틀릴 수 있다. 결정자가 pg-native를 지목했으나 실제로는 pg-cloudflare였다(실측). 증상과 해법은 같지만, 원인 모듈이 틀린 문서는 다음 사람을 엉뚱한 곳으로 보낸다.
@h3 완료한 것
@path CR-066 등록, DEV-548~DEV-553 여섯을 5장에 등록
@b 코드 셋을 upstream에서 고쳤다 — 빈 문자열 fallback(config.ts), Turbopack 외부 모듈 실체화(Dockerfile + 신규 스크립트), 웹훅 경로(런북)
@b 문서 셋을 고쳤다 — 인바운드 차단 절 신설, anchor 얕은 병합 함정, ADMIN_DATABASE_URL의 실제 대가
@b 원장 6.70장이 반입 실행 기록의 정본. 7장 제한 넷, 8장 반입 사실, 인프라 문서 11장 갱신
@b 회귀 일곱 신설 — 코드에서 값을 읽어 문서와 대조하는 형태
@h3 반복해서 나타난 패턴 — 재기 전에는 고쳤다고 말할 수 없다
@path DEV-551에 설정 넷을 시도했고 넷 다 클린 재빌드에서 해시 참조가 남았다. 판정은 배포 트리를 실제로 기동해서 했다. 2026-09-03 2차의 태그 lease와 정확히 같은 형태의 교훈이 다른 도구에서 다시 나왔다.
@p 시험도 두 번 결함을 찾아 줬다 — resolveMirrorConfig의 공백 처리 구멍, 그리고 첫 변이 라운드에서 살아남은 둘(등가 변이가 아니라 시험 구멍이었다).
@h3 검증
@risk 단위 1919 · 통합 1476 · 회귀 371 · a11y 358 · contrast 232쌍 · e2e 157 · typecheck·lint·lint:deps 통과. 문서 검증기는 이전 커밋 대비 새 issue 0건. 변이 열넷 중 열셋이 잡혔고 하나는 등가였다.
@h3 현재 상태 한 줄
@p 반입은 성립했고 그것이 드러낸 여섯을 저장소가 흡수했다. 다만 아직 커밋하지 않았고, 반입 성공과 릴리스 게이트 통과는 다른 판정이다 — Gate 4·5·6은 그대로다.
@h2 2026-09-03 (2차) — 리뷰 부채 종결 라운드
@h3 이 세션의 목표
@p 새 기능이 아니다. 직전 라운드가 "완료"로 보고했으나 실제로는 닫히지 않은 계약을 전부 제거하고, 양쪽 저장소가 unresolved review 0이라는 증거를 가진 상태에서만 다음 단계로 넘어간다.
@h3 핵심 발견
@path 지시받은 해법이 통하지 않는 것을 먼저 실측한 것이 이 라운드에서 가장 값을 했다. 태그 소유권을 git push --force-with-lease=refs/tags/<v>:(빈 기대값)으로 증명하라는 지시를 로컬 bare 원격으로 재현하니, 원격이 이미 같은 커밋이면 git이 보낼 것이 없다고 판단해 lease를 검사하지 않고 Everything up-to-date로 통과했다 — 문제 삼는 경쟁에서 정확히 무력하다. GitHub의 POST /git/refs가 422를 내는 성질로 바꿔 원자성을 얻었다.
@p 게이트의 위치가 실패의 대가를 정한다. 릴리스 태그 검사가 changeset publish 뒤에 있어, 게이트가 막은 시점에 npm은 이미 두 패키지를 받은 뒤였다. 발행은 됐는데 태그가 없는 상태가 남았고, 그것을 손으로 복구해야 했다.
@p 한 정정이 다음 구멍을 연다. 게이트를 "manifest 동일성"으로 고쳤더니 리뷰가 그 판정의 사각을 정확히 짚었다 — 버전이 오른 뒤 npm에 없는 채로 소스가 움직이면 두 커밋이 같은 버전을 선언한다. 발행 전 레지스트리 스냅숏으로 다시 고쳤다.
@h3 완료한 것
@p | 저장소 | PR | 내용 | | --- | --- | --- |
@path | design-system | #20 | CR-037 — 0.3.0 공개 계약을 사후 승인 범위로 (FR-CMP-004·005 AC-6 신설) |
@path | design-system | #21 | DEV-035~038 — RTL 미터·서랍, 조작 대상 34px, aria-sort="other" |
@path | design-system | #22 | version PR — css·react 0.3.1 |
@path | design-system | #23 | DEV-039 — baseline 승인 근거와 validator 기록 |
@path | design-system | #24 | DEV-040 — 게이트가 정상 patch 릴리스를 막았다 |
@path | design-system | #25 | CR-038·DEV-041 — 소유를 발행 전 스냅숏으로 |
@path | pr-search | #129 | DEV-541 — 되돌리기가 남의 태그를 지우지 않는다 |
@path | pr-search | #130 | CR-064·DEV-542·543 — 0.3.1 반영, 모션 스펙 정정, 원장 정리 |
@h3 반복해서 나타난 패턴
@p 머지 후 리뷰가 이 라운드에서만 여섯 번 더 왔고 다섯이 실결함이었다. 고치면 그 고침에 대한 리뷰가 온다 — 그것이 이 저장소의 정상 리듬이다.
@h3 현재 상태 한 줄
@p 양쪽 미해결 리뷰 0, 작업 트리 깨끗, 0.3.1 발행·반영 완료. 남은 것은 사내 반입이다.
@h2 2026-09-03 — 애니메이션·컴포넌트 폴리시 라운드
@h3 이 세션의 목표
@p improve-animations 스킬로 웹 프로젝트의 모션을 감사하고, 발견을 계획서로 만들어 격리 실행·검토를 거쳐 고치고, 추가 기회 탐색과 컴포넌트 폴리시까지 이어 간다. 사용자가 제시한 일곱 단계 절차를 따랐다.
@h3 핵심 발견
@p 감사가 찾은 것 중 셋은 "느리다"가 아니라 "한 번도 동작한 적이 없다"였다. 스피너는 animation 단축 선언에 이징이 둘 들어가 무효였고, 축소 모드 라벨 노출은 레이어 순서에 막혀 있었으며, 앱의 축소 모션은 레이어 밖 import 한 줄에 무력화돼 있었다. 셋 다 시험이 없어 통과하고 있었다.
@p 검토가 실결함을 반복해서 잡았다. Block 판정 둘이 모두 옳았고, 그중 하나는 내가 같은 세션에서 만든 규칙이 내 다른 수정을 무력화한 것이었다. 작성과 검토를 분리한 것이 값을 했다.
@p 계산값 실측이 코드 읽기를 이겼다. 라벨을 드러내자 스피너 원이 무너진 것, 배지 안 SHA의 대비가 AA 미달인 것, ::after 글리프가 접근성 이름에 들어가는 것은 전부 브라우저로 재서 알았다.
@h3 완료한 것
@path pr-search 셋(DEV-538~540)과 Conductor 일곱(DEV-028~034)을 고쳐 PR 여섯으로 병합하고 0.3.0을 발행해 앱에 반영했다. 새 회귀 시험 서른 남짓을 함께 세웠다.
@h3 반복해서 나타난 패턴
@p 한 결함을 고치면 다음 결함이 드러난다. 규칙이 적용된 적이 없으면 그 결과도 검증된 적이 없다. "적용되게 했다"와 "적용된 결과가 옳다"는 다른 주장이다.
@h3 현재 상태 한 줄
@p 양쪽 main이 온전하고 0.3.0이 발행·반영됐다. 남은 것은 Conductor PR들의 머지 후 리뷰 8건이다.
@h2 이 세션의 목표
@path REL-003의 잔여 WP를 끝낸다. 사용자 지시는 세 번에 걸쳐 왔다.
@b 이전 에이전트의 handoff context로 작업 준비만 (구현 금지)
@path WP-027 머지 후 리뷰 정정(CR-032) → WP-028(CR-033) 수직 진행
@path WP-028 완료 판정을 믿지 말고 재실측 → CR-034 → WP-068
@h2 이 세션의 핵심 발견
@p "함수도 있고 라우트도 있고 시험도 초록인데 운영 프로세스가 그것을 부르지 않는다."
@path WP-028은 그 상태로 원장에 done이 기록돼 있었다. 배포된 search-api에는
@path API-ADM-007이 아예 없었고, 재채번 잡을 집는 러너도, 조정 스캔을 띄우는 곳도
@p 없었다. 격리된 함수 시험이 전부 통과한 채로 그랬다.
@p 그래서 이 세션의 실질적 산출물은 수정 목록이 아니라 운영 도달성(runtime reachability) 회귀 계층이다 — 선언 → 기동 → 종료 → 배포 manifest가 한 줄로 이어지는지만 묻는다.
@h2 완료한 것 (전부 main 병합)
@p | CR | 내용 | PR | 병합 | | --- | --- | --- | --- |
@path | CR-032 | WP-027 머지 후 리뷰 정정 (DEV-168~170) | #34 | 1d0dee2 |
@path | CR-033 | WP-028 정합성 점검·조정 스캔 (DEV-171~176) — SRS v2.5 | #35 | 4b954d1 |
@path | CR-034 | WP-028 운영 배선 정정 (DEV-177~184) | #37 | a620899 |
@path | CR-035 | WP-068 팀 접근 범위 (DEV-185~187, DEV-114 해소) | #39 | b55b275 |
@path | CR-036 | WP-068 접근 범위 유출 정정 (DEV-188~190) | #40 | 5e18e00 |
@path 참조 기입 PR #36(ea2663d)·#38(3ba6b9d)도 병합했다.
@b 커밋 15개(+머지 7), 71개 파일 +6,716 / -151
@b 마이그레이션 009·010·011 신설, 배포 manifest 2종 신설
@b 결함 재적용 총 28종 전부 킬
@h2 반복해서 나타난 패턴 — 머지 후 리뷰
@p 이 세션은 머지된 뒤에 Codex 리뷰가 도착하는 일을 다섯 번 겪었다.
@path PR #33 → 3건 (CR-032로 처리)
@path PR #35 → 5건 (CR-034로 처리)
@path PR #39 → 3건 (CR-036으로 처리)
@path PR #36·#37·#38 → 9건 (미해결, 다음 세션 몫)
@p 리뷰가 머지 1분 뒤에 도착하기도 했다. 머지 직후 반드시 다시 확인해야 한다.
@h2 현재 상태 한 줄
@path main = 5e18e00, SRS baseline v2.5, 원장 review v1.8, CR-036·DEV-190까지.
@path REL-003 10/11 (WP-067만 남음). 미해결 리뷰 9건이 두 WP의 done을 조건부로
@p 만든다.
@h2 이전 세션 요약 (보존)
@path WP-026 W-005 릴리스 화면(PR #32, 7c7b180)과 WP-027 선행·후행(PR #33,
@path bc0e931)을 구현했다. CR-030·CR-031 등록·종결, DEV-155~167 해소, SRS v2.4로
@path FR-REL-001을 직접 정정(직접 푸시 커밋 포함). 시작 시점에 직전 웹 세션이 CR-030
@p 캐스케이드를 커밋하지 않고 끝나 유실된 것을 발견해 감사를 재수행했다.
@h2 2026-08-26 인계 세션 (코드 변경 없음)
@p /clear 직후 시작해 새 코딩 작업이 없다. 기존 컨텍스트를 저장소 실제 상태와
@path 대조해 여섯 항목을 재확인했고(main SHA · SRS · 원장 · CR/DEV 최댓값 · 미해결 리뷰
@p 9건 · 문서 모순), 틀린 사실 두 건을 고쳤다 — 전사 위치(exports/ 안이며 .gitignore 대상), 그리고 agent-context/ 변경분이 미커밋이라는 사실.
@path 다음 작업은 바뀌지 않았다 — todos.md 0번(CR-037로 미해결 리뷰 9건 정정)이다.
@p ---
@h1 세션 요약 — REL-003 마감 (2026-08-26, 대형)
@h2 이 세션의 목표
@p 사용자가 3단계 수직 지시서를 줬다. 추가 승인을 요청하지 말 것, 기술 결정은 승인된 것으로 간주.
@p | Phase | 목표 | 결과 | | --- | --- | --- |
@path | A | CR-037로 WP-028·WP-068 머지 후 correctness를 완전히 닫는다 | 머지 7060414 (PR #41) |
@todo | B | WP-067 계약을 next-free CR로 감사·정정하고 구현한다 | CR-038 / 머지 afdf1c2 (PR #42) |
@path | C | REL-003을 11/11로 닫고 REL-004 진입 가능 여부를 판정한다 | 머지 3e3009b (PR #43) |
@h2 시작 실측 — 지시서가 맞았다
@p 이전 세션 컨텍스트는 미해결 리뷰를 9건으로 적고 있었으나 실제는 10건이었다.
@path PR #40의 신규 P1(팀 접근 범위 경주)을 놓치고 있었다. GraphQL 실측으로 확인:
@path #37=7 · #40=1 · #36=1 · #38=1.
@p 교훈: 인계 문서의 숫자는 인계 시점에 다시 센다. 컨텍스트 파일이 "확인했다"고
@path 적어도 그 확인이 모든 PR을 훑었다는 뜻은 아니다 — 그때 나는 #36·#37·#38만 셌다.
@h2 처리한 결함 총 27종
@p | CR | 내용 | DEV | PR | | --- | --- | --- | --- |
@path | CR-037 | WP-028·068 머지 후 정정 | 191~204 (14) | #41 → 7060414 |
@path | CR-038 | WP-067 계약 경화 + 구현 | 205~214 (10) + PR 리뷰 5 | #42 → afdf1c2 |
@path | — | REL-003 마감 판정 | — | #43 → 3e3009b |
@path 리뷰가 지적한 것은 10 + 3(PR #41) + 5(PR #42) + 2(PR #43) = 20건.
@path 스스로 찾은 것이 7건(DEV-200의 22행 실체, DEV-201, changedPaths 실패 삼킴 등).
@h2 이 세션의 가장 큰 발견 셋
@h3 계약이 방아쇠를 잘못 지목하고 있었다 (DEV-206)
@path WP-067 계약은 EVT-ING-003을 방아쇠로 적었다. 그런데 그 이벤트는 커밋 문서
@p 투영의 결과로 나온다 — 문서가 없는 직접 푸시 커밋에는 애초에 발생하지 않는다. 그것만 구독하면 "이미 문서가 있는 커밋만 보강한다"가 되어 고치려던 문제를 정확히 비껴간다. sequence.assigned의 from_seq..to_seq가 새로 채번된 first-parent 구간이고 merge_sequence가 그 SHA를 갖고 있다.
@h3 "실패했는데 아무도 다시 하지 않는다" (PR #42 리뷰 5건 중 4건)
@p 정본을 색인보다 먼저 쓰면 색인 실패 시 스냅숏만 남는데, 스윕이 "스냅숏 없는 커밋"만 찾으면 영원히 재시도되지 않는다. readCommit이 null을 돌려주면 폴백이 돌지 않는다. manifest가 README 적용 순서에 없으면 운영자가 끝내 만들지 않는다. 전부 같은 모양이다.
@h3 WP 완료와 릴리스 게이트 통과는 다른 판정이다
@path 지시서 §43은 "WP-067이 머지되면 REL-003을 11/11로 닫는다"였다. WP는 실제로
@path 11/11이다. 그러나 검증 계획 §4의 필수 게이트 여섯(2·3·4·5·6·7) 중 통과는
@p 둘뿐이다 — Gate 4(권한 78셀·위협 모델·시크릿 스캔), Gate 5(성능 7종), Gate 6(런북·롤백 실측·대시보드·알림)이 미통과다. 둘을 나눠 적었다.
@h2 현재 상태 한 줄
@path main = 3e3009b, SRS baseline v2.5, 원장 review v2.2, CR-038·DEV-214까지.
@path REL-003 WP 11/11 · 릴리스 게이트 미통과. 미해결 리뷰 0건. open DEV 1건(DEV-010, 기존).
@p ---
@h1 세션 요약 — CR-039 / WP-029 참조 간선 (2026-08-26 수직)
@h2 이 세션의 목표
@path REL-004의 첫 WP를 감사부터 머지까지 한 번에. 사용자가 추가 승인 요청을 금지했다.
@h2 이 세션의 핵심 발견
@p 계약이 "만든다"만 말하고 "다시 만든다"를 말하지 않았다.
@p 착수 전 감사가 찾은 열다섯 중 다섯이 그 하나의 공백에서 나왔다. 참조는 본문에서 파생되는데 본문은 수정되고, 대상은 나중에 색인되며, 과거 데이터는 이벤트를 남기지 않았고, 실패한 것은 누군가 다시 해야 한다.
@path 가장 값이 컸던 것은 DEV-217이다. 문서에 적힌 link_id 재료가 FR-REL-003
@path AC-3("대상 색인 시 해결 상태로 갱신한다")을 구조적으로 불가능하게 만들고
@p 있었다 — 축약 SHA가 해결되면 대상이 바뀌고 ID가 함께 바뀌어 갱신이 아니라 새 문서가 된다. reference_key로 정체성을 대상에서 떼어 그 결정을 성립시켰다.
@h2 완료한 것
@p | 단계 | 결과 | | --- | --- |
@path | 재실측 | PR #1~#44 전수 — PR #30의 P1이 미해결로 남아 있었다 (이전 인계가 #33 이상만 셌다) |
@p | housekeeping | agent-context 커밋, 병합된 로컬 브랜치 4개 정리, 틀린 사실 2건 정정 |
@path | CR-039 | DEV-215~229. SRS v2.5 유지, ADR-009 개정, 새 ADR·새 마이그레이션 없음 |
@path | 구현 | 파서·간선 계층·link 역할·EVT-ING-005·JOB-REL-006·manifest·README |
@p | 검증 | 단위 1320 · 통합 849 · 회귀 104 · a11y 192 · contrast 80 · e2e 67 | | 변이 | 29종 + 리뷰 정정 8종 킬, 등가 2종은 킬로 세지 않았다 | | 리뷰 | 머지 전에 6건(P1 셋) 도착, 전부 실결함 — 정정 후 머지 |
@h2 반복해서 나타난 패턴
@p "실패했는데 아무도 다시 하지 않는다"가 이 세션에만 네 번 나왔다.
@path release.ts의 무한 재시도(DEV-228), ready 신호 발행 실패의 영구 유실(리뷰 P1),
@p 해결의 부분 실패를 성공으로 셈(리뷰 P2), 한 페이지만 읽어 나머지를 영영 남김(리뷰 P2).
@path CR-038이 PR #42 리뷰에서 배운 것과 같은 모양이다. 다음 WP에서 먼저 물을 것.
@h2 현재 상태 한 줄
@path main = ea917b8, SRS baseline v2.5, 원장 review v2.3, CR-039·DEV-229까지.
@path REL-004 구현 1/8. 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@p 미해결 리뷰 0건. open DEV 5건(전부 기존).
@p ---
@h1 세션 요약 — CR-040 OD-005 · CR-041 / WP-030 (2026-08-26 3차)
@h2 이 세션의 목표
@p 사용자가 3단계 수직 지시서를 줬다. 추가 승인 요청 금지, 기술 결정은 승인된 것으로 간주.
@path Phase A(OD-005 종결) → Phase B(WP-030 감사~머지) → WP-031 preflight audit에서 정지.
@h2 이 세션의 핵심 발견
@p 계약이 source 본문만 보고 "다른 엔티티의 상태가 바뀐다"를 보지 않았다.
@path WP-029가 참조 축에서 만난 *"만든다만 있고 다시 만든다가 없다"*의 한 겹 아래 형태다.
@p 참조는 source 본문만 보면 답이 나오지만, 되돌림 제목 대조·체리픽 patch-id·스택은 다른 엔티티의 현재 상태가 답을 바꾼다 — 그리고 그때 source에는 아무 이벤트도 오지 않는다.
@p | 시나리오 | source 이벤트 | 그대로 두면 | | --- | --- | --- | | Revert "X" 해결 뒤 같은 제목 PR이 하나 더 | 없다 | 간선이 하나로 남는다 | | patch_id=P 커밋이 나중에 도착 | 없다 | 체리픽 간선이 안 생긴다 | | 상위 PR 머지 | 없다 | 스택 의존이 active로 남는다 |
@p 감사 열여덟 중 열둘이 이 하나의 뿌리에서 나왔다.
@h2 완료한 것
@p | 단계 | 결과 | | --- | --- |
@path | Phase A | CR-040 — OD-005 종결, SRS v2.5 → v2.6, 9문서 캐스케이드. PR #45 → 0a501f3 |
@p | 감사 | 열여덟 (contract gap 13 · implementation gap 4 · stale document 1) |
@path | CR-041 | DEV-230~247. SRS v2.6 유지, 새 ADR 없음, 마이그레이션 014 신설 |
@p | 구현 | 파서 둘·결정론 ID·간선 계층·후보 변화 재평가·지표 2종·범위 요약 이월 종결 | | 검증 | 단위 1342 · 통합 900 · 회귀 124 · a11y 192 · contrast 80쌍 | | 변이 | 1차 18종 + 리뷰 정정 7종. 살아남은 둘이 전부 실결함이었다 |
@path | 리뷰 | PR #45 셋 · PR #46 여섯 — 아홉 전부 실결함, 정정 후 머지 |
@h2 반복해서 나타난 패턴
@p "내가 한쪽만 보고 있었다"가 이 세션의 모양이다.
@b is_reverted는 대상의 필드인데 source만 갱신했다 → reverted_pull_request_count가 구조적으로 틀린 수를 냈다
@b 깊이 상한이 추적 범위인데 간선의 조건으로 썼다 → 깊은 스택의 PR이 부모와의 의존을 잃었다
@b has_stack은 PR 전용 leaf인데 커밋에도 썼다 → dynamic: strict가 잡았다
@p 그리고 주석이 코드와 정반대를 적고 있는 자리가 하나 있었다 — links_pending을 "보존한다"고 써 놓고 매번 덮고 있었다. 리뷰가 잡았다.
@h2 도구가 결함을 찾아 준 자리 셋
@b 변이 M13 — 살아남아 파고들었더니 깊이 상한이 정당한 간선을 지우고 있었다
@b 변이 R7 — 살아남았고, 그것은 "그 경로를 거는 시험이 없다"는 뜻이었다
@b ES dynamic: strict — 커밋 문서의 has_stack을 거부했다. 매핑이 규율을 집행한다
@h2 현재 상태 한 줄
@path main = c4f8a39, SRS baseline v2.6, 원장 review v2.6, CR-041·DEV-247까지, 마이그레이션 014까지.
@path REL-004 구현 2/8. 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건(PR #1~#46). open DEV 5건(전부 기존).
@p ---
@h1 세션 요약 — CR-042 / WP-031 관계 조회·상세 화면 (2026-08-26 4차)
@h2 이 세션의 목표
@p 사용자가 74절 지시서를 줬다. 추가 승인 요청 금지, 기술 결정은 승인된 것으로 간주.
@path 재실측 → baseline 증거 보존 → WP-031 감사 → CR-042 → 구현 → 검증 → PR → 리뷰 정정 →
@path merge → WP-031 done · REL-004 3/8 → WP-032 preflight audit에서 정지.
@h2 이 세션의 핵심 발견
@p 파생은 섰는데 그것을 읽을 경로가 없었다. 그리고 읽는 순간 접근 통제의 축이 바뀐다.
@path 감사 열여덟 중 아홉이 같은 뿌리다 — 계약이 *파생*의 규칙만 정하고 조회가 다른 질문이라는 것을 보지 않았다. 파생에는 요청자가 없어 저장소 안에서 끝나지만, 조회에는 요청자가 있고 대상 쪽을 본다. WP-029가 참조 축에서, WP-030이 후보 축에서 만난 것의 세 번째 형태다.
@path 가장 값이 컸던 것은 DEV-250이다. findLinksTo가 repository_id로 라우팅하는데, 되돌림·체리픽·스택은 동일 저장소 관계라 워커에서는 그것이 옳았고 결함이 드러나지 않았다. 그러나 참조는 저장소를 건너뛰고 간선은 근거를 소유한 저장소에 산다 — acme/b의 PR을 가리키는 참조를 b로 라우팅해 찾으면 acme/a의 간선을 어떤 값으로도 찾을 수 없다. 워커의 조회 형태를 그대로 HTTP에 재사용했다면 cross-repo 역방향이 조용히 침묵했을 것이다.
@h2 완료한 것
@p | 단계 | 결과 | | --- | --- |
@path | 재실측 | PR #1~#46 전수 0건. baseline 통합 900/900·e2e 66/67(실패 시험 이름 보존) |
@p | 감사 | 열여덟 (계약 공백 9 · 문서 오류 5 · UI 범위 공백 2 · 구현 공백 2) |
@path | CR-042 | DEV-248~265. SRS v2.6 유지, 새 ADR 없음, 새 마이그레이션 없음, 12문서 캐스케이드 |
@path | 구현 | API-REL-006 신설 · API-REL-003 구현 · 읽기 seam · C-021·C-015 · 지연 섹션 둘 · 가드레일 확장 |
@p | 검증 | 단위 1380 · 통합 931 · 회귀 146 · a11y 212 · contrast 80쌍 · e2e flow-006 11건 | | 변이 | 22종 + 리뷰 정정 1종 전부 킬. 등가로 세지 않은 변이 없음 |
@path | 리뷰 | PR #47 P1 하나 — 실결함, 정정 후 머지 |
@h2 반복해서 나타난 패턴
@p "선례를 따르지 않은 새 파일이 검사에서 빠진다"가 이 세션의 모양이다.
@b relations-read.ts가 assertNoShardFailures를 빠뜨렸다 — 다른 조회 경로 여섯이 전부 지나는 검사다. 리뷰가 잡았다
@path 같은 파일이 ADR-008 가드레일의 파일 단위 면제를 물려받을 뻔했다 — links.ts 안에 뒀다면 검사기가 침묵했다
@b 가드레일 자신도 get·mget을 보지 않고 있었다 — 대상 내용을 mget으로 붙이는 구현이 그대로 통과했을 자리다
@p 셋 다 "이 경로가 다른 경로와 같은 검사를 지나는가"를 묻지 않아 생겼다.
@h2 도구가 결함·구멍을 찾아 준 자리 넷
@b 변이 M5(신뢰도를 언제나 exact로) — 살아남았고 등가가 아니었다. 순서와 다중 후보 판정은 *원본* 간선의 값을 쓰므로 응답의 confidence를 아무도 단언하지 않고 있었다
@path 픽스처 충돌 — 상한 시험용 간선 101건을 A#10에서 냈더니 "A#10의 참조" 질의에 섞여 대상 간선을 밀어냈다
@b 대역이 서버보다 관대했다 — 고정 link_type을 돌려줘 네 그룹이 전부 references로 그려졌다
@b 회귀가 주석까지 셌다 — "이 필드를 쓰지 않는다"를 파일 전문에 걸었더니 그 사실을 설명한 주석에 걸렸다
@h2 e2e 귀속 — 되돌려서 쟀다
@path 이번 WP는 apps/web을 실제로 바꿨으므로 이전 세션들의 "화면 코드 diff 0" 근거를 쓸 수 없었다.
@p | 상태 | 실행 | 결과 | | --- | --- | --- | | 변경 전 | 1회 | flow-001:127 실패 | | 변경을 stash (화면 = main) | 3회 | 1회 실패 — 같은 시험·단언·오류 | | 변경 적용 | 8회 | 4회 실패 |
@path | flow-001 단독 | 4회 | 4/4 통과 |
@path | flow-006 | 7회 | 7/7 통과 |
@p 기존 취약성이며 이번 변경이 만든 것이 아니다. 실패한 둘 다 뒤로가기 복원을 5초 안에 확인하는 형태이고 관계 코드에 닿지 않는다. flow-003도 같은 계열이라는 것이 새 정보다.
@h2 현재 상태 한 줄
@path main = 5d287b1, SRS baseline v2.6(변경 없음), 원장 review v2.8, CR-042·DEV-265까지, 마이그레이션 014까지.
@path REL-004 구현 3/8. 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건(PR #1~#47). open DEV 5건(전부 기존).
@p ---
@h1 세션 요약 — CR-043~047 계약 경화 5연발 (2026-08-26 5차)
@h2 이 세션의 목표
@path 사용자가 2단계 지시서를 줬다. Phase A는 WP-032 착수 전 감사 → CR-043 → 머지, Phase B는 WP-035 착수 전 감사 → CR → 구현 → 머지 → REL-004 4/8.
@path Phase A는 끝냈고 Phase B는 감사와 계약·배포 자리까지만 했다. WP-035의 본체는 구현하지 않았다 — REL-004는 여전히 3/8이다.
@h2 이 세션의 핵심 발견 둘
@h3 계약이 "이 매핑을 어떻게 배포하는가"를 묻지 않았다 (CR-043)
@path WP-032는 edge_ngram 부분 일치 필드를 요구한다(CR-040이 nori 대신 고른 경로다). 계약은 그 필드가 무엇을 해야 하는지를 정했고 살아 있는 인덱스에 어떻게 들어가는지는 정하지 않았다. 실제 Elasticsearch 8로 재어 보니 벽이 둘이고 서로 독립이다.
@p | # | 벽 | 실측 | | --- | --- | --- | | 1 | edge_ngram은 index.analysis의 비동적 설정 | 열린 인덱스에 PUT _settings → illegal_argument_exception: Can't update ... title.partial 추가(acknowledged: true) 직후 조회 0건 → _update_by_query 뒤 1건 |
@path 두 번째가 더 무겁다. 첫 번째는 배포가 터지므로 반드시 발견되지만, 두 번째는 배포가 성공하고 전문 검색이 과거 데이터에 조용히 적게 답한다. → WP-032의 선행에 WP-035를 넣고 REL-004 실행 순서를 029 → 030 → 031 → 035 → 032로 정정했다.
@h3 승인된 배포 단위 하나가 저장소에 없었다 (CR-045) — 이 세션의 가장 큰 발견
@path WP-035를 감사하러 들어가 재색인보다 먼저 걸린 것이 있었다. JOB-ING-006의 워커는 batch 역할인데 그 역할을 세우는 manifest가 없었다. 인프라 3장은 pipeline-worker:batch를 배포 단위로 승인했고 index.ts에도 갈래가 있었다 — 없는 것은 파일 하나였다.
@path 그리고 그 파일이 없어서 이미 구현된 JOB-ING-007(아웃박스 재적재)이 지금 돌지 않는다.
@p 역할↔기동 사슬을 전수하니 코드가 갈래를 만드는 열 중 넷이 배포되지 않고 있었다.
@p | 역할 | 기동 | 상태 | | --- | --- | --- |
@path | batch | startOutboxRelay (JOB-ING-007) | 고쳤다 — manifest 신설 |
@path | enrich>backfill | startBackfillRunner (JOB-ING-004) | DEV-304 open |
@path | release | startReleaseWorker·startReleaseSweeper (JOB-REL-007) | DEV-305 open |
@path | authz | startAuthzWorker (JOB-AUTH-001) | DEV-306 open — 접근 통제 축 |
@h2 완료한 것
@p | CR | 내용 | PR | 병합 | | --- | --- | --- | --- |
@path | CR-043 | WP-032 계약 경화 (DEV-266~285) — SRS v2.7 | #48 | 3248559 |
@path | CR-044 | PR #48 리뷰 다섯 정정 (DEV-286~291) — SRS v2.8 | #49 | 60402d8 (CI 없이) |
@path | CR-045 | WP-035 계약 경화 + batch 배포 (DEV-292~307) | #50 | f785115 |
@path | CR-046 | PR #50 리뷰 넷 정정 (DEV-308~311) | #51 | 99e2532 |
@path | CR-047 | PR #51 리뷰 하나 정정 (DEV-312) | #51 | 같은 PR |
@path 결국 CI 없이 병합한 것은 PR #49 하나뿐이다 — #50·#51은 러너 큐가 풀려 정상 게이트로 통과했다.
@h2 이 세션이 재현한 결함 (DEV-270)
@p 읽어서 의심하고 실제 PostgreSQL + Elasticsearch로 임시 통합 시험을 써서 재현했다.
@p | 조건 | 결과 | | --- | --- | | 구간 1~60, size=10, 일치가 서수 51 하나뿐인 q | summary.pull_request_count: 1 · items: [] · next_cursor: null | | 같은 질의, size=60 | 서수 51이 나온다 |
@p runRange가 정본 페이지를 먼저 자르고 그 안에서만 q를 판정하는데 요약은 구간 전체를 센다. 절삭이 원인이고 커서가 없어 도달할 수 없다.
@h2 반복해서 나타난 패턴 — 내가 쓴 것을 내가 지키지 않았다
@p 리뷰 라운드 넷이 전부 같은 모양이었다. 이것이 이 세션의 가장 값진 소득이다.
@p | 라운드 | 내가 적은 것 | 내가 한 것 | DEV | | --- | --- | --- | --- |
@path | PR #48 | 정렬 키 여덟의 성질 | updated_at을 불변으로 셌다 | 286 |
@path | PR #48 | DEV-270("실은 것과 진행한 것이 어긋난다")을 재현까지 함 | 고치는 계약에 같은 계열을 새로 만들었다 | 287 |
@path | PR #49 | baseline SRS AC-7 | 하위 문서만 고쳤다 | 291 |
@path | PR #50 | 막아야 할 경주 둘을 표로 | 울타리를 첫 번째만 덮게 | 308 |
@path | PR #50 | "인프라 표를 정본으로 삼는다" | 구현은 코드만 읽었다 | 310 |
@path | PR #51 | "배포되지 않는 단위를 표에 적지 않는다" | 예외를 두 방향에 다 걸어 검사를 삼켰다 | 312 |
@p 적는 것과 지키는 것은 다른 일이고, 그 차이를 잡는 것은 시험뿐이었다. 그래서 정정의 핵심도 매번 시험이었다 — 회귀 146 → 151.
@h2 감사에서 탈락한 후보 (가짜 발견을 남기지 않으려 기록한다)
@path 인계 문서가 "커서의 재료·동률 처리가 어디에도 없다"고 적었으나 ADR-010이 이미 정하고 있었고 동률은 sort.ts의 doc_id로 구현까지 되어 있었다. clampSize(25/200 절삭)·TRACK_TOTAL_HITS(10,000)·query_too_short(파서)·text_ko_en(분석기)도 전부 이미 있었다.
@p → 실측하지 않은 공백 주장은 그 자체가 공백이다. 없는 것을 만들려다 이미 있는 것을 두 번 만들 뻔했다.
@h2 환경 사건 둘 (코드와 무관)
@path GitHub Actions 러너가 76분간 배정되지 않았다. 연속 startup_failure, check-run 0개. private 저장소 + self-hosted 러너 0개. 재트리거(push·close/reopen·rerun) 전부 실패. 그 사이 사용자가 CI 없이 병합을 승인했고, PR #50을 여는 순간 큐가 풀렸다.
@path 백킹 컨테이너 4종이 동시에 exit 255로 죽었다. Docker/WSL 수준 사건. docker start로 복구, 이후 전부 통과.
@h2 현재 상태 한 줄
@path main = 99e2532, SRS baseline v2.8, 원장 review v3.4, 작업 패키지 v1.1, CR-047·DEV-312까지, 마이그레이션 014까지.
@path REL-004 구현 3/8 (WP-035 본체 미구현). 릴리스 게이트 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건(PR #1~#51 전수). open DEV 8건 — 기존 5 + 신규 3(DEV-304·305·306 미배포 역할).
@p ---
@h1 세션 요약 — WP-035 무중단 재색인 구현 · CR-048 authz 배포 (2026-08-27)
@h2 이 세션의 목표
@p 사용자가 55절 지시서를 줬다. 핵심은 검증 정책의 변경이었다.
@p "배터리 게이팅은 최소한으로 진행하고 진척률을 우선한다. 사내 내부망으로 옮긴 후 다시 자체 점검할 것이 많으므로 중요한 것만 빠르게 확인하며 진행"
@p | Phase | 목표 | 결과 | | --- | --- | --- |
@path | A | WP-035 본체 구현 → 검증 → PR → 리뷰 정정 → merge | PR #52 → efea3c0 |
@path | B | DEV-306 authz 배포 도달성 (별도 수직) | PR #53 → f4e8ab6 |
@path | — | (발견) 문서 상태 헤더 갱신 누락 | PR #54 → df9f569 |
@h2 이 세션의 핵심 발견 셋
@h3 재구축을 운영 쓰기 경로로 통과시키면 계약이 구조로 지켜진다
@path WP-035의 가장 큰 설계 위험은 "정본 재구축용 문서 빌더"가 평시 투영과 갈라지는 것이었다(DEV-295가 금지한 것). shadow 전용 쓰기 함수를 만들지 않고 재구축이 withReindexWrite를 그대로 지나게 하니, serving은 같은 document_version이라 조건부 스크립트가 noop으로 끝내고 실제로 채워지는 것은 shadow뿐이다. 두 번째 경로가 생길 자리가 없다.
@h3 스냅숏은 정본이 아니라 투영 시점의 사본이다
@p 리뷰 P1 둘이 같은 뿌리였다. pull_request_snapshot.document를 "정본"으로 읽었는데,
@path repository_archived·allowed_team_ids는 그 뒤 색인에만 소급 반영된다 → 재구축이 스냅숏을 그대로 쓰면 전환이 회수된 팀의 접근을 되살린다 (DEV-314)
@path commit_snapshot은 merge_sequence 기준이라 first-parent 체인만 덮는다 → PR 원본 커밋 문서가 통째로 빠진다 (DEV-315)
@path 버전의 정본은 본문이 아니라 열이다 → 본문에 그 키가 없는 행 하나가 저장소 전체의 재구축을 막는다 (DEV-320, CI가 잡았다)
@p 셋 다 "이 축의 정본이 정확히 무엇인가"를 다시 묻지 않아 생겼다.
@h3 판정의 재료가 그 판정이 뜻하는 것과 다르면 검사가 거짓말을 한다
@path 처리한 *source* 수는 문서 수가 아니다 → 간선 재색인이 언제나 거절된다 (DEV-316)
@path 지우지 못한 것은 처리한 것이 아니다 → 롤백 중인 인덱스가 영영 회수되지 않는다 (DEV-318)
@path 기록하지 못한 전환은 일어나지 않은 전환이 아니다 → 옛 인덱스가 보관 대상에 오르지 않는다 (DEV-319)
@h2 완료한 것
@p | 대상 | 내용 | PR | 병합 | | --- | --- | --- | --- |
@path | WP-035 | 버전 인덱스 · 이중 쓰기 17경로 · 울타리 · 정본 재구축 4종 · JOB-ING-006 · API-ADM-004 · CLI · 보관 | #52 | efea3c0 |
@path | CR-048 | authz 역할 배포 (DEV-306 resolved) | #53 | f4e8ab6 |
@path | — | 문서 상태 헤더 정정 (DEV-323) | #54 | df9f569 |
@b 커밋 9개(+머지 3), 57파일 +5,056 / −528
@b 새 마이그레이션·워커 역할·consumer group·DB 표 없음. SRS baseline v2.8 유지
@path DEV-313~323 등록, CR-048 하나만 열었다
@h2 검증 — targeted gate만 통과했다
@p | 계층 | 결과 | 증감 | | --- | --- | --- | | typecheck · lint · lint:deps · build | 통과 | | | test | 1393 (1 skipped) | 1380 → +13 | | test:integration | 952 (63 파일) | 931 → +21 | | test:integration reindex | 19 | 신규 |
@risk | test:regression | 194 | 151 → +43 |
@p | 문서 검증기 --strict | main 대비 신규 0 | |
@path 돌리지 않은 것: test:a11y · test:contrast · test:e2e — 이번 작업이 apps/web을 한 파일도 바꾸지 않았다.
@p 지시서의 기본 생략과 다르게 돌린 것: 통합 전량. 17개 공유 쓰기 원시체의 시그니처를 바꿨고 그 경로들의 실제 커버리지가 통합 계층에 있다 — 여기서 생략하면 바꾼 것의 대부분이 검증되지 않는다.
@h2 변이 — 14종 전부 킬
@p | 묶음 | 수 | 결과 | | --- | --- | --- | | 지시서가 정한 핵심 (M1~M6) | 6 | 전부 KILLED |
@path | PR #52 리뷰 정정 재적용 (R1a~R4) | 5 | 전부 KILLED |
@p | 버전 열 (C1) | 1 | KILLED |
@path | CR-048 배포 도달성 (A-M1~A-M5) | 5 | 전부 KILLED |
@p 무중단 시험(T9)은 별칭 전환을 두 호출로 쪼개고 150ms 창을 열어 실제로 그 창을 잡는 것까지 확인했다 — 통과하기만 하는 시험이 아니다.
@h2 반복해서 나타난 패턴 — 리뷰가 일곱을 지적했고 일곱 전부 실결함이었다
@p | PR | 지적 | P1 | 실결함 | | --- | --- | --- | --- |
@path | #52 | 5 | 2 | 5 |
@path | #53 | 2 | 0 | 2 |
@p 접근 통제 유출 하나(회수된 팀 부활)와 데이터 완전성 하나(PR 원본 커밋 누락)가 P1이었다. 직접 만든 시험 19건이 전부 초록인 상태에서 리뷰가 그 둘을 잡았다.
@h2 현재 상태 한 줄
@path main = df9f569, SRS baseline v2.8, 원장 review v3.7, 작업 패키지 v1.2, CR-048·DEV-323까지, 마이그레이션 014까지.
@path REL-004 구현 4/8 (WP-035 done). WP-032 차단 해제. 릴리스 게이트 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건. open DEV 7건 — 기존 5 + DEV-304·305 (DEV-306은 resolved).
@p ---
@h1 세션 요약 — PR Search (2026-08-27 2차, WP-032)
@h2 이 세션의 목표
@p 사용자가 준 44절 지시서. Codex 호출 금지(quota 소진), 추가 승인 없이 수직 완주.
@path pre-WP032 위생 정정 → WP-032 전체 → 검증 → PR → 리뷰 정정 → merge → 기록.
@h2 이 세션의 핵심 발견
@p "현재 상태"를 적어 둔 산문이 다섯 자리에서 낡아 있었다. CLAUDE.md가 "documentation only, no code"라고 말하는 동안 저장소에는 400개가 넘는 소스가 있었다. 최신 숫자로 갈아 끼우지 않고 스냅숏 자체를 없앴다 — 갈아 끼우면 다음 WP에서 같은 결함이 난다. 리뷰가 그 정정조차 반쪽임을 잡았다(진입점 둘이 더 있었다).
@p 시험이 무엇을 보고 있는지 세 번 틀렸다. 변이가 살아남을 때마다 경로를 읽었고, 다섯 중 셋이 등가가 아니라 시험 구멍이었다. PIT을 재는 시험이 PIT을 보고 있지 않았고, 부트스트랩 시험이 운영의 출발점을 만들지 않고 있었다.
@p 문서가 코드보다 앞서 있던 자리가 있었다. resolveTeamIds의 주석은 "전부 돌려준다"고
@path 약속하고 구현은 하나만 남겼으며, 그 주석 자신이 "WP-032의 패싯이 다룬다"는 이월을 적어
@p 두었다. 리뷰가 그 만기를 짚었다.
@h2 완료한 것 (전부 main 병합)
@p | PR | 무엇 | 머지 | | --- | --- | --- |
@path | #55 | agent-context 보존 + DEV-324 스냅숏 제거 | f1c28f9 |
@path | #56 | DEV-325·326 — 진입점 둘과 원장 1장 | 437be3d |
@path | #57 | WP-032 패싯·커서·전문 검색 + DEV-327~332 | 3237b6c |
@h2 반복해서 나타난 패턴 — 실물로 재기
@p 이 세션이 "아마 될 것"으로 넘어간 자리가 없다.
@b 매핑을 실 ES 8에 실제로 만들어 보고, 한글 부분 일치를 실제로 질의했다
@path missing 센티널의 format 셋을 다 재서 셋 다 실패함을 확인했다 (DEV-329)
@path applyMappings가 옛 별칭에서 실제로 터지는 것을 보고 방어를 세웠다 (DEV-328)
@b PIT pit_id가 이 버전에서 바뀌지 않는다는 것을 재고, 그래서 대역으로 걸었다
@h2 현재 상태 한 줄
@path main 2d28074 · REL-004 5/8 (WP-029·030·031·035·032 done) · SRS baseline v2.8 ·
@p 원장 v4.1 · 마이그레이션 014 · open DEV 7 · 열린 PR 0 · 미해결 리뷰 0 ·
@path main CI 양쪽 통과 · 다음은 WP-033 저장된 검색 ·
@path REL-003 릴리스 게이트는 여전히 미통과(베타 공개 승인 안 됨).
@h2 다음 에이전트가 먼저 할 것
@path agent-context/_handoff/context-index.md를 읽고 read_order를 따른다
@todo 실측부터 — git rev-parse HEAD · next-free ID · open DEV 계수. 인계 문서의 숫자를
@p 그대로 옮기지 않는다 (risks 22)
@path WP-033의 계약이 닫힌 적이 없다는 점을 확인하고 착수 전 감사 여부를 정한다
@b 배포 전제 둘(SEARCH_CURSOR_HMAC_KEY · es:reindex)이 아직 남아 있다 — todos.md B절
@p ---
@h1 세션 요약 — CR-049 · WP-033 (2026-08-27 3차)
@h2 이 세션의 목표
@path WP-033 착수 전 감사에서 멈추지 않고 계약 종결부터 구현 완주까지 한 번에 간다.
@p 사용자가 제품 결정을 미리 승인했으므로 중간에 묻지 않고 진행했다.
@h2 이 세션의 핵심 발견
@p 승인된 기능이 "누구에게 공유하는가"를 말한 적이 없었다. team 공개 범위는 SRS v2.2부터 승인돼 있었고 saved_search.team_id 열도 마이그레이션 004부터 있었다.
@path 그런데 AC-2의 문장은 "저장자와 같은 팀 구성원"이었고, 이 제품의 사용자는 여러 팀에
@p 속할 수 있다. 열이 있다는 것과 그 열이 무엇을 뜻하는지 정해져 있다는 것은 다른 사실이다. 그 하나의 빈칸이 소유권·이탈 후 처분·스키마 불변식 셋을 함께 비워 두고 있었다.
@h2 완료한 것 (전부 main 병합)
@p | PR | 무엇 | 결과 | | --- | --- | --- |
@path | #59 | CR-049 계약 종결 | SRS baseline v2.8 → v2.9. DEV-333~345 |
@path | #60 | WP-033 구현 | 마이그레이션 015 · API-SRCH-005 · W-001/W-008. DEV-346~349 |
@path REL-004 5/8 → 6/8. 남은 것은 WP-034·WP-036.
@h2 반복해서 나타난 패턴 — 내가 적은 것을 내 구현이 따르지 않는다
@p 일곱 개의 리뷰 지적 중 셋이 그 모양이었다.
@p | 자리 | 내가 적은 것 | 내가 한 것 | | --- | --- | --- |
@path | DEV-343 | 캐스케이드 순서가 매트릭스를 포함한다 | 화면 매핑이 안 바뀌었다고 보고 건너뛰었다 |
@path | DEV-344 | /run에 TOCTOU 방어를 적었다 | 쓰기 경로에는 그 규율을 적용하지 않았다 |
@path | PR #60 P1 | W-008-EDIT가 편집 UI를 정한다 | "편집은 W-001에서" 결정으로 덮었는데 그 화면에 PATCH가 없다 |
@path → 결정을 적는 것과 그 결정이 성립하는 것은 다른 일이다. CR-046·risks 47이 배운
@p 것과 같은 자리이며, 이번에는 계약을 대체하는 결정에서 나왔다.
@h2 두 번 틀린 시험 하나
@p M4(소유자 행 잠금 제거)가 살아남았다. 등가로 세지 않고 실측하니 101건이 됐다 — 시험 구멍이었다. 그런데 그것을 막으려 세운 첫 시험도 변이를 놓쳤다: 외래 키가 잡는 FOR KEY SHARE가 내 FOR UPDATE와 충돌해, 잠금 없는 구현도 함께 멈췄기 때문이다. 시험이 잠금이 아니라 외래 키의 부작용을 재고 있었다.
@p → 초록이 사실을 뜻하려면 무엇을 재고 있는지 확인해야 한다.
@h2 현재 상태 한 줄
@path main 700d825 · SRS baseline v2.9 · REL-004 6/8 · 마이그레이션 015 ·
@todo open DEV 8건 · 미해결 리뷰 0건 · Next = WP-034.
@p ---
@h1 세션 요약 — CR-050 계약 종결 + WP-034 W-009 저장소 개요 (2026-08-28)
@h2 이 세션의 목표
@path 사용자가 준 42절 지시서. "이번 세션은 WP-034 감사만 하고 멈추는 세션이 아니다 — 수직으로 끝낸다."
@path 재실측 → 개발 DB 정합 → 착수 전 감사 → CR-050 → 캐스케이드 → 문서 PR 병합 → fresh main에서 구현 →
@path 마이그레이션 016 → API 둘 → W-009 → 진단 경로 → 이월 정정 둘 → 적대적 검증 → PR/리뷰/병합 →
@todo 원장 기록 → Next = DEV-349.
@path 제품 결정은 지시서가 미리 승인했다 — W-009는 general-user 유지, API-ADM-001/006 authz 완화 기각,
@path 신규 general-user API 승인, 등록 요청 PostgreSQL SoR 승인, 마이그레이션 016 승인, DEV-349는 이번에 섞지 않음.
@p Codex 호출 금지.
@h2 이 세션의 핵심 발견
@p "화면의 권한 축과 API의 권한 축이 어긋나 있는데 아무 문서도 그 모순을 적지 않았다."
@path W-009는 P1 일반 사용자 화면으로 IA에 등재돼 있었고 WP-034의 목표 문장도 "사용자가 스스로
@path 확인한다"였다. 그런데 그 화면이 읽을 것으로 지목된 API-ADM-001·API-ADM-006이 둘 다 operator
@path 전용이었고, 한쪽은 그 제한이 요구사항 본문에 승인돼 있었다(FR-ADMIN-001 AC-4).
@p 각자는 옳았고 둘을 나란히 읽는 사람만 그것을 본다. 이것이 이 감사가 찾은 종류의 공백이다.
@h2 완료한 것 (전부 main 병합)
@p | CR | 내용 | PR | 병합 | | --- | --- | --- | --- |
@path | CR-050 | WP-034 착수 전 감사 — 계약 종결 (DEV-350~356, SRS v2.10) | #61 | 1f7ccf2 |
@path | — | WP-034 구현 (DEV-357·358 리뷰 정정 포함) | #62 | ea08c57 |
@p 인계 기록 c17ed8c(agent-context 직접 커밋 — 관행).
@b 커밋 7개(+머지 2), 신규 파일 14개
@b 마이그레이션 016 신설 (등록 요청 표 + repository 열 둘)
@path 신규 계약 셋: API-ING-002·API-ING-003·ENT-CORE-008
@b 적대적 변이 15종 15킬
@h2 감사가 찾은 일곱
@p | DEV | 유형 | 무엇 | | --- | --- | --- | | 350 | 범위 공백 | 일반 사용자 화면이 operator 전용 API만 지목 | | 351 | 범위 공백 | 파생 UI 넷이 SRS에 없는 등록 요청을 서술 | | 352 | 범위 ... counter뿐 | | 353 | 구현 결함 | allowed_team_ids를 두 경로가 안 넘김 (stale 주석 포함) |
@path | 354 | 구현 결함 | FR-ADMIN-001 AC-1의 만기 지난 이월 |
@p | 355·356 | 문서 오류 | 추적 매트릭스·와이어프레임의 stale 참조 |
@p 셋(354·355·356)은 역방향 grep으로만 보였다 — 대상 FR 본문만 읽었으면 놓쳤을 자리다.
@h2 리뷰가 잡은 둘
@p 자동 리뷰가 P2 둘을 냈고 둘 다 실결함이었다.
@path DEV-357 — 접근 범위 판정을 애플리케이션에 둔 대가를 계산하지 않았다. 스캔 상한에서
@p 커서를 비워 범위 밖이 몰린 구간 뒤의 저장소가 영영 사라졌다. 실 PostgreSQL로 재현했다
@path DEV-358 — 같은 CR에서 신설한 QA-W009-13을 내 구현이 절반만 따랐다. W-002·W-003을 빠뜨렸는데
@path 그 not_found야말로 W-009로 갈 이유가 가장 큰 자리였다
@h2 이 세션이 겪은 것
@p PC가 재부팅으로 중단됐다. 작업 파일은 전부 살아 있었고 컨테이너만 내려가 있었다 — docker start 셋으로 복구하고 중단 지점(typecheck)부터 이어갔다.
@h2 현재 상태 한 줄
@path main = c17ed8c, SRS baseline v2.10, 원장 review v4.6, CR-050·DEV-358까지, 마이그레이션 016까지.
@path REL-004 7/8 — 남은 것은 WP-036 하나. 다음은 DEV-349 좁은 계약 종결이다.
@p ---
@h1 세션 요약 — CR-051 계약 종결 + DEV-349 구현 (2026-08-28 2차)
@h2 이 세션의 목표
@path 사용자의 75절 지시서: "우선순위는 DEV-349다. WP-036보다 먼저 끝낸다."
@path 감사 → CR-051 → 캐스케이드 → 문서 PR 병합 → fresh main 구현 → 검증 → 리뷰 정정 → 병합까지
@path 수직으로. 제품 결정(CR-051 개설, SRS v2.11, 마이그레이션 017, 신규 ID 없음)은 지시서가 미리 승인했다.
@h2 이 세션의 핵심 발견
@p 답이 없던 것이 아니라 질문이 잘못돼 있었다.
@path DEV-349는 계약의 재료 셋이 없어 열려 있었다.
@p | # | 물음 | 실제 답 | | --- | --- | --- | | 1 | 어느 시퀀스 공간의 에폭인가 | 정본이 없던 게 아니라 seq: 질의가 그 물음에 답할 수 있는 모양이 아니었다 | | 2 | 여럿이면 하나만 낡아도 전체를 무효로 보는가 | 물음이 사라졌다 — 1번이 서면 여럿인 경우가 없다 |
@path | 3 | /search가 epoch_stale을 어떻게 싣는가 | 선례가 있었다 — API-SEQ-001이 CR-027부터 쓰는 형태 |
@p seq: 범위 조건이 부정 아닌 repo: 하나와 base: 하나를 요구하게 하자 첫 물음에 답이 생기고
@path 두 번째가 사라졌다. 이 판단은 저장소가 이미 한 번 내렸다 — API-SEQ-004의 "base_branch는
@path 필수다, 서버가 시퀀스 공간을 고르지 않는다"(CR-032, DEV-168).
@h2 가장 무거운 사실
@path ADR-007 규칙 5가 2026-08-19부터 답을 적어 두었다.
@p 시퀀스를 인용하는 모든 저장물(안전 구간 표식, 저장된 검색의 seq: 조건, 공유 URL)은 에폭을 함께 저장한다.
@p 셋 중 실제로 그렇게 하는 것은 safe_marker 하나뿐이었다. 결정은 승인돼 있었고 구현이 몰랐으며 다섯 개 작업 패키지가 그 위를 지나갔다. 그래서 새 ADR을 만들지 않고 Clarification을 붙였다.
@p risks 77("계약을 대체하는 결정을 적었으면 그 대체가 성립하는지 물어라")의 반대 얼굴이다 — 옳게 적힌 결정이 어디까지 닿는지 아무도 확인하지 않았다.
@h2 완료한 것 (전부 main 병합)
@p | PR | 내용 | 병합 | | --- | --- | --- |
@path | #63 | CR-051 계약 — SRS v2.11, 문서 15개 캐스케이드 | 7a2d0e2 |
@path | #64 | DEV-349 구현 — 마이그레이션 017, 코드 54파일 | 2e6194c |
@b 커밋 4개(+머지 2), 69파일 +4156 / -134
@b 신규 소스 4개: sequence-binding.ts, sequence-context.ts, sequence-reference.ts, 마이그레이션 017
@b 신규 시험 3파일: 통합 2 + e2e 1
@path 에폭을 다섯 곳에 실었다: ES 질의 필터, 패싯 집계, 커서 지문, W-001 URL, saved_search 열
@h2 리뷰 — 두 PR에서 아홉, 전부 실결함
@p 셋이 P1이고 그중 둘은 이 CR이 막으려던 실패를 다른 자리에서 되풀이한 것이었다.
@p | PR | 무엇 | | --- | --- |
@path | #63 (넷) | 구현이 없는데 원장이 resolved라고 적어 스스로와 어긋났다 · unavailable에서 저장된 에폭도 빼야 한다 · 성공 예시에 base:가 없어 AC-7이 거절할 질의를 성공으로 보여 줬다 · /run 예시의 query와 navigation_url이 서로 다른 검색 |
@path | #64 (다섯) | 낡은 상태에서 저장하면 본 적 없는 세대가 저장된다 · 화면이 형식 오류를 삼켜 서버의 엄격함을 무력화 · seq_epoch=(빈 값)이 "지정 안 함"으로 읽혀 DEV-363을 이름만 바꿔 남김 · seq: 저장 검색 편집이 통째로 막힘 · /sequence-ranges?q=seq:…가 500 |
@p 마지막 것은 호출부를 놓친 결과다. buildQuery 호출부를 grep으로 훑었는데 glob 패턴이 한
@path 단계만 매칭해 sequence/range.ts가 빠졌다 — 검색 범위를 잘못 잡으면 "전수 확인했다"가 거짓이 된다.
@h2 적대적 변이 15종 — 13 킬 · 1 등가 · 1 시험 구멍
@p | 변이 | 판정 | | --- | --- | | M1 공간 지목 첫 검사 제거 | 등가 — 아래 타입 가드가 같은 required를 낸다. 다만 required/ambiguous 구분을 잃는다(M2가 죽는다). 사유를 코드 주석에 남겼다 | | M4b 패 ... 고정돼도 통과했다. 두 세대를 다 묻도록 고친 뒤 같은 변이를 다시 걸어 죽는 것을 확인 | | 나머지 13종 | KILLED |
@p M4·M8은 무효 변이였다(같은 값 / 주석만). 킬로 세지 않고 실제로 갈라지는 형태로 다시 걸었다.
@h2 검증
@p 단위 1644(+46) · 통합 74파일 1194(+52) · 회귀 213 · a11y 262(+10) axe 0건 · 대비 80쌍 · e2e 113(+5) · build 통과 · 문서 검증기 main 대비 증감 0. 마이그레이션 017은 실 PostgreSQL로 up→down→up과 CHECK를 실측했다. main CI 양쪽 success.
@h2 현재 상태 한 줄
@path main = 2e6194c, SRS baseline v2.11, 원장 review v4.8, CR-051·DEV-365까지, 마이그레이션 017.
@path REL-004 7/8 (변화 없음). open DEV 8건. 다음은 WP-036 하나이며 착수 전 감사가 필요하다.
@p ---
@h1 세션 요약 — PR Search (2026-08-29, CR-053 · WP-037)
@h2 이 세션의 목표
@path 사용자 지시는 하나였다: REL-004 전환을 닫고 CR-053을 거쳐 WP-037까지 완주한다.
@path handoff 준비 → DEV-364 → ACC-06 → 차트 스파이크 → 감사 → 계약 → 구현 순서가 지시서에 있었다.
@h2 이 세션의 핵심 발견
@path "측정하지 못한 것"과 "미달"은 다르다. ACC-06을 합성 픽스처로 채워 95%를 만들 수 있었고
@path 그러지 않았다. test:perf가 낸 4ms를 NFR-001 통과로 적을 수 있었고 그러지 않았다.
@p 두 판정이 이 세션의 성격을 정했다.
@path 미뤄 둔 판정의 근거는 검증되지 않은 채 오래 남는다. DEV-364는 "언제나 0건"이라는 근거로
@p 닫히지 않은 항목이었는데, 부호를 뒤집어 보니 전체를 돌려주고 있었다.
@h2 완료한 것 (전부 main 병합)
@p | PR | 내용 | 병합 | | --- | --- | --- |
@path | #74 | DEV-364·378·379 정정, ACC-06 NOT RUN, 차트 스파이크(DEV-380), 집계 오류 정정 | 76e9cf0 |
@path | #75 | CR-053 집계 계약 종결 (DEV-381~390, SRS v2.13) | 1e3b8be |
@path | #76 | WP-037 집계 API (DEV-391~395) | d3b2590 |
@path 커밋 5개(+머지 3), 신규 파일 8개. REL-004 8/8 → REL-005 1/4.
@h2 반복해서 나타난 패턴 — 세어 보지 않은 수
@path 이 세션에서 세 번 나왔다: open DEV 집계(굵은 open이 grep에서 빠짐), WP-067 완료
@path 날짜(확인 없이 적음), WP-037의 DoD 항목 수(열둘이라 적었으나 열하나).
@path CR-052가 리뷰 수를 정정하며 배운 것과 같은 뿌리다.
@h2 리뷰가 잡은 것
@p | PR | 지적 | 판정 | | --- | --- | --- |
@path | #74 | 둘 (4장 매핑 모순, 날짜 오류) | 둘 다 실결함 — 내 정정 자체를 되돌렸다 |
@path | #75 | 0건 | — |
@path | #76 | 열 (P1 여덟) | 전부 실결함 — 같은 PR에서 고쳤다 |
@path #76의 다섯이 "숫자가 조용히 틀리는" 자리였다. 집계는 답이 하나의 수라 사용자가 검증할
@p 방법이 없다 — timed_out·부분 샤드·상한까지만 센 수·범위 밖 문서·좁히지 않은 모집단이 모두 그 하나에 섞여 들어가며 어느 것도 오류를 내지 않는다.
@h2 현재 상태 한 줄
@path main = d3b2590, SRS baseline v2.13, 원장 review v5.6, CR-053 · DEV-395까지.
@path REL-005 1/4. 다음은 WP-038이며 착수 전에 DEV-380(계열 색)을 정해야 한다.
@h1 세션 요약 — PR #79 · CR-036 dataviz 배포 · WP-038 (2026-08-29 2차)
@h2 목표
@path PR #79 정정·병합 → DEV-380을 design-system 기여로 해소 → Conductor dataviz 0.2.0 배포 → WP-038 완주. Codex 호출 0.
@h2 완료 (두 저장소, 다섯 PR 전부 병합)
@path PR #79(전사 경로) · design-system #9(CR-036 dataviz) · #10(0.2.0 version) · #11(audit overrides) · pr-search #80(WP-038, b5ae933).
@h2 핵심 발견
@b 색을 대비 목표 명도로 역산해야 6표면 3:1을 보장한다(두 테마 결속 표면이 반대).
@b 토큰 추가는 check:api를 깬다. audit 게이트는 기능과 무관하게 배포를 막는다.
@b 백분위 p 접두 키를 놓치면 전부 —인데 화면은 오류를 안 낸다(Codex가 잡음).
@b 집계 mock이 서버 계약과 키를 안 맞추면 계약 버그를 숨긴다.
@h2 리뷰
@path PR #79 P1 하나, design-system #9 P2 둘, PR #80 다섯(P1 둘·P2 셋) — 전부 실결함, 전부 정정.
@h2 현재 상태 한 줄
@path Conductor 0.2.0 배포·DEV-380 resolved. REL-005 2/4(WP-037·038 done). 다음 WP-039·040. 릴리스 게이트 미통과(Gate 5 성능).
@p ---
@h1 세션 요약 — PR #81 증거 정정 · CR-054 감사 계약 · WP-039 (2026-08-29 3차)
@h2 목표
@path PR #81의 미해결 P2 종결 → CR-054로 감사 계약 정본화 → WP-039 수직 완주. Codex 호출 0.
@h2 완료 (PR 넷 전부 병합)
@path #82(증거 정정 ab13baf) · #83(CR-054 3bef4de) · #84(WP-039 c13acc6) ·
@path #85(종료 상태 6bdf395).
@h2 핵심 발견 넷
@path "0건"이 세어 본 적 없는 수였다. 지시서는 PR #81의 P2 하나를 지목했는데, GraphQL로
@p isResolved를 세니 병합된 PR 여덟에 22건이 남아 있었다. "고쳤다"와 "스레드가 닫혔다"를
@path 같은 것으로 세어 왔다(DEV-414).
@p 계약이 자기 대상을 개수로 말하고 있었다. "13종"·14행·18개 — 셋 중 어느 것도 틀리지 않았고 셋이 서로 다른 것을 세고 있었다. 개수는 액션을 하나 더하는 순간 낡는다.
@p 문서가 가리키는 곳을 열어 보니 잡이 없었다. 런북 한 줄의 잘못된 참조를 확인하러 갔다가
@path 파티션 생성 정기 잡이 아예 없다는 것을 찾았다(DEV-417). 배포 후 세 달이면 감사와
@p 원본의 INSERT가 전부 거부되는 자리였다.
@p 시험이 권한 경계를 지나지 않았다. partition-retention 열셋이 초록인 동안 운영에서는
@path 그 잡이 아무 일도 못 했을 것이다 — 마이그레이션 소유자 풀로 돌았기 때문이다(DEV-421).
@h2 리뷰
@path #82 P2 하나(내가 지적한 결함을 내가 다시 만들었다) · #83 P2 셋 · #84 P1 둘 + P2 하나.
@path 전부 실결함, 전부 정정. #84의 셋을 고치다 DEV-420(귀속하지 못한 전량 실패)을 재현·귀속했다 —
@p 열어 둔 것이 값을 냈다.
@h2 검증
@p 단위 1790 · 통합 1324 · 회귀 267 · a11y 283 · 대비 232 · e2e 131 · 빌드 통과. 변이 M1~M11 중 열 즉시 킬, M7이 시험 구멍을 드러냈고 메운 뒤 KILLED.
@path e2e 전량 3회 중 2회에서 DEV-377 플레이크가 재현됐고 그 사실을 적었다.
@h2 현재 상태 한 줄
@path REL-005 3/4. 다음은 WP-040 하나다. 릴리스 게이트는 그대로이고 Gate 5 성능은 NOT RUN.
@p 미해결 리뷰 22건은 열려 있다.
@p ---
@h1 세션 요약 — CR-055 계약 종결과 WP-040 백엔드 (2026-08-30)
@h2 이 세션의 목표
@path 사용자 지시서: P0 = CR-055 + WP-040. 이전 인계 팩이 "백엔드는 이미 전부 서 있다"고
@p 판정한 것을 실측으로 뒤집고, 계약 공백이 있으면 CR을 먼저 닫은 뒤 수직으로 구현한다. Codex 호출 0.
@h2 완료한 것
@p | 무엇 | 상태 | | --- | --- |
@path | 착수 전 계약 감사 | 결함 아홉 확인 (DEV-428~DEV-436) |
@path | CR-055 계약 종결 | 병합 완료 (PR #88 → main 5475249), SRS v2.15 |
@path | WP-040 백엔드 | 커밋 셋, 로컬 브랜치에만 있음 |
@path | WP-040 화면 | 미완 — 컴포넌트 아홉과 화면 셋이 남았다 |
@h2 이 세션의 핵심 발견 셋
@p 인계 팩의 판정이 틀렸다. 화면이 누르라고 정한 운영 동작 다섯이 정본에서 도달할 곳을 갖고 있지 않았다 — 등록 검토 요청의 처리 결과, 조정 스캔의 수동 실행,
@path FR-ADMIN-002 AC-1 다섯 중 둘의 러너, 브랜치 추가 시 채번, 인덱스 상태 읽기 경로.
@path 배포 경계는 실행 경계가 아니다 (PR #88 리뷰 P2). reconcile 역할이 복제본
@p 1개로 배치되어 있다는 사실을 직렬화 근거로 삼았는데, 그것은 프로세스 수를 제한할 뿐 한 프로세스 안의 독립적인 비동기 루프 둘을 직렬화하지 못한다. 락을 더하는 대신 루프를 하나로 만들었다.
@path 전제가 사라져도 결론이 자동으로 뒤집히지 않는다. CR-034(DEV-180)의 근거
@p 둘 중 하나만 없앴으므로, 수동 실행만 잡 행을 쓰고 자동 복구는 계속 버스 이벤트로 간다.
@h2 반복해서 나타난 패턴 — "개수를 계약에 적지 말라"가 또 나왔다
@path CR-054가 감사 대상에서 내린 판단이 이번에 두 번 더 값을 냈다.
@path WP-040의 DoD가 QA-A001-01 ~ QA-A001-14 같은 범위 열거를 갖고 있었는데, 이 CR이 QA 항목 열하나를 더하면서 그 범위가 그 자리에서 낡았다. 범위를 갱신하는 대신 정본을 지목하는 문장으로 바꿨다.
@b 회귀 시험 둘이 잡 유형 목록의 리터럴 전문을 단언하고 있어 목록이 자라자 깨졌다. 성질 단언으로 바꿨다.
@p 두 자리 모두 "지금 맞는 값"을 적어 두면 그것이 낡는 순간을 아무도 모른다는 같은 문제다.
@h2 검증 (마지막)
@p typecheck · lint · lint:deps 통과. 단위 1850(1790 → +60) · 회귀 282(267 → +15).
@risk 문서 검증기는 main 대비 새 ERROR 0건 · 새 WARN 0건.
@p NOT RUN: test:integration · test:e2e · test:a11y · test:contrast · build. 화면이 서지 않아 그 계층의 검증이 성립하지 않는다 — 통과가 아니라 미실행이다.
@h2 현재 상태 한 줄
@path CR-055는 병합됐고 WP-040의 백엔드와 화면 판정 계층이 로컬 브랜치에 서 있다.
@path 남은 것은 컴포넌트 아홉·화면 셋·통합·e2e·a11y·변이·원장이며, REL-005는 아직 3/4이다.
@p ---
@h1 세션 요약 — WP-040 완주, REL-005 4/4 (2026-08-30 2차)
@h2 이 세션의 목표
@path WP-040 운영 콘솔을 끝내고 병합해 REL-005를 4/4로 마감한다. 지시서가 순서를 명시했다 — 화면 작성이 첫 작업이 아니라 백엔드 수직 증명이 먼저다. Codex 호출 0.
@h2 이 세션의 핵심 발견 셋
@path 순서가 결함 하나를 회수했다. 통합 전량 첫 실행에서 둘이 실패했고, 하나(DEV-437)는 기존 시험이 이미 잡아 두었으나 아무도 돌리지 않은 실결함이었다 — resolveJobTarget의 일반 갈래가 슬러그 형식을 검사하지 않아 오타가 404를 받았다. 같은 함수의 다른 갈래는 그 검사를 갖고 있었다.
@path 살아남은 변이 둘의 성격이 정반대였다. M6은 등가(순차 실행이라 겹침이 없다), M8은 진짜 구멍(FR-ING-009 AC-12를 거는 시험이 없었다). 등가로 착각했다면 AC-12가 시험 없이 남았을 것이고, 구멍으로 착각했다면 없는 문제를 고쳤을 것이다.
@p CI가 코드와 무관하게 죽어 있었다. 계정 결제 문제로 잡이 시작조차 하지 않았다(단계 0개). 로컬 전 계층으로 판정하고 그 사실을 적었다.
@h2 완료한 것 (전부 main 병합)
@p | 커밋 | 무엇 | | --- | --- | | c062305 | 인계 문서 cherry-pick (원본 a677b5b) | | 25e410c | 백엔드 수직 증명 — 통합 62건 | | 1fa7ec6 | 화면 셋 + 컴포넌트 아홉 + a11y 63 + e2e 16 |
@path | 88f978b | DEV-437·DEV-438 해소 + 원장 v6.2 |
@path | 7e462e9 | PR #89 병합 |
@h2 검증
@code lang=text sha=03f72dd3bbdc lines=9 kept=9
|typecheck · lint · lint:deps      통과
|단위        1856 (1 skipped)      [1850 → +6]
|통합        1389 (88 파일)         [1324 → +65]
|회귀        284                   [282 → +2]
|a11y        346 (axe 0)           [283 → +63]
|대비        232/232
|e2e         147 (플레이크 0)        [131 → +16]
|빌드        통과 · 문서 검증기 새 경고 0건
|CI          잡 미시작 (계정 결제) — 판정에 쓰지 못했다
@h2 현재 상태 한 줄
@path main = 7e462e9 · REL-005 4/4 DONE · 열린 PR 0건 · open DEV 14건 · 미해결 리뷰 31건 · 릴리스는 승인되지 않았다(Gate 4·5·6 그대로).
@h2 다음 에이전트가 먼저 할 것
@path DEV-414 미해결 리뷰 31건(pr-search 22 · design-system 9)을 HEAD와 하나씩 대조한다. 그 다음이 REL-006(WP-041 → WP-042 → WP-044)이며 WP-043은 착수하지 않는다.
@p GitHub Actions 결제 문제가 풀리기 전까지 CI는 판정 근거가 되지 못한다. 로컬 배터리가 유일한 판정이며 그 사실을 적고 진행한다.
@p ---
@h1 2026-08-30 (2차 후속) — PR #89 머지 후 리뷰 넷
@path 병합 직전 스레드 0건 → 병합 뒤 넷 도착. 전부 실결함이었고 DEV-439~442로 등재·해소했다.
@p | 등급 | 지적 | 고친 방법 | | --- | --- | --- | | P1 | 취소된 조정 스캔이 계속 돈다 | runReconcileSweep에 취소 신호를 넘긴다. 저장소 사이에서만 본다 — 조정 중간에 끊으면 되돌리기가 부분으로 남는다 | | P ... D가 없다 | CreateJobOutcome.conflict가 jobId를 담는다. 경합에서 진 요청도 이긴 행을 다시 읽는다 |
@todo | P2 | 요청 큐 기본 필터가 pending이 아니다 | 계약대로 기본값을 두고, 이력은 status를 명시해 본다 |
@h2 이 리뷰가 되돌려 물은 것
@path "취소가 완료로 덮이지 않는다"를 다섯 유형 전부에 걸었는데 그것으로 충분하지 않았다. 그 성질은 잡 행의 라벨에 관한 것이었고, 일이 실제로 멈추는가는 묻지 않았다. DEV-436과 DEV-439는 같은 조작의 서로 다른 절반이다.
@p 화면과 서버를 같은 세션에서 만들면 어긋남이 시험에 나타나지 않는다. 화면이 detail.job_id를 읽도록 썼고 서버는 싣지 않았는데, e2e 목이 계약이 아니라 내 가정을 흉내 내 초록이었다. 목은 계약 문서의 필드 이름을 보고 만든다.
@h2 문서 갱신
@b pr_search_api_contracts.md v0.18 — 409의 detail.job_id, PATCH의 유형별 제한
@b pr_search_async_events_jobs.md v0.6 — "중단은 상태 보존이 아니라 실행 정지다"
@path 원장 v6.3 — 6.52.4장, DEV-439~442
@p ---
@h1 세션 종료 (2026-08-30 2차)
@h2 최종 상태
@path main = 1df0191 (PR #89·#90·#91·#92 병합) — 이 인계 커밋이 한 번 더 옮긴다.
@p | 항목 | 값 | | --- | --- |
@path | REL-005 | 4/4 DONE |
@p | 릴리스 | NOT APPROVED — Gate 4·5·6 그대로 | | 열린 PR | 0건 | | 이번 세션 PR의 미해결 스레드 | 0건 |
@path | 전체 미해결 리뷰 | 22건 (pr-search) + 9건 (design-system) — DEV-414 |
@p | open DEV | 14건 | | 문서 | SRS baseline v2.15 · 원장 review v6.3 · API 계약 v0.18 · 비동기 잡 v0.6 | | CI | 판정에 쓰지 못했다 — 계정 결제 문제 |
@h2 산출물
@path 전사: exports/202608302158.md (.gitignore:24가 무시한다 — 실측 확인)
@path worklog: Obsidian dailywork/2026-08-30_PR-Search-WP-040-운영-콘솔-완주와-REL-005-마감.md
@h2 이 세션이 남기는 한 문장
@path 돌리지 않은 계층이 남기는 것은 통과가 아니라 미실행이고, 병합이 남기는 것은 완료가 아니라 재확인해야 할 상태다. 이 세션은 두 규율을 각각 한 번씩 실증했다 — 통합을 먼저 돌려 DEV-437을, 병합 후 재확인으로 리뷰 넷을 회수했다.
@h2 다음
@path DEV-414 미해결 리뷰 31건 대조. 그 다음이 REL-006(WP-041 → WP-042 → WP-044)이며 WP-043은 착수하지 않는다. GitHub Actions 결제 문제가 풀리기 전까지 CI는 판정 근거가 되지 못한다.
@p ---
@h1 세션 요약 — 리뷰 부채 완전 청산 (2026-08-30~31, 3차)
@h2 이 세션의 목표
@path 사용자 지시서: P0 = PR #91 신규 P1/P2 정정, P1 = WP-040 상태 sync + 계수 도구 회귀, P2 = DEV-414 전수 대조·종결. WP-041 구현에는 들어가지 않는다. Codex 호출 0.
@h2 완료한 것 (전부 병합)
@p | PR | 무엇 | 결과 | | --- | --- | --- |
@path | pr-search #94 | PR #91 리뷰 둘 + 그 정정의 리뷰 하나 | DEV-443·444·445·446·447·448 |
@path | pr-search #95 | CR-056 — 분포가 0을 세고 모름을 0이라 말하지 않는다 | DEV-449~457, SRS v2.16 |
@path | design-system #12 | 리뷰 부채 8건 | Select.Root·IconButton·FP-002·단위 규칙·릴리스 태그·버전 커밋 범위 |
@path | pr-search #96 | DEV-414 종결 | 양 저장소 미해결 리뷰 0건 |
@h2 이 세션의 핵심 발견 넷
@path 정정한 자리가 다음 리뷰의 첫 자리다. 세 PR이 모두 자기 정정에 새 지적을 받았고 전부 실결함이었다. 그중 둘은 이 세션의 판단을 정면으로 뒤집었다 — 드릴다운을 갈아 끼우기로 정한 근거(DEV-455)와 모름의 판정 재료(DEV-457)다. PR #96은 문서만 바꿨는데도 산술 오류 둘을 지적받았다.
@path 문장으로 쓴 근거는 코드로 확인하기 전까지 결함 후보다. 같은 세션에서 양방향으로 겪었다. DEV-443에서는 이전 주석의 "부분으로 남는다"를 코드로 확인해 그 근거가 틀렸음을 밝혔고(재개 커서가 없다), DEV-455에서는 내가 쓴 근거가 정확히 반대였다(집계가 이미 교집합을 센다).
@path 개수를 세는 단언이 세 파일에 흩어져 있었다. 지원 키를 "17종"으로 세는 자리가 단위 하나와 통합 둘에 있었고, 키를 늘리자 셋 다 깨졌다. CR-054(감사 대상)와 DEV-438(열 목록)에 이은 세 번째다. 그리고 PR #96에서 같은 실수를 검증 기록의 정본에서 저질렀다 — 표를 만들며 각 칸을 다시 세는 대신 앞서 쓴 문장의 수를 옮겼고, 리뷰가 그 산술을 잡았다.
@p CI 상태는 저장소마다 확인한다. pr-search는 계정 결제로 잡이 시작조차 하지 않는데(steps=0), design-system은 verify 27단계와 visual regression을 정상 통과했다. 이전 세션들의 "CI가 죽어 있다"는 기록은 한 저장소 이야기였다.
@h2 DEV-414 — 세는 것과 대조하는 것은 다르다
@path 이 DEV의 표제는 pr-search 24건이었고, 실측은 두 저장소를 합쳐 35건이었다(+2는 PR #91 신규, +9는 design-system). 그와 별개로 인계 문서의 계수 명령이 last: 30으로 되돌아가 첫 실측이 22건이라 답했다 — DEV-425가 이미 고친 결함의 재발이며 DEV-446으로 등재했다.
@p 판정은 이미 정정 20 · 이 세션이 고침 2 · 실결함 13으로 갈렸다. "이미 정정됨" 스무 건 모두 현재 HEAD를 열어 근거를 확인한 뒤 답변에 적었고, 일괄 resolve하지 않았다.
@h2 검증
@code lang=text sha=a5f15679f5b5 lines=8 kept=8
|pr-search    typecheck · lint · lint:deps 통과
|             단위 1883 · 통합 1399 · 회귀 288 · 문서 검증기 새 경고 0건
|             변이 열둘 (M1~M8, N1~N4) 전부 킬
|             CI 판정 불가 — 계정 결제로 steps=0
|design-system  typecheck · lint · lint:deps · lint:tokens 통과
|               단위 530 · a11y 165 · 대비 232/232 · check:api · 빌드
|               CI 정상 — verify(node 20·22) 27단계 + visual regression
@h2 현재 상태 한 줄
@path 양 저장소 미해결 리뷰 0건 · SRS baseline v2.16 · 원장 review v6.7 · REL-005 4/4 · open DEV 14건 · 릴리스는 승인되지 않았다.
@h2 세션 산출물
@path worklog: Obsidian dailywork/2026-08-31_PR-Search-리뷰-부채-청산과-CR-056-분포-정정.md
@todo 전사: exports/202608311129.md — pending /export, 사용자 입력이 남았다
@cmd design-system 0.2.1 npm 게시 완료 (태그 셋이 릴리스 HEAD 702cf0b을 가리킨다)
@h2 다음 에이전트가 먼저 할 것
@path REL-006(WP-041 → WP-042 → WP-044)이며 WP-043은 착수하지 않는다. CR-056이 신설한 WP-069(작성자 소속 팀 채우기)도 REL-006에 있다. 착수 전 계약 감사가 이 저장소의 관행이며 CR-056이 열여섯 번째였다.
@p ---
@h1 세션 요약 — CR-057 · WP-041 안전 구간 표식 (2026-08-31 2차)
@h2 이 세션의 목표
@path 사용자 지시서: CR-057로 WP-041 착수 전 계약 공백과 OD-008을 종결한 뒤, 안전 구간 표식을 백엔드부터 UI까지 수직으로 구현·검증·병합하고 병합 후 새 리뷰까지 확인한다. WP-042·WP-043·WP-069는 범위 밖. Codex 호출 0.
@h2 완료한 것 (전부 병합)
@p | PR | 무엇 | 결과 | | --- | --- | --- |
@path | #101 | CR-057 — API-SEQ-004 상세 절 신설, OD-008 종결 | DEV-458~467, SRS v2.17 |
@path | #102 | 계수 창 두 겹 | DEV-468·469 |
@path | #103 | WP-041 백엔드 + 화면 | DEV-470~474, SRS v2.18 |
@h2 이 세션의 핵심 발견 넷
@path "검사했다"와 "검사한 값이 쓸 때까지 유효하다"는 다른 주장이다 (DEV-471). 계약이 정한 검사 순서 여덟을 세웠지만 그 순서가 한 트랜잭션 안에 있다고 착각했다. 재채번이 그 사이에 커밋하면 과거 merge_sequence 행이 남아 서수 확인도 통과하고, 무효한 표식이 created로 저장되며 응답은 epoch_stale: false를 말한다 — 틀린 데이터를 쓰면서 옳다고 답한다. 리뷰 P1이 그것을 찾았다.
@p 정정한 자리가 다음 리뷰의 첫 자리다 — 다섯 번째다. 세 PR이 모두 자기가 방금 쓴 자리에서 지적받았고 전부 실결함이었다. 그중 셋은 문장으로 적어 둔 근거가 코드와 달랐던 자리다.
@path 상한을 키우는 방식은 언젠가 그 상한에 닿는다 (DEV-468). 병합 PR이 101개가 되자 계수 창이 앞쪽을 잘랐다. 그리고 창은 둘이었다 — 바깥만 고치고 안쪽을 탐지만 한 것이 DEV-469이며 리뷰가 잡았다.
@path 시험이 통과하는데 잡지 못하는 상태가 있었다 (DEV-474). 순서 단언 셋이 존재 확인 없이 indexOf만 비교해 그 코드를 통째로 지운 변이를 통과시켰다. 정정에 변이를 걸다 찾았다 — 변이를 걸지 않았으면 그대로 남았을 자리다.
@h2 검증
@code lang=text sha=6550c864aa18 lines=6 kept=6
|typecheck · lint · lint:deps 통과
|단위 1903 (1 skipped) · 통합 1432 · 회귀 299 · a11y 358 · 대비 232/232 · e2e 155 · 빌드
|targeted: 통합 safe-marker 33 · e2e safe-marker 8
|변이 21종 전부 킬 (백엔드 12 · 화면 4 · 리뷰 정정 5)
|문서 검증기 main 대비 새 경고 0건
|CI 판정 불가 — pr-search는 계정 결제로 steps=0
@p 정직하게 남긴 것 둘. 첫 배터리에서 통합 2건이 실패했으나 단독 재실행 두 번이 연속 통과했고 원인을 특정하지 못했다 — 실패 로그를 요약만 남긴 것이 그 한계다. 그리고 원인을 찾으려다 통합 실행을 겹쳐 돌려 공유 DB를 오염시켰고, 그때 나온 103건·30건 실패는 코드의 사실이 아니라 절차의 산물이다. 둘 다 원장 6.56.3에 적었다.
@h2 현재 상태 한 줄
@path REL-006 1/4 · SRS baseline v2.18 · 원장 review v6.11 · 양 저장소 미해결 리뷰 0건(전량 순회) · 오픈 결정 0건 · open DEV 14건 · 릴리스는 승인되지 않았다.
@h2 다음 에이전트가 먼저 할 것
@path WP-042 착수 전 API-SEQ-005 계약 감사. CR-057이 API-SEQ-004를 채우면서 그것도 상세 절이 없다는 것을 확인했고 의도적으로 함께 채우지 않았다. CR-057이 세운 것(에폭 재검증·조건부 갱신·오류 어휘)을 그대로 쓸 자리가 많지만, 복사하지 말고 FR-SEQ-007이 요구하는 것을 먼저 읽어라 — 탐색 상태는 사용자별이라 겨루는 것이 다르다.
@p ---
@h1 세션 요약 — CR-058 · WP-069 작성자 소속 팀 (2026-08-31 3차)
@h2 이 세션의 목표
@todo 사용자가 이전 인계의 권고를 결정자 판단으로 뒤집었다. API-SEQ-005 감사 → WP-042 대신 CR-058 → WP-069을 먼저 하도록 지시했다. 근거는 요구사항 등급이다 — WP-042의 FR-SEQ-007과 WP-044의 FR-SRCH-012가 Could인데 WP-069이 완성해야 하는 FR-SRCH-005·FR-STAT-006은 이미 승인된 Must다.
@h2 완료한 것 (전부 병합)
@p | PR | 무엇 | 결과 | | --- | --- | --- |
@path | #105 | CR-058 — 계약 공백 일곱 + 릴리스 회계 공백 넷 | DEV-477~487, SRS 변경 없음 |
@path | #106 | WP-069 구현 + 리뷰 정정 + 자체 점검 | DEV-488·489, 마이그레이션 021 |
@h2 이 세션의 핵심 발견 넷
@path 문서 하나만 갱신하면 같은 슬라이스가 세 크기로 보인다. CR-056이 WP-069을 신설하면서 작업 패키지만 고치고 원장 진행 표와 로드맵을 따라가지 않았다. REL-006이 작업 패키지에서는 다섯, 원장에서는 넷, 로드맵에서는 존재하지 않았고, 인계와 보고가 계속 "1/4"을 세고 있었다. 원장 3장 진행 표에 없는 WP는 아무도 세지 않는다.
@path ADR-004가 저장 위치를 강제한다. "모든 엔티티 문서는 PostgreSQL 데이터만으로 재구성 가능"이고 reindex.ts는 GHE를 한 번도 부르지 않는다. DoD가 "소속 변경이 재색인으로 반영된다"를 요구하므로 소속이 PostgreSQL에 있어야 하고, team_member는 app_user 외래 키 때문에 미로그인 작성자를 담지 못한다. 이 두 사실이 마이그레이션 021을 여는 근거였다 — 다음 빈 번호가 021이라는 것은 이유가 아니다.
@path 기능이 선 순간 그 기능이 없다고 말하던 문장이 전부 거짓이 된다 (DEV-488). DoD에 "계약의 그 문장을 사실에 맞게 고친다"를 적어 두고도 같은 문서 안의 짝을 세지 않아 리뷰가 잡았고, 전수로 찾자 넷이 더 나왔다. 넷 다 이 세션이 방금 쓴 자리였다.
@path 자체 재시도를 가진 함수는 트랜잭션 안에서 그 재시도를 쓸 수 없다 (DEV-489). upsertTeam의 23505 재시도는 "그 사이 상대가 커밋해 행이 존재한다"는 전제인데, PostgreSQL이 오류에 트랜잭션을 중단시키므로 재시도가 25P02로 다시 실패한다. 회복 가능한 경합이 동기화 전체의 실패가 된다.
@h2 확정한 설계 판단 셋
@p | 결정 | 버린 대안과 이유 | | --- | --- | | 범위는 그 PR 저장소의 조직 | 등록된 모든 조직: 한 조직의 동기화 실패가 모든 문서를 모름으로 만든다 | | 조회는 조직 단위로 모음 | 작성자마다 isTeamMember: 작성자 × 팀이 ... 로 는다 | | 경계는 org_team_sync.synced_at | 고정 TTL만 두기: 무엇이 사실인지 판정할 재료가 없다 |
@h2 검증
@code lang=text sha=d0cfd793eb42 lines=6 kept=6
|typecheck · lint · lint:deps 통과
|단위 1911 (1 skipped) · 통합 1471 · 회귀 316 · a11y 358 · 대비 232/232 · e2e 155 · 빌드
|targeted: team-membership 9 · author-teams 22 · jobs/reindex 24 · jobs/backfill 17 · analytics 35
|적대적 변이 24종 전부 킬 — 셋이 살아남았고 셋 다 등가가 아니었다
|문서 검증기 main 대비 새 경고 0건
|CI 판정 불가 — pr-search는 계정 결제로 steps=0
@path 정직하게 남긴 것 하나. e2e 전량 첫 실행에서 1건이 실패했고 원인을 특정하지 못했다. flow-003.spec.ts:176이 getByRole('searchbox')를 5초 안에 찾지 못했다. 단독 재실행 14/14, 전량 재실행 세 번 모두 155/155로 재현되지 않았다. 플레이크라고 단정하지 않았고, 이 작업이 apps/web 아래 어떤 파일도 건드리지 않았다는 사실과 함께 원장 6.58.4에 적었다.
@h2 반복해서 나타난 패턴 — 정정한 자리가 다음 리뷰의 첫 자리다
@path 일곱 번째다. 이번에는 그 자리가 코드가 아니라 내가 방금 쓴 문서였다. 그리고 그 정정을 마친 뒤 스스로 다시 읽다 코드 결함을 하나 더 찾았다(DEV-489) — 리뷰가 지적한 자리를 고치는 것으로 끝내지 않은 것이 그 결함을 찾은 이유다.
@h2 변이가 시험 구멍을 셋 드러냈다
@p M10(잠금 뒤 재확인)·M18(stop()의 await)·M19(첫 회차 순서)가 처음에 살아남았고 셋 다 등가가 아니었다. 각각 동시 호출 시험, 차단 가능한 대역 시험, 순서 기록 대역 시험을 새로 세운 뒤 킬했다. 시험이 통과하는데 잡지 못하는 상태를 변이가 세 번 드러낸 셈이다.
@h2 절차 사고 둘 (규율로 남긴다)
@risk 블록을 빈 문자열로 치환한 변이가 원복에 실패했다. 이후 변이는 전부 같은 길이의 무해한 형태(if (false), void x;, throw error;)로 바꿨다 — 원복이 언제나 정확히 1건을 맞히는 형태여야 한다
@b 차단 대역을 풀어 주지 않은 시험이 파일을 멈추게 했고, 그 멈춤은 "변이가 살아남았다"와 구분되지 않았다. 대역을 푸는 자리를 finally로 옮겼다
@h2 현재 상태 한 줄
@path REL-006 active 2/4 · SRS baseline v2.18 · 원장 review v6.15 · 양 저장소 미해결 리뷰 0건(전량 순회) · 오픈 결정 0건 · open DEV 14건 · 릴리스는 승인되지 않았다.
@h2 다음 에이전트가 먼저 할 것
@path WP-042 착수 전 API-SEQ-005 계약 감사(CR-059). 감사 후보 여덟과 이미 서 있는 것 넷(bisect_session 표, BISECT_CONTRADICTION, 감사 미기록 결정, 재채번이 쓰지 않는다는 규칙)을 todos.md A절에 실측으로 정리해 두었다. CR-058이 세운 것을 복사하지 말고 FR-SEQ-007이 요구하는 것을 먼저 읽어라 — 탐색 상태는 사용자별이라 겨루는 것이 다르다.
@h1 세션 요약 — 첫 사내 반입 가능선 (2026-09-01)
@h2 이 세션의 목표
@path 결정자가 순서를 바꿨다. WP-042(이분 탐색) → WP-044(내보내기) → 배포를 중단하고, 첫 사내 반입 경로를 수직으로 완성한다. 단일 호스트 Compose · 오프라인 번들 · 영구 다운스트림 단방향 계승.
@h2 완료한 것 (전부 병합)
@p | PR | 무엇 | 결과 | | --- | --- | --- |
@path | #107 | CR-059 — 배포·반입 계약, ADR-021 신설 | DEV-490~499, SRS v2.19 |
@path | #108 | 통합 시험 픽스처의 시간 의존 | DEV-500·501 |
@path | #109 | WP-070 — 이미지·Compose·번들·런북 | 여정 전체 관통, DEV-503 |
@path | #110 | CR-060 — 머지 후 리뷰 열둘 | DEV-504~517, 마이그레이션 022 |
@path | #111 | CR-061 — 한 겹 아래의 결함 둘 | DEV-518·519, 마이그레이션 023 |
@path | #112 | 재색인 상한을 .env에서 읽는다 | DEV-520 |
@h2 이 세션의 핵심 발견 다섯
@todo 실제로 세워 보지 않으면 배포 문서는 검증되지 않는다. WP-070이 단일 호스트에서 실제 기동을 시도하자 넷이 드러났다 — 접속 주체 미정의, Next.js standalone 기동 실패, pg_restore --clean의 파티션 부분 실패, alpine의 localhost IPv6. 문서를 읽거나 시험을 돌리는 것으로는 하나도 나오지 않았을 것들이다.
@path 가장 무거운 것은 리뷰가 지적하지 않은 자리에서 나왔다 (DEV-517). DEV-511을 고쳐 재색인을 실제로 예약하게 하자 세 잡이 permission denied로 죽었다. 마이그레이션 005가 표를 열거해 권한을 주므로 그 뒤 다섯 표에 SELECT조차 없었고, 통합 시험이 소유자 롤로 돌아 여섯 달 가까이 묻혀 있었다. Profile A만의 문제가 아니다.
@path 정정이 시한폭탄을 옮길 수 있다 (DEV-509). DEV-500의 롤링 3개월 창은 2026-12-01에 같은 실패를 되살리고 그때도 회귀는 초록이었다.
@path 전수 검사를 만들 때 "무엇의 전수인가"를 먼저 물어라 (DEV-518). 022가 표를 훑으며 시퀀스를 세지 않아 한 겹 아래에 같은 결함이 남았다.
@path 실패를 출력만 하고 종료 코드 0으로 끝내지 마라 (DEV-519). 그 증상("재색인 예약 2/4")을 이미 보고 있었으면서 왜 실패했는지 쫓지 않은 것이 원인이다.
@h2 머지 후 리뷰 — 15건, 전부 실결함
@p "정정한 자리가 다음 리뷰의 첫 자리다"가 아홉 번째였고, 이번에는 그 자리가 내가 방금 쓴 계약과 방금 만든 절차였다.
@h2 검증
@code lang=text sha=0ee96ffbc1f5 lines=5 kept=5
|typecheck · lint · lint:deps 통과
|단위 1911 (1 skipped) · 통합 1476 [1472 → +4] · 회귀 335 [316 → +19] · 빌드
|문서 검증기 main 대비 새 경고 0건
|CI 실제로 돎 — BILLING_BLOCKED 해소
|변이 여덟 전부 킬
@path 실제 실행으로 관통한 것: 번들 2.6GB 생성 → checksum 10개 → prs/* 전부 삭제 후 번들에서만 적재(pull 없음) → migration → 접속 주체 → ES mapping → 18개 서비스 → health → 조회 왕복 스모크 → 계보 확인(fetch한 커밋이 manifest와 일치) → 백업 → 파괴적 복구 → 재색인 4/4 완료.
@h2 현재 상태 한 줄
@p 첫 사내 반입 가능선이 섰다 · SRS baseline v2.20 · 원장 v6.22 · open DEV 16건 · 미해결 리뷰 0건 · 릴리스는 승인되지 않았다.
@h2 다음 에이전트가 먼저 할 것
@path 외부 저장소에서 더 만들 것이 없다. 다음은 사내 환경에서만 실행할 수 있다 — 번들을 만들어 반입하고 deploy/single-host/RUNBOOK.md를 따른다. WP-042·WP-044는 그 뒤이며, 둘의 요구사항은 Could 등급이다.
@h2 세션 종료 실측 (2026-09-01)
@p main = e1603b2 · PR 아홉 병합 · 미해결 리뷰 0건 · open DEV 15건 · 열린 PR 0건.
@p 머지 후 리뷰가 16건 왔고 전부 실결함이었다. 갈래별로: 계약 모순·범위 위반 넷, 배포 절차의 실행 불가 넷, 시험의 시간 의존 하나, 거짓 완료 주장 둘, 데이터 정합 하나, DoD 공백 하나, 권한 셋(자체 발견 포함).
@path "정정한 자리가 다음 리뷰의 첫 자리다"가 아홉 번째였고, 이번에는 그 자리가 내가 방금 쓴 계약과 방금 만든 절차였다. CR-060이 CR-059·WP-070을 고쳤고, CR-061이 CR-060을 고쳤으며, DEV-522가 그 정정 자체의 계수 오류를 고쳤다.
@path 산출물: 전사 exports/202609011607.md · worklog dailywork/2026-09-01_PR-Search-첫-사내-반입-가능선-구축-(WP-070-·-CR-059~061).md · 인계 팩 재생성.
@p ---
@h1 세션 요약 — 2026-09-02 (CR-062 · WP-071)
@h2 이 세션의 목표
@path WP-070의 "반입 가능성" 위에 사람이 실제로 수행할 "반입 절차"를 세운다 — 외부망에서 파일 하나, 사내에서 런북을 위에서 아래로.
@h2 완료한 것 (전부 main 병합)
@path CR-062 (PR #116): 착수 전 감사가 운반 단위 부재(DEV-523)와 최초 설치 순서의 실행 불가(DEV-524)를 찾았다. WP-071 신설. SRS·ADR·마이그레이션 변경 없음
@path WP-071 (PR #117): build-bundle.sh가 pr-search-<version>-offline.tar.gz를 만들고, prsctl load가 docker load 전에 .env를 요구하며, 런북이 외부망(A)/사내망(B·C·D)으로 재구성됐다. 회귀 다섯. 구현 중 DEV-525(FR 참조 부재, 머지 후 리뷰)·DEV-526(번들 텍스트 CRLF, 실제로 풀어 보다 발견)을 같은 PR에서 닫았다
@path DEV-527 (PR #118): PR #117 머지 후 리뷰(P1). 런북이 GHE App 자격을 install 뒤에 넣게 했다 — 검증에서 install을 돌리지 않은 자리였다. 런북만 고쳤다
@h2 핵심 발견
@path 번들 내용이 빌더의 checkout 상태에 따라 달라졌고 어제의 성공은 우연이었다 (DEV-526). 운반 아카이브의 정합은 풀어 나온 사본에서만 증명된다. fail-fast는 검사의 존재가 아니라 위치다.
@h2 검증
@path 풀린 사본 24/24 · 변이 여섯 킬 · 단위 1911 · 회귀 340 · CI 통과 · 문서 검증기 새 경고 0건.
@h2 현재 상태 한 줄
@path main = 7a58fd9 · WP-071 done · open DEV 15 · 미해결 리뷰 0건 — pr-search 118개·design-system 12개 전량 순회 (PR #117·#118의 마지막 리뷰까지 해소) · 릴리스 NOT APPROVED.
@h2 다음 에이전트가 먼저 할 것
@p todos.md 「시작하기 전에」로 실측한 뒤 A절 — 실제 사내 반입. 외부 기능 개발을 자동으로 시작하지 않는다.
@p ---
@h1 세션 요약 — CR-063 · WP-072 · 0.1.0-pilot.2 발행 (2026-09-02 2차)
@h2 이 세션의 목표
@path WP-071이 정한 운반 파일 하나가 경계를 어떻게 건너는지를 정한다. 결정자가 사내 어느 위치에서든 github.com에 닿는다고 확인하면서 물리 반입 채널이 필요 없어졌고, 운반 경로를 GitHub Release 자산 발행 → 사내에서 읽기 토큰으로 다운로드 → SHA-256 대조로 정했다. 마지막에 0.1.0-pilot.2를 실제로 발행했다.
@h2 완료한 것 (전부 main 병합)
@p | PR | 무엇 | 결과 | | --- | --- | --- |
@path | #119 | CR-063 — 운반 경로 계약, ADR-021 정정 절, 보안·인프라·로드맵 cascade | DEV-528(오프라인 전제 불일치)·DEV-529(런북 디스크 요건) |
@path | #120 | WP-072 — build-bundle.sh --release, 런북 2장 재구성, 회귀 시험 | 시험 릴리스 둘 발행·수신·관통, DEV-530(릴리스는 저절로 불변이 아니다) |
@cmd | #121 | DEV-531(초안 id 조회 실패 시 잔재)·DEV-532(curl 경로가 자산 id를 못 뽑음) | 임시 릴리스로 curl 실측, 가짜 gh로 조회 실패 경로 |
@cmd | #122 | DEV-533(되돌리기 실패를 "되돌렸다"로 보고) | 가짜 gh 두 시나리오 |
@cmd | #123 | DEV-534(태그 삭제 결과 미보고) | 가짜 git 도입, 두 시나리오 |
@path | #124 | DEV-535(발행 응답 유실 시 태그 건너뜀 → 원격 태그 직접 확인) · DEV-536 등록 | 통합 잡 시간 의존 실패 2회 뒤 통과 |
@path | #125 | DEV-537(남의 태그 삭제 → 커밋 일치 시에만) | 두 시나리오 |
@path | 발행 | 0.1.0-pilot.2 (main 0a73065) | 자산 대조 통과, SHA-256 4b0741… |
@h2 이 세션의 핵심 발견 다섯
@b "3.7GB"는 오기였다. 운반 파일은 1,073MB 하나다(원본 2.6GB가 gzip으로 줄어든 것). 크기를 말하기 전에 잰다.
@path 문서의 "사내에서 인터넷에 닿지 않는다"는 실측 없는 가정이었다 (DEV-528). 결정자 확인 하나로 물리 반입 채널이 사라졌다. 전제를 사실로 적을 때는 누가 언제 확인했는지를 함께 적는다.
@path 릴리스는 저절로 불변이 아니다 (DEV-530). immutable releases가 꺼진 저장소에서는 쓰기 권한자가 발행 뒤 자산·태그를 바꿀 수 있고, 켜져도 본문은 편집 가능하다. 정본은 담당자가 별도 채널로 전달하는 SHA-256이다.
@path 되돌리기는 "무엇을 되돌렸는가"를 사실대로 말해야 한다 (DEV-531~535, 537). id 조회 실패 · 릴리스 삭제 실패 · 태그 삭제 실패 · 발행 응답 유실 · 남의 태그 — 다섯 경계마다 "되돌렸다"가 거짓이 되는 자리가 있었고, 각각 가짜 gh/git로 실제로 돌려 확인했다.
@b 검증 실행을 오염시키는 것은 코드가 아니라 내 편집이었다. 실행 중 스크립트 편집(bash가 바뀐 바이트를 읽음), 백그라운드 시나리오 중 문서 편집(dirty guard), 전제 검사까지 id를 돌려준 가짜 도구 — 셋 다 재실행으로 풀었다.
@h2 머지 후 리뷰 — 8건, 7건 실결함
@path "정정한 자리가 다음 리뷰의 첫 자리다"가 이 세션에서만 여섯 번 반복됐다. P1·P2가 번갈아 왔고 매번 앞 정정의 한 겹 아래였다. 결정자가 "1~3 마친 뒤 내가 지시"라고 정해 #125의 리뷰(P1)는 손대지 않았다.
@h2 검증
@code lang=text sha=3825835b4feb lines=5 kept=5
|typecheck · lint · lint:deps 통과 (패키지 13개, 위반 0건)
|단위 1911 (1 skipped) · 회귀 347 [340 → +7] · 빌드 · bash -n
|문서 검증기 main 대비 새 경고 0건 · CI verify·integration 통과 (PR #119~#125)
|시험 릴리스 2회 발행 → 토큰만으로 수신 → digest 삼중 일치 → 풀린 사본 verify·load·lineage·git fetch
|변이 8종 킬 · 가짜 gh/git 시나리오 8 · 실패 경로 3 · curl 경로 실측 · --release 없는 실행
@h2 현재 상태 한 줄
@path main = 0a73065 · 0.1.0-pilot.2 발행됨 · WP-072 done · open DEV 16 · 미해결 리뷰 1(#125, 결정 대기) · 릴리스 NOT APPROVED · 사내 반입은 결정자의 토큰 발급과 전달로 시작한다.
@h2 다음 에이전트가 먼저 할 것
@path todos.md 「시작하기 전에」로 실측한 뒤, 결정자의 지시를 기다린다. 후보는 셋이다 — #125 리뷰 처리(DEV-538 후보), 0.1.0-pilot.2 발행 사실의 원장 기록, 사내 반입 결과의 반영. 외부 기능 개발(WP-042·WP-044)을 자동으로 시작하지 않는다.
