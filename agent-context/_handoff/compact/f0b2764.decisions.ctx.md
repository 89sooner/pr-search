#hidden
# aci:v1 id=f0b2764 src=agent-context/decisions.md
@kv sha256=e90a4a31097fec7bb36e82311edcfde577af1b80ef0b95e05bb2cd2bc7b13a8e bytes=6127 lines=67 title=확정한-설계-결정과-이유
@sig agent-context/decisions.md;docs/00_governance/change_control.md;docs/40_delivery/pr_search_implementation_traceability.md;lib/nav.ts;packages/domain/src/anchor.ts;change_control;pr_search_implementation_traceability;DEV;GET;releases;API;REL;containments;release;comparisons;SEQ;runRange;unreleased;W005;empty_no_release;not_indexed;repository;findContainingReleases;findLatestReleaseSeq
@h1 확정한 설계 결정과 이유
@path 이 저장소의 결정은 docs/00_governance/change_control.md(CR)와
@path docs/40_delivery/pr_search_implementation_traceability.md(DEV)에 정본이 있다.
@p 여기에는 왜 그렇게 정했는지와 버린 대안만 적는다.
@h2 CR-030 (WP-026 / W-005) — 2026-08-25 closed
@p | DEV | 결정 | 왜 | | --- | --- | --- |
@path | 155 | GET /releases(API-REL-005) 신설 | 릴리스 목록을 줄 경로가 없었다. /containments는 대상을 지목해 묻는 조회, /release-comparisons는 구간 비교다 |
@p | 155 | "직전"은 시각이 아니라 서수 기준 + 비교 대상 태그명 동봉 | 태그는 나중에 옛 커밋을 가리키며 생길 수 있어 시각 순서와 서수 순서가 어긋난다. 구간은 서수로 잘린다 |
@path | 156 | API-SEQ-003은 runRange 재사용 | 두 API가 같은 구간에 다른 숫자를 말하지 않게 하려고. 이 API가 소유하는 것은 to=unreleased·서수 기준 방향 정규화·size=0 셋뿐 |
@path | 157 | 경로는 /releases?repo=&branch= | 셸(lib/nav.ts, 기병합)이 /releases를 이미 소유했고 문서는 /releases/[owner]/[repo]였다 — 그대로 두면 "릴리스" 내비 항목이 404. DEV-153과 같은 원칙(구현된 셸 쪽으로 통일) |
@path | 158 | 목록은 저장소 스코프, 브랜치는 표기·필터 | 브랜치를 고정하면 "다른 대상 브랜치 릴리스 2건 선택"(AC-3 / QA-W005-03)이라는 상황 자체가 만들어지지 않아 그 규칙을 검증할 길이 사라진다 |
@p | 159 | empty_no_release와 not_indexed를 하나로 합침 | repository 표에 릴리스 동기화 마커가 없어 서버가 둘을 판별할 수 없다. 판별 못 하는 것을 두 상태로 그리면 하나는 반드시 거짓이 된다 |
@path 버린 대안: 화면이 branch를 필터로 보내는 안(AC-3 도달 불가), 빈 상태를
@path 두 갈래로 나누는 안(판별 근거 없음), W-009로 링크하는 안(그 화면이 REL-004~005).
@h2 DEV-160 (구현 중 발견, CR 불필요) — 판정을 하나로 모았다
@p 포함 판정(findContainingReleases)과 마지막 릴리스 판정 (findLatestReleaseSeq)이 릴리스의 저장 base_branch·merge_seq를
@path 조건으로 썼다. 동기화(JOB-REL-007)가 채번보다 먼저 돌면 그 둘이 NULL인 채
@p 커밋만 체인 위에 있고, 그러면 그 릴리스가 포함 목록에서 통째로 빠진다 — 이미 배포된 PR이 "미배포"로 보인다.
@path 앵커 해석은 DEV-149대로 이미 다시 찾고 있었는데 포함 판정만 저장 열을 읽었다.
@p 서수는 언제나 현재 에폭 체인에서 commit_sha로 다시 찾도록 통일했고, findLatestReleaseSeq는 findLatestRelease에 위임한다.
@p CR을 열지 않은 이유: 문서가 요구한 동작을 코드가 지키지 못한 것이라
@path 문서 변경이 없다. 유형은 구현 결함(DEV-107의 선례).
@h2 CR-031 (WP-027 / 선행·후행) — 2026-08-25 closed, SRS v2.4
@p | DEV | 결정 | 왜 | | --- | --- | --- |
@path | 161 | 이웃 목록에 직접 푸시 커밋 포함 (SRS 문장·AC-2 정정) | PR만 실으면 서수가 1339 → 1341처럼 건너뛴 채 보이고 사용자가 누락으로 읽는다. "서수는 git log --first-parent로 검증 가능해야 한다"(ADR-007)가 화면에서 깨진다. 하위 문서는 SRS 범위를 넓힐 수 없으므로 SRS를 직접 고쳤다 |
@path | 162 | 펼칠 때 1회 조회 (진입 조회 금지) | WP-017이 "진입 요청은 문서 하나"를 a11y·e2e 다섯 곳에 걸었고 WP-024가 CI에서 한 번 깨고 되돌렸다. C-020과 같은 구조로 통일 |
@path | 163 | 앵커 pr_number \| commit_sha (둘 다면 400) | 직접 푸시 커밋은 PR이 없어 PR 번호로 자기 위치를 물을 수 없다. W-003이 그 경로를 쓴다 |
@path | 164 | 409는 NO_SEQUENCE 하나, 사유를 detail.reason으로 분리 | 새 오류 코드를 만들지 않는다 — API-SEQ-002의 tag_not_found/release_not_indexed와 같은 관용구. C-014(DEV-077)가 "머지되지 않았다"(사실 주장)와 "아직 모른다"를 같이 그리는 것을 금지한다 |
@p | 164 | 화면은 미머지면 요청 자체를 안 한다 | PR 문서의 state로 아는 사실을 409로 되묻지 않는다 |
@path | 165 | QA-W002-16(에폭 경고)을 WP-027이 갖는다 | DEV-087이 "도달 불가라 만들지 않는다 — WP-021이 채울 때 정한다"고 이월했고 그 조건이 충족됐다 |
@p | 166 | 이웃 멤버십은 PostgreSQL, 색인은 표시값만 | 색인에서 range(merge_seq)로 고르면 반영이 늦은 항목이 빠지고 더 먼 항목이 올라와 "인접"이 거짓이 된다 |
@path | 167 | "범위로 확장"은 지금 보는 창 (첫 항목 제외 · 마지막 포함) | 화면이 본 것과 W-004가 조회할 것을 같게 한다. W-004가 앵커에 "제외/포함" 라벨을 상시 표시하므로 첫 행이 왜 빠졌는지 그 화면에서 읽힌다 |
@h2 서수를 URL 앵커로 넘길 때 — seq: 접두는 필수다
@path classifyAnchor(packages/domain/src/anchor.ts)는 맨 숫자를 의도적으로
@p ambiguous로 판정한다 (PR 번호인지 서수인지 시스템이 고르지 않는다).
@path from=6&to=8로 넘기면 W-004가 두 앵커를 모두 해석하지 못해 조회 버튼이
@path 잠긴 채로 도착한다. WP-026에서 실제로 낸 결함이고(Codex P1) WP-027의
@p neighborRangeHref도 같은 규칙을 따른다.
@p 서수 0은 앵커로 표현할 수 없다(positiveWithin이 1 이상만 받는다). 채번된 릴리스가 없는 공간의 미배포 구간이 그 값을 내며, 그때는 from을 싣지 않고 화면이 "시작은 직접 고르세요"를 말한다.
@h2 시험 방법에 관한 결정
@b 변이가 실제로 답을 바꾸는지부터 확인한다. 두 WP 연속으로 등가 변이를
@p 만났다 (risks.md 참조).
@b 대역(mock)이 실제보다 관대하면 그만큼이 사각지대다. e2e 앵커 대역이
@p 무엇이든 해석해 줘서 P1 결함이 CI를 통과했다. 지금은 서버와 같은 갈래로 판정한다(맨 숫자·seq:0은 400).
