#hidden
# aci:v1 id=f527103 src=agent-context/commands.md
@kv sha256=d2266836bd71a6a2544a6ab791425f9c10e538bf3a1dd47b721a95247da6623f bytes=11164 lines=249 title=명령어-시험-결과-실패한-명령과-원인
@sig agent-context/commands.md;HOME/.nvm/versions/node/v22.23.2/bin;prs/web;web/lib/neighbors;sequence/neighbors;apps/web;/node_modules/.bin/playwright;e2e/flow-002.spec.ts;prs-postgres/redis/elasticsearch;dist/cli.js;9200/_cat/indices;repos/89sooner/pr-search/pulls/33/comments;pass/fail;dev/null;docs/00_governance/change_control.md;home/roqkf/.claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py;ops/sequence-integrity;pipeline-worker/integration/sequence/repair;authz/team-scope;packages/authz/src/team-scope;search-api/src/runtime;repos/89sooner/pr-search/pulls/;acme/payments;acme/integrity-wp028
@h1 명령어 · 시험 결과 · 실패한 명령과 원인
@h2 필수 전제 — Node PATH
@p 모든 pnpm 명령 앞에 이것이 필요하다. 셸 기본값(v20.12.0)으로는 pnpm install 자체가 거부된다.
@code lang=bash sha=41c745cf8440 lines=1 kept=1
|export PATH=$HOME/.nvm/versions/node/v22.23.2/bin:$PATH
@p .nvmrc가 22이므로 사용자가 nvm use를 쓰면 자동으로 잡힌다. 전역 기본값은 바꾸지 않았다.
@h2 검증 배터리 (전 계층, 마지막 실행 결과)
@code lang=bash sha=d68d42a4da38 lines=10 kept=10
|pnpm typecheck        # tsc --build + tests + web
|pnpm lint             # eslint . — 마지막 파일까지 쓴 뒤 다시 돌릴 것
|pnpm run lint:deps    # 패키지 13개, 위반 0건
|pnpm run test                 # 단위 1161 통과 (1 skipped)
|pnpm run test:a11y            # 186 통과 (axe 0건)
|pnpm run test:integration     # 674 통과 — 실 PG·Redis·ES 필요
|pnpm run test:regression      # 27 통과
|pnpm run test:contrast        # 80쌍 통과
|pnpm --filter @prs/web run build   # e2e 전에 필수 (아래 참조)
|pnpm run test:e2e             # 65 통과
@p 부분 실행:
@code lang=bash sha=afe79c520aec lines=4 kept=4
|pnpm run test web/lib/neighbors
|pnpm run test:integration sequence/neighbors
|pnpm run test:a11y pr-detail
|cd apps/web && ./node_modules/.bin/playwright test e2e/flow-002.spec.ts
@h2 함정: e2e는 빌드를 하지 않는다
@todo pnpm run test:e2e는 next start로 기존 .next를 재사용한다. 화면을
@path 고쳤으면 pnpm --filter @prs/web run build를 먼저 돌려야 한다. 안 그러면
@p 새 라우트가 404가 되고, 그 실패를 플레이크로 오해하기 쉽다(이전 세션이 실제로 겪었다). CI는 build 스텝이 선행한다.
@h2 실패했던 명령과 원인
@p | 명령 | 증상 | 원인·해결 | | --- | --- | --- | | pnpm install --frozen-lockfile | ERR_PNPM_UNSUPPORTED_ENGINE | 기본 Node v20.12.0. eslint-visitor-keys@ ... 0) | ERR_REQUIRE_ESM (std-env) | require(esm)는 22.12+ — 22.0.0은 너무 이르다 |
@p | pnpm test:integration | database "prs_test" does not exist | docker exec prs-postgres psql -U prs -d prs -c "CREATE DATABASE prs_test OWNER prs;" |
@path | playwright test | Executable doesn't exist ... chromium_headless_shell-1200 | ./node_modules/.bin/playwright install chromium |
@p | pnpm lint (Node 22) | 미사용 import vi | Node 문제가 아니었다. 시험 파일을 lint 뒤에 썼기 때문 |
@h2 환경 준비 (새 머신이라면)
@code lang=bash sha=f5e88448699f lines=4 kept=4
|docker ps --format '{{.Names}}\t{{.Status}}'      # prs-postgres/redis/elasticsearch
|docker exec prs-postgres psql -U prs -lqt | grep prs   # prs, prs_test
|pnpm run db:migrate                                # 개발 DB prs (dist/cli.js 필요 → 빌드 선행)
|curl -s "localhost:9200/_cat/indices?h=index"      # prs-{commits,pull-requests,links,releases}-v1
@p 접속 기본값은 .env.example과 같다(.env 파일은 없다). 컨테이너 설정과 일치한다.
@h2 변이 시험 (이 저장소의 관행)
@p 작은 헬퍼로 변이 → 시험 → 원복을 반복했다.
@code lang=python sha=530a2537412e lines=2 kept=2
|# 파일, 옛 문자열, 새 문자열을 받아 정확히 1건일 때만 치환 (CRLF 보존)
|python3 mutate.py <path> "<old>" "<new>"
@p 주의: 신규(untracked) 파일은 git checkout --으로 원복되지 않는다. 역방향 치환으로 되돌리고, 마지막에 전 계층을 다시 돌려 원복을 확인할 것.
@h2 Git · PR
@code lang=bash sha=7920e6a3601f lines=3 kept=3
|gh pr checks 33
|gh pr view 33 --json state,isDraft,mergeStateStatus,reviews
|gh api repos/89sooner/pr-search/pulls/33/comments --jq '.[] | "\(.path):\(.line)\n\(.body)"'
@p CI는 verify(약 2분 50초)와 integration(약 3분)이다. 감시할 때는
@path integration 줄만 겨냥할 것 — "아무 체크나 pass/fail"로 조건을 걸면
@p verify가 끝나는 순간 빠져나와 미완인 상태를 결과로 오해한다.
@code lang=bash sha=05b00cffedb1 lines=1 kept=1
|timeout 1800 bash -c 'until gh pr checks 33 2>/dev/null | grep "^integration" | grep -qE "\t(pass|fail)\t"; do sleep 20; done'
@h2 파일 편집 시 개행 주의
@path docs/와 apps/web·packages/의 대부분 소스가 CRLF다(git
@p core.autocrlf=true). Python으로 편집할 때 newline=''로 읽고 쓰며, 삽입하는 블록도 \r\n으로 맞춰야 앵커 매칭이 되고 diff가 깨끗하다.
@path docs/00_governance/change_control.md와 pr_search_api_contracts.md는 LF다.
@p ---
@h1 2026-08-25 후반 세션 갱신
@h2 검증 배터리 (마지막 실행 결과 — main 5e18e00)
@code lang=bash sha=3914c521e783 lines=12 kept=12
|export PATH=$HOME/.nvm/versions/node/v22.23.2/bin:$PATH
|pnpm typecheck                # 통과
|pnpm lint                     # 통과 — 마지막 파일까지 쓴 뒤 다시 돌릴 것
|pnpm run lint:deps            # 패키지 13개, 위반 0건
|pnpm run test                 # 단위 1243 통과 (1 skipped)   [1161 → +82]
|pnpm run test:a11y            # 192 통과 (axe 0건)            [186 → +6]
|pnpm run test:integration     # 746 통과                      [674 → +72]
|pnpm run test:regression      # 64 통과                       [27 → +37]
|pnpm run test:contrast        # 80쌍 통과
|pnpm --filter @prs/web run build   # e2e 전에 필수
|pnpm run test:e2e             # 67 통과                       [65 → +2]
|pnpm build                    # 통과
@h2 문서 검증기 (저장소에 없다 — 스킬 디렉터리에 있다)
@code lang=bash sha=27005d6b0b0a lines=3 kept=3
|V=/home/roqkf/.claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py
|python3 $V --root . --strict
|python3 $V --root . --report --code-root .
@p --strict는 오류 2건으로 끝나는 것이 정상이다 — change_control.md 9건,
@todo 원장 5건의 "미정/TODO" 플레이스홀더가 기존 문서 산문이다. 깨끗한 main에서도
@p 같은 수가 나온다. 작업 전후로 그 수가 늘지 않았는지를 보면 된다.
@h2 이 세션에서 쓴 부분 실행
@code lang=bash sha=6a359e749ed9 lines=6 kept=6
|pnpm run test:integration ops/sequence-integrity
|pnpm run test:integration pipeline-worker/integration/sequence/repair
|pnpm run test:integration authz/team-scope        # team-scope + team-scope-es 둘 다
|pnpm run test:integration pr-snapshot
|pnpm run test packages/authz/src/team-scope
|pnpm run test search-api/src/runtime
@h2 머지 후 리뷰 확인 (이 세션에서 다섯 번 필요했다)
@code lang=bash sha=13a0496d806a lines=4 kept=4
|gh api repos/89sooner/pr-search/pulls/<N>/comments --jq 'length'
|gh api graphql -f query='{ repository(owner:"89sooner",name:"pr-search"){ pullRequest(number:<N>){
|  reviewThreads(first:20){ nodes{ id isResolved isOutdated path line
|    comments(first:2){ nodes{ author{login} body } } } } } } }'
@p reply / resolve:
@code lang=bash sha=948e248f823f lines=5 kept=5
|gh api graphql -f query='mutation($tid: ID!, $body: String!) {
|  addPullRequestReviewThreadReply(input: {pullRequestReviewThreadId: $tid, body: $body}) { comment { url } } }' \
|  -f tid="<PRRT_...>" -f body="$(cat reply.md)"
|gh api graphql -f query='mutation($tid: ID!) {
|  resolveReviewThread(input: {threadId: $tid}) { thread { isResolved } } }' -f tid="<PRRT_...>"
@p 코드를 고치고 CI가 초록이 된 뒤에만 reply → resolve 한다.
@h2 CI 대기 (verify와 integration 둘 다 봐야 한다)
@code lang=bash sha=46ccadb75230 lines=6 kept=6
|for i in $(seq 1 30); do
|  ok=$(gh pr checks <N> 2>/dev/null | grep -cE "^(verify|integration)\s+(pass|fail)" || echo 0)
|  [ "$ok" = "2" ] && break
|  sleep 20
|done
|gh pr checks <N>
@p integration만 보고 나가면 verify가 아직 도는 상태(UNSTABLE)에서 머지를 시도하게 된다 — 이 세션에서 실제로 한 번 겪었다.
@h2 변이 시험 헬퍼 (정확히 1건일 때만 치환, CRLF 보존)
@code lang=bash sha=4ca0d0663b25 lines=1 kept=1
|python3 mutate.py <path> "<old>" "<new>"
@path 중복 매칭이 안전장치다. WP-028에서 findPointByPullRequest와 똑같은 질의를
@p 가진 기존 함수가 있어 2건이 잡혔고, 그 덕에 내가 만들려던 함수가 이미 있다는 것을 발견해 중복 구현을 지웠다.
@h2 실패했던 명령과 원인 (이 세션)
@p | 명령 | 증상 | 원인·해결 | | --- | --- | --- |
@path | pnpm run test:integration ops/sequence-integrity | duplicate key ... repository_owner_name_key | acme/payments를 다른 시험이 이미 쓴다 → 고유 픽스처 이름(acme/integrity-wp028) |
@p | 같은 시험 | app_user_login_key 중복 | 시험용 사용자 login도 고유해야 한다 | | 같은 시험 | new_epoch_expected가 8 | beforeEach가 seq_epoch를 리셋하지 않았다 | | pnpm typecheck ... kill %1 포함 명령 | exit 144, 뒤 명령 미실행 | 같은 명령줄에서 백그라운드 작업을 죽이면 셸 자체가 죽는다 |
@h2 파일 편집 시 개행 주의 (변경 없음)
@path docs/와 apps/web·packages/의 대부분 소스가 CRLF다(git core.autocrlf=true).
@p Python으로 편집할 때 newline=''로 읽고 쓰며, 삽입 블록도 \r\n으로 맞춰야 앵커 매칭이 되고 diff가 깨끗하다. agent-context/*.md도 CRLF다.
@p ---
@h1 인계 검증 명령 (2026-08-26 세션에서 실제로 쓴 것)
@p 문서 버전과 ID 최댓값을 한 번에 확인한다 — 인계받자마자 이것부터.
@code lang=bash sha=1e8d2aa6393e lines=4 kept=4
|sed -n '1,4p' docs/10_requirements/srs_final.md                         # baseline v2.5
|sed -n '1,4p' docs/40_delivery/pr_search_implementation_traceability.md # review v1.8
|grep -oE 'CR-[0-9]{3}' docs/00_governance/change_control.md | sort -u | tail -3
|grep -oE 'DEV-[0-9]{3}' docs/40_delivery/pr_search_implementation_traceability.md | sort -u | tail -3
@p 미해결 리뷰 스레드 수만 세는 짧은 형태 (위쪽의 전체 조회보다 빠르다):
@code lang=bash sha=4e49ab236ef3 lines=6 kept=6
|for n in 36 37 38; do
|  gh api graphql -f query="{ repository(owner:\"89sooner\",name:\"pr-search\"){
|    pullRequest(number:$n){ reviewThreads(first:30){ nodes{ isResolved isOutdated path } } } } }" \
|    --jq '[.data.repository.pullRequest.reviewThreads.nodes[] | select(.isResolved==false)] | length'
|done
|# 2026-08-26 결과: 1 / 7 / 1 = 9건. 전부 아직 열려 있다
@todo 문서 모순(원장 done vs 작업 패키지 todo) 확인:
@code lang=bash sha=3d46dda31cc5 lines=2 kept=2
|sed -n '48p;50p' docs/40_delivery/pr_search_work_packages.md   # WP-028 · WP-068 → 아직 todo
|grep -nE '^\| WP-(028|068) ' docs/40_delivery/pr_search_implementation_traceability.md
@h2 handoff pack 재생성
@code lang=bash sha=bba11eeee908 lines=3 kept=3
|python /home/roqkf/.claude/skills/agent-context-handoff/scripts/context_handoff.py \
|  build --root . --source agent-context --output agent-context/_handoff
|python agent-context/_handoff/reader.py list --output agent-context/_handoff
@path 입력에서 제외되는 것: agent-context/_handoff/ 자신, 그리고 exports/의 전사.
@p 전사는 사람이 읽는 원본이며 compact 대상이 아니다.
