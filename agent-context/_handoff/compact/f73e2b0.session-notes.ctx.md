#hidden
# aci:v1 id=f73e2b0 src=agent-context/session-notes.md
@kv sha256=36b33b3462688943b2be31cea340a16d24fb0844b0d70bcbffe85e66e27a0c2c bytes=60256 lines=1000 title=Session-2026-08-25-후반-CR-032-036-WP-028-WP-068-완료
@sig agent-context/session-notes.md;10/11;Risks/gotchas;docs/00_governance/change_control.md;exports/202608260047.md;dailywork/2026-08-25_PR-Search-WP-028;exports/202608251453.md;exports/202608260113.md;11/11;exports/202608261008.md;1/8;2/8;exports/202608262010.md;dailywork/2026-08-26_PR-Search-OD-005-;W-002/W-003;3/8;exports/202608262224.md;4/8;apps/search-api/integration/sequence/_repro-w004.test.ts;exports/202608270742.md;dailywork/2026-08-27_PR-Search-WP-035-;pr-search/202608271346.md;5/8;docs/README.md
@h1 Session: 2026-08-25 (후반) — CR-032~036, WP-028·WP-068 완료
@h2 Goal
@path 사용자의 말: "이어서 진행해야 하는곳을 파악해봐라" → "CR-032 + WP-027 post-merge
@path review correction" → "보고된 WP-028 완료 판정을 그대로 믿지 말고 현재
@p GitHub/코드/production composition을 다시 실측한다".
@h2 Current state
@b main = 5e18e00. 작업 브랜치 없음(전부 병합·삭제)
@path SRS baseline v2.5, 원장 review v1.8, CR-036까지, DEV-190까지
@todo REL-003 10/11 — WP-067만 todo
@path 미해결 리뷰 9건 (PR #36 1 · #37 7 · #38 1) — 전부 코드로 재현 확인
@h2 Decisions
@p decisions.md 참조. 이 세션의 가장 무거운 결정 셋:
@b 점검은 관찰이다 — 그래프를 못 읽어도 sequence_space.state를 바꾸지
@path 않는다. SRS v2.5로 FR-ADMIN-003 예외 처리를 직접 고쳤다. 갈래 B(check_failed
@p 상태 신설)는 기각.
@b 운영 조립을 시험 가능한 자리로 꺼낸다 — runtime.ts의 buildServerDeps를
@p index.ts와 시험이 같이 부른다.
@b 수동 복구는 자동 재작성과 다른 전략을 쓴다 — 검증된 prefix + 최초 불일치
@p 부터 재계산. 자동 경로의 merge-base 최적화는 head가 움직였을 때만 참이다.
@h2 Changed files
@p files.md 참조. 이 세션의 커밋(머지 제외, 시간순):
@path e514bfe docs: CR-032 등록 / 394854a fix: 선행·후행 브랜치 정확·복구 가능
@path b9d3b1c docs: CR-033 등록 / 4d192b2 feat: WP-028 / f9df507 docs: 원장
@b a21cdeb docs: 원장·CR 참조 기입
@path 223e84d docs: CR-034 등록 / e363b97 feat: 운영 배선 / 97d0637 docs: 원장
@path c568f7c docs: CR-034 병합 확인
@path 5f76b1a docs: CR-035 등록 / c2ff217 feat: WP-068 / 1cfc53a docs: 원장
@path 76f4134 docs: CR-036 등록 / 305040d fix: 팀 회수 색인 반영
@h2 Commands
@p commands.md 참조. 요점 넷: Node PATH 필수, e2e 전에 빌드, lint는 마지막 파일을 쓴 뒤에, 머지 직후 리뷰를 다시 확인.
@h2 Next steps
@path CR-037로 미해결 9건 정정 (todos.md 0번) — P1 4건 중 둘이 접근 통제·
@p 데이터 정확성에 직접 걸린다
@b work_packages.md 상태표를 원장과 일치시키기 (이 세션이 만든 모순)
@todo WP-067 착수 전 계약 감사 → next free CR
@path WP-067 구현
@h2 Risks/gotchas
@p risks.md 참조. 이 세션이 새로 배운 것: 격리된 함수 시험이 전부 초록이어도 운영이 그것을 부르지 않을 수 있다. 그리고 그것을 막으려고 만든 회귀 시험조차 첫 형태는 import 줄 때문에 통과했다.
@h2 References
@path PR #34 1d0dee2 / #35 4b954d1 / #36 ea2663d / #37 a620899 /
@path #38 3ba6b9d / #39 b55b275 / #40 5e18e00
@path CR-032~036: docs/00_governance/change_control.md
@path DEV-168~190: 원장 5장
@b 검증 기록: 원장 6.27.1·6.28·6.28.1·6.30·6.30.1장
@path 전사(transcript): exports/202608260047.md (/export 완료, .gitignore 대상)
@path worklog: Obsidian dailywork/2026-08-25_PR-Search-WP-028·WP-068-완료와-운영-배선-정정.md
@p ---
@h1 Session: 2026-08-26 — 인계 검증 (코드 변경 없음)
@h2 Goal
@p 사용자의 말: "지금까지의 코딩 작업 세션 내용을 다음 에이전트가 이어받을 수 있도록 정리해줘." 직전 대화가 /clear된 상태로 시작했으므로 이 세션에는 새 코딩 작업이 없다. 한 일은 기존 agent-context/*.md를 저장소 실제 상태와 대조해 검증하고, 틀린 사실 두 건을 고치고, handoff pack을 다시 만든 것뿐이다.
@h2 실측으로 재확인한 것
@p | 기록된 주장 | 확인 결과 | | --- | --- | | main = 5e18e00 | 일치 (git log) | | SRS baseline v2.5 | 일치 (헤더) | | 원장 review v1.8 | 일치 (헤더) |
@path | CR-036 · DEV-190까지 | 일치 (문서 grep 최댓값) |
@path | 미해결 리뷰 9건 | 현재도 전부 열려 있다 — GraphQL 실측 PR #36=1 · #37=7 · #38=1 |
@todo | work_packages.md가 WP-028·068을 todo로 표시 | 모순 여전 — 48·50행이 todo, 원장 47·54행은 done |
@h2 고친 것 — 문서 사실 정정 2건
@b 전사 위치가 틀렸다. "저장소 루트"라고 적혀 있었으나 실제로는
@path exports/202608260047.md이고 exports/는 .gitignore에 있다
@path (직전 세션 전사 exports/202608251453.md도 같은 자리). "커밋에 딸려 들어갈 수
@p 있으니 stage 전에 확인" 경고는 근거가 없어졌다.
@b agent-context/ 변경분이 커밋되지 않았다. 직전 세션이 7개 파일과
@p _handoff/ 전체를 쓰고 커밋 없이 끝냈다 — risks.md 4번이 경고하는 바로 그 상황인데 경고문 자체가 거기 걸려 있다. risks.md 13번으로 올렸다.
@h2 Next steps
@path 바뀌지 않았다. todos.md 0번 — CR-037로 미해결 리뷰 9건 정정부터다.
@h2 전사 아카이브
@todo 예정 경로 exports/202608260113.md — pending /export (사용자가 실행해야
@p 확정된다). 스킬 기본값(<루트>/pr-search-2026-08-26.md) 대신 저장소 관행을 따랐다: exports/에는 전용 .gitignore 항목과 설명 주석이 있고 기존 전사 2건이 모두 YYYYMMDDHHMM.md다. 루트에 두면 무시되지 않아 오히려 커밋에 딸려 갈 수 있다.
@p ---
@h1 Session: 2026-08-26 (대형) — CR-037·CR-038·REL-003 마감
@h2 Goal
@p 사용자가 준 3단계 지시서. "사용자에게 추가 승인을 요청하지 않는다. 아래 기술 결정은 이미 승인된 것으로 간주한다."
@h2 Current state
@b main = 3e3009b. 작업 브랜치 전부 병합 (로컬 브랜치 4개는 삭제만 안 됨)
@path SRS baseline v2.5(변경 없음), 원장 review v2.2, CR-038·DEV-214까지
@path REL-003 WP 11/11 done / 릴리스 게이트 미통과 (Gate 4·5·6)
@path 미해결 리뷰 0건 (PR #33~#43 전수)
@path 다음: WP-029 착수 전 계약 감사 (REL-004)
@h2 Decisions
@p decisions.md의 "2026-08-26 대형 세션" 절 참조. 무거운 것 넷:
@b 팀 범위 동기화에 세션 advisory lock — 트랜잭션 락이면 GHE 왕복 내내
@p 트랜잭션이 열린다. 프로세스 mutex는 복제본이 여럿이라 경계가 아니다
@cmd 메타데이터 보강은 document_version을 올리지 않는다 — 불변 git 사실이라
@p 새/옛 개념이 없다. 기존 조건부 스크립트로는 대입이 안 되므로 전용 경로를 뒀다
@b direct_push는 근거를 요구한다 — first-parent + 매핑 없음. 매번 정본에서
@p 다시 계산해 매핑이 생기면 교정된다
@path REL-003 게이트를 미통과로 판정 — 지시서는 11/11 닫기를 지시했고 WP는 11/11
@p 이지만, 게이트는 별개다. 조건부 승인도 아니다
@h2 Changed files
@p files.md 참조. 이 세션의 커밋(머지 제외, 시간순):
@b 983e9d7 docs: agent-context 인계 갱신
@path a3c2cf2 docs: CR-037 등록 / 38578e9 팀 범위 직렬화 / 5c058c8 수동 복구 울타리
@b f928522 취소 CAS / dc2bceb 지문 접근 통제 / 2899393 스냅숏 부트스트랩
@path df95c72 docs: 원장 CR-037 / 4d6967b PR #41 리뷰 P1 셋
@path 2affb13 docs: CR-038 계약 경화 / 7fbdf77 논리 소비자 그룹·커밋 읽기
@path 4f6cd13 WP-067 구현 / b898f37 docs: 원장 WP-067 / e1ead79 PR #42 리뷰 다섯
@path 4d2bd86 docs: REL-003 마감 / d7cf78c docs: 게이트별 판정
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것: 통합 시험이 전역 질의를 쓰면 공유 prs_test의 다른 파일 데이터에 오염된다(3회 겪었다). ES deleteByQuery 앞 refresh는 또 나왔다.
@h2 Next steps
@todo WP-029 착수 전 계약 감사 → next-free CR(CR-039 예상, 실측할 것) → 구현
@b 릴리스 게이트 4·5·6은 별도 작업 — 이 환경에서 돌지 않는 항목이 섞여 있다
@path flow-001 e2e 간헐 실패 원인 규명 (WP-016 소관)
@h2 Risks/gotchas
@p risks.md 참조. 이 세션이 새로 배운 것: 변이가 살아남은 것이 세 번이었고 전부 "시험이 운영 경로가 아니라 내가 직접 부른 함수를 보고 있었다"였다.
@h2 References
@path PR #41 7060414 / #42 afdf1c2 / #43 3e3009b
@path CR-037·CR-038: docs/00_governance/change_control.md
@path DEV-191~214: 원장 5장 / 검증 기록: 원장 6.32·6.33·6.31.1장
@path 게이트 로그: docs/00_governance/change_control.md 4장
@path 전사(transcript): exports/202608261008.md — exports/는 .gitignore
@p 대상이므로 커밋에 딸려 가지 않는다 (2026-08-26 실측 정정)
@p ---
@h1 Session: 2026-08-26 (수직) — CR-039 · WP-029 참조 간선
@h2 Goal
@path 사용자의 3단계 지시서. 재실측 → housekeeping → WP-029 계약 감사 → CR-039 →
@p 구현 → 재파생 → 운영 배선 → 전 계층 검증 → PR → 리뷰 정정 → 머지 →
@path 머지 직후 재확인 → WP-029 done / REL-004 진행률. WP-030은 구현하지 않는다.
@h2 Current state
@path main = ea917b8 (PR #44 머지)
@b SRS baseline v2.5(변경 없음), 원장 review v2.3, 작업 패키지 v0.4
@path CR-039 closed, DEV-215~229 resolved
@path REL-004 구현 1/8 (WP-029 done)
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨
@path 미해결 리뷰 0건 (PR #1~#44 전수)
@h2 Decisions
@path decisions.md의 "CR-039" 절 참조. 무거운 것 넷:
@b 참조 간선의 정체성은 대상이 아니라 표현이다 — reference_key. link_id에
@path 가변 to_id가 들어 있으면 AC-3이 요구하는 "갱신"이 구조적으로 불가능하다
@path 직접 푸시 커밋에는 EVT-ING-003이 오지 않는다 — EVT-ING-005 신설.
@path CR-038이 DEV-206에서 잡은 것과 같은 모양이 한 홉 아래에 있었다
@b 한 source의 참조는 완전한 파생 집합 — 사라지면 지운다. 단 실패 회차는 아니다
@b retry 예산은 핸들러가 집행한다 — 어댑터는 상한을 보지 않는다.
@path release.ts가 실제로 무한 재시도 상태였다 (PR #30 P1, 미해결로 남아 있었다)
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@path 713bbed docs: agent-context 정리 / e327ccf docs: CR-039 계약 경화
@path 3224a00 feat: 참조 간선 파생·해결 / ab917ed fix: 재시도 예산 (DEV-228)
@b f5f0f5a test: 파생·조정·재파생 / 3648589 docs: 원장 6.34장
@path b99d326 fix: PR #44 리뷰 여섯 / 522d2bd docs: WP-029 done · REL-004 1/8
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것: 변이 시험이 내 결함을 찾아 줬다 (markPending의 reference_count: -1). 그리고 내 회귀 시험 하나가 틀린 것을 단언하고 있었다 — 리뷰가 지적한 순서를 시험이 굳히고 있었다.
@h2 Next steps
@todo WP-030 착수 전 계약 감사 → next-free CR(CR-040 예상, 실측할 것) → 구현
@b 릴리스 게이트 4·5·6은 별도 작업 — 이 환경에서 돌지 않는 항목이 섞여 있다
@path flow-001 e2e 간헐 실패 원인 규명 (WP-016 소관)
@path OD-005(nori) 사용자 결정 — 기한이 도래했다
@h2 Risks/gotchas
@p risks.md 참조. 이 세션이 새로 배운 것: 리뷰가 머지 전에 왔고 여섯 전부 실결함이었다. 그중 셋이 "실패했는데 아무도 다시 하지 않는다"의 변주다.
@h2 References
@path PR #44 ea917b8 — CR-039 / WP-029
@path CR-039: docs/00_governance/change_control.md (3장 대장 + 5장 반영 내역)
@path DEV-215~229: 원장 5장 / 검증 기록: 원장 6.34·6.34.1장
@path PR #30·#32의 묵은 스레드 3건도 함께 종결했다 (재실측으로 발견)
@p ---
@h1 Session: 2026-08-26 (3차) — CR-040 OD-005 · CR-041 / WP-030 관계 파생
@h2 Goal
@p 사용자가 준 3단계 지시서. "사용자의 추가 승인을 기다리지 말고 끝까지 수직으로 진행한다."
@path Phase A: OD-005 overdue decision을 별도 CR로 즉시 종결 → merge.
@path Phase B: WP-030 착수 전 계약 감사 → CR → 마이그레이션 → 구현 → 검증 → PR → 리뷰 정정 → merge → WP-030 done · REL-004 2/8.
@path WP-031 구현은 하지 않는다 — WP-031 preflight audit에서 멈춘다.
@h2 Current state
@b main = c4f8a39. 작업 트리 clean, origin 동기, 로컬 브랜치는 main 하나
@path SRS baseline v2.6(CR-040이 올렸다) · PRD v1.2 · 원장 review v2.6 · 작업 패키지 v0.6
@path CR-041 closed, DEV-247까지. 마이그레이션 014까지
@path REL-004 구현 2/8 (WP-029·WP-030 done)
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨
@path 미해결 리뷰 0건 (PR #1~#46 전수), 열린 PR 0건, open DEV 5건(전부 기존)
@h2 Decisions
@path decisions.md의 "CR-040"·"CR-041" 절 참조. 무거운 것 다섯:
@path nori를 초기 REL-004 의존성에서 제외 — FR-SRCH-011 AC-4는 분석기 구현이 아니라 행위를 정한다. 재검토 조건 셋(AND)으로 되돌릴 수 있게 남겼다
@b 후보(candidate)의 변화가 관계를 바꾼다 — 이 WP의 중심 계약. source-ready만 구독하면 관계가 수렴하지 않는다
@b reference_key를 일반화하지 않는다 — 대상이 나중에 밝혀지는 참조의 문제를 푸는 수단이고, 세 계열은 대상을 알아낸 뒤에 간선을 만든다
@b 계열마다 수명이 다르다 — reverts·cherry_picks는 제거, stacks_on은 detached
@path 마이그레이션 014를 만들었다 — CR-039가 "빈 마이그레이션을 만들지 않는다"고 한 것과 다른 판단이다. 후보 탐색 인덱스가 없으면 ADR-004가 이 축에서 깨진다
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@path 6917c17 docs: CR-040 OD-005 종결 (SRS v2.6) / c5de774 docs: PR #45 리뷰 셋
@path f19ac26 docs: CR-041 계약 경화 (DEV-230~247)
@b fe8458a feat(link): 되돌림·체리픽·스택 파생 / d05a2a3 test(link)
@path b06f636 docs: 원장 6.35장 / f712381 fix: PR #46 리뷰 여섯
@path c4f8a39 docs: WP-030 done · REL-004 2/8
@path 머지: PR #45 → 0a501f3, PR #46 → 926b888. 36파일 +4,059 / -147.
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것 셋:
@b 문서 검증기 --strict는 종료 코드 1이다(main에서도). 판정 기준은 exit 0이 아니라 main 대비 증감 0
@b 그 게이트는 자기 검색어를 센다 — 오탐을 설명하려 낱말을 인용하면 카운트가 늘어난다
@b SET LOCAL은 트랜잭션 안에서만 뜻이 있다. 풀의 query로 EXPLAIN 계획을 강제하려면 withTransaction으로 감싸야 한다
@h2 Next steps
@todo WP-031 착수 전 계약 감사 → next-free CR(CR-042 예상, 실측할 것) → 구현
@b 감사에서 이미 다섯을 찾아 뒀다 (todos.md A절) — 가장 무거운 것은 관계 조회 API가 존재하지 않는다
@b 릴리스 게이트 4·5·6은 별도 작업
@path flow-001 e2e 간헐 실패 원인 규명 (WP-016 소관)
@h2 Risks/gotchas
@p risks.md 참조. 이 세션이 새로 배운 것: 변이가 살아남았을 때 등가인지 묻기 전에 경로를 읽으라는 규율이 두 번 값을 했다 — M13과 R7 둘 다 살아남았고 둘 다 실결함이었다. 그리고 주석이 코드와 정반대를 적고 있는 자리를 리뷰가 잡았다.
@h2 References
@path PR #45 0a501f3 — CR-040 / OD-005
@path PR #46 926b888 — CR-041 / WP-030
@path CR-040·041: docs/00_governance/change_control.md (3장 대장 + 5장 반영 내역)
@path DEV-230~247: 원장 5장 / 검증 기록: 원장 6.35·6.35.1장
@path 전사(transcript): exports/202608262010.md — 실측 확인. /export 출력은 저장소 루트를 가리켰으나 실제 파일은 exports/에 있고 .gitignore 24행이 덮는다. 미추적 0건
@path worklog: Obsidian dailywork/2026-08-26_PR-Search-OD-005-종결과-WP-030-관계-파생.md
@p ---
@h1 Session: 2026-08-26 (4차) — CR-042 / WP-031 관계 조회·상세 화면
@h2 Goal
@p 사용자가 준 74절 지시서. "결정자가 승인한 범위 안에서 추가 승인 요청 없이 끝까지 수직으로 진행한다."
@path 재실측 → baseline flake 증거 보존 → WP-031 착수 전 계약 감사 → CR-042 → cascade →
@path API-REL-006 → API-REL-003 → W-001 배지 → W-002/W-003 관계 섹션 → 접근 통제 → 운영 도달성 →
@p 전 계층 검증 → adversarial mutation → PR → 리뷰 정정 → merge → 머지 직후 전수 재확인 →
@path WP-031 done · REL-004 3/8 → WP-032 preflight audit를 Next로 기록.
@path WP-032 구현은 시작하지 않는다.
@h2 Current state
@b main = 5d287b1. 작업 트리 clean, origin 동기, 로컬 브랜치는 main 하나, 미추적 0건
@b SRS baseline v2.6 (변경 없음) · PRD v1.2 · 원장 review v2.8 · 작업 패키지 v0.8
@path CR-042 closed, DEV-265까지. 마이그레이션 014까지 — 이번 WP는 만들지 않았다
@path REL-004 구현 3/8 (WP-029·WP-030·WP-031 done)
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨
@path 미해결 리뷰 0건 (PR #1~#47 전수), 열린 PR 0건, open DEV 5건(전부 기존)
@path API-REL-006 신설. 기존 API-REL은 001~005였다
@h2 Decisions
@path decisions.md의 "CR-042" 절 참조. 무거운 것 다섯:
@b 조회는 파생과 다른 질문이다 — 이 세션의 중심. 파생에는 요청자가 없어 저장소 안에서 끝나지만, 조회에는 요청자가 있고 대상 쪽을 본다. 감사 열여덟 중 아홉이 이 하나의 공백에서 나왔다
@b 역방향 조회는 저장소 라우팅을 포기한다 — 간선은 근거를 소유한 저장소(source)에 살기 때문에 대상으로 라우팅하면 cross-repo 참조를 구조적으로 놓친다. 경계는 라우팅이 아니라 강제 필터가 만든다
@b 방향마다 규칙을 가르지 않는다 — outgoing은 라우팅해도 옳지만, 갈라 두면 다음 사람이 incoming에도 "최적화"로 넣는다
@b 워커의 조회 형태를 HTTP에 재사용하지 않는다 — scrollLinks는 끝까지 읽고 StoredLink에는 confidence·evidence가 없다. 사용자 대면 읽기 투영을 따로 뒀다
@path 가드레일 면제는 파일 단위다 — links.ts에 사용자 대면 조회를 넣으면 워커용 면제를 물려받는다. 조회 계층을 그 파일 밖에 뒀다 (DEV-265)
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@path 60181f3 docs: CR-042 계약 경화 (DEV-248~265)
@b 96bdcba feat(relations): 저장된 간선 조회와 상세 화면 관계 섹션
@b 9ca089f test(relations): 접근 통제·정확성·도달성·표시 규칙
@b cb4254e docs: 원장 6.36장 — 검증 기록과 e2e 귀속 실측
@path c1fd36b fix: PR #47 리뷰 P1 — 부분 관계 검색 거절
@path 5d287b1 docs: WP-031 done · REL-004 3/8 · CR-042 종결
@path 머지: PR #47 → 9084033.
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것 넷:
@b e2e 귀속은 되돌려서 재라. 화면 코드를 바꾼 WP에서는 "diff 0" 근거를 쓸 수 없다. git stash로 화면을 main과 같게 만들고 같은 횟수를 돌려 비교한다
@b 전량 실행 출력을 파일로 보존한다. 직전 세션이 실패 시험 이름을 잃었던 자리다 — 이번에는 baseline부터 tee로 남겼고 그 덕에 같은 시험임을 즉시 대조했다
@b 금지를 거는 회귀는 주석을 걷어 낸 코드만 봐야 한다. 파일 전문에 걸면 그 사실을 설명한 주석에 걸린다
@b 대역이 요청을 되돌려주게 하라. 고정 응답을 주는 대역은 실제 서버보다 관대해 네 유형이 전부 같은 것으로 그려졌다
@h2 Next steps
@todo WP-032 착수 전 계약 감사 → next-free CR(CR-043 예상, 실측할 것) → 구현
@path 감사에서 물을 것 넷을 원장 §8에 남겼다 — 가장 무거운 것은 커서 계약이 실재하지 않는다(ADR-010은 "커서 전용"만 정하고 재료·불안정성 처분·동률 처리가 없다)
@b 릴리스 게이트 4·5·6은 별도 작업
@path flow-001·flow-003 e2e 간헐 실패 원인 규명 (WP-016 소관) — 이번에 뒤로가기 복원 타이밍이라는 공통 모양을 확인했다
@h2 Risks/gotchas
@p risks.md 참조. 이 세션이 새로 배운 것: 선례를 따르지 않은 새 파일이 검사에서 빠진다. 다른 조회 경로가 전부 assertNoShardFailures를 지나는데 새로 만든 파일만 빠졌고, 하필 라우팅을 포기해 샤드 전부를 도는 질의였다 — 내가 내린 결정이 그 실패 모드의 확률을 올렸는데 대가를 계산하지 않았다. 리뷰가 잡았다.
@h2 References
@path PR #47 9084033 — CR-042 / WP-031
@path CR-042: docs/00_governance/change_control.md (3장 대장 + 5장 반영 내역)
@path DEV-248~265: 원장 5장 / 검증 기록: 원장 6.36·6.36.1장
@b e2e 귀속 실측: 원장 6.36장 표 + §7 갱신
@todo 전사(transcript): 예정 경로 exports/202608262224.md — pending /export (사용자가 실행해야 확정된다). exports/는 .gitignore 24행 대상이라 커밋에 딸려 가지 않는다. 기존 전사 다섯이 모두 YYYYMMDDHHMM.md다
@p ---
@h1 Session: 2026-08-26 (5차) — CR-043~047, 계약 경화 5연발
@h2 Goal
@p 사용자가 준 2단계 지시서. "사용자에게 추가 기술 승인을 요청하지 않는다. 아래 결정은 승인된 것으로 간주한다."
@path Phase A: WP-032 착수 전 감사 → CR-043 → PR/리뷰/머지
@path Phase B: WP-035 착수 전 감사 → CR → 구현 → 검증/변이 → PR/머지 → REL-004 4/8
@path 지시서가 정정을 하나 담고 있었다 — "PR #48은 존재하지 않는다. 이전 handoff의 'PR #1~#48 전수'를 복사하지 마라." 실측하니 맞았다(당시 47개). 인계 문서의 숫자를 그대로 옮기면 다음 인계도 틀린다.
@p 세션 도중 사용자가 두 번 개입했다: "기다리면 되나?"(CI 큐 대기 상황 질문), 그리고 "CI 없이 머지해라"(게이트 완화 승인).
@h2 Current state
@b main = 99e2532. 작업 트리 clean, origin 동기, 로컬 브랜치는 main 하나, 미추적 0건
@b SRS baseline v2.8 · PRD v1.3 · 원장 review v3.4 · 작업 패키지 v1.1 · 추적 매트릭스 v0.5
@path CR-047 closed, DEV-312까지. 마이그레이션 014까지 (이 세션은 만들지 않았다)
@path REL-004 구현 3/8 — WP-035는 계약과 배포 자리만 서고 본체는 미구현
@path 미해결 리뷰 0건 (PR #1~#51 전수), 열린 PR 0건
@path open DEV 8건 — 기존 5(DEV-001·006·010·016·026) + 신규 3(DEV-304·305·306)
@h2 Decisions
@path decisions.md의 "CR-043"~"CR-047" 절 참조. 무거운 것 여섯:
@path SRS를 두 번 올렸다 (v2.6 → v2.7 → v2.8). 최근 CR 여덟 중 일곱이 correction이었는데 CR-043은 범위를 넓혔다 — W-004의 패싯·커서는 SRS가 승인한 적이 없었다. 지우는 선택도 있었으나 PRD SCN-002가 이미 서술하고, 5만 건 구간에 페이지 200이면 끝까지 훑을 방법이 없고, 그 자리를 파다 실제 결함이 나왔다
@path 커서 봉인 값을 "완결 서수"로 정의했다 — 그 서수 이하의 일치를 모두 반환했음이 보장되는 지점. DEV-270("전진하지 못한다")과 DEV-287("너무 멀리 간다") 두 실패가 하나의 규칙에서 나온다
@b PIT을 정렬 키와 무관하게 모든 순회에 연다 — 키별로 가르는 안 기각. 판정이 필요한 규칙은 판정하는 사람이 틀릴 때마다 깨지고, 그 판정의 첫 시도가 이미 틀렸다
@path WP-032의 선행에 WP-035를 넣었다. WP ID를 재번호화하지 않는다 — 순서를 정하는 것은 번호가 아니라 의존이다
@b batch manifest 신설은 새 역할을 만드는 것이 아니라 승인된 배포 단위를 실재시키는 것이다
@b 미배포 역할 셋(authz·release·backfill)을 고치지 않고 등재만 했다 — 각자 다른 WP의 소산이고 release는 미러 PVC 배치가, authz는 위협 모델이 함께 필요하다
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@path e730f6d docs: CR-043 계약 경화 (DEV-266~285, SRS v2.7)
@path f768bba docs: CR-044 리뷰 다섯 정정 / d55eccf fix: AC-7 자체 정정 (SRS v2.8) / 6abc1ed docs: §7 정정
@path 50797c6 docs: CR-045 계약 경화 / dcff79e feat(deploy): batch 배포·역방향 회귀 / 23781b2 docs: 원장 6.38장
@path 2d30627 docs: CR-046 리뷰 넷 정정 / 6129f33 fix: CR-047 예외 범위 정정
@path 머지: PR #48 → 3248559, #49 → 60402d8, #50 → f785115, #51 → 99e2532.
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것 다섯:
@b 실제 ES로 계약 주장을 증명할 수 있다. curl로 probe 인덱스를 만들어 edge_ngram 두 벽을 확인했다 — 문서만 읽고 "아마 안 될 것"이라고 적지 않았다
@path 임시 통합 시험으로 결함을 재현하고 지운다. apps/search-api/integration/sequence/_repro-w004.test.ts를 써서 DEV-270을 재현하고 로그를 남긴 뒤 삭제했다
@b git checkout --는 커밋 전 변경분을 통째로 되돌린다. 변이 원복에 썼다가 아직 커밋하지 않은 회귀 추가분까지 잃었다 — 변이는 역방향 치환으로 되돌린다
@b edit.py after를 제목에 걸면 그 제목 *뒤*에 들어간다. 두 번 밟았다(change_control 캐스케이드, async 3.5장). 새 절은 다음 제목을 앵커로 replace 하는 편이 안전하다
@b CI는 커밋 SHA의 check-runs로 본다. run 수준 startup_failure인데 job은 success인 경우가 있었다
@h2 Next steps
@path WP-035 본체 구현 — 계약은 원장 6.38장·비동기 3.5장에 섰고 러너가 살 자리(batch 배포)도 생겼다
@path DEV-306 authz 미배포 — 접근 통제 축. 별도 CR로 위협 모델과 함께
@path DEV-305 release, DEV-304 backfill 미배포
@b 릴리스 게이트 4·5·6은 별도 작업 — 이 환경에서 돌지 않는 항목이 섞여 있다
@h2 Risks/gotchas
@p risks.md 참조. 이 세션이 새로 배운 것: "적어 둔 규칙을 내가 어긴다"가 리뷰 라운드 넷에서 반복됐다. 막아야 할 경주를 표로 적고 울타리를 짧게 쳤고, 정본이 무엇인지 WP에 적고 구현은 절반만 읽었고, "표에 먼저 적지 않는다"를 문서에 쓰고 시험이 강제하지 않았다. 문서에 규칙을 적었으면 그것을 강제하는 시험이 있는지 같은 pass에서 물어라.
@h2 References
@path PR #48 3248559 — CR-043 / WP-032 계약
@path PR #49 60402d8 — CR-044 (CI 없이 병합)
@path PR #50 f785115 — CR-045 / WP-035 계약 + batch 배포
@path PR #51 99e2532 — CR-046·CR-047
@path CR-043~047: docs/00_governance/change_control.md (3장 대장 + 5장 반영 내역)
@path DEV-266~312: 원장 5장 / 검증 기록: 원장 6.37 · 6.37.1 · 6.38 · 6.38.1장
@path 전사(transcript): exports/202608270742.md (119KB, 07:42 — 실측 확인). /export 출력은 저장소 루트를 가리켰으나 실제 파일은 exports/에 있고 .gitignore 24행이 덮는다. 이전 세션들이 두 번 틀리게 적은 자리다 — 경로 주장은 ls로 확인한다. 기존 전사 다섯도 같은 자리(202608251453 · 202608260047 · 202608261008 · 202608261327 · 202608262010 · 202608262226)
@p ---
@h1 Session: 2026-08-27 — WP-035 무중단 재색인 구현 · CR-048 authz 배포
@h2 Goal
@p 사용자가 준 55절 지시서. "진척률 우선 / 핵심 검증만 / 추가 승인 요청 금지."
@path Phase A: WP-035 본체 구현 → 핵심 검증 → PR → 리뷰 정정 → merge → REL-004 4/8
@path Phase B: DEV-306 authz 배포 도달성을 작은 별도 수직으로 (WP-032보다 먼저)
@b 검증 정책이 이 세션부터 바뀌었다 — 전 계층 배터리를 WP마다 기계적으로 반복하지 않는다. 사내 내부망 이전 후 재점검할 것이 많으므로 고위험 축만 직접 검증한다
@path WP-035에 대해 또 광범위한 preflight audit를 반복하지 않는다. 계약은 CR-045~047에서 닫혔고 이 세션은 그 구현이다
@h2 Current state
@b main = df9f569. 작업 트리 clean(전사 파일 하나 미추적 — 아래 참조), origin 동기, 로컬 브랜치는 main 하나
@b SRS baseline v2.8(변경 없음) · PRD v1.3 · 원장 review v3.7 · 작업 패키지 v1.2 · 인프라 v0.5 · API 계약 v0.8 · 로드맵 v0.5
@path CR-048 closed, DEV-323까지. 마이그레이션 014까지(이 세션은 만들지 않았다)
@path REL-004 구현 4/8 (WP-029·030·031·035 done). WP-032 차단 해제
@path REL-003 릴리스 게이트 여전히 미통과 — 베타 공개 승인 안 됨
@path 미해결 리뷰 0건 (PR #52·#53·#54 전부 resolve). 열린 PR 0건
@path open DEV 7건 — 기존 5(DEV-001·006·010·016·026) + DEV-304·305 (DEV-306은 CR-048로 resolved)
@h2 Decisions
@path decisions.md의 "WP-035 구현"·"CR-048" 절 참조. 무거운 것 여섯:
@path 재구축이 운영 이중 쓰기 경로를 그대로 지난다 — 이 세션의 중심 판단. shadow 전용 쓰기 함수를 따로 두면 그것이 곧 "두 번째 문서 생성 경로"이고(DEV-295가 금지), 평시 투영이 바뀔 때 함께 바뀌지 않는다. serving은 같은 document_version이라 noop으로 끝난다
@b 울타리 키가 하나다(reindex:fence) — 논리 쓰기 하나가 여러 별칭에 걸치므로 별칭별 키는 락 여럿을 순서대로 잡게 만든다(교착). 동시 실행 상한이 1이라 키 하나가 같은 배제 집합이다
@b 공유·배타로 가른다 — 논리 쓰기는 공유(서로 안 막음), 활성화·전환은 배타. 평시 처리량이 그대로다
@path OPERATOR_JOB_TYPES를 둘로 갈랐다 — 계약이 reindex 등재(DEV-301)와 API-ADM-002 생성 차단(DEV-302)을 동시에 요구하는데 하나의 목록이 둘을 함께 쓰고 있었다
@b 커버리지는 재구축이 쓴 문서 id 수로 판정한다(리뷰 P1) — source 수로 판정하면 간선이 영원히 전환하지 못한다. 셀 수 없는 축(prs-links)은 null로 두어 그 항목을 판정하지 않는다
@path CR을 하나만 열었다 — WP-035 본체는 CR 없이(계약이 이미 닫혔다), DEV-306은 CR-048로. 지시서 §0의 기준을 그대로 적용했다
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@b 9fd391d feat(es,db): 이중 쓰기 대상 해석과 재색인 울타리 — 열일곱 경로 전부
@path ca9c661 feat(reindex): JOB-ING-006 러너 · API-ADM-004 · CLI
@path d294fc2 test(reindex): 전환·이중 쓰기·보관·WP-032 선행 조건
@path 725270f docs: WP-035 done · REL-004 4/8 (원장 6.39장, DEV-313)
@path 8a628dc fix: PR #52 리뷰 다섯 (DEV-314~319)
@path 49d2ad6 fix: 재구축의 버전 정본은 본문이 아니라 열이다 (DEV-320)
@path d9486f0 fix(deploy): authz 역할 배포 (CR-048, DEV-306)
@path 510f696 fix: PR #53 리뷰 둘 (DEV-321·322)
@path 9b581a5 docs: 상태 헤더 갱신 누락 (DEV-323)
@path 머지: PR #52 → efea3c0, #53 → f4e8ab6, #54 → df9f569. 57파일 +5,056 / −528
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것 넷:
@b open(path, 'w')는 인자 검증보다 먼저 truncate한다. 잘못된 newline 값으로 열었더니 ValueError가 나기 전에 파일이 0바이트가 됐다 — 방금 쓴 통합 시험 전체를 다시 썼다
@path git checkout --를 또 밟았다. 이번엔 변이 원복이 아니라 "복구"에 썼다가 미커밋 문서 편집을 잃었고, 같은 스크립트를 다시 돌려 복구하려다 멱등이 아니라 표 행이 두 줄이 됐다(DEV-321)
@b CI가 로컬 통합 전량이 놓친 결함을 잡았다 — 공유 prs_test의 내용이 환경마다 다르다. 로컬에는 그 형태의 스냅숏이 없었다
@b 변이 스크립트에 치환 건수 검증이 없으면 "SURVIVED"가 오독이 된다. CRLF 때문에 치환이 아예 안 걸린 것을 살아남은 것으로 읽을 뻔했다
@h2 Next steps
@path WP-032 착수 — 패싯·커서 페이지네이션·전문 검색. 계약은 CR-043·044에서 닫혔고 WP-035가 edge_ngram 배포 기계를 세웠다. 착수 전 감사를 다시 하지 않는다(계약이 이미 닫혔다) — 지시서 §0의 기준으로만 CR을 연다
@path DEV-305 release 배포 (미러 PVC 배치 필요, WP-024 소관)
@path DEV-304 backfill 배포 (enrich 파드 역할 구성 판단 필요, WP-019 소관)
@b 저장소 루트의 전사 파일 정리 (아래 Risks 참조)
@b 릴리스 게이트 4·5·6은 별도 작업 — 이 환경에서 돌지 않는 항목이 섞여 있다
@h2 Risks/gotchas
@p risks.md 60~64번 참조. 이 세션이 새로 배운 것: 리뷰가 지적한 일곱이 전부 실결함이었고 그중 다섯이 한 뿌리였다 — "판정의 재료가 그 판정이 뜻하는 것과 같은가." 처리한 source 수는 문서 수가 아니고, 지우지 못한 것은 처리한 것이 ... 환이 아니다. 그리고 스냅숏은 정본이 아니라 투영 시점의 사본이라는 사실을 축마다 다시 묻지 않아 접근 통제 유출을 만들 뻔했다.
@h2 References
@path PR #52 efea3c0 — WP-035 무중단 재색인
@path PR #53 f4e8ab6 — CR-048 / DEV-306 authz 배포
@path PR #54 df9f569 — 문서 상태 헤더 정정 (DEV-323)
@path CR-048: docs/00_governance/change_control.md (3장 대장 + 5장 반영 내역)
@path DEV-313~323: 원장 5장 / 검증 기록: 원장 6.39 · 6.39.1장
@path 계약: 비동기 3.5장(JOB-ING-006), API 계약 ### API-ADM-004
@path worklog: Obsidian dailywork/2026-08-27_PR-Search-WP-035-무중단-재색인-구현과-authz-배포-정정.md
@path 전사(transcript): pr-search/202608271346.md (저장소 루트, 99KB) — /export에 상대 경로를 줘서 루트에 떨어졌다. .gitignore 24행은 exports/만 덮으므로 이 파일은 무시되지 않는다(git check-ignore로 실측). exports/로 옮기거나 지워야 한다
@p ---
@h1 Session: 2026-08-27 (2차) — 위생 정정 둘 + WP-032 완주
@h2 Goal
@p 사용자가 준 44절 지시서. "Codex는 quota 소진 — 호출하지 마라. 추가 승인 없이 끝까지 수직 진행."
@path Phase 1 pre-WP032 hygiene → Phase 2 WP-032 전체 → 검증 → PR → 리뷰 정정 → merge → 기록.
@h2 Current state
@b main = 2d28074. 작업 트리 clean, origin 동기, 로컬 브랜치는 main 하나, 미추적 0건
@path (WP-032 병합은 3237b6c, 2d28074는 그 뒤의 인계 기록 커밋. main CI 양쪽 통과)
@b SRS baseline v2.8(변경 없음) · 원장 review v4.1 · 작업 패키지 v1.4 · 로드맵 v0.6
@path CR-048(변화 없음) · DEV-332까지 · 마이그레이션 014까지
@path REL-004 구현 5/8 (WP-029·030·031·035·032 done)
@path 미해결 리뷰 0건 (PR #55·#56·#57). 열린 PR 0건
@path open DEV 7건 — 기존 5 + DEV-304·305
@h2 Decisions
@p decisions.md 참조. 이 세션의 무거운 것 일곱:
@path 스냅숏을 갈아 끼우지 않고 없앴다 — CLAUDE.md·AGENTS.md·docs/README.md·원장 1장·8장이
@p "현재 상태"를 적고 있었다. 최신 숫자로 바꾸면 다음 WP에서 같은 결함이 난다
@path 매핑 버전을 올렸다(v1 → v2) — WP-032의 partial 필드는 살아 있는 인덱스에 못 넣는다.
@path 그리고 applyMappings가 옛 버전을 든 별칭을 침범하지 않게 막았다 (DEV-328)
@path missing: '_last'를 명시적 센티널로 바꿨다 (DEV-329) — 그 값은 커서로 되먹일 수 없다.
@p format 셋을 다 재도 우회되지 않았다
@b PIT을 모든 순회에 연다 — 정지된 시험 인덱스로는 이것을 관측할 수 없어 시험을 두 번 고쳤다
@path W-001과 W-004의 커서를 합치지 않았다 — 순회하는 정본이 다르다
@path resolveTeamIds가 ID를 전부 돌려준다 (DEV-331) — 주석이 오래 약속하고 구현이 지키지
@path 않던 것. WP-068이 이월해 둔 자리의 만기가 이 WP였다
@b CR을 하나도 열지 않았다 — 지시서 §0의 세 조건 중 어느 것도 실물로 증명되지 않았다
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@path 8c75cf2 agent-context 보존 / ebc8b71 DEV-324 스냅숏 제거 (PR #55)
@path 8fe4122 DEV-325·326 진입점 둘과 원장 1장 (PR #56)
@path 2912ca0 백엔드 / 984c06a 화면 / cb8b7d5 DEV-327~329 / 08b9865 시험 /
@p 9f07d4e 변이가 드러낸 구멍 / 7834ee5 문서 / fda1459 CI 정정 /
@path cb5b9e8 리뷰 셋 (PR #57)
@path 머지: #55 → f1c28f9, #56 → 437be3d, #57 → 3237b6c
@b 2d28074 agent-context 기록 — PR 없이 main에 직접 push했다 (risks 73)
@h2 머지 직후 재확인 (실측)
@p | 항목 | 값 | | --- | --- | | 열린 PR | 0 |
@path | 미해결 리뷰 | 0 (PR #55·#56·#57 전수) |
@p | main CI (2d28074) | verify success · integration success | | 단위 / 회귀 | 1481 / 194 | | typecheck · lint · lint:deps · build | 통과 |
@path | open DEV | 7 (기존 5 + DEV-304·305) |
@path | REL-004 | 5/8 — WP-029·030·031·035·032 done |
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것 넷:
@b run_in_background에 nohup ... &를 겹치면 두 실행이 겹친다. 바깥 셸이 즉시 0으로
@p 끝나 "완료" 알림이 오고, 그 뒤에 시작한 실행과 DB를 공유해 76건이 거짓 실패했다
@path 통합 결과가 이상하면 prs_test를 drop/create 하고 한 번만 돌린다
@b ES는 와일드카드 삭제를 거절한다 (action.destructive_requires_name) — 이름을 지목한다
@b 변이 하니스는 mutate_many가 필요하다 — 한 자리만 바꾸면 등가가 되는 결함이 있다
@h2 Next steps
@path WP-033 저장된 검색. 계약이 닫힌 적이 없으므로 착수 전 감사가 필요할 가능성이 높다
@b 배포에서 SEARCH_CURSOR_HMAC_KEY와 es:reindex 둘이 선행한다 (todos.md B절)
@path DEV-305 release / DEV-304 backfill 미배포는 그대로
@b 릴리스 게이트 4·5·6은 별도 작업
@h2 Risks/gotchas
@p risks.md 67~72번 참조. 이 세션이 새로 배운 것: 살아남은 변이 다섯 중 셋이 시험 구멍이었고 둘이 등가였다. 그 다섯을 "등가겠지"로 넘겼다면 PIT도, 매핑 버전도, 부트스트랩 방어도 아무 시험이 지키지 않는 상태로 병합됐다. 그리고 리뷰가 잡은 셋 중 하나는 문서가 코드보다 앞서 있던 자리였다 — 주석이 약속한 것을 구현이 지키지 않았고,
@path 그 주석 자신이 "WP-032가 다룬다"고 이월을 적어 두었다.
@h2 References
@path PR #55 f1c28f9 — DEV-324 / PR #56 437be3d — DEV-325·326 / PR #57 3237b6c — WP-032
@path DEV-324~332: 원장 5장 / 검증 기록: 원장 6.40장
@b 계약: API 계약 「커서 계약」·「패싯 계약」·「전문 검색 계약」·「구간 커서와 패싯」
@h2 전사 아카이브
@todo 예정 경로 exports/202608271750.md — pending /export (사용자가 실행해야 확정된다).
@p 스킬 기본값(<루트>/pr-search-2026-08-27.md) 대신 저장소 관행을 따른다. .gitignore 24행이 exports/를 덮고 기존 전사 여덟이 전부 YYYYMMDDHHMM.md다. 루트에 두면 무시되지 않아 git add -A에 딸려 간다 — 직전 세션이 실제로 그렇게 99KB를 루트에 떨어뜨렸고 옮겨야 했다 (risks 60~64 절의 환경 항목).
@p /export가 출력하는 경로는 저장소 루트를 가리킬 수 있다. ls로 실제 위치를 확인한다 — 이전 세션들이 두 번 틀리게 적은 자리다.
@p ---
@h1 Session: 2026-08-27 (3차) — CR-049 계약 종결 + WP-033 저장된 검색 완주
@h2 Goal
@path 사용자의 지시서: "이번 세션은 WP-033 착수 전 감사에서 멈추는 세션이 아니다" —
@path 감사 → CR-049 → 캐스케이드 → 문서 PR 병합 → fresh main에서 구현 → 마이그레이션 015 →
@todo API-SRCH-005 → W-001/W-008 → 검증 → 리뷰 정정 → 병합 → 원장 기록 → Next = WP-034.
@p 제품 결정은 지시서가 미리 승인했다(대상 팀 하나·team_id 정체성·소유자만 수정·
@path 질의 문자열만 저장·PostgreSQL 키셋 커서·/run과 /share-targets를 API-SRCH-005 하위로).
@h2 Current state
@b main = 700d825. 작업 브랜치 없음(둘 다 병합·삭제). main CI 통과
@path SRS baseline v2.9 · CR-049까지 · DEV-349까지 · 마이그레이션 015까지
@path REL-004 6/8 (WP-029·030·031·035·032·033 done). 남은 것은 WP-034·WP-036
@path 미해결 리뷰 0건 (PR #59·#60). 열린 PR 0건. open DEV 8건
@h2 Decisions
@p decisions.md 참조. 이 세션의 가장 무거운 결정 셋:
@b team 공개 범위는 팀 하나를 명시적으로 지정한다 — 승인된 기능이 "누구에게
@p 공유하는가"를 말한 적이 없었고, 그 하나의 빈칸이 소유권·이탈 후 처분·스키마 불변식 셋을 함께 비워 두고 있었다
@b /run이 검색을 대신하지 않는다 — 지킬 규칙을 없애는 편이 규칙을 지키는 것보다
@p 안전하다. 저장된 검색이 접근 통제 경로에 아예 참여하지 않게 했다
@path DEV-349는 고치지 않고 남겼다 — FR-SEQ-005 AC-4가 저장된 검색을 지목하지만
@p 그것을 성립시킬 계약의 재료가 없다. 승인 없이 정하면 그것이 승인 없는 제품 결정이다
@h2 Changed files
@p files.md 참조. 이 세션의 커밋(머지 제외, 시간순):
@path 7d6fd20 docs: CR-049 / 8f70372 docs: PR #59 리뷰 셋 / eabf97c test: DEV-346
@b 652f4f9 feat: 정본·API·런타임 / 6bd23a1 fix: 시험 구멍 + CASCADE
@path f8c5710 feat(web): W-001·W-008 / 9478579 docs: WP-033 done
@path 61ec1e4 fix: PR #60 리뷰 넷
@h2 Commands
@p commands.md 참조. 새로 배운 것 셋:
@b 새 표를 만들면 전 계층 통합을 돌려라 (단일 파일로는 외래 키 충돌이 안 나온다)
@b 잠금 배타를 재려면 외래 키가 잡는 것과 같은 잠금(FOR KEY SHARE)으로 밖에서 잡아라
@b 지우는 변이는 주석으로 치환하라 (빈 문자열은 원복할 수 없다)
@h2 Next steps
@path WP-034 W-009 저장소 개요 화면 — 착수 전 감사부터. 감사할 때 대상 FR의 ID를
@path 다른 문서에서 역방향으로 grep 한다(DEV-349가 그것을 놓쳐 생겼다)
@path DEV-349는 별도 CR — 해법이 조회 계층일 수 있으므로 그 판단이 먼저다
@path 배포: 마이그레이션 015 적용 + WP-032가 남긴 두 전제
@h2 Risks/gotchas
@p risks.md 74~80번. 요약하면:
@b 살아남은 변이가 등가가 아니었고, 그것을 막는 첫 시험도 틀렸다 (74)
@b 빈 표는 어떤 정책 위반도 드러내지 않는다 (75)
@b 감사 대상 FR만 읽으면 그 자원을 지목하는 다른 FR을 놓친다 (76)
@b 결정을 적는 것과 그 결정이 성립하는 것은 다른 일이다 (77)
@h2 References
@path PR #59 (CR-049 문서, ee16ba0) · PR #60 (WP-033 구현, 700d825)
@b 인계 기록 커밋 93841d5 (agent-context 직접 커밋 — 관행)
@path 원장 6.41·6.41.1장 · change_control.md CR-049 반영 내역
@path 전사(transcript): exports/202608280030.md — /export가 저장소 루트에
@p 떨어뜨린 것을 옮겼다. .gitignore 24행이 exports/를 덮으므로 커밋에 딸려 가지 않는다. 이번에도 루트에 떨어졌다 — todos G절의 경고가 유효하다는 증거이며, /export에 절대 경로를 주어도 같은 결과일 수 있으니 실행 뒤에 ls로 확인하는 것이 유일하게 믿을 만한 방법이다
@path 직전 세션 전사: exports/202608271806.md
@p ---
@h1 Session: 2026-08-28 — CR-050 계약 종결 + WP-034 W-009 저장소 개요 완주
@h2 Goal
@path 사용자가 준 42절 지시서. "이번 세션은 WP-034 감사만 하고 멈추는 세션이 아니다 — 수직으로 끝낸다."
@path 재실측 → 개발 DB 정합 → 착수 전 감사 → CR-050 → 캐스케이드 → 문서 PR 병합 → fresh main에서 구현 →
@path 마이그레이션 016 → API-ING-002·003 → W-009 → 진단 경로 연결 → WP-068 이월 정정 → API-ADM-006 정정 →
@todo 적대적 검증 → PR/리뷰/병합 → 원장 기록 → Next = DEV-349.
@path 제품 결정은 지시서가 미리 승인했다(W-009는 general-user 유지, API-ADM-001/006 authz 완화 기각,
@path 신규 general-user API 승인, 등록 요청 SoR 승인, migration 016 승인, DEV-349는 이번에 섞지 않음).
@p 세션 도중 PC가 재부팅으로 중단됐다. 작업 파일은 전부 살아 있었고 컨테이너만 내려가 있었다 — docker start 셋으로 복구하고 중단 지점(typecheck)부터 이어갔다.
@h2 Current state
@b main = ea08c57. 작업 브랜치 없음(둘 다 병합·삭제). main CI 양쪽 통과
@path SRS baseline v2.10 · CR-050까지 · DEV-358까지 · 마이그레이션 016까지
@path REL-004 7/8 (WP-029·030·031·035·032·033·034 done). 남은 것은 WP-036 하나
@path 미해결 리뷰 0건 (PR #61·#62). 열린 PR 0건. open DEV 8건(변화 없음)
@h2 Decisions
@p decisions.md 참조. 이 세션의 가장 무거운 결정 넷:
@path 권한을 완화하지 않고 경로를 세웠다 — API-ADM-006을 여는 쉬운 길이 있었지만 그 API가 담는
@path 것은 저장소를 식별하지 않는 조직 전체 수치이고 그 성질 때문에 FR-AUTH-002 AC-5의 예외로
@path 승인됐다. W-009가 필요로 하는 것은 정반대로 저장소별 정보다 — 섞으면 그 예외의 근거가 무너진다
@b 등록 요청은 GHE에 묻지 않는다 — 확인하면 그 응답이 곧 비공개 저장소의 존재 신탁이 된다.
@p 그래서 모듈에 GitHub 클라이언트를 주입조차 하지 않았다. 부를 수 없으면 실수로 부를 일도 없다
@b allowedTeamIds를 필수 인자로 올렸다 — 선택 인자가 빠지면 오류 없이 좁게 답하고, 그 실패는
@p fail-closed라 접근 통제 시험이 전부 통과한 채로 다섯 WP를 살았다. 타입으로 막고 parity를 걸었다
@b 접근 범위 판정을 SQL로 옮기지 않았다(리뷰 P2 뒤에도) — 규칙이 두 곳에 살면 한쪽만 넓어지는
@p 날 아무 오류도 안 난다. 대신 스캔 상한에서 커서를 남기는 쪽으로 고쳤다
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@path 3f6bb99 docs: CR-050 계약 종결 (DEV-350~356, SRS v2.10) — PR #61
@b 65b2d13 feat(db): 마이그레이션 016
@path 5bba023 fix: DEV-353·354·352 정정
@path e99c23a feat(api): API-ING-002·003 + runtime
@path ac6aef4 feat(web): W-009 + 진단 경로
@path 04e5fdb docs: WP-034 done · REL-004 7/8 (원장 6.42장)
@path cdfaafc fix: PR #62 리뷰 둘 (DEV-357·358)
@path 머지: PR #61 → 1f7ccf2, PR #62 → ea08c57.
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것 셋:
@b cat >>로 덧붙이면 CRLF 저장소에 LF가 섞인다. core.autocrlf=true 환경에서 워킹 디렉터리는
@p CRLF인데 덧붙인 부분만 LF가 되어 편집 앵커가 조용히 어긋난다. 변이가 걸리지 않았는데 시험은 통과했고, 치환 건수 검증이 없었다면 그것을 SURVIVED로 읽었을 것이다
@b 재부팅 뒤에는 컨테이너부터 확인한다. 작업 파일은 살아 있어도 docker ps가 비어 있으면
@p 통합 시험이 통째로 실패한다. docker start prs-postgres prs-redis prs-elasticsearch 뒤 elasticsearch가 healthy가 될 때까지 기다려야 한다
@b toAccessScope는 저장소 500개 이하를 언제나 explicit으로 준다. HTTP 경로로 org_team
@p 표현을 만들려면 픽스처에 501개가 필요하므로, parity는 함수 수준에서 거는 편이 읽기도 쉽다
@h2 Next steps
@path DEV-349 좁은 계약 종결 — 계약의 재료 셋이 없다. 해법이 조회 계층일 수 있으므로 그 판단이 먼저다
@path 그 뒤 WP-036 원본 아카이브 레인 → REL-004 구현 8/8
@path 배포: 마이그레이션 016 적용. WP-032가 남긴 두 전제(SEARCH_CURSOR_HMAC_KEY, es:reindex)는 그대로
@b 릴리스 게이트 4·5·6은 별도 작업
@h2 Risks/gotchas
@path risks.md 81~85번 참조. 이 세션이 새로 배운 것: 적어 둔 계약을 내 구현이 절반만 따랐다(DEV-358).
@path 같은 CR에서 신설한 QA 항목인데 W-001·W-005만 연결하고 두 상세 화면을 빠뜨렸다. 그리고 설계 판단의
@path 대가를 계산하지 않았다(DEV-357) — 접근 범위 판정을 애플리케이션에 두는 것은 옳았지만 그러면 한
@p 요청이 정본을 다 훑지 못한다는 결과가 따라오고, 그것을 처리하지 않아 저장소가 조용히 사라졌다. 둘 다 리뷰가 잡았다.
@h2 References
@path PR #61 1f7ccf2 — CR-050 계약 / PR #62 ea08c57 — WP-034 구현
@path CR-050: docs/00_governance/change_control.md (3장 대장 + 5장 반영 내역)
@path DEV-350~358: 원장 5장 / 검증 기록: 원장 6.42장
@path 계약: API 계약 ### API-ING-002·### API-ING-003, 데이터 모델 마이그레이션 016 두 절
@h2 전사 아카이브
@todo 예정 경로 exports/202608281027.md — pending /export (사용자가 실행해야 확정된다).
@p 스킬 기본값(<루트>/pr-search-2026-08-28.md) 대신 저장소 관행을 따른다. .gitignore 24행이 exports/를 덮고(실측 확인) 기존 전사가 전부 YYYYMMDDHHMM.md다. 루트에 두면 무시되지 않아 git add -A에 딸려 간다 — 2026-08-27 세션이 실제로 99KB를 루트에 떨어뜨려 옮겨야 했다.
@p /export가 출력하는 경로는 저장소 루트를 가리킬 수 있다. 실행 뒤 ls로 실제 위치를 확인한다 — 이전 세션들이 세 번 틀리게 적은 자리다.
@p ---
@h1 Session: 2026-08-28 (2차) — CR-051 계약 종결 + DEV-349 구현 완주
@h2 Goal
@path 사용자의 75절 지시서: "이번 세션의 우선순위는 DEV-349다. WP-036보다 먼저 끝낸다."
@path 순서를 고정했다: 실측 → DEV-349 역방향 감사 → CR-051 → SRS v2.11 캐스케이드 → 문서 PR 병합 →
@path fresh main에서 구현 → 마이그레이션 017 → API-SRCH-004 에폭 바인딩 → W-001 URL/낡음 상태 →
@todo API-SRCH-005 에폭 영속 → W-008 표시 → 적대적 검증 → 구현 PR → 리뷰 정정 → 병합 → NEXT = WP-036.
@path 제품 결정은 지시서가 미리 승인했다: CR-051 개설, SRS v2.11, 마이그레이션 017, 신규 FR·NFR·
@path API·엔티티 ID 없음, 새 ADR 없이 ADR-007 clarification, Codex 호출 금지.
@path 우선순위의 근거도 지시서에 있었다 — WP-036은 원본 보관/운영 경로지만 DEV-349는 사용자가
@p 조사에 쓰는 seq:가 재채번 뒤 오류 없이 다른 커밋을 가리키는 silent correctness defect다.
@h2 Current state
@b main = 2e6194c. 작업 브랜치 없음(둘 다 병합·삭제). main CI verify·integration 통과
@path SRS baseline v2.11 · CR-051까지 · DEV-365까지 · 마이그레이션 017까지
@path REL-004는 그대로 7/8 — DEV-349는 기존 WP의 교차 결함 종결이지 새 WP가 아니다
@path 미해결 리뷰 0건 (PR #63·#64). 열린 PR 0건. open DEV 8건 (기존 7 + DEV-364)
@path DEV-349 resolved, DEV-359~363·DEV-365 resolved
@h2 Decisions
@p decisions.md 참조. 이 세션의 가장 무거운 결정 다섯:
@path 질문이 잘못돼 있었다 — DEV-349는 "어느 공간의 에폭인가"에 답할 정본이 없어 열려 있었는데,
@p 정본이 없던 것이 아니라 seq: 질의가 그 물음에 답할 수 있는 모양이 아니었다. 공간을 지목하지 않는 조건은 접근 범위의 모든 공간에 걸치고, 그때 "그 공간의 에폭"이 존재하지 않는다
@path ADR-007 규칙 5가 처음부터 답을 적어 두었다 — "시퀀스를 인용하는 모든 저장물(안전 구간
@p 표식, 저장된 검색의 seq: 조건, 공유 URL)은 에폭을 함께 저장한다". 셋 중 실제로 그렇게 하는 것은 safe_marker 하나뿐이었고 다섯 WP가 그 위를 지나갔다. 새 ADR을 만들지 않고 Clarification을 붙였다 — 같은 규칙을 두 번 적으면 정본이 둘이 된다
@b buildQuery를 fail-closed로 — seq: 범위가 있는데 에폭이 없으면 던진다. 선택 인자면
@path 호출부가 빠뜨렸을 때 오류 없이 모든 세대를 함께 돌려주고, 그 fail-open은 DEV-353보다 나쁘다
@b 접근 범위를 필요할 때만 산출 — 모든 목록 조회에 그 의존을 지우면 저장된 검색이 접근 통제
@path 경로에 참여한다(CR-049가 /run에서 피한 자리). 전 계층 통합이 외래 키 연쇄로 이것을 잡았다
@b 소급 채움 거절 — 마이그레이션이 기존 seq: 검색에 현재 에폭을 넣으면 모든 경고가 사라지지만,
@p 저장 당시의 값은 복원할 수 없으므로 그것은 추측이다. unbound로 남긴다
@h2 Changed files
@p files.md 참조. 커밋(머지 제외, 시간순):
@path 9f5e303 docs: CR-051 시퀀스 인용 계약 종결 (DEV-359~365, SRS v2.11) — PR #63
@path 1bb089b docs: PR #63 리뷰 넷 — 계약이 자기 예시와 어긋났다
@path 9f4acec feat: DEV-349 시퀀스 인용 에폭 바인딩 (CR-051, 마이그레이션 017) — PR #64
@path c13e9b4 fix: PR #64 리뷰 다섯 — 막으려던 실패를 다른 자리에서 되풀이했다
@path 머지: PR #63 → 7a2d0e2, PR #64 → 2e6194c. 전체 69파일 +4156 / -134.
@h2 Commands
@p commands.md 참조. 이번에 새로 배운 것 넷:
@path grep의 glob을 믿지 마라 — apps/search-api/src//*.ts는 globstar가 꺼져 있으면 한 단계만
@path 매칭한다. sequence/range.ts가 목록에서 빠져 그 경로가 500이 됐고 리뷰가 잡았다
@b 변이 헬퍼에 CRLF 처리가 필요하다 — 저장소가 CRLF라 \n 앵커가 안 걸린다. 치환 건수 검증이
@p 그것을 잡았다(risks 84의 재확인)
@b 공유 prs_test에서 픽스처 이름이 유일해야 한다 — app_user.login, repository(owner,name)이
@path 유니크다. 새 시험이 acme/payments를 쓰면 다른 파일과 충돌한다
@b afterAll에서 내 픽스처를 치운다 — 남기면 다른 파일의 전역 DELETE FROM app_user가 외래 키로
@p 막힌다. 단독은 통과하고 전 계층에서만 깨지는 종류다
@h2 Next steps
@path WP-036 원본 아카이브 레인 — 착수 전 감사부터. 선행 자산 실측은 todos.md A절에 있다
@path 배포: 마이그레이션 017 적용. WP-032가 남긴 둘은 변화 없다
@path DEV-364(스칼라 seq:) 처분은 제품 결정이 필요하다
@b 릴리스 게이트 4·5·6은 별도 작업
@h2 Risks/gotchas
@p risks.md 86~91번 참조. 이 세션이 배운 것 중 둘이 특히 값지다:
@b 내가 막으려던 실패를 다른 자리에서 되풀이했다 — 리뷰 P1 셋 중 둘이 그것이다. 화면이 형식
@path 오류를 삼켜 서버의 엄격함을 무력화했고, seq_epoch=(빈 값)을 "지정 안 함"으로 읽어 DEV-363을
@p 이름만 바꿔 남겨 두었다
@b 시험 구멍은 한 값만 검사할 때 생긴다 — 패싯 시험이 에폭 하나만 봐서, 패싯 질의가 그 값으로
@p 고정돼도 통과했다. 두 세대를 다 물어야 "따라온다"가 증명된다
@h2 References
@path PR #63 7a2d0e2 — CR-051 계약 / PR #64 2e6194c — DEV-349 구현
@path CR-051: docs/00_governance/change_control.md (3장 대장 + 5장 반영 내역)
@path DEV-359~365: 원장 5장 / 검증 기록: 원장 6.43장
@path 계약: API 계약 「시퀀스 인용 계약」 일곱 규칙, ADR-007 Clarification
@path worklog: Obsidian Vault/dailywork/2026-08-28_PR-Search-시퀀스-번호-뜻-지키기-(DEV-349).md
@h2 전사 아카이브
@todo 예정 경로 exports/202608281330.md — pending /export (사용자가 실행해야 확정된다).
@p 스킬 기본값(<루트>/pr-search-2026-08-28.md) 대신 저장소 관행을 따른다. .gitignore 24행이 exports/를 덮고 기존 전사가 전부 YYYYMMDDHHMM.md다. 루트에 두면 무시되지 않아 git add -A에 딸려 간다. /export 실행 뒤 ls로 실제 위치를 확인한다 — 이전 세션들이 네 번 틀리게 적은 자리다.
@p ---
@h1 Session: 2026-08-29 — CR-053 · WP-037 · DEV-364 · ACC-06
@h2 Goal
@path 이전 세션의 handoff가 CR-051 시점이라 WP-036을 다음 작업으로 지목했으나 이미 끝나 있었다.
@path 실측으로 기준선을 다시 잡고, REL-004 → REL-005 전환을 닫은 뒤 WP-037까지 완주했다.
@h2 Current state
@path main d3b2590 · SRS baseline v2.13 · 원장 review v5.6 · CR-053 · DEV-395까지.
@path REL-004 8/8, REL-005 1/4. open DEV 12건. 미해결 리뷰 0건, 열린 PR 0건.
@h2 무엇을 했나 (PR 셋)
@p | PR | 무엇 | 병합 | | --- | --- | --- |
@path | #74 | DEV-364 정정 + ACC-06·차트 스파이크 판정 + 집계 오류 정정 | 76e9cf0 |
@path | #75 | CR-053 집계 계약 종결 (SRS v2.13) | 1e3b8be |
@path | #76 | WP-037 집계 API 구현 | d3b2590 |
@h2 핵심 발견
@path handoff 팩이 stale했다. 08-28 13:19 시점이었고 그 뒤 18:13~21:43의 CR-052·WP-036 작업 전체가
@p 빠져 있었다. 마지막 커밋(22:48)이 그 옛 내용을 그대로 커밋했다. → 팩이 main에 있다는 이유로 그 내용이 현재 사실이라고 믿지 않는다.
@path DEV-364의 판정이 절반만 맞았다. "스칼라는 언제나 0건"이라는 근거로 미뤄 둔 항목인데,
@p 부정형은 필터가 사라진 전체였다. 같은 결함이 merged·created에도 있었다.
@path ACC-06을 측정할 수 없었다. 실제 관계 표본이 없다 — 합성 픽스처로 숫자를 만들지 않고
@path NOT RUN으로 판정했다. → W-007 NOT ACTIVATED.
@path 차트 스파이크의 결론은 ADR-006이 이미 적어 두었고, 진짜 공백은 계열 색 토큰이 264개 중
@path 0개라는 사실이었다(DEV-380).
@path CR-053이 찾은 것은 CR-052와 다른 모양이었다. 그때는 "A에서 B로 가는 길이 있는가"였고
@p 이번에는 "같은 것을 말하는 두 문서가 같은 것을 말하는가"였다. 뿌리는 용어집 한 줄이었다 — "팀 = 권한 판정과 집계 그룹의 단위".
@p 머지 전 리뷰 열이 전부 실결함이었고 다섯이 "숫자가 조용히 틀리는" 자리였다. 집계는 답이 하나의 수라 사용자가 검증할 방법이 없다.
@h2 Commands
@path agent-context/commands.md의 2026-08-29 절 참조. pnpm test:perf가 이 세션에서 생겼다.
@h2 Next steps
@path WP-038 착수 전 DEV-380 결정 — 20계열을 어떻게 구분할 것인가
@path WP-038 W-006 통계 대시보드
@b 배포: pnpm es:apply-mappings(백필 포함), 마이그레이션 017
@h2 Risks/gotchas
@path agent-context/risks.md의 2026-08-29 절 참조. 통합 시험의 정리도 오염이라는 것과,
@p 필터를 검증하려면 질의가 그 일을 대신하지 않아야 한다는 둘이 가장 값이 크다.
@h2 References
@path PR #74 · #75 · #76 · #77 · #78 · 원장 6.45~6.48장 · CR-053
@h2 세션 종료 상태 (2026-08-29 03:11)
@p main 651b1ad · 작업 트리 깨끗 · 로컬 브랜치 main 하나(병합된 16개 정리) · 열린 PR 0건 · 미해결 리뷰 0건.
@p 이 세션의 작업 기록은 Obsidian vault에도 남겼다 —
@path dailywork/2026-08-29_PR-Search-집계-API-구현과-REL-005-진입.md
@p (worklog.py check 오류 0건, 인덱스 33개 노트). 저장소 밖이므로 이 팩에는 담기지 않는다. 그 노트는 세션 서사이고 이 팩이 작업 재개용 정본이다.
@path 전사(/export)는 이전 세션에서 이미 실행되어 exports/202608290944.md에 저장되어 있다.
@p 이 경로는 .gitignore의 exports/ 규칙 아래에 있어서 추적되지 않으므로, 작업 트리를 더럽히거나 실수로 커밋될 위험이 없다. 전사를 저장소 루트에 두지 않는다: 루트 경로는 어떤 무시 규칙에도
@path 걸리지 않아 git add에 휩쓸릴 수 있고, 그것이 PR #77·#78이 기록해 둔 실패 양식이다. 새 전사가
@p 필요하면 반드시 exports/ 아래에 둔다.
