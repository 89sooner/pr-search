#hidden
# aci:v1 id=f54408e src=agent-context/todos.md
@kv sha256=3fa93a76a53d96cbd1f55326a8b9e7b84400abc34d8a2e80a6cc8c5b6cf87137 bytes=146894 lines=1785 title=다음-작업-미해결-항목-확인할-사항
@sig agent-context/todos.md;home/roqkf/design-system;agent-context/count-unresolved-reviews.py;89sooner/pr-search;git/refs;refs/tags/;regression/release-tag-ownership.test.ts;docs/40_delivery/pr_search_implementation_traceability.md;conductor-by-89soone/css;2/4;deploy/single-host/;/deploy/single-host/build-bundle.sh;releases/tags/;deploy/single-host;repos/89sooner/pr-search/releases/tags/0.1.0-pilot.2;vendor/upstream;company/main;24/24;deploy/single-host/RUNBOOK.md;deploy/single-host/bundle/pr-search-;deploy/single-host/.env.example;/prsctl;deploy/single-host/prsctl;GET/POST/DELETE
@h1 다음 작업 · 미해결 항목 · 확인할 사항
@p 최신 기준 (2026-09-03 2차 · 리뷰 부채 종결 라운드 — Conductor 0.3.1 발행, unresolved review 0)
@p main = 503cc64 — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
@path SRS baseline v2.20(변경 없음) · 원장 review v6.40 · 파생 토큰 문서 v0.3(CR-064) · 그 밖 변경 없음.
@path Conductor(/home/roqkf/design-system) main = 49675ea — SRS baseline v1.6, 원장 v0.32, CR-040·DEV-044까지. 패키지는 css 0.3.1 · react 0.3.1 · tokens 0.3.0이고 릴리스 태그 셋이 원격 검증을 통과한다.
@path 미해결 리뷰 0건 — 양쪽 저장소 전량 순회로 확인했다(python3 agent-context/count-unresolved-reviews.py). 이 값도 실측하라. 이 라운드는 시작 시점 아홉 건으로 출발해 그 수정에 대한 리뷰가 열 라운드 넘게 이어졌고, 처리 건수를 문서에 고정값으로 적으려다 네 번 틀렸다 — 진행 중인 라운드의 건수는 문서를 쓰는 동안에도 늘어난다. 처리 내역의 정본은 각 저장소의 원장과 change_control.md다.
@path 이 라운드가 연 것 (pr-search): CR-064 · DEV-541(태그 소유권) · DEV-542(0.3.1 반영) · DEV-543(plans/). 셋 다 resolved.
@path 이 라운드가 연 것 (Conductor): CR-037~CR-040 넷 · DEV-035~DEV-044 열. 전부 closed. Conductor의 open DEV는 0건이다.
@path 릴리스 게이트가 네 번 고쳐졌다 — DEV-040(버전이 오르지 않은 패키지를 막음) → DEV-041(manifest 동일성이 발행 여부를 답하지 못함) → DEV-042(게이트가 발행 뒤에 있어 늦음, 조회 오류를 미발행으로 셈) → DEV-043(가벼운 태그가 발행 전 게이트를 통과). 한 정정이 다음 구멍을 여는 것을 네 번 겪었고 매번 머지 후 리뷰가 찾았다.
@path open DEV 16건 — 이전과 같다 (DEV-006·010·016·026·058·061·304·305·395·399·427·433·447·492·494·536). 이 라운드가 새로 연 open은 없다.
@path 다음 빈 ID: CR-065 · DEV-544 · WP-073 · 마이그레이션 024 (측정값. 쓰기 전 다시 잰다)
@h3 A. 지금 당장 — 실제 사내 반입 (0.1.0-pilot.2)
@p 외부 저장소에서 더 만들 것이 없다. 리뷰 부채가 0이고 릴리스 자동화의 P1이 닫혔다.
@path 결정자가 fine-grained PAT를 발급한다 — 대상 89sooner/pr-search 하나, 권한 Contents Read-only 하나, 만료 90일 이내. 값은 어디에도 적지 않는다
@path 결정자가 사내 운영자에게 릴리스와 별도 채널로 셋을 전달한다 — 버전 0.1.0-pilot.2 · 읽기 토큰 · 자산 SHA-256 4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3
@b 사내에서 런북 2.B 1단계부터 위에서 아래로 실행한다. GHE App 자격은 3단계(install 전)에 넣는다
@b 반입 결과를 원장에 기록하는 것이 다음 세션의 첫 문서 작업이다. 발행 사실 자체는 이미 6.66.6장에 있다
@path 주의: 앱 CSS가 0.3.1로 바뀌었으므로 다음 번들은 새 버전을 담는다. 그리고 build-bundle.sh의 되돌리기가 이 라운드에서 바뀌었다 — 태그 소유를 원자적 생성으로 증명한다(DEV-541). 발행 흐름은 같다.
@h3 B. 미뤄 둔 것 — 이전과 같다
@path DEV-536의 소유 WP 판단(전용 성능 레인 / 러너 기준선 상대 판정 / 통합 레인에서 제외) · DEV-492 · DEV-494 · DEV-447 · DEV-433 · DEV-427 · DEV-304·305 · DEV-058. 압축 방식 변경(zstd)·백킹 제외 업그레이드 번들·immutable releases 켜기는 필요가 실증될 때 별도 CR.
@p find-animation-opportunities가 고른 Drawer·Tooltip·Menu 진입 모션은 Conductor에 들어갔으나 pr-search가 그 컴포넌트를 아직 쓰지 않는다. 채택 시 확인한다.
@h3 C. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
@path Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(Profile A에서는 롤백 10분을 실측해 판정한다). ACC-06 NOT RUN → W-007·WP-043 NOT ACTIVATED.
@h3 D. 이 라운드가 깔아 둔 자리 — 다시 만들지 말 것
@path build-bundle.sh의 태그 소유권: 발행 직전 gh api repos/<slug>/git/refs로 원자적 생성 → 성공했을 때만 TAG_CREATED_BY_THIS_RUN=1. 되돌리기는 --force-with-lease="refs/tags/<v>:<UPSTREAM_COMMIT>". 빈 기대값 lease로 되돌리지 마라 — 원격이 이미 같은 커밋이면 git이 Everything up-to-date로 통과한다(실측)
@risk regression/release-tag-ownership.test.ts: git은 진짜, 원격은 로컬 bare, gh·docker만 대역. 대역 gh의 ref 생성이 원격 상태를 보고 422를 낸다. 대역 둘은 .gitattributes가 LF로 고정한다 — 확장자가 없어 *.sh에 안 걸린다
@b plans/는 .gitignore에 있다. 되돌리면 build-bundle.sh의 계보 검사가 미추적 파일로 보아 번들 빌드를 막는다
@path 파생 토큰 문서 §9는 "Conductor 컴포넌트가 쓰는 토큰"만 적는다. 섹션 확장·탭 전환을 다시 넣지 마라 — 앱에 CSS 파일이 0건이라 제품이 자체 전환을 만들 수단이 없다(CR-064)
@path Conductor 쪽: 릴리스 게이트가 발행 전 레지스트리 스냅숏으로 소유를 가른다(CR-038~CR-040). changeset publish 앞의 --snapshot 단계를 지우지 마라 — 그 단계가 낡은 태그(가벼운 태그·HEAD 아닌 커밋)를 게시 전에 막는다. npm view가 404 아닌 이유로 실패하면 미발행으로 세지 않고 중단한다
@path Conductor 쪽: --snapshot은 레지스트리에 이미 있는 버전의 태그를 보지 않고, --published-before는 세 패키지 전부를 보되 "태그 대상 == 릴리스 HEAD" 요구만 면제한다. 두 모드를 뭉뚱그리면 스냅숏 실패를 진단할 때 오진한다(DEV-044)
@b 이전 세션 것 그대로: 아카이브 생성 위치·-C "$OUT_ROOT"·sed -i 's/\r$//'·.gitattributes·cmd_load의 require_env 위치·런북 2.B 3단계의 GHE App 자격·provision_app_role 위치·fixtureMonths·audit-grants.test.ts 전수 검사·compose.yml의 127.0.0.1
@p ---
@h3 시작하기 전에 — 이 인계의 값을 실측하라
@code lang=bash sha=08ad96cf7c8f lines=7 kept=7
|git -C . rev-parse --short HEAD
|gh pr list --state open --json number --jq 'length'
|grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
|python3 agent-context/count-unresolved-reviews.py
|gh release list -R 89sooner/pr-search; git ls-remote --tags origin
|npm view @conductor-by-89soone/css version   # 0.3.1이어야 한다
@p ---
@p 최신 기준 (2026-09-03 · 애니메이션·컴포넌트 폴리시 라운드 완료 — Conductor 0.3.0 발행·반영)
@p main = 08250a8 — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
@path SRS baseline v2.20(변경 없음) · 원장 review v6.37 · 그 밖 문서 변경 없음. Conductor(/home/roqkf/design-system) main = 2549675, 패키지 0.3.0 발행 완료.
@path 이 라운드는 WP가 아니다. improve-animations·emil-design-eng 스킬로 디자인 품질을 훑고 고친 것이며 REL-006은 그대로 2/4다.
@path pr-search에서 연 것: DEV-538(축소 모션 무효) · DEV-539(맨 button·code) · DEV-540(Conductor 0.3.0 반영). 셋 다 resolved, PR #126·#127·#128 병합.
@path Conductor에서 연 것: DEV-028~DEV-034 일곱. 전부 closed, PR #14·#18·#19 병합 후 0.3.0 발행.
@path open DEV 16건 — 이전과 같다(DEV-006·010·016·026·058·061·304·305·395·399·427·433·447·492·494·536). 이 라운드가 새로 연 open은 없다.
@path 다음 빈 ID: CR-064 · DEV-541 · WP-073 · 마이그레이션 024 (측정값. 쓰기 전 다시 잰다)
@p A. 지금 당장 — Conductor PR의 머지 후 리뷰 8건
@path python3 agent-context/count-unresolved-reviews.py가 design-system에서 8건을 센다: #14(2) · #15(4) · #16(1) · #18(1). 이 저장소의 관행대로 머지 뒤에 오는 자동 리뷰이며, 이번 세션에서만 그런 리뷰가 여러 번 실결함을 잡았다. pr-search는 #125(1건)뿐이고 그것은 결정자 지시로 남겨 둔 것이다.
@p B. 사내 반입은 그대로 남아 있다
@path 0.1.0-pilot.2 발행 상태 그대로다(태그 → 0a73065, SHA-256 4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3). 이 라운드는 반입에 아무 영향이 없다. 다만 앱 CSS가 0.3.0으로 바뀌었으므로 다음 번들은 새 버전을 담는다.
@p C. 미뤄 둔 것
@b find-animation-opportunities가 고른 셋 중 Drawer·Tooltip·Menu 진입 모션은 넣었으나 pr-search가 그 컴포넌트를 아직 쓰지 않는다. 채택 시 확인한다.
@path <datalist>(최근 질의) — 저장 위치 미정(DEV-079)이라 목록이 비어 있어 손대지 않았다.
@b 파생 스펙 pr_search_design_system_tokens.md §9(180행)가 "섹션 확장·탭 전환 → motion.standard"를 배정하지만 구현은 hidden 토글로 즉시다. 고빈도 탭 전환은 즉시가 옳으므로 스펙을 CR로 현행화하는 편이 맞다 — 결정자 판단 필요.
@b flow-003의 뒤로가기 e2e가 6워커 병렬에서 두 번 흔들렸다(단독·워커1·재실행은 통과). 반복되면 별도로 다룬다.
@path 최신 기준 (2026-09-02 2차 · CR-063·WP-072 완료 — 운반 경로가 GitHub Release로 정해졌고 0.1.0-pilot.2가 발행됐다)
@p main = 0a73065 — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
@p SRS baseline v2.20(변경 없음) · 원장 review v6.34 · 작업 패키지 v2.21 · 인프라 v0.12 · 보안 v1.3 · 로드맵 v0.14 · ADR v0.7 · 데이터 모델 v0.17(변경 없음)
@path WP-072 done — 배포 마일스톤 후속이다. REL이 아니므로 REL-006은 그대로 2/4
@path 0.1.0-pilot.2가 발행됐다 — 태그 → 0a73065, 자산 pr-search-0.1.0-pilot.2-offline.tar.gz(1,132,734,876바이트), SHA-256 4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3. 원격 태그·릴리스는 이것 하나. immutable releases는 꺼져 있다(결정)
@b 릴리스는 승인되지 않았다 — Gate 4·5·6 그대로
@path open DEV 16건 — 어제의 15(DEV-006·010·016·026·058·061·304·305·395·399·427·433·447·492·494) + DEV-536(CI 통합 p95 시험의 러너 의존). 이 세션이 연 DEV-528~DEV-537 중 나머지 아홉은 resolved
@path 미해결 리뷰 1건 — PR #125(P1). 결정자 지시("1~3 마친 뒤 내가 지시")로 손대지 않았다. 아래 B절
@path 다음 빈 ID: CR-064 · DEV-538 · WP-073 · 마이그레이션 024 · C-072 (측정값. 쓰기 전 다시 잰다)
@path CI 정상 — verify·integration 둘 다 판정 근거 (PR #119~#125). 통합에 시간 의존 시험이 둘 있다(DEV-427·DEV-536) — 빨간 통합은 재실행하되 병합하지 않고 원장에 적는다
@b Codex는 호출하지 않았다. 자동 리뷰는 머지 뒤에 온다 — 이 세션에서 여덟 건, 일곱이 실결함
@p 이 세션이 배운 것 다섯.
@path 릴리스는 저절로 불변이 아니다 (DEV-530). 정본은 담당자가 별도 채널로 전달하는 자산 SHA-256이고, 사내는 그 값·자산 digest·sha256sum 셋을 대조한다.
@path 되돌리기는 "무엇을 되돌렸는가"를 사실대로 말해야 한다 (DEV-531·533·534·535·537). 다섯 경계마다 "되돌렸다"가 거짓이 되는 자리가 있었다.
@path 실행 중인 스크립트와 deploy/single-host/*를 편집하면 실행이 무효가 된다. 백그라운드 실행 전에 커밋하고, 문서는 실행이 끝난 뒤 쓴다.
@b 가짜 도구는 전제 검사의 호출까지 설계한다. 첫 조회는 비우고 그다음부터 답한다.
@path 문서의 전제를 사실로 적을 때는 누가 언제 확인했는지를 함께 적는다 (DEV-528). 오프라인 전제 하나가 물리 반입 채널 전체를 만들고 있었다.
@h2 이번 세션이 세운 것 — GitHub Release 운반 경로
@code lang=text sha=74f084559886 lines=5 kept=5
|외부망: 깨끗한 main → ./deploy/single-host/build-bundle.sh <version> --release
|  → 번들 + 아카이브 → 초안 → 자산 → 발행 → 자산 재독 대조 → 성공 출력(버전·SHA-256·사내 명령)
|──────── github.com (HTTPS · 이 저장소 한정 읽기 토큰) · 버전·토큰·SHA-256은 별도 채널 ────────
|사내망: gh release download → gh api …/releases/tags/<v> --jq '.assets[].digest' → sha256sum(셋 일치)
|  → tar -xzf → cd deploy/single-host → prsctl verify → .env → load → install → smoke → lineage → 2.C → 2.D
@h2 A. 지금 당장 — 실제 사내 반입 (0.1.0-pilot.2)
@p 외부 저장소에서 더 만들 것이 없다. 이 작업이 끝났다고 다음 외부 WP를 자동으로 시작하지 않는다.
@path 결정자가 fine-grained PAT를 발급한다 — 대상 저장소 89sooner/pr-search 하나, 권한 Contents Read-only 하나, 만료 90일 이내. 값은 어디에도 적지 않는다
@path 결정자가 사내 운영자에게 릴리스와 별도 채널로 셋을 전달한다 — 버전 0.1.0-pilot.2 · 읽기 토큰 · 자산 SHA-256 4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3
@b 사내에서 런북 2.B 1단계부터 위에서 아래로 실행한다. GHE App 자격은 3단계(install 전)에 넣는다
@path 사내에서만 검증 가능한 것을 그때 실행하고 기록한다 — 사내 위치에서 github.com 도달, 실제 GHE App·웹훅·OIDC·CA·프록시·DNS·실서버 성능·실제 롤백 소요·ACC-06. 런북 7장 표의 NOT RUN 행이 목록이다
@b 반입 결과(성공·실패·사내 실측값)를 원장에 기록하는 것이 다음 세션의 첫 문서 작업이다
@h2 B. 결정 대기 — PR #125 머지 후 리뷰 (P1)
@p 같은 버전을 같은 커밋으로 두 작업이 겹쳐 발행하면, 다른 작업이 만든 태그의 OID가 UPSTREAM_COMMIT과 같아 이 실행의 되돌리기가 그 태그를 지운다는 지적. 커밋 일치는 대상을 증명하지 소유를 증명하지 않는다. 첫 반입에는 영향이 없다(발행 주체가 하나다). 선택지 셋 — 결정자가 고른다.
@cmd git push --force-with-lease=refs/tags/<v>:<UPSTREAM_COMMIT> origin :refs/tags/<v>로 기대 값이 같을 때만 지우게 고친다(DEV-538 후보, 가짜 git 시나리오 하나 추가, 약 20분)
@path DEV-538로 등록만 하고 "한 버전은 한 담당자가 한 번 발행한다"는 운영 규칙으로 갈음, 스레드에 근거를 답하고 해소
@b 그대로 두고 다음 세션으로
@h2 C. 원장에 아직 없는 것
@path 0.1.0-pilot.2 발행 사실(태그·커밋·크기·SHA-256·발행 시각 2026-09-02T08:51:55Z). 6.66장 계열에 한 절로 적는다 — 이 세션은 발행 뒤 문서 PR을 더 열지 않았다
@path DEV-536의 소유 WP 판단(전용 성능 레인 / 러너 기준선 상대 판정 / 통합 레인에서 제외). 기준값을 근거 없이 올리지 않는다
@h2 D. 배포에서 해야 할 것 — 어제와 같다
@path 마이그레이션 023까지 · POSTGRES_APP_USER에 prs_app 금지(DEV-503) · clearUnknownSizes 소급(pnpm es:apply-mappings) · ADMIN_DATABASE_URL 없으면 JOB-AUD-001만 서지 않는다 · Prometheus 경보 규칙 NOT RUN · authz 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름. 사내 호스트 디스크는 첫 설치 시점에 6GB 이상(DEV-529).
@h2 E. 미뤄 둔 항목
@path DEV-536(위) · DEV-492(Profile B web 매니페스트) · DEV-494(프록시 — 취득 단계의 gh·curl은 HTTPS_PROXY를 읽으므로 영향 없음, 앱은 별개) · DEV-447 · DEV-433 · DEV-427(CI 통합 link-rebuild 되먹임 — 이 세션에서 #124의 재실행 실패로 다시 관찰됐다) · DEV-304·305 · DEV-058. 압축 방식 변경(zstd, 약 30% 절감)·백킹 제외 업그레이드 번들(약 180MB)·immutable releases 켜기는 필요가 실증될 때 별도 CR.
@h2 F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
@path Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(Profile A에서는 롤백 10분을 실측해 판정한다). ACC-06 NOT RUN → W-007·WP-043 NOT ACTIVATED.
@h2 G. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
@b build-bundle.sh 발행 전제 검사의 순서: 초안 잔재(LEFTOVER_DRAFT) → gh release view → 태그(TAG_TARGET·TAG_EXISTED) → IMMUTABLE. 이미지 빌드 앞이다. 뒤집으면 초안 잔재에 틀린 처방이 나오거나 몇 분짜리 빌드 뒤에 멈춘다
@path 같은 파일의 발행 순서: tar -tzf 재독 → gh release create --draft → lookup_draft_id 3회(실패 시 undo_release → die … $(undo_note)) → PATCH draft=false → releases/tags/<v> 재독으로 이름·크기·digest 대조. --draft를 빼거나 대조를 빼지 마라
@path undo_release(): id를 모르면 lookup_draft_id로 재조회 · 태그는 TAG_EXISTED=0이고 git ls-remote --tags origin "refs/tags/<v>" "refs/tags/<v>^{}"가 있고 그 대상이 UPSTREAM_COMMIT과 같을 때만 push로 지운다 · 결과 플래그 UNDONE·RELEASE_DELETED·TAG_LEFT·TAG_DELETED·TAG_FOREIGN · PUBLISHED 플래그를 되살리지 마라(DEV-535) · || true로 태그 push를 무시하지 마라(DEV-534)
@path undo_note()가 모든 되돌리기 뒤의 die에 붙는다. die "…초안을 되돌렸다" 같은 단정 문구는 회귀가 금지한다(DEV-533)
@path 성공 출력의 "별도 채널로 전달할 것 셋"과 immutable 상태 줄 — 회귀가 잰다(DEV-530)
@path 런북 2.A "전달할 것은 셋이다" · 2.B 1단계 순서(gh release download → --jq '.assets[].digest' → sha256sum → tar -xzf)와 단계 번호(# 3) 구성 작성, # 5) 설치) · 2.B의 "담당자가 별도 채널로 전달한 SHA-256" — 회귀가 잰다
@cmd 런북 curl 경로의 python3 -c 'import json,sys; …["assets"]…' — 옛 grep으로 되돌리지 마라(DEV-532)
@b .env.example에 GH_TOKEN·GITHUB_TOKEN을 넣지 마라
@path 회귀 describe('사내 반입 운반이 GitHub Release와 같은 말을 한다 (WP-072 / CR-063)') 일곱 — 문자열 검사이며 실행 검증(6.66~6.66.5장)과 짝이다
@b 이전 세션 것 그대로: 아카이브 생성 위치·-C "$OUT_ROOT"·sed -i 's/\r$//'·.gitattributes·cmd_load의 require_env 위치·런북 2.B 3단계의 GHE App 자격·provision_app_role 위치·|| ... orcelain·fixtureMonths·audit-grants.test.ts 전수 검사·compose.yml의 127.0.0.1
@p ---
@h2 시작하기 전에 — 이 인계의 값을 실측하라
@code lang=bash sha=85e2d8bc5683 lines=7 kept=7
|git -C . rev-parse --short HEAD
|gh pr list --state open --json number --jq 'length'
|grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
|python3 agent-context/count-unresolved-reviews.py
|gh release list -R 89sooner/pr-search; git ls-remote --tags origin
|gh api repos/89sooner/pr-search/releases/tags/0.1.0-pilot.2 --jq '{draft, immutable, target_commitish, assets: [.assets[] | {name, size, digest}]}'
@p ---
@h2 (2026-09-02 · CR-062·WP-071 종료 시점의 기록)
@path 최신 기준 (2026-09-02 · CR-062·WP-071 완료 — 사내에서 그대로 실행 가능한 반입 지침이 섰다)
@p main = 7a58fd9 — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
@p SRS baseline v2.20(변경 없음) · 원장 review v6.26 · 작업 패키지 v2.19 · 인프라 v0.11 · 로드맵 v0.13 · 데이터 모델 v0.17 · 보안 v1.2 · ADR v0.6(변경 없음)
@path WP-071 done — 배포 마일스톤 후속이다. REL이 아니므로 REL-006은 그대로 2/4
@b 릴리스는 승인되지 않았다 — Gate 4·5·6 그대로
@path open DEV 15건 — 목록은 어제와 같다(DEV-006·010·016·026·058·061·304·305·395·399·427·433·447·492·494). 이 세션이 연 DEV-523~527 다섯은 전부 resolved
@path 미해결 리뷰 0건 — pr-search 118개·design-system 12개 전량 순회 (PR #117·#118의 마지막 리뷰까지 해소)
@path 다음 빈 ID: CR-063 · DEV-527 · WP-072 · 마이그레이션 024 · C-072 (측정값. 쓰기 전 다시 잰다)
@path CI 정상 — verify·integration 둘 다 판정 근거로 썼다 (PR #116·#117·#118)
@path Codex는 호출하지 않았다. 자동 리뷰가 머지 뒤에 오면 소스로 검증해 정정한다 — PR #116의 P1(DEV-525)과 PR #117의 P1(DEV-527)이 그랬고 둘 다 실결함이었다
@p 이 세션이 배운 것 다섯.
@path 번들 내용이 빌더의 checkout 상태에 따라 달라졌다 (DEV-526). .gitattributes가 고정하지 않은 텍스트는 autocrlf 트리에서 CRLF로 복사되고, 어제의 번들이 LF였던 것은 우연이었다. 산출물이 커밋을 가리키면서 내용이 커밋과 다른 자리다 — 빌드가 정규화하고 속성이 고정한다.
@b 운반 아카이브의 정합은 풀어 나온 사본에서만 증명된다. 원래 디렉터리에서 verify하면 아카이브가 아예 없어도 통과한다.
@path fail-fast는 검사의 존재가 아니라 위치다 (DEV-524). require_env는 있었다 — docker load 뒤에.
@b 커밋 전 파일에 git checkout --을 쓰지 마라. 변이 원복에 썼다가 파일 전체가 정정 전으로 돌아갔고, 이후 변이 셋의 결과에 그 실수가 섞였다. 원복은 역방향 치환으로 한다.
@path 검증에서 돌리지 않은 단계가 결함이 숨은 자리였다 (DEV-527). install을 돌리지 않았고, 런북의 GHE App 자격 순서 결함은 정확히 install에서 드러나는 것이었다. 머지 후 리뷰가 찾았다. 건너뛴 단계는 "바꾸지 않았다"가 아니라 "검증하지 않았다"로 적는다.
@h2 이번 세션이 세운 것 — 사내에서 그대로 실행 가능한 반입 지침
@code lang=text sha=c7124a8e7c6f lines=4 kept=4
|외부망: 깨끗한 checkout → ./deploy/single-host/build-bundle.sh <version>
|  → pr-search-<version>-offline/  +  pr-search-<version>-offline.tar.gz   (사내 반입 파일은 둘째)
|사내망: tar -xzf → cd deploy/single-host → prsctl verify → cp .env.example .env → prsctl load
|  → prsctl install → prsctl smoke → prsctl lineage → git fetch <bundle> HEAD:vendor/upstream → company/main
@path 풀어 나온 사본에서 24/24로 검증했다 (원장 6.64장). deploy/single-host/RUNBOOK.md가 정본이며 번들 안에 함께 들어간다. install·smoke는 이 세션이 바꾸지 않았고 WP-070이 관통한 것이다.
@h2 A. 지금 당장 — 실제 사내 반입
@path 외부 저장소에서 더 만들 것이 없다. 이 작업이 끝났다고 다음 외부 WP를 자동으로 시작하지 않는다 — 반입이 바로 진행되지 못하는 상황에서만 결정자가 WP-042·WP-044를 고른다. WP-043은 ACC-06 NOT RUN이라 여전히 착수 금지다.
@path 깨끗한 트리에서 ./deploy/single-host/build-bundle.sh <version> → deploy/single-host/bundle/pr-search-<version>-offline.tar.gz
@b 그 파일 하나를 사내 호스트로 옮긴다
@b 런북 2.B → 2.C → 2.D를 위에서 아래로 실행한다
@path 사내에서만 검증 가능한 것을 그때 실행한다 — 실제 GHE App·웹훅·OIDC·CA·프록시·DNS·실서버 성능·실제 롤백 소요·ACC-06
@h2 B. 사내 반입을 막는 것 — 어제와 같다
@path 기술적 blocker는 없다. 사내 GHE App·OIDC 클라이언트 등록은 사내 조치이고, 사설 CA는 NODE_EXTRA_CA_CERTS로 코드 변경 없이 성립하며, 프록시는 DEV-494 open(사내망이 강제한다는 증거가 나오면 GitHubTransport의 fetchImpl 주입 지점으로 최소 수정)이다. RPO 0은 WAL 아카이빙 구성이 있어야 성립한다(DEV-504).
@h2 C. 배포에서 해야 할 것 — 어제와 같다
@path 마이그레이션 023까지 · POSTGRES_APP_USER에 prs_app 금지(DEV-503) · clearUnknownSizes 소급(pnpm es:apply-mappings) · ADMIN_DATABASE_URL 없으면 JOB-AUD-001만 서지 않는다 · Prometheus 경보 규칙 NOT RUN · authz 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름.
@h2 D. 미뤄 둔 항목 — 어제와 같다
@path DEV-492(Profile B web 매니페스트) · DEV-494(프록시) · DEV-447(조정 스캔 30초 강제 종료 미증명) · DEV-433 · DEV-427(이제 CI가 도니 확인 가능) · DEV-304·305(Profile B) · DEV-058(합성 데이터셋 부재).
@h2 E. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
@path Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(Profile A에서는 롤백 10분을 실측해 판정한다). ACC-06 NOT RUN → W-007·WP-043 NOT ACTIVATED.
@h2 F. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
@b build-bundle.sh의 ARCHIVE="${BUNDLE}.tar.gz"와 tar -czf "$ARCHIVE" -C "$OUT_ROOT" "$(basename "$BUNDLE")" — checksum·시크릿 검사 뒤다. 앞으로 옮기면 검사 전 내용이 ... "$OUT_ROOT"를 빼면 아카이브가 절대 경로를 담는다. 뒤의 tar -tzf는 "만든 것을 다시 읽어 본다"이며 빼지 않는다
@path 같은 파일의 복사 루프 안 sed -i 's/\r$//' — 빼면 빌더의 checkout이 CRLF일 때 .env.example이 CRLF로 나가 load가 멀쩡한 값을 거부한다 (DEV-526). checksum은 그 뒤에 계산된다
@path .gitattributes의 deploy/single-host/.env.example·RUNBOOK.md eol=lf
@path prsctl cmd_load의 첫 줄 require_env — docker load 뒤로 옮기면 fail-fast가 아니다 (DEV-524). require_env의 파서(env_value)는 \r을 지우지 않는다 — 의도적으로 건드리지 않았고 런북 8장이 안내한다
@path 런북 2.B의 명령 순서와, 2.A 표에서 prsctl load를 ./ 없이 쓴 것 — 회귀 시험이 2.B 안에서 ./prsctl 형태의 순서를 잰다
@b 회귀 describe('사내 반입 절차가 실행 도구와 같은 말을 한다') 다섯 — 문자열 검사이며 실행 검증(6.64장)과 짝이다. 하나만 있으면 변이가 살아남는다
@b 바깥 checksum sidecar를 만들지 마라 — 정본이 둘이 된다. 사내 정책이 요구한다는 증거가 나올 때만 별도로 연다
@path 런북 2.B 3단계의 GHE App 자격은 install 전이다 — 2.C로 되돌리면 enrich·reconcile이 기동을 거부해 install의 health에서 깨진다 (DEV-527). restart는 .env를 다시 읽지 않는다
@b 이전 세션 것 그대로: prsctl의 provision_app_role 위치·|| true 넷·재색인 직렬화, build-bundle.sh의 git status --porcelain, 통합 헬퍼의 fixtureMonths, audit-grants.test.ts의 전수 검사 둘, compose.yml의 127.0.0.1
@p ---
@h2 시작하기 전에 — 이 인계의 값을 실측하라
@code lang=bash sha=74f0416e2226 lines=5 kept=5
|git -C . rev-parse --short HEAD
|gh pr list --state open --json number --jq 'length'
|grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
|python3 agent-context/count-unresolved-reviews.py
@p ---
@h2 (2026-09-01 · CR-059·WP-070·CR-060·CR-061 종료 시점의 기록)
@path 최신 기준 (2026-09-01 · CR-059·WP-070·CR-060·CR-061 완료 — 첫 사내 반입 가능선이 섰다)
@p main = e1603b2 — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
@path SRS baseline v2.20 · 원장 review v6.24 · 작업 패키지 v2.17 · 인프라 v0.10 · 데이터 모델 v0.17 · 보안 v1.2 · 로드맵 v0.11 · 릴리스 검증 v0.6 · 추적 매트릭스 v1.3 · ADR v0.6 (ADR-021 신설)
@path WP-070 done — 배포 마일스톤이다. REL이 아니므로 REL-006의 진행률(2/4)은 그대로다
@todo REL-006: WP-041·WP-069 done · WP-042·WP-044 todo · WP-043 착수 금지(ACC-06 NOT RUN)
@b 릴리스는 승인되지 않았다 — Gate 4·5·6 그대로
@path open DEV 15건 — DEV-006·010·016·026·058·061·304·305·395·399·427·433·447·492·494
@path 계수는 상태 칸의 머리를 읽는다 — 판정을 덧붙이기만 하면 사람과 도구가 갈린다 (DEV-522). 그리고 상태를 세는 표의 칸에 그 표를 세는 패턴을 리터럴로 쓰지 마라 — 그 인용문이 패턴에 걸린다 (실제로 겪었다)
@path 미해결 리뷰 0건 — pr-search 115개·design-system 12개 전량 순회 (PR #114의 마지막 리뷰까지 해소)
@path 다음 빈 ID: CR-062 · DEV-523 · WP-071 · 마이그레이션 024 · C-072 (측정값. 쓰기 전 다시 잰다)
@b CI가 실제로 돈다. 인계에 적혀 있던 BILLING_BLOCKED는 해소됐다 — verify·integration 둘 다 판정 근거다
@b 오픈 결정 0건
@p 이 세션이 배운 것 다섯.
@todo 실제로 세워 보지 않으면 배포 문서는 검증되지 않는다. WP-070이 단일 호스트에서 실제 기동을 시도하자 넷이 드러났다 — 접속 주체 미정의(DEV-503), Next.js standalone 기동 실패, pg_restore --clean의 파티션 부분 실패, alpine의 localhost IPv6 우선 해석. 문서를 읽거나 시험을 돌리는 것으로는 하나도 나오지 않았을 것들이다.
@path 정정이 시한폭탄을 옮길 수 있다. DEV-500은 롤링 과거 3개월 창으로 고쳤는데 2026-12-01에 같은 실패를 되살리고 그때도 회귀는 초록이었다(DEV-509). 롤링 창을 없애고 픽스처가 자기 달을 선언하게 하자 실패가 즉시 오게 됐다.
@path 전수 검사를 만들 때 "무엇의 전수인가"를 먼저 물어라. 022가 표의 권한 공백을 메우며 시퀀스를 세지 않아 한 겹 아래에 같은 결함이 남았다(DEV-517 → DEV-518).
@path 실패를 출력만 하고 종료 코드 0으로 끝내지 마라. 복구가 색인 넷을 지우고 하나만 다시 만들면서 성공으로 보고했다(DEV-519). 그 증상("재색인 예약 2/4")을 이미 보고 있었으면서 왜 실패했는지 쫓지 않은 것이 원인이다.
@b set -e는 진단 앞에서 스크립트를 죽인다. 스모크 변이가 exit 1을 내면서 어느 검사가 실패했는지 말하지 않았다 — 실패를 말하지 않는 실패는 성공으로 접는 것과 같다.
@h2 이번 세션이 세운 것 — 첫 사내 반입 가능선
@code lang=text sha=b49b20da054a lines=4 kept=4
|외부망: main의 커밋 → 이미지 6종 → 오프라인 번들 2.6GB
|  → checksum 검증 → 이미지 적재(pull 없음) → migration → 접속 주체
|  → ES mapping → compose up(18 서비스) → health → 조회 왕복 스모크
|  → 계보 확인(vendor/upstream fetch → manifest의 커밋과 일치)
@path 전부 실제 실행으로 검증했다. deploy/single-host/RUNBOOK.md가 정본이며 번들 안에 함께 들어간다.
@h2 A. 지금 당장 — 실제 사내 반입
@p 외부 저장소에서 더 만들 것이 없다. 다음 단계는 사내 환경에서만 실행할 수 있다.
@path ./deploy/single-host/build-bundle.sh <version> 으로 번들을 만든다 (작업 트리가 깨끗해야 한다 — 미추적 파일도 검사한다)
@b 사내 호스트로 옮긴다
@b RUNBOOK.md의 반입 절차를 따른다
@b 사내에서만 검증 가능한 것을 그때 실행한다: 실제 GHE App·웹훅·저장소 권한, 사내 OIDC와 그룹 클레임, 사내 CA·프록시·DNS, 실서버 성능, 실제 롤백 소요
@path WP-042·WP-044는 그 뒤다. 둘의 요구사항은 Could 등급이고, 지금 우선순위는 지금까지 만든 것을 사내에서 실제로 쓰는 것이다 (로드맵 4.1장).
@h2 B. 사내 반입을 막는 것 — 정확히 무엇인가
@p 기술적 blocker는 없다. 남은 것은 조직의 결정과 사내 환경 접근이다.
@p | 항목 | 상태 | | --- | --- | | 번들 생성·반입·기동·복구 | VERIFIED (external) | | 사내 GHE App 등록·웹훅 | 사내 조치 필요 | | 사내 OIDC 클라이언트 등록 | 사내 조치 필요 | | 사내 CA (있다면) | NODE_EXTRA_CA_CERTS로 코드 변경 없이 성립한다 |
@path | 사내 프록시 (있다면) | DEV-494 open — Node 22의 fetch가 프록시 환경 변수를 보지 않는다. git은 받는다. 사내망이 프록시를 강제하면 GitHubTransport의 fetchImpl 주입 지점으로 최소 수정이 필요하다 |
@p | 호스트 사양 | RAM 8GB 이상 · 디스크 이미지 2GB + 데이터 |
@path | RPO 0을 지키려면 | 연속 WAL 아카이빙을 독립 저장소로 보내는 구성이 필요하다 (DEV-504) — 없으면 실효 RPO는 마지막 백업 이후 경과 시간이다 |
@h2 C. 배포에서 해야 할 것
@path 마이그레이션 023(CR-061)·022(CR-060)·021(WP-069)·020(CR-055)·018·019(WP-039)·017(WP-037)
@path POSTGRES_APP_USER에 prs_app을 주지 마라 — NOLOGIN 그룹 롤이다. prsctl install이 다른 이름의 주체를 만든다 (DEV-503)
@path clearUnknownSizes 소급 (CR-056 / DEV-456) — pnpm es:apply-mappings
@path ADMIN_DATABASE_URL (WP-039) — 없으면 JOB-AUD-001만 서지 않는다
@b Prometheus 경보 규칙 적용은 여전히 NOT RUN
@b authz 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름이다
@h2 D. 미뤄 둔 항목
@path DEV-492 open — Profile B의 web 매니페스트가 없다. 클러스터가 없어 검증할 수 없는 매니페스트를 지금 만들지 않는다. Profile B 승격 시
@path DEV-494 open — 프록시 (위 B절)
@path DEV-502 resolved지만 DEV-447 open — 조정 스캔의 30초 강제 종료는 여전히 증명되지 않았다. Promise.race로 감싸 "강제 종료했다"고 보고하지 마라
@path DEV-433 open — API-ADM-001·API-ADM-003의 오프셋
@path DEV-427 open — CI 통합의 link-rebuild 되먹임. 이제 CI가 도니 확인할 수 있다
@path DEV-304·DEV-305 — Profile A에서 해소, Profile B는 그대로 열려 있다
@path DEV-058의 머리글을 그대로 믿지 마라 — 스크립트도 설정도 실재한다. open인 이유는 1,000만 문서 합성 데이터셋 부재다
@h2 E. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
@path Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(Profile A에서는 롤백 10분을 실측해 판정한다 — CR-060이 그 면제를 되돌렸다). ACC-06은 NOT RUN이고 W-007·WP-043은 NOT ACTIVATED다.
@h2 F. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
@path deploy/single-host/prsctl의 provision_app_role — 마이그레이션 뒤에 부른다. 앞으로 옮기면 prs_app이 아직 없어 GRANT가 실패한다 (DEV-510)
@b 같은 파일의 || true — 명령 치환 넷에 붙어 있다. 빼면 set -e가 진단 앞에서 스크립트를 죽여 무엇이 실패했는지 말하지 않는다
@path 같은 파일의 재색인 직렬화 — 동시 실행 상한이 1이라 연달아 예약하면 셋이 거절된다 (DEV-519)
@path build-bundle.sh의 git status --porcelain — git diff --quiet로 되돌리지 마라. 미추적 파일을 보지 못해 계보 증명이 거짓이 된다 (DEV-512)
@path 통합 헬퍼 넷의 fixtureMonths — 롤링 과거 창으로 되돌리지 마라. 실패가 미래로 미뤄진다 (DEV-509)
@path audit-grants.test.ts의 전수 검사 둘 — 표와 시퀀스 양쪽이다 (DEV-517·DEV-518)
@b compose.yml의 127.0.0.1 healthcheck — localhost로 되돌리면 alpine이 ::1로 풀어 서비스가 멀쩡한데 unhealthy로 보인다
@b .gitattributes — 셸 스크립트가 CRLF로 커밋되면 리눅스에서 bad interpreter로 죽는다. 사내 반입 뒤에는 고칠 방법이 그 자리에 없다
@b 이전 세션 것 그대로: team-membership.ts의 upsertTeam 위치, author-teams.ts의 잠금 뒤 재확인·루프 순서·stop()의 await, documents.ts의 pr?.author == null 판정, reindex.ts의 명시적 delete
@p ---
@h2 시작하기 전에 — 이 인계의 값을 실측하라
@code lang=bash sha=74f0416e2226 lines=5 kept=5
|git -C . rev-parse --short HEAD
|gh pr list --state open --json number --jq 'length'
|grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
|python3 agent-context/count-unresolved-reviews.py
@path 미해결 리뷰는 스크립트가 정본이다 (DEV-468·469). 한 줄 명령으로 되돌아가지 마라 — 창이 둘이고 둘 다 넘칠 수 있으며, 그 스크립트는 순회한 수를 각 totalCount와 대조해 "전량 순회=아니다"면 종료 코드 1을 낸다.
@h2 A. 지금 당장 — WP-042 착수 전 API-SEQ-005 계약 감사 (CR-059)
@path WP-069이 끝났고 REL-006의 다음은 WP-042(이분 탐색 보조)다.
@path API-SEQ-005도 계약 4장에 상세 절이 없다. CR-057이 API-SEQ-004를 채우면서 그 사실을 확인했고 의도적으로 함께 채우지 않았다 — 그것은 이 WP의 감사 대상이며, "SEQ API 전부 정리"로 만들면 검증하지 않을 계약을 함께 쓰게 된다. 착수 전 감사가 이 저장소의 관행이며 CR-058이 열여덟 번째였다.
@p 감사 후보 (이전 세션이 실측으로 모아 둔 것. 확정 결함이 아니라 판정 대상이다).
@p | 후보 | 무엇이 비어 있는가 | 근거 | | --- | --- | --- |
@path | 상세 절 전체 | 요청·응답·오류 정의가 없다 | 4장에 ### API-SEQ-005 0건, 3장 카탈로그 36행 한 줄뿐 |
@path | 권한 구분 | 카탈로그가 인증만 말하고 접근 범위를 요구하지 않는다 | 이분 탐색은 저장소의 시퀀스 공간을 읽는다. DEV-461과 같은 모양 |
@path | 초기 구간의 출처 | good_seq·bad_seq가 nullable인데 null의 뜻과 초기 구간이 정의되지 않았다 | bisect_session에 조사 구간 경계 열이 없고 W-004의 구간은 URL 파라미터에만 있다 |
@path | 후보 구간의 경계 | 열린 구간인지 닫힌 구간인지 없다 | AC-4의 "올림 log2"가 이 정의에 따라 값이 달라진다 |
@path | 중앙값 동점 처리 | 같은 거리에 커밋이 둘이면 무엇을 고르는지 없다 | AC-2가 "가장 가까운"만 말한다 |
@p | 에폭 무효화의 응답 모양 | 읽기에서 에폭이 바뀌었을 때 200 봉투인지 409인지 없다 | SEQUENCE_EPOCH_STALE은 쓰기 요청으로 정의되어 있다 |
@path | DELETE의 멱등성 | 세션이 없을 때 무엇을 반환하는지 없다 | 카탈로그가 GET/POST/DELETE만 적는다 |
@todo | 응답 필드 대응 | C-029의 remaining·estimatedSteps·nextSeq에 대응하는 정의가 없다 | pr_search_ui_component_spec.md:295 |
@p 이미 서 있는 것 (다시 만들지 마라).
@b bisect_session 표가 마이그레이션 002에 이미 있다. 005_roles.up.sql에 권한 부여도 있다
@risk BISECT_CONTRADICTION(409)이 packages/contracts/src/error-codes.ts와 계약 6장에 이미 등재돼 있다
@b 백엔드 6.2가 이분 탐색을 감사 미기록으로 정했고 멱등 키를 (user, repo, branch)로 정했다
@path 재채번은 이분 탐색 세션에 아무것도 쓰지 않는다 (CR-026, DEV-126). 조회가 현재 에폭과 비교해 epoch_stale을 계산한다
@path CR-058이 세운 것을 그대로 쓸 자리가 많지만 복사하지 말고 FR-SEQ-007이 요구하는 것을 먼저 읽어라 — 탐색 상태는 사용자별이라 겨루는 것이 다르다.
@h2 B. 그 뒤 — WP-044
@path WP-044 검색 결과 내보내기 — 감사 액션 export.create를 활성으로 옮긴다. safe_marker.set이 그 전환의 선례다 (SRS v2.18): 상태 칸은 "계약이 승인했으나 기능이 아직 없다"를 뜻하므로 기능이 서는 순간 이전 값이 거짓이 된다. 남은 미활성은 export.create 하나뿐이다.
@path WP-043은 착수하지 않는다 — ACC-06이 NOT RUN이라 활성화 조건이 성립하지 않는다.
@h2 C. 배포에서 해야 할 것
@path WP-069이 마이그레이션 021을 더했다.
@path 마이그레이션 021(WP-069), 020(CR-055), 018·019(WP-039), 017(WP-037) — pnpm run db:migrate
@path clearUnknownSizes 소급 (CR-056 / DEV-456) — pnpm es:apply-mappings
@path ADMIN_DATABASE_URL (WP-039) — 없으면 JOB-AUD-001만 서지 않는다
@b SEARCH_CURSOR_HMAC_KEY, pnpm es:reindex
@b Prometheus 경보 규칙 적용은 여전히 NOT RUN
@b authz 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름이다. 스윕이 돌지 않고 기동 로그가 그 사실을 경고한다
@h2 D. GitHub Actions — pr-search만 결제 차단이다
@p verify·integration 모두 steps=0이고 주석이 The job was not started because recent account payments have failed를 말한다. 잡이 뜨지도 않은 것이지 시험이 실패한 것이 아니다.
@b 재시도를 누르지 않는다 — 이 세션이 PR 둘에서 같은 상태를 확인했다
@b design-system은 다르다. 그 저장소에서는 CI가 판정 근거다
@b pr-search에서는 로컬 배터리가 유일한 판정이며 그 사실을 원장·PR·인계에 적는다
@h2 E. 미뤄 둔 항목
@path DEV-447 open — 조정 스캔의 30초 강제 종료가 증명되지 않았다. Promise.race로 감싸 "강제 종료했다"고 보고하지 마라
@path DEV-433 open — API-ADM-001·API-ADM-003의 오프셋
@path DEV-427 open — CI 통합의 link-rebuild 되먹임. CI가 돌지 않아 확인할 길이 없다
@path DEV-395·DEV-399·DEV-058·DEV-061 — 변화 없음
@path DEV-058의 머리글을 그대로 믿지 마라. "pnpm test:perf 스크립트가 저장소에 없다"고 적혀 있으나 스크립트도 vitest.perf.config.ts도 perf/analytics.perf.test.ts도 실재한다. 여전히 open인 이유는 1,000만 문서 합성 데이터셋이 없어 실측 p95가 NOT RUN이기 때문이다
@h2 F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
@path Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 1,000만 문서 합성 데이터셋 부재) · Gate 6 운영(불가 — 실제 K8s 없음). ACC-06은 NOT RUN이고 W-007·WP-043은 NOT ACTIVATED다.
@h2 G. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
@path packages/db/src/repositories/team-membership.ts의 upsertTeam(pool, ...) — 트랜잭션 밖이다. 안으로 옮기면 23505 재시도가 25P02로 죽는다 (DEV-489)
@b 같은 파일의 org_team_sync 갱신 — 교체와 같은 트랜잭션 안이다. 밖으로 빼면 실패한 조직이 신선해 보이고 모름이 빈 배열로 바뀐다
@path apps/pipeline-worker/src/author-teams.ts의 잠금 뒤 신선도 재확인 — 지우면 동시 요청이 조직 팀 전체를 두 번 훑는다 (M10)
@b 같은 파일의 루프 순서 (sweep → sleep) — 뒤집으면 냉시작 후 한 주기 내내 모든 문서가 모름을 답한다 (M19)
@b 같은 파일의 stop()의 await loop — 빼면 종료가 진행 중인 동기화 위로 풀을 닫는다 (M18)
@path apps/pipeline-worker/src/documents.ts의 pr?.author == null 판정 — 호출부의 값보다 먼저다. 작성자를 모르면 소속도 모름이다 (DEV-487)
@b 같은 파일의 removed 배열 — 변경 규모 넷과 작성자 팀 하나가 함께 쓴다. 규모를 보는 시험은 sizeRemovals로 자기 몫만 거른다
@path apps/pipeline-worker/src/reindex.ts의 delete doc['author_team_ids'] — undefined 스프레드로 되돌리지 마라. JSON.stringify가 키를 버리는 것에 기대는 암묵적 표현이다
@risk regression/runtime-reachability.test.ts의 계약 정합 시험 — 계약 두 자리가 같은 사실을 말하는지 본다. 한쪽만 갱신하면 이것이 먼저 깨진다 (DEV-488)
@p ---
@h1 이전 세션 기록 (보존)
@h2 (2026-08-31 2차 · CR-057 · WP-041 종료 시점)
@path 최신 기준 (2026-08-31 · WP-041 완료, REL-006 1/4)
@p main = 5dab7f0 — 실측하라. 이 인계 커밋이 한 번 더 옮긴다
@path SRS baseline v2.18 (CR-057) · 원장 review v6.11 · 작업 패키지 v2.11 · API 계약 v0.22 · 추적 매트릭스 v1.2 · 화면 흐름 v0.5
@todo REL-006 1/4. WP-041 done · WP-042·WP-044·WP-069 todo · WP-043 착수 금지
@p 릴리스는 승인되지 않았다 — Gate 4·5·6 그대로
@path open DEV 14건 — DEV-001·006·010·016·026·058·061·304·305·395·399·427·433·447
@path 다음 빈 ID: CR-058 · DEV-477 · WP-070 · 마이그레이션 021 · C-072 (측정값. 쓰기 전 다시 잰다)
@p 미해결 리뷰 0건 — pr-search 103개·design-system 12개 전량 순회
@path 오픈 결정 0건 — OD-008이 CR-057로 닫혔고 그것이 마지막이었다
@p 이 세션이 배운 것 넷.
@path "검사했다"와 "검사한 값이 쓸 때까지 유효하다"는 다른 주장이다. WP-041은 계약이 정한 검사 순서 여덟을 세웠지만 그 순서가 한 트랜잭션 안에 있다고 착각했다 — 5·6은 라우트의 읽기였고 7·8만 트랜잭션 안이었다. 재채번이 그 사이에 커밋하면 무효한 표식이 created로 저장되며 응답은 epoch_stale: false를 말한다(DEV-471). DEV-464(멱등이 시간을 건너 성립하지 않는다)와 같은 모양이며, 그때는 표식이, 여기서는 에폭이 시간에 걸쳐 바뀐다.
@path 정정한 자리가 다음 리뷰의 첫 자리다 — 이제 다섯 번째다. PR #101이 자기 계약에서 넷, #102가 자기 정정에서 하나, #103이 자기 구현에서 셋. 전부 실결함이었다.
@path 상한을 키우는 방식은 언젠가 반드시 그 상한에 닿는다. 병합 PR이 101개가 되자 last: 100이 앞쪽을 잘랐다(DEV-468). 그리고 창은 둘이었다 — 바깥만 고치고 안쪽(스레드)을 탐지만 한 것이 DEV-469다.
@path 순서 단언은 존재 확인을 먼저 해야 한다. indexOf가 없는 문자열에 -1을 주므로, toBeLessThan만 걸면 그 코드를 통째로 지운 변이가 통과한다(DEV-474). expectOrder 헬퍼가 그 자리다.
@h2 시작하기 전에 — 이 인계의 값을 실측하라
@code lang=bash sha=74f0416e2226 lines=5 kept=5
|git -C . rev-parse --short HEAD
|gh pr list --state open --json number --jq 'length'
|grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
|python3 agent-context/count-unresolved-reviews.py
@path 미해결 리뷰는 스크립트가 정본이다 (DEV-468·469). 한 줄 명령으로 되돌아가지 마라 — 창이 둘이고 둘 다 넘칠 수 있으며, 그 스크립트는 순회한 수를 각 totalCount와 대조해 "전량 순회=아니다"면 종료 코드 1을 낸다.
@h2 A. 지금 당장 — WP-042 착수 전 API-SEQ-005 계약 감사
@path WP-041이 끝났고 REL-006의 다음은 WP-042(이분 탐색 보조)다.
@path API-SEQ-005도 계약 4장에 상세 절이 없다. CR-057이 API-SEQ-004를 채우면서 그 사실을 확인했고 의도적으로 함께 채우지 않았다 — 그것은 이 WP의 감사 대상이며, "SEQ API 전부 정리"로 만들면 검증하지 않을 계약을 함께 쓰게 된다. 착수 전 감사가 이 저장소의 관행이며 CR-057이 열일곱 번째였다.
@path CR-057이 세운 것을 그대로 쓸 자리가 많다. 이분 탐색 세션도 시퀀스 공간에 붙고 에폭을 저장하므로, SEQUENCE_EPOCH_STALE·FOR SHARE 재검증·expected_* 조건부 갱신이 같은 모양으로 필요할 수 있다. 다만 그대로 복사하지 말고 FR-SEQ-007이 요구하는 것을 먼저 읽어라 — 탐색 상태는 사용자별이라 겨루는 것이 다르다.
@h2 B. 그 뒤 — WP-044 · WP-069
@path WP-044 검색 결과 내보내기 — 감사 액션 export.create를 활성으로 옮긴다. safe_marker.set이 그 전환의 선례다(SRS v2.18): 상태 칸은 "계약이 승인했으나 기능이 아직 없다"를 뜻하므로 기능이 서는 순간 이전 값이 거짓이 된다.
@path WP-069 작성자 소속 팀 채우기 — author_team_ids가 비어 있어 group_by=team과 author_team:이 언제나 빈 결과를 낸다.
@path WP-043은 착수하지 않는다 — ACC-06이 NOT RUN이라 활성화 조건이 성립하지 않는다.
@h2 C. 배포에서 해야 할 것
@path WP-041이 더한 항목은 없다 — 신규 마이그레이션이 없다.
@path clearUnknownSizes 소급 (CR-056 / DEV-456) — pnpm es:apply-mappings
@path 마이그레이션 020 (CR-055), 018·019 (WP-039), 017 (WP-037) — pnpm run db:migrate
@path ADMIN_DATABASE_URL (WP-039) — 없으면 JOB-AUD-001만 서지 않는다
@b SEARCH_CURSOR_HMAC_KEY, pnpm es:reindex
@b Prometheus 경보 규칙 적용은 여전히 NOT RUN
@h2 D. GitHub Actions — pr-search만 결제 차단이다
@p verify·integration 모두 steps=0이고 주석이 The job was not started because recent account payments have failed를 말한다. 잡이 뜨지도 않은 것이지 시험이 실패한 것이 아니다.
@b 재시도를 누르지 않는다 — 이 세션이 세 PR에서 같은 상태를 확인했다
@b design-system은 다르다. 그 저장소에서는 CI가 판정 근거다
@b pr-search에서는 로컬 배터리가 유일한 판정이며 그 사실을 원장·PR·인계에 적는다
@h2 E. 미뤄 둔 항목
@path DEV-447 open — 조정 스캔의 30초 강제 종료가 증명되지 않았다. Promise.race로 감싸 "강제 종료했다"고 보고하지 마라
@path DEV-433 open — API-ADM-001·API-ADM-003의 오프셋
@path DEV-427 open — CI 통합의 link-rebuild 되먹임. CI가 돌지 않아 확인할 길이 없다
@path DEV-395·DEV-399·DEV-058·DEV-061 — 변화 없음
@h2 F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
@path Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 1,000만 문서 합성 데이터셋 부재) · Gate 6 운영(불가 — 실제 K8s 없음). ACC-06은 NOT RUN이고 W-007·WP-043은 NOT ACTIVATED다.
@h2 G. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
@path packages/db/src/repositories/safe-marker.ts의 FOR SHARE 에폭 재검증 — 대체 트랜잭션 안에서 다시 읽는다. 라우트의 검사만 믿으면 재채번이 그 사이에 커밋한다 (DEV-471)
@path 같은 파일의 검사 순서 7 → 8 — 완전 일치가 expected 대조보다 먼저다. 뒤집으면 정직한 재시도가 자기가 만든 상태로 409를 받는다 (DEV-464)
@b 같은 파일의 safeMarkerLockKey — 채번 락(sequenceLockKey)을 함께 쓰지 마라. 사람이 누르는 요청이 파이프라인을 밀어낸다
@path apps/web/components/RangesView.tsx의 공간 변경 effect — 비우는 것은 공간이 바뀔 때다. loadMarker 안으로 옮기면 등록 결과를 보여 줄 카드가 사라진다 (DEV-472)
@path apps/web/components/SafeMarkerCard.tsx의 제출 앞 setResult(null) — 표식 변화를 보는 effect로 되돌리지 마라 (DEV-473)
@risk regression/runtime-reachability.test.ts의 expectOrder — 순서 단언은 존재 확인을 먼저 한다 (DEV-474)
@path agent-context/count-unresolved-reviews.py — 창이 둘이다. 한 줄 명령으로 되돌아가지 마라 (DEV-468·469)
@p ---
@h1 이전 세션 기록 (보존)
@h2 (2026-08-30 2차 · WP-040 병합 + 머지 후 리뷰 정정 시점)
@path 최신 기준 (2026-08-30 2차 · WP-040 병합 + 머지 후 리뷰 정정)
@path main 1df0191 (PR #89·#90·#91·#92 병합) — 이 인계 커밋이 한 번 더 옮긴다. HEAD를 실측하라
@p main CI는 판정에 쓰지 못했다 — 아래 D절 SRS baseline v2.15 · 원장 review v6.3 · 작업 패키지 v2.8
@path REL-005 4/4 DONE. WP-037·038·039·040 전부 done
@p 릴리스는 승인되지 않았다 — Gate 4·5·6은 그대로다 (F절)
@path open DEV 14건 — DEV-001·006·010·016·026·058·061·304·305·395·399·414·427·433
@path 다음 빈 ID: CR-056 · DEV-443 · 마이그레이션 021 · C-072 (측정값. 쓰기 전 다시 잰다)
@path 미해결 리뷰 31건 (pr-search 22 · design-system 9, DEV-414). PR #89의 넷은 이 세션이 고쳤다
@path PR #89의 머지 후 리뷰 넷이 전부 실결함이었다 (DEV-439~442). 병합 직전에는 스레드가 0건이었고 병합 뒤에 도착했다 — 병합 후 재확인이 그것을 잡았다. 가장 값이 큰 것은 P1이다: 이 세션은 "취소가 완료로 덮이지 않는다"를 유형 다섯 전부에 걸었는데, 그것은 잡 행의 라벨에 관한 성질이었고 일이 실제로 멈추는가는 묻지 않았다.
@p 이 세션이 배운 것 셋.
@path 돌리지 않은 계층이 남기는 것은 통과가 아니라 미실행이다. 이전 세션이 백엔드를 만들고 test:integration을 돌리지 않았다. 이 세션은 화면보다 백엔드 수직 증명을 먼저 했고, 통합 전량 첫 실행에서 실패한 둘 중 하나(DEV-437)는 기존 시험이 이미 잡아 두었으나 아무도 돌리지 않은 실결함이었다. 화면을 먼저 세웠다면 그것을 화면 결함으로 오인했을 것이다.
@b 변이가 살아남았다고 곧바로 시험 구멍이라 부르지 마라. M6(주기 갈래를 else if에서 if로)은 살아남았지만 등가였다 — 둘이 await로 순차 실행되어 겹침이 생기지 않는다. 실제 겹침을 만드는 변이(M6b: 주기 스윕을 await하지 않음)를 따로 걸어 시험이 그것을 잡는지 확인했다. 반대로 M8은 진짜 구멍이었다.
@path 구현이 옳아도 그것을 거는 시험이 없으면 다음 사람이 지운다. FR-ING-009 AC-12(브랜치 추가가 채번을 요청한다)는 구현이 정확했는데 거는 시험이 하나도 없었다. 인자를 빈 배열로 바꿔도 통합 열아홉이 전부 초록이었다.
@h2 시작하기 전에 — 이 인계의 값을 실측하라
@p 여기 적힌 수는 전부 적은 순간의 값이다. 이전 세션이 그것을 실증했다 — 인계 팩이 "백엔드는 이미 전부 서 있다"고 적었는데 틀렸고, 이번 세션은 그 팩이 "26건"이라 적은 미해결 리뷰가 실제로는 22건인 것을 다시 확인했다.
@code lang=bash sha=928d00462e29 lines=4 kept=4
|git -C . rev-parse --short HEAD                      # main HEAD (이 인계 커밋이 옮긴다)
|gh pr list --state open --json number --jq 'length'  # 열린 PR
|grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1   # next-free는 이 값 + 1
@p 미해결 리뷰 계수 전문은 commands.md의 「미해결 리뷰를 세는 법 (두 저장소)」에 있다.
@h2 A. 지금 당장 — DEV-414 미해결 리뷰 31건
@path REL-005가 끝났으므로 다음은 이 위생 작업이다. pr-search 22건(#65(5) #66(3) #67(3) #68(1) #69(2) #70(2) #72(1) #75(5))과 design-system 9건(#1(1) #3(1) #6(1) #8(6))을 HEAD와 하나씩 대조한다.
@b 이미 정정된 것은 근거와 함께 답변하고 resolve한다
@b 아직 고쳐지지 않은 지적은 새 DEV로 등재한다
@b 일괄 resolve하지 않는다 — 그러면 남은 결함이 기록 없이 사라진다
@path 계수 명령 전문은 commands.md의 「미해결 리뷰를 세는 법」에 있다. 저장소 이름은 89sooner/pr-search와 89sooner/design-system이다.
@h2 B. 그 뒤 — REL-006 (WP-041 → WP-042 → WP-044)
@path WP-041(안전 구간 표식)과 WP-044(내보내기)가 감사 액션 safe_marker.set·export.create를 각자 활성으로 옮긴다. WP-040이 미리 활성화하지 않았다. WP-043은 ACC-06이 NOT RUN이라 착수하지 않는다.
@h2 C. 배포에서 해야 할 것
@path 마이그레이션 020 (신규, CR-055) — 등록 검토 요청의 처리 결과 열. pnpm run db:migrate
@path ADMIN_DATABASE_URL (WP-039) — 없으면 JOB-AUD-001만 서지 않는다
@path 마이그레이션 018·019 (WP-039), 017 (WP-037)
@b pnpm es:apply-mappings (changed_lines 백필), SEARCH_CURSOR_HMAC_KEY, pnpm es:reindex
@b Prometheus 경보 규칙 적용은 여전히 NOT RUN
@path 새 환경 변수는 없다 — API-ADM-009의 커서 서명자가 기존 SEARCH_CURSOR_HMAC_KEY를 그대로 쓴다
@h2 D. GitHub Actions가 돌지 않는다 — 사용자 조치가 필요하다
@p CI 잡이 시작조차 하지 않는다. 주석이 그대로 말한다: The job was not started because recent account payments have failed or your spending limit needs to be increased. 재시도 1회도 같은 이유로 끝났고 단계가 0개다.
@path 이것은 코드 실패가 아니다. WP-040은 로컬 전 계층(typecheck·lint·lint:deps·단위 1856·통합 1389·회귀 284·a11y 346·대비 232쌍·e2e 147·빌드)으로 판정했다
@b 실제 시험 실패를 인프라 실패로 부르지 않는다 — 이것은 잡이 뜨지도 않은 경우이며 그 둘은 다르다
@b 다음 세션이 CI를 판정 근거로 쓰려면 결제 문제가 먼저 풀려야 한다. 그전까지는 로컬 배터리가 유일한 판정이며, 그 사실을 적고 진행한다
@h2 E. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
@p | DEV | 역할 | 왜 아직 | 소관 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
@path | 304 | backfill (JOB-ING-004) | enrich 파드 역할 구성 판단 | WP-019 |
@h2 F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
@path REL-005 4/4는 작업 패키지가 끝났다는 뜻이지 배포해도 된다는 뜻이 아니다.
@b Gate 4 보안 (일부 가능)
@b Gate 5 성능 (불가 — 1,000만 문서 합성 데이터셋 부재)
@b Gate 6 운영 (불가 — 실제 K8s 없음)
@path ACC-06은 NOT RUN이고 W-007·WP-043은 NOT ACTIVATED다
@h2 G. 미뤄 둔 항목
@path DEV-433 open — API-ADM-001·API-ADM-003 목록의 오프셋이 공통 원칙 7·ADR-010과 어긋난다. CR-055가 걷어 내지 않고 등재만 했다 — 두 API의 소비자를 전부 다시 세는 별도 작업이다. 신설한 API-ADM-009에는 처음부터 오프셋이 없다
@path DEV-427 open — CI 통합의 link-rebuild 되먹임 시험. 이번 세션의 통합 전량 두 번에서 재현되지 않았으나 그것을 해소로 적지 않는다. 그리고 CI가 돌지 않아 CI 쪽 재현은 확인할 길이 없었다
@path DEV-395 자기 리뷰 소급 · DEV-399 원장 5장 파이프 이스케이프 회귀 · DEV-058 test:perf(Gate 5) · DEV-061 direct_push — 변화 없음
@b 문서 검증기 --strict 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다. 판정은 언제나 main 대비 diff이며 이번 세션의 새 경고는 0건이었다
@h2 H. WP-040이 깔아 둔 자리 — 다시 만들지 말 것
@path apps/web/lib/{ops-jobs,ops-repositories,ops-pipeline}.ts — 화면 판정의 전부. 컴포넌트 안에 판정을 흩뿌리지 않는다
@path apps/web/components/JobStatusBadge.tsx — 잡 상태 여섯을 Conductor Status 일곱으로 옮기는 유일한 자리. A-002의 채번 안내도 이것을 쓴다
@b 파괴적 조작의 방어선은 핸들러 안의 조건이지 disabled가 아니다. 네 곳(재채번·해제·요청 종료·일괄 재처리)이 같은 모양이며 e2e가 네트워크 호출 수로 센다 — 버튼 비활성 스냅숏으로 바꾸지 마라
@b page-guard.tsx의 GuardedPageContext — 관문이 역할과 인증 구성 여부를 본문에 넘긴다. 라우트가 세션을 직접 읽지 않는다는 규율은 그대로이며 architecture.test.ts가 센다
@path resolveJobTarget의 두 갈래가 모두 슬러그 형식을 검사한다 (DEV-437). 회귀가 그 대칭을 센다
@path apps/search-api/integration/admin/repositories.test.ts의 채번 예약 시험 셋 — 차분으로 센다(등록이 이미 예약한 것과 이 조작이 더한 것을 가른다). 절대값으로 바꾸면 변이 M8이 되살아난다
@path apps/web/a11y/setup.ts의 ResizeObserver 대역 — Radix Switch가 요구한다. 크기를 재는 동작을 흉내 내지 않는다
@p ---
@p ---
@h2 (2026-08-30 · CR-055 병합 · WP-040 구현 중 시점)
@path 최신 기준 (2026-08-30 · CR-055 병합 · WP-040 구현 중)
@path main 5475249 (PR #88 병합) · main CI verify·integration 통과
@path 구현 브랜치 claude/wp-040-operations-console이 로컬에만 있다 — 커밋 셋(d877b1b·1c41e11·35b9ff0), push 안 됨, PR 없음
@p SRS baseline v2.15 · 원장 review v6.1 · 작업 패키지 v2.8
@path REL-005 3/4. WP-040은 진행 중이며 완료가 아니다 — 백엔드와 판정 계층은 섰고 화면 셋이 남았다
@path open DEV 15건 — 기존 13 + DEV-433·DEV-436
@path 다음 빈 ID: CR-056 · DEV-437 · 마이그레이션 021 · C-072 (측정값. 쓰기 전 다시 잰다)
@path 미해결 리뷰 33건 (pr-search 24 · design-system 9, DEV-414). 실측한 수이며 잘림 없음
@p 이 세션이 배운 것 셋.
@path 인계 문서의 판정을 실측 없이 믿지 마라. 이전 팩이 "백엔드는 이미 전부 서 있으므로 WP-040은 사실상 프런트엔드 전용"이라 적었는데 틀렸다. 화면이 누르라고 정한 동작 다섯이 정본에서 도달할 곳을 갖고 있지 않았다. 팩은 의도를 복원하지 진실을 복원하지 않는다.
@b 배포 경계와 실행 경계는 다르다. 조정 스캔의 직렬화 근거로 "복제본 1개"를 들었는데, 그것은 프로세스 수를 제한할 뿐 한 프로세스 안의 독립적인 비동기 루프 둘을 직렬화하지 못한다. 락을 더하는 대신 루프를 하나로 만들었다 — 겹칠 수 있는 구조를 만든 뒤 막는 것보다 겹칠 수 없는 구조가 낫다.
@path 전제가 사라졌다고 결론까지 뒤집지 마라. CR-034(DEV-180)가 sequence_assign 잡 행을 거절한 근거는 둘이었다. 러너를 세워 첫 번째("러너가 없다")는 없앴지만 두 번째("영원히 queued인 행이 이후의 모든 복구를 막는다")는 그대로라, 수동 실행만 잡 행을 쓰고 조정 스캔의 자동 복구는 계속 버스 이벤트로 간다.
@h2 A. 지금 당장 — WP-040 화면 셋 (구현 브랜치를 이어받는다)
@path 브랜치가 로컬에만 있다. git checkout claude/wp-040-operations-console으로 이어받고, 세션 후반의 rate limit을 고려해 초기 골격이 서면 PR을 먼저 연다 (risks.md의 「GitHub secondary rate limit」).
@path 이미 선 것을 다시 만들지 말 것 — 아래 「WP-040이 깔아 둔 자리」.
@p 남은 작업은 순서대로 다음과 같다.
@path 컴포넌트 아홉 — C-040 PipelineMetricGrid, C-041 DeadLetterTable, C-042 ScanResultCard, C-043 RepositoryRegistrationForm, C-044 JobTable, C-045 JobRunForm, C-046 IndexStatusPanel, C-047 IntegrityReportCard, C-071 RegistrationRequestQueue. 계약은 pr_search_ui_component_spec.md v0.10이 정본이다.
@path 화면 셋 — apps/web/app/ops/{pipeline,repositories,jobs}/page.tsx. apps/web/app/ops/audit/page.tsx가 구조 선례다(GuardedPage + 클라이언트 뷰, export const dynamic = 'force-dynamic').
@b 통합 시험 — 등록 요청 수명주기, 러너 둘의 도달, 취소 경합. 실 PostgreSQL이 필요하다.
@path e2e FLOW-007·FLOW-008 — 확인 다이얼로그는 실제 네트워크 호출 수로 증명한다. 버튼 비활성 스냅숏은 증명이 아니다.
@b a11y·대비 — axe 위반 0건.
@b 변이 시험 — 지시서의 M1~M12. 치환 건수가 APPLIED (1)인지 반드시 확인한다.
@path 원장 갱신 — WP-040 상태와 검증 기록, DEV-436 종결.
@path 마이그레이션 020 왕복 — 실 PostgreSQL에서 up/down/up.
@h3 WP-040이 깔아 둔 자리 — 다시 만들지 말 것
@path apps/search-api/src/ops/jobs.ts의 resolveJobTarget — 잡 유형마다 다른 요청 본문을 target 하나로 옮기는 유일한 자리다. reconcile은 대상을 받지 않고 sequence_assign은 저장소·브랜치를 받아 서버가 조립한다. 라우트가 문자열을 직접 만들지 않는다.
@path apps/search-api/src/ops/jobs.ts의 allowedActionsForJob — 상태가 정한 상한을 잡 유형이 더 좁힌다. reconcile은 pause를 지원하지 않는다. 화면이 이 판정을 다시 하지 않는다.
@path packages/db/src/repositories/job.ts의 allowedActionsFor — 전이 표(ALLOWED_FROM)에서 파생한다. 표를 두 벌 만들지 않는다.
@path packages/domain/src/sequence.ts의 parseSequenceSpaceLabel — sequenceSpaceLabel의 역함수이며 첫 @에서 자른다(브랜치 이름에 @가 있을 수 있다). 러너마다 파서를 두지 않는다.
@path apps/search-api/src/ops/registration-request-cursor.ts — PostgreSQL 키셋. 지문에 접근 범위를 넣지 않는다. 지문 불일치는 CURSOR_QUERY_MISMATCH이며 CURSOR_INVALID가 아니다.
@path apps/search-api/src/ops/repositories.ts의 enqueueSequenceAssign·newBranches — 브랜치 추가의 채번 예약. 빠진 브랜치는 돌려주지 않는다.
@path apps/pipeline-worker/src/reconcile.ts — 주기와 수동이 한 루프다. runReconcileSweep 호출 지점이 둘(수동·주기)뿐이며 회귀 시험이 그 수를 센다. 두 번째 루프를 만들지 마라.
@path apps/web/lib/{ops-jobs,ops-repositories,ops-pipeline}.ts — 화면 판정의 전부. 컴포넌트 안에 판정을 흩뿌리지 않는다.
@h2 B. 그 뒤 — DEV-414 미해결 리뷰 33건
@path WP-040이 병합되면 REL-005가 4/4이며, 그 다음이 이것이다. pr-search 24건과 design-system 9건을 HEAD와 대조해 하나씩 실측한다. 이미 정정된 것은 근거와 함께 답변하고 resolve하며, 아직 고쳐지지 않은 지적은 새 DEV로 등록한다. 일괄 resolve하지 않는다.
@p 계수 명령 전문은 commands.md의 「미해결 리뷰를 세는 법」에 있다.
@h2 C. 그 뒤 — REL-006 (WP-041 → WP-042 → WP-044)
@path WP-041(안전 구간 표식)과 WP-044(내보내기)가 감사 액션 safe_marker.set·export.create를 각자 활성으로 옮긴다. WP-040이 미리 활성화하지 않았다. WP-043은 ACC-06이 NOT RUN이라 착수하지 않는다.
@h2 D. 배포에서 해야 할 것
@path 마이그레이션 020 (신규, CR-055) — 등록 검토 요청의 처리 결과 열. pnpm run db:migrate
@path ADMIN_DATABASE_URL (WP-039) — 없으면 JOB-AUD-001만 서지 않는다
@path 마이그레이션 018·019 (WP-039), 017 (WP-037)
@b pnpm es:apply-mappings (changed_lines 백필), SEARCH_CURSOR_HMAC_KEY, pnpm es:reindex
@b Prometheus 경보 규칙 적용은 여전히 NOT RUN
@path 새 환경 변수는 없다 — API-ADM-009의 커서 서명자가 기존 SEARCH_CURSOR_HMAC_KEY를 그대로 쓴다.
@h2 E. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
@p | DEV | 역할 | 왜 아직 | 소관 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
@path | 304 | backfill (JOB-ING-004) | enrich 파드 역할 구성 판단 | WP-019 |
@h2 F. 릴리스 게이트 4·5·6 (변화 없음)
@path Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 1,000만 문서 합성 데이터셋 부재) · Gate 6 운영(불가 — 실제 K8s 없음). ACC-06은 NOT RUN이고 W-007·WP-043은 NOT ACTIVATED다.
@h2 G. 미뤄 둔 항목
@path DEV-433 open — API-ADM-001·API-ADM-003 목록의 오프셋이 공통 원칙 7·ADR-010과 어긋난다. CR-055가 걷어 내지 않고 등재만 했다 — 두 API의 소비자를 전부 다시 세는 별도 작업이다. 신설한 API-ADM-009에는 처음부터 오프셋이 없다.
@path DEV-436 open — 백필 러너의 취소 경합. 코드는 고쳤으나 원장 상태를 닫지 않았다. 구현 PR 병합 시 닫는다.
@path DEV-427 open — CI 통합의 link-rebuild 되먹임 시험. 이번 세션에서 재현되지 않았으나 그것을 해소로 적지 않는다.
@path DEV-395 자기 리뷰 소급 · DEV-399 원장 5장 파이프 이스케이프 회귀 · DEV-058 test:perf(Gate 5) · DEV-061 direct_push — 변화 없음
@b 문서 검증기 --strict 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다. 판정은 언제나 main 대비 diff다
@p ---
@h2 (2026-08-29 3차 · CR-054 · WP-039 종료 시점)
@path 최신 기준 (2026-08-29 3차 · CR-054 · WP-039 종료 시점)
@p main 6bdf395 · main CI verify·integration 통과 SRS baseline v2.14 · 원장 review v6.0 · 작업 패키지 v2.7
@path REL-004 8/8. REL-005 3/4 — WP-037·038·039 done, 남은 것은 WP-040 하나
@path REL-003 릴리스 게이트는 여전히 미통과. Gate 5 성능은 NOT RUN(1,000만 문서 합성 데이터셋 부재)
@p 열린 PR 0건(양 저장소)
@path 미해결 리뷰는 0건이 아니다 — 병합된 PR 열에 24건이 남아 있다(DEV-414). 아래 C절
@path open DEV 13건 — DEV-001·006·010·016·026·058·061·304·305·395·399·414·427
@path 다음 빈 ID: CR-055 · DEV-428 (측정값. 쓰기 전 다시 잰다)
@path DEV-427 열려 있다 — CI 통합의 link-rebuild 되먹임 시험이 한 번 실패했고 세 번
@p 재현하지 못했다. 재시도 통과를 해소로 적지 않는다
@path DEV-377(flow-003 뒤로가기)이 닫혔다 — 진단이 달랐고 CI가 그것을 드러냈다(DEV-424)
@p 이 세션이 배운 것 셋.
@b "미해결 리뷰 0건"은 세어 본 적이 없는 수였다. 근거는 "후속 PR에서 고쳤다"였지
@p GitHub 스레드 상태가 아니었다. 이제 GraphQL로 isResolved를 직접 센다. 명령 전문은 commands.md의 「미해결 리뷰를 세는 법」에 있다 — 여기 옮겨 적지 않는다.
@path 처음 쓴 last:25가 PR #32·#49를 조용히 빼고 22건이라 답했고(실제 24건), --paginate는
@p 더 나빴다(중복 다섯·누락 하나·오류 종료). 상한을 100으로 올리고 두 pageInfo를 읽어
@path 잘림을 드러내는 것이 정답이다 (DEV-425).
@path 개수를 계약에 적지 말라. WP-039가 "13종", 보안 문서 표가 14행, 액션 문자열이 18개였고
@path 셋 중 어느 것도 틀리지 않았다. CR-054가 정본을 목록으로 바꿨다.
@path 문서가 가리키는 곳을 실제로 열어 보라. RB-18의 잘못된 잡 참조를 확인하러 갔다가
@path 파티션 생성 정기 잡이 아예 없다는 것을 찾았다(DEV-417). 배포 후 세 달이면 감사와
@p 원본의 INSERT가 전부 거부되는 자리였다.
@h2 A. 지금 당장 — WP-040 A-002·A-003 운영 콘솔 (REL-005 마지막)
@path 관련 요구사항은 작업 패키지가 정본이다 — FR-ING-009·FR-ADMIN-002·FR-ADMIN-003·FR-ING-006·FR-ING-008
@path (pr_search_work_packages.md WP-040). 여기 옮겨 적은 값이 아니라 그 문서를 읽는다.
@path 선행 WP-010·019·028·035(전부 done).
@path 화면 셋이 남았다: A-001 파이프라인(아카이브 섹션 포함), A-002 저장소 등록, A-003 잡 운영.
@path WP-039가 진입점을 이미 올바르게 갈랐다 — nav.ts의 allowedRoles가 operator에게
@path 파이프라인·저장소 등록을, security_officer에게 감사 기록과 파이프라인(A-001-ARCHIVE만)을
@p 보인다. 화면 본체가 그 규칙을 다시 만들지 않게 하라 — 서버가 403을 내고 화면은
@path no_permission을 그린다(A-004 선례).
@path 착수 전 계약 감사(레포 관례, CR-054가 열다섯 번째였다): QA-A001-01~14가 실제로 셋인지,
@path A-001-ARCHIVE의 archive_only 상태가 구현 가능한지, API-ADM-002의 pause/resume가
@p 화면에 있는지.
@h3 WP-039가 깔아 둔 자리 — 다시 만들지 말 것
@path packages/domain/src/audit.ts — 감사 액션의 정본 어휘다. 활성·미활성·legacy 셋을 가르며
@path @prs/domain/audit 서브패스로 브라우저도 쓴다. 개수를 세지 않는다.
@path apps/search-api/src/audit/recorder.ts — 공용 실패 격리 경계 하나다. 호출부마다
@p try/catch를 두지 않는다. 회귀가 auditRepo.recordAudit(의 직접 호출을 다섯 파일에서 금지한다.
@path apps/search-api/src/audit/cursor.ts — PostgreSQL 키셋. 지문에 접근 범위를 넣지 않는다
@p (감사는 행위의 기록이라 저장소 권한이 답을 바꾸지 않는다).
@path packages/db/src/partitions.ts — runPartitionRetention이 만들고 나서 지운다. 경계는
@p 카탈로그에서 읽고 이름으로 추측하지 않는다.
@path apps/web/lib/nav.ts — 항목마다 allowedRoles. 새 항목·새 역할의 기본값은 보이지 않음이다.
@path AuditRecordTable — C-013을 쓰지 않는다(RangeResultTable 선례, DEV-419).
@h2 B. 그 뒤 — REL-006 (WP-041·042·044)
@path WP-041(안전 구간 표식)과 WP-044(내보내기)는 감사 액션을 함께 활성화한다.
@path 정본 표(srs_final.md FR-AUTH-004 AC-1)의 safe_marker.set·export.create를 활성으로
@path 바꾸고 배선을 넣는다. 지금 그것이 기록되지 않는 것은 누락이 아니다(DEV-403).
@path WP-043은 착수하지 않는다 — ACC-06이 NOT RUN이라 활성화 조건이 성립하지 않는다.
@h2 C. 미해결 리뷰 24건 (DEV-414)
@p 세어 보니 0건이 아니었다. 그리고 처음 센 수도 잘려 있었다.
@p | PR | 미해결 | 라운드 | | --- | --- | --- |
@path | #32 | 1 | CR-030 |
@path | #49 | 1 | CR-043 |
@path | #65 | 5 | CR-052 |
@path | #66 | 3 | CR-052 |
@path | #67 | 3 | CR-052 |
@path | #68 | 1 | CR-052 |
@path | #69 | 2 | CR-052 |
@path | #70 | 2 | CR-052 |
@path | #72 | 1 | CR-052 |
@path | #75 | 5 | CR-053 |
@path 처음 쓴 계수 명령이 last:25라 #32·#49를 조용히 빼고 22건이라 답했다. last:100으로
@p 다시 세니 24건이다 — commands.md의 명령이 두 pageInfo를 읽어 잘림을 드러낸다. design-system 저장소에도 9건이 있다(같은 방식으로 다시 세야 한다).
@p 지적 자체는 대부분 후속 PR에서 이미 정정된 것으로 보이나 실측 없이 그렇게 단정하지 않는다. 스레드마다 실측해 정정된 것은 답변·resolve하고, 아직 고쳐지지 않은 지적이 있으면 DEV로 등록한다.
@h2 D. 배포에서 해야 할 것
@path ADMIN_DATABASE_URL (WP-039 신규) — 없으면 JOB-AUD-001만 서지 않는다. 값은
@path prs_admin이 아니라 그 롤의 멤버십을 가진 로그인 주체를 가리킨다(DEV-416).
@p CREATE ROLE prs_retention LOGIN ...; GRANT prs_admin TO prs_retention;
@b 마이그레이션 018·019 — pnpm run db:migrate. 019는 파티션 표의 소유권을 prs_admin으로
@p 옮긴다. prs_app의 INSERT·SELECT는 그대로다(실측 확인).
@path pnpm es:apply-mappings(changed_lines 백필), 마이그레이션 017 — WP-037이 남긴 것, 아직 미적용.
@path SEARCH_CURSOR_HMAC_KEY(32자 이상, 복제본 공통), pnpm es:reindex — WP-032가 남긴 것.
@b 실제 Prometheus 경보 규칙 적용은 NOT RUN이다. 지표(audit_record_failed_total)와 계약은
@p 섰고 환경 적용만 남았다.
@h2 E. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
@p | DEV | 역할 | 왜 아직 | 소관 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
@path | 304 | backfill (JOB-ING-004) | enrich 파드 역할 구성 판단 | WP-019 |
@h2 F. 릴리스 게이트 4·5·6 (변화 없음)
@p Gate 4 보안(일부 가능) · Gate 5 성능(여전히 불가 — test:perf harness는 있으나 1,000만 문서
@path 합성 데이터셋이 없다) · Gate 6 운영(불가 — 실제 K8s 없음). ACC-06은 NOT RUN이고
@path W-007·WP-043은 NOT ACTIVATED다.
@h2 G. 미뤄 둔 항목
@path ~~DEV-377 flow-003 뒤로가기~~ → 닫혔다(DEV-424). 진단이 "5초 초과"였는데 실제 원인은
@path 히스토리 경합이었다 — 첫 뒤로가기의 전이가 끝나기 전에 두 번째를 불렀다. WP-039가
@p e2e를 9건 더해 부하가 커지자 CI를 실제로 막았고 그 로그가 원인을 드러냈다. 두 번째 goBack() 전에 URL을 먼저 확인하도록 고친 뒤 전량 4회 연속 통과. 증상으로 적어 둔 판정은 오래 열려 있는다
@path DEV-399 원장 5장 아홉 행의 파이프 이스케이프 + 칸 수를 세는 회귀 시험
@path DEV-395 자기 리뷰 소급 — 재투영이 필요하고 그것은 백필·조정 스캔의 기계다
@path DEV-058 test:perf — harness는 있고 릴리스 규모 실측만 남았다 (Gate 5)
@path DEV-061 direct_push — AC-3 충족을 실측하지 못해 열어 둔다
@b 문서 검증기 --strict 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다.
@p 판정은 언제나 main 대비 diff다
@h1 이전 세션 기록 (보존)
@h2 (2026-08-29 2차 · CR-036 · WP-038 종료 시점)
@h2 A. 지금 당장 — WP-039 감사 기록과 A-004 (REL-005)
@path FR-AUTH-004, NFR-006. 감사 대상 13종 기록(보안 문서 7장), security_officer 한정 조회, A-004 화면(C-013 재사용).
@path 선행 WP-012·WP-002(done). API-ADM-005, ENT-CORE-007. JOB-AUD-001 보존 만료 파티션 드롭.
@b 착수 전 계약 감사(레포 관례): 감사 액션 목록·비동기 기록의 실패 격리·감사 화면 진입 자체 기록.
@h2 B. 그 다음 — WP-040 A-002·A-003 운영 콘솔 (REL-005 마지막)
@h2 C. 배포에서 해야 할 것 (WP-037·CR-053이 남긴 것 — 아직 실환경 미적용)
@b pnpm es:apply-mappings(changed_lines 백필), 마이그레이션 017.
@path WP-032가 남긴 SEARCH_CURSOR_HMAC_KEY, pnpm es:reindex.
@h2 D. 릴리스 게이트 4·5·6 (변화 없음)
@path Gate 5 성능은 여전히 불가 — test:perf harness는 있으나 1000만 문서 합성 데이터셋이 없다. ACC-06 NOT RUN(W-007·WP-043 NOT ACTIVATED).
@p ---
@h2 (완료) WP-038 W-006 통계 대시보드 — 2026-08-29 (2차) done (원장 6.49장, PR #80 b5ae933)
@path 아래는 착수 시점 기록이다. DEV-380은 CR-036 dataviz 0.2.0 배포로 해소됐고 이 WP가 소비한다.
@path 착수 전에 결정이 하나 필요하다: DEV-380. Conductor 토큰 264개에 계열 구분 색이 없다
@p (chart·viz·series·categorical 어느 이름도 0건). 그런데 화면 계약은 20계열을 색으로
@path 구분하라고 요구한다(W-006-TIMESERIES, C-033). accent는 넷뿐이고 status·severity를
@path 돌려 쓰는 길은 ADR-006이 어휘 오염으로 금지한다.
@p 결정은 둘 중 하나다 — design-system 기여(계열 팔레트 신설)와 제품 측 파생(기존 토큰에서 파생한
@path 스케일). 어느 쪽이든 ADR-006의 의미를 건드리면 CR이 먼저다. 완화는 이미 계약 안에 있다:
@path C-033·C-034가 표 대체를 요구하므로 색만으로 정보를 주는 화면은 승인되지 않았다(NFR-007).
@path WP-037이 깔아 둔 자리 — 다시 만들지 말 것
@path apps/search-api/src/analytics/prepare.ts — 질의 준비의 유일한 자리다. 파싱 → 모집단 확정
@path → 접근 범위 → 시퀀스 문맥 → 에폭 순서가 CR-051이 /search에 세운 것과 같다. 새 집계
@p 엔드포인트가 생겨도 이 순서를 다시 만들지 않는다
@path apps/search-api/src/analytics/execute.ts — 근사 여부는 _count로 세어 정한다.
@p track_total_hits로 대신하지 않는다: 그것은 히트 계수만 제한하고 집계 순회는 제한하지
@path 않으며, 상한까지만 센 수를 모집단 크기로 쓰면 표본 비율이 1에 가까워진다(PR #76 리뷰 P1 둘).
@p timed_out도 함께 본다 — 200에 붙어 오는 부분 결과다
@b ANALYTICS_TARGET은 prs-pull-requests 단독이다. 목록(SEARCH_TARGET)과 다르며,
@p 커밋을 넣으면 그룹 키 셋과 지표 둘이 조용히 0이나 unknown이 된다
@b drill_down_query는 replaceEquality를 쓴다 — 더하면 OR가 남아 목록이 버킷보다 큰 수를
@p 보인다. kind:pull_request가 반드시 들어가고 team 그룹은 author_team:을 쓴다
@b 그룹 키 표시값은 일괄 해석한다(resolveGroupDisplay). org_id·author_team_ids는
@p 숫자인데 질의는 이름·slug를 받으므로, 숫자를 그대로 넣으면 눌러도 0건이다
@path 리드타임 백분위는 is:merged로 모집단을 좁힌다 (FR-STAT-003 AC-2). 좁히지 않으면 열린 PR이
@p 제외로 세어져 no_review로 잘못 분류된다
@b pnpm test:perf는 왕복 수를 잰다. 지연은 데이터 크기를 따라가지만 "그룹마다 다시 묻는가"는
@p 크기와 무관하다. 클러스터를 부르는 모든 길(search·count·msearch)을 센다
@h2 B. 배포에서 해야 할 것
@b pnpm es:apply-mappings를 돌린다 — changed_lines 매핑과 기존 문서 백필이 함께 간다
@p (backfillDerivedFields). 돌리지 않으면 과거 PR이 전부 분포의 unknown 구간에 들어간다
@path 마이그레이션 017을 적용한다 — pnpm run db:migrate (CR-053은 새 마이그레이션을 만들지 않았다)
@path WP-032가 남긴 둘은 변화 없다: SEARCH_CURSOR_HMAC_KEY(32자 이상, 복제본 공통)와
@p pnpm es:reindex --alias prs-pull-requests·--alias prs-commits
@h2 C. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
@p | DEV | 역할 | 왜 아직 | 소관 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
@path | 304 | backfill (JOB-ING-004) | enrich 파드 역할 구성 판단 | WP-019 |
@h2 D. 릴리스 게이트 4·5·6 (변화 없음)
@p Gate 4 보안(일부 가능) · Gate 5 성능(여전히 불가 — test:perf harness는 생겼으나 1000만 문서 합성 데이터셋이 없다) · Gate 6 운영(불가 — 실제 K8s 없음).
@path ACC-06은 NOT RUN이다 (2026-08-29 판정). 검수 가능한 실제 관계 표본이 없다 — prs-links
@p 1건, 정본 전부 0건, 이 저장소 자체에도 되돌림·체리픽 0건. 미달이 아니라 측정하지 못한 것이며
@path → W-007·WP-043은 NOT ACTIVATED다.
@h2 E. 미뤄 둔 항목
@path DEV-395 자기 리뷰 소급 — WP-037이 판정을 고쳤으나 이미 색인된 문서는 옛 값을 유지한다.
@p changed_lines와 달리 재료가 문서 안에 없어 update_by_query로 되계산할 수 없다(어느 리뷰어가 언제 리뷰했는지가 문서에 없다). 재투영이 필요하고 그것은 백필·조정 스캔의 기계다
@path DEV-380 계열 색 — 위 A절
@path DEV-058 test:perf — harness는 생겼고 릴리스 규모 실측만 남았다 (Gate 5)
@path DEV-364 스칼라 seq: → 닫혔다 (2026-08-29). 부정형이 0건이 아니라 전체였고(DEV-378)
@path 같은 결함이 merged·created에도 있었다(DEV-379)
@path DEV-377 flow-003 뒤로가기 5초 — WP-016 소관. 재시도 통과를 해소로 적지 않는다
@path DEV-061 direct_push — 상태 칸에 해소 기록이 있으나 AC-3 충족을 실측하지 못해 열어 둔다
@b 문서 검증기 --strict 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다
@p ---
@h1 이전 세션 기록 (보존)
@path 아래 A절은 CR-052·WP-036 구현으로 해소됐다 — REL-004가 8/8이 됐고, 그 뒤 CR-053과
@path WP-037까지 들어와 REL-005가 1/4이다. 다음 작업은 위 A절(WP-038)이다.
@p 아래는 그때의 판단 근거를 남긴 것이며 현재 상태의 주장이 아니다.
@h2 (2026-08-28 CR-051 · DEV-349 종료 시점의 기록)
@h2 A. 지금 당장 — WP-036 원본 아카이브 레인 (REL-004 마지막)
@path 착수 전 감사부터 한다. FR-ING-010의 AC 다섯은 SRS에 서 있지만 Filebeat 설정·ILM 정책·
@p 아카이브 인덱스가 SRS 밖에서 검증된 적이 없어 보인다 — 계약 공백이 나올 가능성이 높다.
@path "착수 전 감사에서 공백이 나왔다"가 CR-029 이후 거의 매번 반복됐다.
@todo next-free는 실측할 것(CR-052 / DEV-366 / 마이그레이션 018 예상이지만 추측해 쓰지 않는다):
@code lang=bash sha=e93add1a64fe lines=3 kept=3
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|ls packages/db/migrations/*.up.sql | tail -1
@p 착수 전에 실측한 선행 자산 (이 세션에서 확인했다)
@p | 있는 것 | 없는 것 | | --- | --- |
@path | apps/ingest-gateway/src/archive.ts — NDJSON 아카이브 기록부 (WP-004 산출물) | packages/es/src/에 prs-raw-events-* 인덱스 정의 |
@p | 문서 언급 — ADR·시스템 아키텍처·인프라 운영·데이터 모델·관측 다섯 | Filebeat 배포 manifest |
@path | deploy/k8s/README.md의 적용 순서 표 | ES의 아카이브 인덱스 (현재 prs-{commits,pull-requests}-v2·{links,releases}-v1 넷뿐) |
@p 감사에서 물을 것. archive.ts가 실제로 기록하는 NDJSON 형식과 오프셋 처리가 Filebeat 설정의 전제를 만족하는가. ILM 보존 기간이 어디에 정해져 있는가. operator·security_officer 한정 조회를 어느 API가 제공하는가 — 없으면 그것이 계약 공백이다.
@p 새 역할을 만들면 셋이 함께 간다: ① 코드 갈래 ② manifest ③ 인프라 3장 배포 단위 표.
@path 표에 먼저 올리는 길은 시험이 막고(DEV-312), 표에 중복 행을 만들지 않으며(DEV-321), 표 아래
@path 산문도 함께 고친다(DEV-322).
@h3 CR-051이 깔아 둔 자리 — 다시 만들지 말 것
@path packages/query/src/sequence-binding.ts — seq:가 어느 공간을 뜻하는지 판정하는 유일한 자리다.
@p 순수 함수이며 DB도 ES도 모른다(브라우저가 같은 코드를 쓴다). 라우트마다 다시 만들면 규칙이 두 곳에 살고, 한쪽만 넓어지는 날 아무 오류도 안 난다
@path packages/es/src/query-builder.ts의 buildQuery는 seq: 범위가 있는데 에폭이 없으면 던진다.
@p 선택 인자로 되돌리지 않는다 — 빠지면 오류 없이 모든 세대를 함께 돌려주고, 그 실패는
@path 결과를 재는 시험에 잡히지 않는다 (fail-open이라 DEV-353보다 나쁘다)
@path apps/search-api/src/search/sequence-context.ts — 검색의 4-1단계. 접근 범위 산출 뒤에 온다
@p (공간 해석이 접근 통제를 지나야 하고, 미등록과 범위 밖이 같은 404여야 한다). 새 해석 경로를 만들지 않고 resolveSpace를 재사용한다
@path apps/search-api/src/saved-search/sequence-reference.ts — 목록 판정은 페이지 단위 일괄이다.
@p toResource() 안에서 DB를 부르지 않는다. needsSequenceReference로 인용이 있을 때만 접근 범위를 산출한다 — 모든 조회에 그 의존을 지우면 저장된 검색이 접근 통제 경로에 참여한다
@path (CR-049가 /run에서 피한 자리이며, 전 계층 통합이 외래 키 연쇄로 실제로 잡았다)
@b readEpochParam은 세 상태(absent/value/invalid)를 구분한다. absent는 파라미터가 정말로
@p 없을 때뿐이다 — 빈 값·비문자열은 invalid다. 옛 parseEpochParam은 지웠다
@path apps/web/lib/query-url.ts의 QueryState.seqEpoch은 문자열 원문이다. 화면이 형식을 판정하지
@p 않는다 — 접으면 서버가 거절할 기회를 잃는다
@path 커서 계열 넷(W-001 PIT·W-004 정본 서수·W-008·W-009 키셋)은 여전히 합치지 않는다
@h2 B. 배포에서 해야 할 것
@b 마이그레이션 017을 적용한다 — pnpm run db:migrate
@path WP-032가 남긴 둘은 변화 없다: SEARCH_CURSOR_HMAC_KEY(32자 이상, 복제본 공통)와
@p pnpm es:reindex --alias prs-pull-requests·--alias prs-commits
@h2 C. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
@p | DEV | 역할 | 왜 아직 | 소관 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
@path | 304 | backfill (JOB-ING-004) | enrich 파드 역할 구성 판단 | WP-019 |
@h2 D. 릴리스 게이트 4·5·6 (변화 없음)
@p Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 합성 데이터셋·test:perf 없음) · Gate 6 운영(불가 — 실제 K8s 없음).
@h2 E. 미뤄 둔 항목
@path DEV-364 스칼라 seq:1234 — 파서를 통과하지만 query-builder의 TERM_FIELDS에 seq가 없어
@p MATCH_NONE이 되고 조용히 0건이 된다. 고치려면 제품 결정이 필요하다: 파서가 문법 오류로
@path 거절할 것인가, merge_seq 단일값 조회로 승인할 것인가. DEV-349와 같은 축이 아니다 —
@p 스칼라는 언제나 0건이라 "다른 커밋을 가리키는" 실패가 아니다
@path flow-001·flow-003 e2e 간헐 실패 — WP-016 소관. 재시도 통과를 해소로 적지 않는다
@b 문서 검증기 --strict 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다
@path PIT의 클러스터 자원 비용 · 패싯·커서의 운영 규모 성능 (DEV-058) — NOT RUN
@b prs-links 재색인 커버리지 판정
@b 저장된 검색 목록의 시퀀스 판정 비용 — 항목 수에 비례하지 않는다는 것은 통합이 질의 수를
@p 세어 걸었으나, 그 비용을 운영 규모로 재지는 않았다
@h2 F. 확인할 사항
@b open DEV는 8건이다. 세는 법:
@code lang=bash sha=26f559716f1d lines=1 kept=1
|  grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p 주의: open처럼 굵게 쓰면 이 grep에 안 잡힌다. 새 행은 | open ... 형식으로 쓴다 —
@path 이번 세션에서도 DEV-364를 굵게 써서 한 번 놓쳤고 실측으로 잡았다
@path 호출부를 grep으로 훑을 때 glob을 믿지 마라. apps/search-api/src//*.ts는 bash의 globstar가
@path 꺼져 있으면 한 단계만 매칭한다. 이번에 sequence/range.ts가 목록에서 빠져 그 경로가
@p 500이 됐고 리뷰가 잡았다. grep -rn '패턴' apps packages처럼 디렉터리를 통째로 준다
@path 남은 오픈 결정은 OD-008 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
@path 문서 본문을 고치면 같은 편집에서 상태 헤더를 함께 만진다 (DEV-323)
@b agent-context/는 main에 tracked다. git add -A·git add .는 쓰지 않는다
@b CI 확인은 커밋 SHA의 check-runs로 한다 — gh pr checks는 옛 실행 결과를 보여 줄 수 있다
@b 전사는 exports/에 둔다. /export에 상대 경로를 주면 저장소 루트에 떨어진다
@p ---
@h1 이전 세션 기록 (보존)
@path A절은 CR-051 계약과 구현으로 해소됐다 — PR #63(7a2d0e2)·PR #64(2e6194c)가
@path 들어왔고 DEV-349가 닫혔다. REL-004 수는 그대로 7/8이다 — DEV-349는 기존 WP의
@p 교차 결함 종결이지 새 작업 패키지가 아니다. 아래는 그때의 판단 근거를 남긴 것이다.
@h2 (2026-08-28 CR-050 · WP-034 종료 시점의 기록)
@path 최신 기준 (2026-08-28 CR-050 · WP-034 종료 시점)
@p main ea08c57 (문서 병합 1f7ccf2, 구현 병합 ea08c57) · main CI verify·integration 통과 SRS baseline v2.10 · PRD v1.5 · 용어집 v0.3 · 원장 review v4.6 · 작업 패키지 ... · 와이어프레임 v0.6 · 상태 매트릭스 v0.7 · 컴포넌트 v0.6 · QA 체크리스트 v0.7 · 추적 매트릭스 v0.7
@path CR-050 · DEV-358까지 · 마이그레이션 016까지(WP-034가 만들었다)
@path REL-004 구현 7/8 (WP-029·030·031·035·032·033·034 done). 남은 것은 WP-036 하나
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건 (PR #61·#62 전부 resolve). 열린 PR 0건. 로컬 브랜치는 main 하나. 미추적 0건
@path open DEV 8건 — 기존 5(DEV-001·006·010·016·026) + DEV-304·305 + DEV-349
@path (CR-050이 등록한 DEV-350~358 아홉은 전부 resolved)
@h2 A. 지금 당장 — DEV-349 좁은 계약 종결 (WP-036보다 먼저)
@path 이것은 구현이 아니라 계약을 여는 일이다. FR-SEQ-005 AC-4가 저장된 검색의 seq: 조건을
@p 명시적으로 지목하는데 그것을 성립시킬 재료가 어디에도 없다 — 지시서 §0의 "정본이 실제로
@todo 없음"에 해당하므로 별도 CR이 옳다. next-free는 실측할 것(CR-051 / DEV-359 예상):
@code lang=bash sha=e93add1a64fe lines=3 kept=3
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|ls packages/db/migrations/*.up.sql | tail -1
@p 정해야 할 것 셋. (1) 어느 시퀀스 공간의 에폭을 기록하는가 — 질의에 repo:가 없으면 접근 범위의 모든 저장소가 대상이고, 여럿이면 공간도 여럿이다. (2) 공간이 여럿일 때 하나만 낡아도 전체를 무효로 보는가. (3) /search 응답이 epoch_stale을 어떻게 싣는가 — 지금 그 필드는
@path W-004 계열에만 있고 검색 응답 계약에 없다.
@path 해법이 저장 계층이 아닐 수 있다. W-001의 seq: 질의는 URL에 담기는 순간 이미 같은 성질을
@p 갖고, 저장된 검색은 그것을 더 오래 보관할 뿐이다 — 조회 계층에서 푸는 길을 먼저 검토한다. 그 판단이 서기 전에는 열 하나로 임의 해법을 만들지 않는다.
@h3 WP-034가 깔아 둔 자리 — 다시 만들지 말 것
@path apps/search-api/src/repositories/overview.ts — 접근 범위 판정이 SQL이 아니라 여기 있다.
@p 규칙을 SQL에 복제하면 두 곳이 갈라지고 한쪽만 넓어지는 날 아무 오류도 안 난다. 대신 한 요청이
@path 정본을 다 훑지 못할 수 있으므로 스캔 상한에서 커서를 반드시 남긴다(DEV-357). 빈 페이지 +
@p 유효한 커서는 정상 상태다
@path isRepositoryInScope의 allowedTeamIds는 필수 인자다(DEV-353). 선택으로 되돌리지 않는다 —
@p 빠지면 오류 없이 조용히 좁게 답하고, 그 실패는 다섯 WP 동안 아무도 못 봤다
@b ES 문서 수 집계는 PG가 이미 걸렀어도 applyMandatoryScopeFilter를 다시 지난다. 생략을 한 번
@p 허용하면 그 자리가 곧 우회 경로가 된다
@path apps/search-api/src/repositories/cursor.ts — PostgreSQL 키셋. 지문에 접근 범위 해시를 담고
@p 원본 식별자는 싣지 않는다 (봉투는 서명될 뿐 암호화되지 않는다)
@path repository_registration_request에 승인·반려 열을 더하지 않는다 — 그 수명주기는 A-002를
@path 세우는 WP-040이 검증한다. 지금 만들면 saved_search.team_id가 겪은 "뜻이 정해지지 않은 열"이 된다
@path apps/web/lib/repository-overview.ts — 화면 판정은 전부 여기 있다. null·0·unavailable
@p 셋을 구분하는 것이 이 화면의 전부다
@path 웹은 /api/repositories를 부른다. /api/v1을 적으면 프록시가 /api/v1/v1/...을 만든다
@h2 B. 그 다음 — WP-036 원본 아카이브 레인 (REL-004 마지막)
@path WP-036이 들어오면 REL-004 구현이 8/8이 된다. 다만 패키지 8/8과 릴리스 승인은 다른 것이다 —
@path Gate 4·5·6은 별개이며 REL-003조차 아직 미통과다.
@p ---
@h2 (그 이전 기록)
@path A절은 WP-034 구현으로 해소됐다 — PR #61(계약)·#62(구현)가 들어왔고 REL-004가 7/8이 됐다.
@p 아래는 그때의 판단 근거를 남긴 것이다.
@h3 WP-033이 깔아 둔 자리 — 다시 만들지 말 것
@path packages/db/src/repositories/saved-search.ts — 접근 규칙이 질의 안에 있다. 라우트가
@p "소유자인가"를 판정해 넘기는 구조로 만들지 않는다. 새 조회 경로도 visibleTo()를 지난다
@b 두 개의 잠금이 이 자원의 무게중심이다 — 상한은 소유자 행 FOR UPDATE, 구성원 자격은
@p team_member 행 FOR SHARE. 지우면 각각 101건과 이탈자 공유가 된다. 회귀가 건다
@path apps/search-api/src/saved-search/cursor.ts — PostgreSQL 키셋. PIT도 search_after도 없다.
@p 지문에 팀 소속이 들어가고 접근 범위 버전은 들어가지 않는다(무관한 권한 변경이 순회를 끊는다)
@b SavedSearchRow.created_at은 ISO 마이크로초 문자열이다. Date로 바꾸지 않는다 —
@p 밀리초로 잘리면 키셋이 같은 밀리초의 행을 건너뛴다
@path apps/web/components/SaveSearchDialog.tsx는 create/edit 두 모드다. 편집 전용 대화상자를
@p 새로 만들지 않는다 — 받는 것이 같으므로 갈라 두면 한쪽에만 고쳐지는 규칙이 생긴다
@path apps/web/lib/saved-search.ts — 화면 판정은 전부 여기 있다. 컴포넌트에서 판정하지 않는다
@path 웹은 /api/saved-searches를 부른다. /api/v1을 적으면 프록시가 /api/v1/v1/...을 만든다
@h2 B. 남은 큰 것 — DEV-349 (별도 CR)
@path FR-SEQ-005 AC-4가 저장된 검색을 명시적으로 지목하는데 구현되지 않았다. 원장 4장이
@path "saved_search가 없어 도달 불가"라고 예고해 둔 자리이고, WP-033이 표를 만들면서 도달
@p 가능해졌다. 재채번으로 에폭이 오르면 저장된 seq:1280..1342가 조용히 다른 커밋을 가리킨다.
@p 고치려면 계약의 재료가 필요하고 그것이 없다:
@b 어느 시퀀스 공간의 에폭을 기록하는가 (질의에 repo:가 없으면 접근 범위 전체가 대상)
@b 여럿이면 하나만 낡아도 전체를 무효로 보는가
@path /search 응답이 epoch_stale을 어떻게 싣는가 (지금 그 필드는 W-004 계열에만 있다)
@path 이 문제는 저장된 검색이 만든 것이 아니다 — W-001의 seq: 질의는 URL에 담는 순간 같은
@p 성질을 갖는다. 해법이 조회 계층일 수 있으므로 그 판단이 먼저다. 원장 7장에 등재돼 있다.
@h2 C. 배포에서 해야 할 것
@b 마이그레이션 015를 적용한다. pnpm run db:migrate
@path WP-032가 남긴 것 (변화 없음): SEARCH_CURSOR_HMAC_KEY(32자 이상, 복제본 공통)와
@p pnpm es:reindex --alias prs-pull-requests·--alias prs-commits
@path WP-033은 새 배포 전제를 만들지 않았다 — 커서 서명 키를 검색·구간과 같은 값으로 쓴다
@h2 D. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
@p | DEV | 역할 | 왜 아직 | 소관 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
@path | 304 | backfill (JOB-ING-004) | enrich 파드 역할 구성 판단 | WP-019 |
@h2 E. 릴리스 게이트 4·5·6 (변화 없음)
@p Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 합성 데이터셋·test:perf 없음) · Gate 6 운영(불가 — 실제 K8s 없음).
@h2 F. 미뤄 둔 항목
@path flow-001·flow-003 e2e 간헐 실패 — WP-016 소관. 재시도 통과를 해소로 적지 않는다
@b 문서 검증기 --strict 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다
@path PIT의 클러스터 자원 비용 · 패싯·커서의 운영 규모 성능 (DEV-058) — NOT RUN
@b prs-links 재색인 커버리지 판정
@path ingest-gateway/integration/load.test.ts의 enqueueFailed — 전 계층 실행에서 한 번
@p 2건으로 실패했고 이후 세 번의 정상 실행에서 재현되지 않았다. 그 실행은 다른 여덟 파일이 외래 키 위반으로 죽던 실행이다. 단독으로는 통과한다. 재현되면 그때 §7에 올린다
@h2 G. 확인할 사항
@b open DEV는 8건이다. 세는 법:
@code lang=bash sha=26f559716f1d lines=1 kept=1
|  grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p 주의: open처럼 굵게 쓰면 이 grep에 안 잡힌다. 새 행은 | open ... 형식으로 쓴다 — 이번 세션에서 그 함정을 그대로 밟았고 실측으로 잡았다
@path 남은 오픈 결정은 OD-008 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
@path 문서 본문을 고치면 같은 편집에서 상태 헤더를 함께 만진다 (DEV-323)
@b agent-context/는 main에 tracked다. git add -A·git add .는 쓰지 않는다
@b CI 확인은 커밋 SHA의 check-runs로 한다 — gh pr checks는 옛 실행 결과를 보여 줄 수 있다
@b 전사는 exports/에 둔다. /export에 상대 경로를 주면 저장소 루트에 떨어진다
@p ---
@h1 이전 세션 기록 (보존)
@path A절은 WP-033 구현으로 해소됐다 — PR #60(700d825)이 본체를 넣었고 REL-004가 6/8이 됐다.
@p 아래는 그때의 판단 근거를 남긴 것이다.
@path 최신 기준 (2026-08-27 WP-032 종료 시점)
@path main 2d28074 (WP-032 병합은 3237b6c, 그 뒤 인계 기록 커밋 하나) · main CI verify·integration 통과
@p SRS baseline v2.8(변경 없음) · PRD v1.3 · 원장 review v4.1 · 작업 패키지 v1.4 · 로드맵 v0.6 · 인프라 v0.5 · API 계약 v0.8 · 추적 매트릭스 v0.5
@path CR-048(변화 없음) · DEV-332까지 · 마이그레이션 014까지(WP-032는 만들지 않았다)
@path REL-004 구현 5/8 (WP-029·030·031·035·032 done)
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건 (PR #55·#56·#57 전부 resolve). 열린 PR 0건. 로컬 브랜치는 main 하나. 미추적 0건
@path open DEV 7건 — 기존 5(DEV-001·006·010·016·026) + DEV-304·305
@h2 A. 지금 당장 — WP-033 저장된 검색
@path 착수 전 감사를 다시 할지부터 정한다. WP-032는 계약이 CR-043·044에서 이미 닫혀 있어 감사 없이 구현했고 그것이 옳았다. WP-033은 계약이 닫힌 적이 없다 — saved_search 표도 API도 SRS 밖에서 검증된 적이 없으므로 착수 전 감사가 필요할 가능성이 높다.
@todo next-free는 실측할 것 — CR-049 / DEV-333 / 마이그레이션 015가 예상이지만 추측해 쓰지 않는다:
@code lang=bash sha=e93add1a64fe lines=3 kept=3
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|ls packages/db/migrations/*.up.sql | tail -1
@path WP-032가 깔아 둔 자리 — 다시 만들지 말 것
@path apps/search-api/src/cursor/envelope.ts — HMAC 봉인·두 오류 타입. 순회의 뜻은 여기 없다. 새 커서 계열이 생기면 봉인만 물려받고 순회는 자기 모듈에 둔다
@path apps/search-api/src/search/cursor.ts(PIT+search_after) · sequence/range-cursor.ts(정본 서수) — 합치지 않는다. 합치면 W-004 멤버십이 ES로 넘어가 DEV-130이 되돌아온다
@path apps/search-api/src/search/facets.ts — computeFacets는 ScopedQuery만 받는다. 새 축은 FacetAxis에 더한다. 표시값 해석이 필요한 축은 display를 붙이고 일괄 해석한다(N+1 금지)
@path packages/es/src/highlight.ts — 표식은 사설 사용 영역 문자다. 응답에 마크업을 내보내지 않는다
@path packages/es/src/sort.ts의 MISSING_SENTINEL — _last로 되돌리지 않는다 (DEV-329). 새 정렬 키를 더하면 그 타입의 센티널이 있어야 한다
@path packages/es/src/indices.ts — PR·커밋은 -v2다. 매핑을 또 바꾸면 버전을 올리고 재색인으로 배포한다. applyMappings는 별칭이 옛 버전을 들고 있으면 물러난다
@h2 B. 배포에서 해야 할 것 (WP-032가 만든 전제)
@b SEARCH_CURSOR_HMAC_KEY(32자 이상, 복제본 공통)를 시크릿에 넣는다. 없으면 search-api가 운영에서 기동하지 않는다
@b pnpm es:reindex --alias prs-pull-requests · --alias prs-commits로 v2에 올린다. 부트스트랩으로 먼저 해결하려 하지 않는다 — 빈 인덱스를 만들거나 별칭을 둘로 건다
@b 그 전까지 전문 검색은 과거 데이터에 조용히 적게 답한다
@h2 C. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
@p | DEV | 역할 | 왜 아직 | 소관 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
@path | 304 | backfill (JOB-ING-004) | enrich 파드 역할 구성 판단 | WP-019 |
@h2 D. 릴리스 게이트 4·5·6 (변화 없음)
@p Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 합성 데이터셋·test:perf 없음) · Gate 6 운영(불가 — 실제 K8s 없음).
@h2 E. 미뤄 둔 항목
@path flow-001·flow-003 e2e 간헐 실패 — WP-016 소관. 재시도 통과를 해소로 적지 않는다
@b 문서 검증기 --strict 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다
@b PIT의 클러스터 자원 비용 — 조회마다 PIT을 여는 대가를 운영 규모로 재지 않았다 (NOT RUN)
@path 패싯·커서의 운영 규모 성능 — 예산 1.5초가 어디서 걸리는지 (DEV-058)
@b prs-links 재색인 커버리지 판정
@p ---
@path 최신 기준 (2026-08-27 WP-035 · CR-048 종료 시점)
@p main df9f569 · SRS baseline v2.8(변경 없음) · PRD v1.3 · 원장 review v3.7 · 작업 패키지 v1.2 · 인프라 v0.5 · API 계약 v0.8 · 로드맵 v0.5 · 추적 매트릭스 v0.5
@path CR-048 · DEV-323까지 · 마이그레이션 014까지(이 세션은 만들지 않았다)
@path REL-004 구현 4/8 (WP-029·030·031·035 done). WP-032 차단 해제
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건 (PR #52·#53·#54 전부 resolve). 열린 PR 0건. 로컬 브랜치는 main 하나
@path open DEV 7건 — 기존 5(DEV-001·006·010·016·026) + DEV-304·305 (DEV-306은 CR-048로 resolved)
@p ⚠️ 미추적 1건 — 202608271346.md(전사, 99KB)가 저장소 루트에 있다. 아래 E절 참조
@h2 A. 지금 당장 — WP-032 (패싯 · 커서 페이지네이션 · 전문 검색)
@path 착수 전 감사를 다시 하지 않는다. WP-032의 계약은 CR-043(SRS v2.7)·CR-044(SRS v2.8)에서 이미 닫혔다 — 커서 계약(재료·봉인 값·불안정성 처분·동률), 패싯 계약(bucket 단위 검증·별도 요청 1.5초·facets_status 넷), 전문 검색 계약(강조 평문·커밋 축 first-parent·query_too_short 코드 포인트), ADR-010 Amendment(모든 순회에 PIT)가 전부 서 있다.
@path WP-035가 차단을 풀었다. edge_ngram은 index.analysis의 비동적 설정이라 열린 인덱스에 못 넣고 putMapping으로 더한 서브필드는 기존 문서에서 비어 있는데, 이제 그것을 배포하는 기계가 있다. 시험용 v2로 실제 증명까지 했다(통합 WP-032 선행 조건 증명).
@p CR은 지시서 §0 기준으로만 연다 — 승인된 계약으로 구현이 불가능 / 정본이 실제로 없음 / 새 제품 결정 필요, 셋 중 하나가 실물로 증명될 때만. 단순 구현·리팩터·파일 배치·시험 편의로는 열지 않는다.
@todo next-free는 실측할 것 — CR-049 / DEV-324 / 마이그레이션 015가 예상이지만 추측해 쓰지 않는다:
@code lang=bash sha=89efda661ea8 lines=4 kept=4
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'API-SRCH-[0-9]{3}' docs/ | sort -u   # 새 API ID는 충돌부터
|ls packages/db/migrations/*.up.sql | tail -1
@path WP-035가 깔아 둔 자리 — 다시 만들지 말 것
@path packages/es/src/versioned-index.ts — 버전 인덱스 생성·원자 전환. WP-032의 실제 edge_ngram 필드는 여기 없다 — 통합 시험이 wp035_probe라는 임시 이름으로 능력만 증명했고 이름을 선점하지 않았다. WP-032가 ENTITY_INDICES의 매핑을 바꾸면 이 기계가 그것을 배포한다
@path packages/es/src/write-targets.ts — 새 쓰기 경로를 만들면 WriteTargets를 인자로 받아야 하고 packages/es/src/dual-write.test.ts의 DUAL_WRITE_PATHS(현재 17)에 등재해야 한다. 등재하지 않으면 아키텍처 시험이 잡는다
@path packages/db/src/reindex-fence.ts — 운영 쓰기는 전부 withReindexWrite를 지난다. 새 호출부는 DUAL_WRITE_CALLERS에도 올린다
@path apps/pipeline-worker/src/documents.ts — registryOwnedFields(레지스트리 소유 필드)와 buildProjectedCommitDocument(PR 유래 커밋 문서). 투영과 재구축이 같은 함수를 쓴다 — 사본을 만들지 않는다
@path apps/search-api/src/ops/jobs.ts — OPERATOR_JOB_TYPES("러너가 있다")와 CREATABLE_GENERIC_JOB_TYPES("API-ADM-002로 만들 수 있다")가 갈려 있다. 새 잡 유형은 어느 쪽인지 정해서 넣는다
@h2 B. 별도 CR 둘 — 남은 미배포 역할
@risk authz는 CR-048로 닫혔다. 둘 남았고 회귀 예외 목록(regression/runtime-reachability.test.ts의 UNDEPLOYED_ROLE_ALLOWLIST)에 사유·DEV와 함께 있다.
@p | DEV | 역할 | 영향 | 왜 아직 안 고쳤나 | | --- | --- | --- | --- |
@path | 305 | release (JOB-REL-007) | 릴리스 수집이 안 돌아 prs-releases가 비고 W-005·API-REL-002·005가 빈 상태 — WP-024·WP-026이 만든 기능 전부 | 이 역할은 미러 볼륨을 요구한다(DEV-143). PVC 배치가 함께 정해져야 해 다른 manifest 복사로 못 만든다. WP-024 소관 |
@path | 304 | backfill (JOB-ING-004) | 백필 잡을 만들면 아무도 집지 않고 job_active_uk가 이후 요청을 막는다 — DEV-178과 같은 모양 | enrich 파드의 역할 구성을 바꾸는 일이라 그 워커의 확장 축(실시간 유량) 판단이 필요하다. WP-019 소관 |
@path 정상 경로 (CR-048이 처음으로 밟았다): manifest를 만들고 → 예외 목록에서 지우고 → 인프라 3장 배포 단위 표에 올린다. 표에 먼저 올리는 길은 시험이 막는다(DEV-312). 그리고 표에 중복 행을 만들지 않는다(DEV-321) — 검사가 생겼다.
@h2 C. 릴리스 게이트 4·5·6 (REL-003 미통과 사유 — 변화 없음)
@p | 게이트 | 남은 일 | 이 환경에서 가능? | | --- | --- | --- | | Gate 4 보안 | 권한 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능 |
@path | Gate 5 성능 | 성능 목표 7종 (DEV-058) | 불가 — 합성 데이터셋도 test:perf도 없다 |
@p | Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | 불가 — 실제 Kubernetes 없음 |
@h2 D. 미뤄 둔 항목
@path flow-001·flow-003 e2e 간헐 실패 — WP-016 소관, 원장 §7. CI에서도 재현된다는 것이 확인돼 있다(PR #49). 재시도 통과를 해소로 적지 않는다
@b 문서 검증기 --strict의 자기참조 오탐 — 원장 §7 기술 부채. 문서를 고쳐 게이트를 통과시키지 않는다
@path 백필(JOB-ING-004)의 updated 정렬 열거, JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
@b prs-links 재색인의 커버리지 판정 — 지금은 하지 않는다(expectedDocuments = null). 재구축 포트가 처리한 source 수만 돌려주기 때문이며, 나머지 검증 6항으로만 건다. 간선 문서 수를 셀 방법이 생기면 그때 판정한다
@b 실제 운영 규모 재색인 — NOT RUN. 성능 harness가 서면 함께 잰다
@h2 E. 확인할 사항
@path ⚠️ 전사 파일이 .gitignore 밖에 있다. pr-search/202608271346.md(99KB). /export에 상대 경로를 줘서 저장소 루트에 떨어졌고, .gitignore 24행은 exports/만 덮는다 — git check-ignore로 실측했다. git add -A를 쓰면 커밋에 딸려 간다. exports/로 옮기거나 지운다
@b open DEV는 7건이다. 세는 법:
@code lang=bash sha=26f559716f1d lines=1 kept=1
|  grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p 주의: open처럼 굵게 쓰면 이 grep에 안 잡힌다. 새 행은 | open ... 형식으로 쓴다
@path 남은 오픈 결정은 OD-008 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
@path 새 역할을 추가하면 ① 코드 갈래 ② manifest ③ 인프라 3장 표 셋이 함께 가야 한다. 그리고 표에 중복 행을 만들지 않고, 표 아래 산문도 함께 고친다 (DEV-321·322)
@path 문서 본문을 고치면 같은 편집 안에서 상태 헤더(버전·갱신일)를 함께 만진다 (DEV-323)
@b agent-context/는 main에 tracked다. .gitignore에 넣지 않는다. git add -A·git add .는 쓰지 않는다 — 경로를 지목해 stage 한다
@b CI 확인은 커밋 SHA의 check-runs로 한다 — gh pr checks는 옛 실행 결과를 그대로 보여 줄 수 있다
@p ---
@h1 이전 세션 기록 (보존)
@path A절은 WP-035 구현으로 해소됐다 — PR #52(efea3c0)가 본체를 넣었고 REL-004가 4/8이 됐다.
@path B절의 authz도 CR-048(PR #53, f4e8ab6)로 닫혔다.
@p 아래는 그때의 판단 근거를 남긴 것이다.
@h2 (2026-08-26 CR-043~047 종료 시점의 기록)
@path 최신 기준 (2026-08-26 CR-043~047 종료 시점)
@p main 99e2532 · SRS baseline v2.8 · PRD v1.3 · 원장 review v3.4 · 작업 패키지 v1.1 · 추적 매트릭스 v0.5
@path CR-047 · DEV-312까지 · 마이그레이션 014까지(이 세션은 만들지 않았다). REL-004 구현 3/8 (WP-029·030·031 done)
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건 (PR #1~#51 전수 실측). 열린 PR 0건. 로컬 브랜치는 main 하나. 미추적 0건
@path open DEV 8건 — 기존 5(DEV-001·006·010·016·026) + 신규 3(DEV-304·305·306 미배포 역할)
@h2 A. 지금 당장 — WP-035 본체 구현
@path 감사와 계약은 끝났다. 원장 6.38장과 비동기 계약 3.5장에 계약이 서 있고, 러너가 살 자리(deploy/k8s/pipeline-worker-batch.yaml)도 생겼다. CR을 다시 열 필요가 없다 — CR-045·046·047이 이미 닫혔고 구현이 그 계약을 따르면 된다.
@todo next-free는 실측할 것 — CR-048 / DEV-313 / 마이그레이션 015가 예상이지만 추측해 쓰지 않는다:
@code lang=bash sha=e93add1a64fe lines=3 kept=3
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|ls packages/db/migrations/*.up.sql | tail -1
@path 남은 구현 (WP-035 계약 순서 그대로)
@p | # | 항목 | 자리 | | --- | --- | --- |
@path | 1 | 버전 인덱스 생성 · 원자 별칭 전환 | packages/es/ 신규 (bootstrap.ts는 ensureIndex+putAlias뿐이고 ENTITY_INDICES.index가 상수 -v1이다) |
@path | 2 | 이중 쓰기 seam — 열일곱 경로 | 대상 해석은 앱 계층(의존 방향). @prs/es 원시체는 대상을 인자로 받는다 |
@path | 3 | 활성화·전환 울타리 | packages/db/src/advisory-lock.ts에 세션·트랜잭션 범위가 이미 있다. 울타리는 두 인덱스 쓰기와 실패 기록까지 쥔다(DEV-308) |
@path | 4 | 정본 재구축 | PR=pull_request_snapshot, 커밋=commit_snapshot, 릴리스=release 표, 간선=JOB-REL-006 재파생. 운영 빌더를 재사용하고 두 번째 생성 경로를 만들지 않는다 |
@path | 5 | JOB-ING-006 러너 | apps/pipeline-worker/src/index.ts의 roles.includes('batch') 블록. claimNextJob('reindex') |
@path | 6 | API-ADM-004 라우트 | apps/search-api/src/ops/. operator 전용, alias만 받는다 |
@path | 7 | CLI pnpm es:reindex --alias | API와 같은 enqueue seam. packages/es/src/cli.ts는 apply-mappings만 받는다 |
@p | 8 | 보관 7일 스윕 | 정본은 완료 잡의 progress(source_index·switched_at). 새 표 없음 |
@path | 9 | OPERATOR_JOB_TYPES에 reindex | apps/search-api/src/ops/jobs.ts. 러너와 같은 커밋에서 |
@p | 10 | 적대적 변이 ~20종 | 지시서 §65의 R1~R20 목록이 그대로 쓸 만하다 |
@p DoD에서 특히 주의할 것 셋
@path 대상 버전은 "아직 쓰이지 않은 다음 번호"다 (DEV-309) — v1 + 1을 그대로 쓰면 실패로 남은 shadow가 재시도를 막는다
@b shadow의 item 단위 부분 실패가 전환을 막아야 한다 — HTTP 200이 완료가 아니다
@path WP-032 선행 조건 증명: v1에 없는 분석기·다중 필드를 가진 시험용 v2를 이 기계로 세워 별칭을 옮긴다. WP-032의 실제 필드 이름을 선점하지 않는다
@h2 B. 별도 CR 셋 — 미배포 역할 (감사가 드러냈다)
@risk 셋 다 각자 다른 WP가 만든 기능이 배포에서 죽어 있는 것이다. 회귀 예외 목록(regression/runtime-reachability.test.ts의 UNDEPLOYED_ROLE_ALLOWLIST)에 사유·DEV와 함께 등재돼 있다.
@p | DEV | 역할 | 영향 | 왜 이 세션이 안 고쳤나 | | --- | --- | --- | --- |
@path | 306 | authz (JOB-AUTH-001) | 권한 회수가 캐시에 반영되지 않는다. EVT-AUTH-001을 소비하는 곳이 없어 회수된 사용자가 TTL 만료까지 그 범위로 조회한다 (FR-AUTH-003 AC-2) | 접근 통제 축이다. CR-035·036이 같은 축에서 유출을 고쳤고 이것은 그 결정이 반영되는 경로다 — 위협 모델과 함께 별도 CR이 옳다. WP-012 소관 |
@path | 305 | release (JOB-REL-007) | 릴리스 수집이 안 돌아 prs-releases가 비고 W-005·API-REL-002·005가 빈 상태 — WP-024·WP-026이 만든 기능 전부 | 이 역할은 미러 볼륨을 요구한다(DEV-143). PVC 배치가 함께 정해져야 해 다른 manifest 복사로 못 만든다. WP-024 소관 |
@path | 304 | backfill (JOB-ING-004) | 백필 잡을 만들면 아무도 집지 않고 job_active_uk가 이후 요청을 막는다 — DEV-178과 같은 모양 | enrich 파드의 역할 구성을 바꾸는 일이라 그 워커의 확장 축(실시간 유량) 판단이 필요하다. WP-019 소관 |
@path 고칠 때 정상 경로: manifest를 만들고 → 예외 목록에서 지우고 → 인프라 3장 배포 단위 표에 올린다. 표에 먼저 올리는 길은 시험이 막는다(DEV-312).
@h2 C. 릴리스 게이트 4·5·6 (REL-003 미통과 사유 — 변화 없음)
@p | 게이트 | 남은 일 | 이 환경에서 가능? | | --- | --- | --- | | Gate 4 보안 | 권한 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능 |
@path | Gate 5 성능 | 성능 목표 7종 (DEV-058) | 불가 — 합성 데이터셋도 test:perf도 없다 |
@p | Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | 불가 — 실제 Kubernetes 없음 |
@h2 D. 미뤄 둔 항목 (별도 CR)
@path flow-001·flow-003 e2e 간헐 실패 — WP-016 소관, 원장 §7. 이 세션이 기록된 믿음 하나를 반증했다: CI에서도 재현된다. 문서만 바꾼 PR #49의 verify가 flow-003.spec.ts:176에서 실패했다(run 32983119303). §7의 "CI에서는 통과한다 → 로컬 환경 문제"라는 근거가 무너졌다. 재시도 통과를 해소로 적지 않는다
@b 문서 검증기 --strict의 자기참조 오탐 — 원장 §7 기술 부채. 문서를 고쳐 게이트를 통과시키지 않는다
@path 백필(JOB-ING-004)의 updated 정렬 열거, JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
@h2 E. 확인할 사항
@path open DEV는 8건이다 (기존 5 + DEV-304·305·306). 세는 법:
@code lang=bash sha=26f559716f1d lines=1 kept=1
|  grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p 주의: open처럼 굵게 쓰면 이 grep에 안 잡힌다. 새 행은 | open ... 형식으로 쓴다
@path 남은 오픈 결정은 OD-008 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
@b 회귀가 이제 배포 단위를 양방향으로 검사한다. 새 역할을 추가하면 ① 코드 갈래 ② manifest ③ 인프라 3장 표 셋이 함께 가야 한다
@b agent-context/는 main에 tracked다. .gitignore에 넣지 않는다. git add -A·git add .는 쓰지 않는다 — 경로를 지목해 stage 한다
@path 전사는 exports/에 있다 (exports/202608270742.md). /export 출력은 저장소 루트를 가리키지만 실제 위치는 exports/이고 .gitignore 24행이 덮는다 — 이전 세션들이 두 번 틀리게 적은 자리다
@p ---
@path A절은 CR-043으로 해소됐다 — WP-032 착수 전 감사가 스물을 찾았고 3248559로 머지됐다.
@p 아래는 그때의 판단 근거를 남긴 것이다.
@h2 (2026-08-26 CR-042 · WP-031 종료 시점의 기록)
@path 최신 기준 (2026-08-26 CR-042 · WP-031 종료 시점)
@p main 5d287b1 · SRS baseline v2.6(변경 없음) · PRD v1.2 · 원장 review v2.8 · 작업 패키지 v0.8 ·
@path CR-042 · DEV-265까지 · 마이그레이션 014까지(CR-042는 만들지 않았다). REL-004 구현 3/8 (WP-029·WP-030·WP-031 done).
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건 (PR #1~#47 전수 실측). 열린 PR 0건. 로컬 브랜치는 main 하나. 미추적 0건.
@path API-REL-006 신설 — 기존은 001~005였다.
@h2 A. 지금 당장 — WP-032 착수 전 계약 감사 (REL-004)
@p 구현부터 시작하지 않는다. "착수 전 감사에서 계약 공백이 나왔다"가 아홉 번 연속이다
@path (CR-029·030·031·033·035·038·039·041·042). 같은 순서를 지킨다: 감사 → CR → 구현.
@path 대상: WP-032 패싯·커서 페이지네이션·전문 검색 (docs/40_delivery/pr_search_work_packages.md)
@todo next-free는 실측할 것 — CR-043 / DEV-266이 예상이지만 추측해 쓰지 않는다:
@code lang=bash sha=d66ccb4ce2a3 lines=4 kept=4
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|grep -rohE 'API-SRCH-[0-9]{3}' docs/ | sort -u   # 새 API ID는 충돌부터
|ls packages/db/migrations/*.up.sql | tail -1     # 다음은 015
@h3 감사에서 물을 것 넷 (원장 §8에도 남겼다)
@p | # | 물음 | 왜 | | --- | --- | --- |
@path | 1 | OD-005가 정한 대체 경로가 계약에 실제로 반영돼 있는가 | CR-040이 nori를 초기 의존성에서 제외하고 standard + edge_ngram으로 정했다. 매핑·분석기·질의 빌더가 그 결정을 아는지 본다 — 문서만 바꾸고 코드가 모르는 상태일 수 있다 |
@path | 2 | 커서 계약이 실재하는가 (가장 무거울 것) | ADR-010은 "커서 전용, 오프셋 없음"만 정한다. 커서의 재료·불안정성 처분(QA-W001-15의 "질의 변경 후 이전 커서")·정렬 동률 처리가 어디에도 없다. WP-031은 관계 목록에 커서를 세우지 않았고 그 이유를 계약에 남겼다 — WP-032는 세워야 한다 |
@p | 3 | 패싯이 목록과 같은 질의 조건으로 계산되는가(QA-W001-16) | "건수 일치"를 무엇으로 확인하는지가 정해져 있지 않으면 시험이 그것을 걸 수 없다 |
@path | 4 | 이 조회의 답을 바꾸는 것이 무엇이고 그것이 바뀔 때 누가 깨우는가 | WP-030(후보 변화)·WP-031(조회 축)이 연속으로 그 자리에서 공백을 찾았다 |
@h3 감사에서 함께 볼 것
@path 새 파일이 다른 경로와 같은 검사를 지나는가 — PR #47 P1이 정확히 그 자리였다(assertNoShardFailures). ADR-008 가드레일 면제가 파일 단위라는 것도 함께 본다 (risks 38·40)
@path 화면이 그릴 상태 중 데이터가 없는 것이 있는가 (DEV-077·083·094·257 계열)
@path 계약이 요구하는 QA 항목을 이 WP로 실제로 통과할 수 있는가 (DEV-236 계열)
@b 실패하면 누가 다시 하는가 — 완결 표식·성공 카운트·페이지 상한 셋을 의심하라
@h3 WP-031이 깔아 둔 자리 — 다시 만들지 말 것
@path packages/es/src/relations-read.ts — links.ts 밖에 있는 것이 설계다. 사용자 대면 조회는 이쪽 계열에 둔다 (DEV-265)
@path ADR-008 가드레일이 이제 mget·es.get도 검사한다. client.get은 받지 않는다(Redis 핸들과 이름이 같다)
@path 커서를 세우지 않은 이유가 API-REL-006 상세 절에 적혀 있다 — WP-032가 커서를 세울 때 그 문단을 갱신한다
@path apps/web/lib/relations.ts의 LinkSummaryView가 웹의 유일한 요약 타입이다. Record<string, unknown> 복사를 퍼뜨리지 않는다
@path apps/web/components/RelationSection.tsx·CoChangeSection.tsx가 지연 섹션 선례다 — 접힘 기본 + 펼칠 때 1회 + 실패 시 재시도 버튼
@h2 B. 릴리스 게이트 4·5·6 (REL-003 미통과 사유)
@p 원장 6.31.1장과 change_control.md 4장에 게이트별로 적혀 있다. 변화 없음.
@p | 게이트 | 남은 일 | 이 환경에서 가능? | | --- | --- | --- |
@path | Gate 4 보안 | 권한 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능. 화면 축은 화면이 더 서야 함 — WP-031로 W-001·W-002·W-003의 관계 축은 증거가 생겼다 |
@path | Gate 5 성능 | 성능 목표 7종 (DEV-058) | 불가 — 합성 데이터셋도 test:perf도 없다 |
@p | Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | 불가 — 실제 Kubernetes 없음 |
@h2 C. 미뤄 둔 항목 (별도 CR)
@path flow-001·flow-003 e2e 간헐 실패 — WP-016 소관, 원장 §7. 이번 세션이 되돌려서 쟀다: 화면 코드를 main과 같게 만든 상태에서도 3회 중 1회가 같은 시험·단언·오류로 실패한다. 단독은 4/4. 둘 다 뒤로가기 복원을 5초 안에 확인하는 형태이고 이것이 원인 규명의 실마리다. 재시도로 가리지 않는다
@b 문서 검증기 --strict의 자기참조 오탐 — 원장 §7에 기술 부채로 등록돼 있다. 문서를 고쳐 게이트를 통과시키지 않는다
@path 백필(JOB-ING-004)의 updated 정렬 열거 (원장 §7)
@path JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
@b script_score 동시 변경의 운영 규모 비용 — 이번에 NOT RUN이다. 성능 harness가 서면 함께 잰다
@h2 D. 확인할 사항
@path open DEV는 5건이다 (DEV-001·006·010·016·026). 전부 기존 환경 제약·범위 공백이다. 세는 법:
@code lang=bash sha=26f559716f1d lines=1 kept=1
|  grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@path 남은 오픈 결정은 OD-008 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전). OD-005는 CR-040으로 닫혔다
@path link 역할에 GHE_BASE_URL이 필요하다 — 없으면 URL 참조를 아예 만들지 않는다(THR-036, fail closed). 조용히 적게 만드는 것이라 눈치채기 어렵다
@path apps/web을 바꾸면 e2e 전에 pnpm --filter @prs/web run build가 필수다. 안 그러면 새 화면이 404가 되고 그 실패를 플레이크로 오해한다
@b CI 확인은 커밋 SHA의 check-runs로 한다 — gh pr checks는 옛 실행 결과를 그대로 보여 줄 수 있다 (이번에 겪었다)
@b agent-context/는 main에 tracked다. .gitignore에 넣지 않는다. git add -A·git add .는 쓰지 않는다 — 경로를 지목해 stage 한다
@path A절은 CR-042로 해소됐다 — WP-031 착수 전 감사가 열여덟을 찾았고 9084033로 머지됐다.
@p 아래는 그때의 판단 근거를 남긴 것이다.
@h2 (2026-08-26 CR-040 / CR-041 · WP-030 종료 시점의 기록)
@path 최신 기준 (2026-08-26 CR-040 / CR-041 · WP-030 종료 시점)
@p main c4f8a39 · SRS baseline v2.6 · PRD v1.2 · 원장 review v2.6 · 작업 패키지 v0.6 ·
@path CR-041 · DEV-247까지 · 마이그레이션 014까지. REL-004 구현 2/8 (WP-029·WP-030 done).
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건 (PR #1~#46 전수 실측). 열린 PR 0건. 로컬 브랜치는 main 하나.
@h2 A. 지금 당장 — WP-031 착수 전 계약 감사 (REL-004)
@p 구현부터 시작하지 않는다. "착수 전 감사에서 계약 공백이 나왔다"가 여덟 번 연속이다
@path (CR-029·030·031·033·035·038·039·041). 같은 순서를 지킨다: 감사 → CR → 구현.
@path 대상: WP-031 관계 조회 API와 상세 화면 관계 섹션 (docs/40_delivery/pr_search_work_packages.md)
@todo next-free는 실측할 것 — CR-042 / DEV-248이 예상이지만 추측해 쓰지 않는다:
@code lang=bash sha=641fd929d842 lines=3 kept=3
|  grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2
|  grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|  grep -rohE 'API-REL-[0-9]{3}' docs/ | sort -u     # 새 API ID는 충돌부터
@h3 이번 세션이 이미 찾아 둔 다섯 (감사만 했고 CR은 열지 않았다)
@p | # | 발견 | 근거 (실측) | | --- | --- | --- |
@path | 1 | 관계 조회 API가 존재하지 않는다 (가장 무거움) | API-REL-001=/sequence-neighbors(FR-REL-001, WP-027), 002=/containments(WP-024), 003=/co-changes(FR-REL-007), 004=/relation-graphs(FR-REL-008, WP-043), 005=/releases(WP-026). prs-links 간선을 조회할 경로가 없다. WP-031 구현 범위 첫 줄이 "관계 조회(정방향·역방향)"인데 그것을 줄 API ID가 없다 — CR-030 DEV-155("릴리스 목록을 줄 경로가 없었다" → API-REL-005 신설)와 같은 모양 |
@path | 2 | ### API-REL-003 상세 절이 아예 없다 | pr_search_api_contracts.md에 001·002·004·005만 있다. WP-031 범위인 /co-changes의 요청·응답 형식이 정의된 곳이 없다 |
@path | 3 | THR-034가 WP-031 DoD에 없다 | CR-041이 선행 조건 문단에만 적었다. 그리고 대상 저장소 범위를 어떻게 교집합하는지가 미정이다 (대상 문서 조회에 applyMandatoryScopeFilter를 다시 태우는가, 간선만 주고 내용은 별도 조회로 미루는가) |
@path | 4 | detached 표시 규칙이 없다 | C-021 상태 목록은 collapsed/loading/ready/links_pending/unresolved뿐이고 UI 문서 전체에 detached가 0회 등장한다. WP-030이 방금 그 필드를 채우기 시작했다. 해제된 의존을 "관계 없음"으로 그리면 FR-REL-006 AC-3이 보존하려는 사실이 화면에서 사라진다 |
@path | 5 | FLOW-006 관련 요구사항에 FR-REL-006(스택)이 없다 | 003·004·005·008뿐이다. 화면이 스택을 그릴지가 흐름 명세에 없다 |
@h3 감사에서 탈락한 후보 둘 (가짜 발견을 남기지 않으려 기록한다)
@path is:reverted는 이미 전 구간 배선돼 있다 — 파서(packages/query/src/keys.ts:70) → packages/es/src/query-builder.ts:73 → ES term 필터, 시험까지. WP-013이 만들었고 WP-030이 is_reverted에 실제 값을 채우면서 FR-REL-004 AC-4가 이번 머지로 실제로 성립했다
@path LinkSummary.has_stack은 CR-039에서 이미 선택 필드였다 (packages/domain/src/entities.ts:48)
@h3 감사에서 물을 것 (WP-030이 같은 계열로 열둘을 찾았다)
@path 이 파생/조회의 답을 바꾸는 것이 source 말고 또 무엇인가, 그것이 바뀔 때 누가 깨우는가 — 이것이 WP-030의 뿌리였다
@path 조회가 대상의 내용을 반환하는가 — 그렇다면 THR-034 교집합이 필수 수용 기준이다
@path 화면이 그릴 상태 중 데이터가 없는 것이 있는가 (DEV-077·083·094 계열)
@path 계약이 요구하는 QA 항목을 이 WP로 실제로 통과할 수 있는가 (DEV-236 계열)
@b 실패하면 누가 다시 하는가 — 완결 표식·성공 카운트·페이지 상한 셋을 의심하라
@h2 B. 릴리스 게이트 4·5·6 (REL-003 미통과 사유)
@p 원장 6.31.1장과 change_control.md 4장에 게이트별로 적혀 있다. 변화 없음.
@p | 게이트 | 남은 일 | 이 환경에서 가능? | | --- | --- | --- | | Gate 4 보안 | 권한 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능 |
@path | Gate 5 성능 | 성능 목표 7종 (DEV-058) | 불가 — 합성 데이터셋도 test:perf도 없다 |
@p | Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | 불가 — 실제 Kubernetes 없음 |
@h2 C. 미뤄 둔 항목 (별도 CR)
@path flow-001 e2e 간헐 실패 — 이 세션에서도 귀속 배제를 실측했다(apps/web diff 0, 모든 /api/ 가로챔, 단독 4/4, 전량 3회 중 1회 실패). WP-016 소관, 원장 §7. 재시도로 가리지 않는다
@b 문서 검증기 --strict의 자기참조 오탐 — 원장 §7에 기술 부채로 등록했다. 검사 범위를 코드 펜스 밖으로 좁히거나 무시 표식을 정한다. 문서를 고쳐 게이트를 통과시키지 않는다
@path 백필(JOB-ING-004)의 updated 정렬 열거 (원장 §7)
@path JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
@h2 D. 확인할 사항
@path 통합 전량 5회 중 1회 실패했고 그 회차의 실패 시험을 기록하지 못했다. 출력을 요약 줄만 남기고 버린 명령 실수다. 이후 4회 연속 900/900 통과. 원인 미규명이며 원장 6.35장에 해소하지 않은 채 남겼다. 공유 prs_test·공유 ES의 파일 간 오염이 유력한 후보다 — 다음 세션이 같은 것을 보면 그때 귀속한다. 전량 실행 시 출력을 통째로 파일에 남겨라
@path open DEV는 5건이다 (DEV-001·006·010·016·026). 전부 기존 환경 제약·범위 공백이다. 세는 법:
@code lang=bash sha=26f559716f1d lines=1 kept=1
|  grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@path 남은 오픈 결정은 OD-008 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전). OD-005는 CR-040으로 닫혔다
@path link 역할에 GHE_BASE_URL이 필요하다 — 없으면 URL 참조를 아예 만들지 않는다(THR-036, fail closed). 조용히 적게 만드는 것이라 눈치채기 어렵다. WP-030의 세 계열은 GHE를 부르지 않으므로 이 제약과 무관하다
@path 전사는 exports/202608262010.md에 있다 — 실측 확인. /export 출력은 저장소 루트를 가리켰으나 실제 파일은 exports/에 있고 .gitignore 24행이 덮는다. 미추적 0건. 그래도 git add -A·git add .는 쓰지 않는다 — 경로를 지목해 stage 한다
@b agent-context/는 main에 tracked다. .gitignore에 넣지 않는다
@path A절은 CR-041로 해소됐다 — WP-030 착수 전 감사가 열여덟을 찾았고 926b888로 머지됐다.
@p 아래는 그때의 판단 근거를 남긴 것이다.
@h2 (2026-08-26 CR-039 종료 시점의 기록)
@path 최신 기준 (2026-08-26 CR-039 / WP-029 종료 시점)
@p main ea917b8 · SRS baseline v2.5 · 원장 review v2.3 · 작업 패키지 v0.4 ·
@path CR-039 · DEV-229까지. REL-004 구현 1/8 (WP-029 done).
@path REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.
@path 미해결 리뷰 0건 (PR #1~#44 전수 실측). 브랜치 없음.
@h2 A. 지금 당장 — WP-030 착수 전 계약 감사 (REL-004)
@p 구현부터 시작하지 않는다. "착수 전 감사에서 계약 공백이 나왔다"가 일곱 번 연속이다
@path (CR-029·030·031·033·035·038·039). 같은 순서를 지킨다: 감사 → CR → 구현.
@path 대상: WP-030 되돌림·체리픽·스택 관계 파생 (docs/40_delivery/pr_search_work_packages.md)
@todo next-free는 실측할 것 — CR-040 / DEV-230이 예상이지만 추측해 쓰지 않는다:
@code lang=bash sha=01dbe3d21f0a lines=3 kept=3
|  grep -ohE 'CR-[0-9]{3}' docs/00_governance/change_control.md | sort -u | tail -2
|  grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
|  grep -rohE 'JOB-[A-Z]+-[0-9]{3}' docs/ | sort -u   # 새 ID는 충돌부터
@path WP-029가 깔아 둔 자리 — 다시 만들지 말 것:
@b - link 역할·pipeline-worker-link.yaml·README 적용 순서가 이미 있다
@b - link_summary의 네 leaf(has_revert·is_reverted·has_cherry_pick·has_stack)와
@path detached가 WP-030 소유로 명시돼 있다 (DEV-222·223). 객체 통째 대입 금지 —
@p leaf 단위 스크립트(LINK_SUMMARY_SCRIPT)가 선례다
@path - commit_snapshot.patch_id가 체리픽 판정 근거로 보존된다 (CR-038, DEV-208)
@b - STORED_LINK_TYPES에 reverts·cherry_picks·stacks_on이 이미 어휘로 있다
@path 감사에서 물을 것 (WP-029가 같은 계열로 다섯을 찾았다):
@path 1. 방아쇠가 문서 존재를 전제하지 않는가 — JOB-REL-002·003·004도 EVT-ING-003만 적혀 있다
@b 2. 되돌림·체리픽 간선의 link_id 재료가 안정적인가 — 대상이 나중에 밝혀지는 경로가 있나
@path 3. 본문이 바뀌면 간선이 사라지는가 — WP-029의 완전 파생 집합 규율을 이어받는가
@path 4. 과거 데이터 재파생 경로가 JOB-REL-006에 확장되는가
@path 5. FR-REL-005 AC-2가 조건부임을 계약이 반영하는가 (CR-024, DEV-111 — blob 인출 기본 차단)
@h2 B. 릴리스 게이트 4·5·6 (REL-003 미통과 사유)
@p 원장 6.31.1장과 change_control.md 4장에 게이트별로 적혀 있다. 이 환경에서 돌지 않는 항목이 섞여 있으므로 무엇을 지금 할 수 있는지부터 가른다.
@p | 게이트 | 남은 일 | 이 환경에서 가능? | | --- | --- | --- | | Gate 4 보안 | 권한 매트릭스 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능. 화면 축은 화면이 더 서야 함 |
@path | Gate 5 성능 | 성능 목표 7종 (DEV-058) | 불가 — 1000만 문서 합성 데이터셋도 test:perf 스크립트도 없다 |
@p | Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 구성 | 불가 — 실제 Kubernetes 없음 |
@h2 C. 미뤄 둔 항목 (별도 CR)
@path flow-001 e2e 간헐 실패 — 로컬 전체 실행 5회 중 4회 실패, 단독 4/4 통과,
@path CI는 통과. 이 세션이 apps/web을 한 파일도 안 바꿨고 그 시험은 모든
@path /api/를 가로채므로 이번 변경이 닿을 수 없다. WP-016 소관, 원장 §7.
@p 재시도로 가리지 않는다
@path 백필(JOB-ING-004)의 updated 정렬 열거 — 부트스트랩만 created로 고쳤다.
@dec 백필은 조정 스캔이 보정하므로 미뤘다. DEV-098의 direction: asc 의미와 커서
@p 재개 규칙을 함께 봐야 하는 변경이라 별도 CR (원장 §7)
@path JOB-MIR-002 스윕의 배치 공정성 — repository_id 순 500건이라 영구 실패
@p 커밋이 매 회차 같은 자리를 차지한다. 정확성은 안 깨진다 (원장 §7)
@h2 D. 확인할 사항
@path open DEV는 5건이다 — DEV-001·006·010·016·026. 이전 인계가 "1건(DEV-010)"이라
@p 적었으나 실측은 다섯이다. 전부 기존 환경 제약·범위 공백이며 이번 작업이 만든 것이 아니다. 세는 법:
@code lang=bash sha=26f559716f1d lines=1 kept=1
|  grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@path OD-005(nori 플러그인)의 기한이 도래했다. "REL-004 착수 전"인데 WP-029가
@path REL-004의 첫 WP였다. 전문 검색(WP-032) 소관이라 WP-029·030을 막지 않으므로
@p 결정하지 않았다 — 사용자 결정 항목으로 열려 있다 (change_control.md 6장)
@b link 역할이 배포에 추가됐다. 운영 적용 시 pipeline-worker-link.yaml이
@p 적용 순서에 들어 있는지 확인할 것 — 회귀가 그것을 검사한다
@b GHE_BASE_URL이 link 역할에 반드시 필요하다. 없으면 URL 참조를 아예
@path 만들지 않는다 (THR-036, fail closed) — 조용히 적게 만드는 것이라 눈치채기 어렵다
@path 전사는 exports/202608261008.md에 있다 (2026-08-26 실측 정정 — 이전 기록의
@p "저장소 루트" 주장은 틀렸다). exports/는 .gitignore 대상이라 커밋에 딸려 갈 위험이 없고, 미추적 파일도 현재 0건이다. 앞선 전사 둘(202608251453.md· 202608260047.md)도 같은 자리다. 그래도 git add -A·git add .는 쓰지 않는다 — 경로를 지목해 stage 한다
@path 로컬 브랜치 4개가 병합된 채 남아 있다 (claude/cr-031-wp-027,
@path claude/cr-037-post-merge-correctness, claude/cr-038-wp-067-commit-metadata,
@path claude/rel-003-closure). git branch --no-merged main은 비어 있다 — 삭제만
@p 안 된 상태다. "브랜치 없음"을 근거로 판단하지 않는다
@b agent-context/는 main에 tracked다. .gitignore에 넣지 않는다
@path open DEV는 1건(DEV-010, 기존 스트림 동시성 표기)이다. 이 세션이 만든 것 아님
@path 0번은 해소됐다 — CR-037이 미해결 리뷰 10건을 전부 닫고 7060414로 머지됐다.
@path 1번(WP-067 계약 감사)도 CR-038로 해소됐다. 아래는 그때의 판단 근거를 남긴 것이다.
@p 상태 기준: main 5e18e00, SRS baseline v2.5, 원장 review v1.8,
@todo CR-036·DEV-190까지. REL-003 10/11 (WP-067만 todo).
@p 브랜치 없음 — 전부 병합·삭제했다.
@h2 지금 당장 — CR-037로 미해결 리뷰 9건 정정 (블로킹)
@path 전부 코드로 재현 확인했다. WP-028·WP-068의 done 판정을 실질적으로
@path 약화시키므로 WP-067보다 먼저다. 다음 free 번호는 CR-037 / DEV-191이지만
@p 착수 시 반드시 실측할 것.
@h3 PR #37 (CR-034) — P1 4건
@p | # | 결함 | 재현 근거 | | --- | --- | --- |
@path | 1 | repairSequence가 락 획득 후 head_sha를 재확인하지 않는다 (seq_epoch만 검사) | apps/pipeline-worker/src/sequence.ts 818행 부근. 그래프 walk 중 정상 채번이 head를 전진시키면 head를 뒤로 되돌리고 이미 채번된 커밋을 누락한다 |
@path | 2 | 마이그레이션 010이 기존 문서를 백필하지 않는다 | 업그레이드 시 기존 PR 문서는 ES에만 남는다. 투영은 바뀐 PR만 쓰고 조정 스캔은 이미 색인된 것을 건너뛰므로, 손대지 않은 운영 데이터는 스냅숏을 영영 못 얻고 extra_in_es로 보고된다 (DEV-190과 같은 계열) |
@path | 3 | 지문에 org_id·visibility 누락 | apps/pipeline-worker/src/consistency.ts의 CANONICAL_FIELDS. packages/es/src/scoped-query.ts가 org_team 필터에 쓰는 바로 그 필드인데 대조에서 빠져, 접근 통제 데이터가 불일치해도 일치로 보고한다 |
@path | 4 | 실행 중 잡 취소가 무시된다 | apps/pipeline-worker/src/sequence-repair-runner.ts 96행 부근. 운영자가 cancelled로 바꿔도 무조건 finishJob이 completed/failed로 덮어쓰고, 비가역 복구도 계속 진행된다 |
@h3 PR #37 — P2 3건
@path 수동 복구가 sequence.reassigned(EVT-SEQ-002)를 발행하지 않는다 —
@p reassignSequence와 달리 알림 소비자가 수동 복구를 놓친다
@b reassigning이 트랜잭션 밖에서 관측되지 않는다 — bumpEpoch가 같은
@p 트랜잭션 안에서 설정하고 advanceHead가 즉시 ok로 되돌린다. 긴 재구축 중 조회가 "정상"으로 옛 에폭을 낸다. reassignSequence처럼 markReassigning을 먼저 따로 커밋해야 한다
@b 그래프를 읽지 못한 실패가 stale로 영속화되지 않는다 — repairSequence가
@p stale outcome을 반환만 하고 markStale을 부르지 않아, 잠재적으로 손상된 공간이 ok로 광고된다
@h3 PR #36 · #38 — P2 2건
@path PR #36: WP-027 원장 행에 화면 커밋 / PR #33 플레이스홀더가 남아 있고
@path 실제 화면 커밋 594fa94와 CR-032 정정이 빠졌다
@path PR #38: docs/40_delivery/pr_search_work_packages.md의 순서표가
@todo WP-028·WP-068을 아직 todo로 표시한다 (48·50행). 원장은 done이고 DoD
@p 체크박스도 미체크다. 이 세션이 만든 문서 모순이다 — 후속 에이전트가 작업 패키지 문서를 기준으로 판단하면 재작업하게 된다
@h2 WP-067 — 착수 전 계약 감사 필요 (CR 먼저)
@path 현재 WP-067 계약을 그대로 구현하면 안 된다. 다음 다섯을 먼저 정리할 것.
@path JOB-MIR-002가 EVT-ING-003를 소비할 때 prs:projected의 기존 link
@p consumer group과 같은 group을 쓰면 안 된다 — 독립 처리라 별도 group 필요
@b 현재 계약은 "기존 commit document 부분 갱신만"이라 direct-push commit
@p document가 아예 없는 문제를 해결하지 못한다
@b sequence projection도 update_by_query뿐이라 없는 문서를 만들지 않는다
@b PR 상세의 source_commits는 SHA 객체만 만들고 commit document를 join하지
@p 않는다 — 메타데이터를 채워도 화면에는 계속 SHA만 나온다
@b Neighbor API의 direct-push 행도 PR document만 표시 소스로 읽는다 — 같은 이유
@path 우선순위 근거: WP-027이 직접 푸시 커밋을 선행·후행에 실제 노출하기 시작해
@p 이제 사용자에게 보이는 조사 품질이다(원장 §8).
@h2 미뤄 둔 항목
@b flow-001 "히스토리 규율" e2e 간헐 실패 — 이 세션에서 깨끗한 main
@path (bc0e931)으로 재현해 기존 문제로 귀속 확인했다(2회 중 1회 실패). WP-016
@p 소관, 원장 §7. 사용자가 "나중에"로 미뤘다
@path REL-003 잔여: WP-067뿐
@h2 확인할 사항
@b agent-context/는 이제 main에 tracked다(6d2577f). .gitignore에 넣지
@p 않고 삭제하지도 않는다 — 지시서 확인 완료
@b .gitignore에 exports/가 있다(/export 전사 아카이브용)
@path 전사는 exports/202608260047.md에 있고 exports/는 .gitignore 대상이다
@p (2026-08-26 실측 정정 — 이전 기록의 "저장소 루트" 주장은 틀렸다). 직전 세션
@path 전사 exports/202608251453.md도 같은 자리다. 커밋에 딸려 갈 위험은 없다
@b agent-context/ 변경분이 커밋되지 않은 채 인계된다 — *.md 7개 +
@p _handoff/ 전체. 코드 작업을 시작하기 전에 이것부터 커밋할지 정할 것 (risks.md 13번). git add -A는 쓰지 말 것
