#hidden
# aci:v1 id=f54408e src=agent-context/todos.md
@kv sha256=d8ba63e06571467c4500fb66a67742dde117b2ce2e4603f7b8fbdef9269695ea bytes=232019 lines=2685 title=다음-작업-미해결-항목-확인할-사항
@sig agent-context/todos.md;packages/es/src/query-builder.ts;Risks/gotchas;packages/query/src/keys.ts;CR/DEV/WP;packages/contracts/src/source.ts;feature/rel007-capability-registry;fix/main-ci-s0-flaky;feature/rel007-r0-pr-list;docs/cr-092-merge-record;ops/repositories;home/roqkf/design-system;api/v1/me;/prsctl;agent-context/upstream-feedback.md;home/roqkf/pr-search-wt/cr092;home/roqkf/pr-search-wt/cr092-record;fix/cr092-pilot7-feedback;docs/cr-091-merge-record;docs/pilot7-published;home/roqkf/pr-search-wt/cr091-auth;home/roqkf/pr-search-wt/cr091-record;fix/cr091-pilot6-auth-feedback;docs/cr-090-merge-record
@h1 다음 작업 · 미해결 항목 · 확인할 사항
@path 최신 기준 (2026-09-17 10차 · CR-103(WP-085 정정·WP-090)·CR-104(WP-091 무한스크롤)·CR-105(DEV-722 검색 과매칭) main 반영, 최종 커밋 750fb91, CI green · 컨텍스트 한도로 아래 두 항목은 다음 세션으로 넘김)
@risk main은 750fb91이다. 착수 전 git status로 공유 checkout에 다른 세션의 미커밋 작업이 있는지 먼저 확인하라 — 2026-09-17 10차 세션 중 다른 세션의 CR-102(Regression 확장 제안) 미커밋 문서·packages ... lder.ts 변경과 동시에 작업한 이력이 있다(상세: session-notes.md의 "10차" 절 Risks/gotchas).
@h2 먼저 할 것 — 2026-09-17 10차에서 이어받을 것 (사용자 방향은 이미 확정, 다시 안 물어도 됨)
@todo [ ] 범위 검색·M번호 필터. FR-SRCH-005/FR-SRCH-006 개정(PR번호 범위 키 신설, M번호 범위 키 신설, SHA 범위는 프런트엔드가 seq:로 변환하는 것으로 문서화) → packages/query/src/keys.ts → packa ... 수 전 CR/DEV/WP 번호 재실측 필수. 상세 설계 메모는 session-notes.md 10차 절 Next steps 1번.
@todo [ ] Commit history PR번호 병기. FR-SRC-002 AC-1 개정 → packages/contracts/src/source.ts의 SourceCommit에 pr_number 추가 → /history API가 기존 SHA→PR 역해석 재사용 → SourceHistory.tsx 표시+카피. 상세는 session-notes.md 10차 절 Next steps 2번.
@b [ ] worktree 3개(cr102-frontend-fixes·cr103-infinite-scroll·cr105-search-fix) 정리 여부를 사용자에게 물을 것(병합 완료, 삭제는 [Git Destructive]로 막힐 수 있음).
@h2 이전 (8차 이하, 아래는 2026-09-15 시점 기준 — 이후 진행 여부 미확인, 착수 전 실제 상태 재확인)
@path [ ] REL-007 다음 판의 순서를 받는다. 남은 완료 조건은 아래 6차 절이다(GATE-GH-01e·06·08, 사내 GHES 확인 DEV-674). 스냅숏 활성화 조건(DEV-685)은 CR-090이 운영 승인으로 종결했다.
@path [ ] 이전 판의 병합된 기능 브랜치(feature/rel007-capability-registry·fix/main-ci-s0-flaky, 로컬·원격)와 feature/rel007-r0-pr-list 원격을 지울지 정한다. squash 병합이라 git branch --merged main에는 잡히지 않는다. CR-089·CR-090이 만든 브랜치는 6차 절의 정리 대상이다.
@path [ ] 착수할 때 새 번호를 main에서 다시 잰다. 2026-09-15 8차 기준 가장 큰 ID는 CR-092 · DEV-700 · WP-080 · QA-GH-49 · OD-009 · ADR-023 · THR-054 · RB-26 · ENT-GH-014이다. 병렬 세션이 있으면 채번 현황을 묻는다.
@h2 2026-09-15 (8차) — CR-092 뒤에 남은 것
@h3 이 세션이 이어서 할 것
@path [x] 기록 PR(docs/cr-092-merge-record) 병합 — PR #195 → 8c567b7, main CI run 34974241562 success(2026-09-15)
@path [ ] 마감 handoff 갱신(agent-context/*.md 마감 문단·_handoff/)을 커밋할지 사용자에게 묻는다 — 공유 체크아웃 main에서 만들었으므로 커밋한다면 워크트리의 docs 브랜치와 PR로 옮긴다(직접 main 커밋 금지) — 원장 6.91장 PR CI·병합·main CI, CR-092 closed, upstream-feedback 회신, 8차 절과 handoff pack. 병합 커밋은 git log로 확인한다.
@h3 피드백 반영 범위 (사용자에게 확인해 준 기준)
@path 코드 수정 4: DEV-698 접근 범위 · DEV-699 복귀 주소 · DEV-700 로그아웃 · DEV-697 smoke.
@path 코드 변경 없음 2: 로그인 시 팀 동기화(원인 아님), smp* 시퀀스 브랜치(운영자가 /ops/repositories에서 변경).
@b 제외 1: 디자인 시스템 개선(별도 트랙).
@b 발행 전이라 사내 서버에는 넷 모두 아직 적용되지 않았다.
@h3 결정자 확인 (사용자)
@b [ ] 다음 발행 여부와 시점 — 이번 지시는 「릴리스를 발행하진 말아라 아직」이다. 발행하면 원장 6.91장의 사내 확인 항목을 함께 전달한다.
@path [ ] 디자인 시스템 개선 트랙의 착수와 방식(Conductor 원본 저장소 개선·발행 → 제품 고정 버전 상향). /home/roqkf/design-system에 다른 세션의 미커밋 작업이 있었다.
@b [ ] smp* 저장소 브랜치 변경은 사내 운영자가 운영 화면에서 한다 — 이름 규칙 자동 적용을 원하면 새 CR.
@h3 사내 반입 뒤 (사용자, NOT RUN)
@path 조직 Members 권한 없이 저장소 셋으로 /api/v1/me 200. 503이면 search-api 로그 「접근 범위를 조회하지 못했다」의 stage·status·required_permission.
@b nginx 뒤에서 GHE 로그인 뒤 서비스 주소로 복귀, 상단 로그인 이름 메뉴 → 「로그아웃」 → 완료 화면.
@path ./prsctl smoke의 헬스체크 두 줄이 ✓.
@b 손으로 넣은 permission_cache 행 삭제.
@path agent-context/upstream-feedback.md 각 항목 아래에 확인 결과 기록.
@h3 후속 후보 (결정자가 정한다)
@b AUTH_LOGIN_PATH 로드 시 검증(검토 A minor — 지금은 배포 정의가 값을 전달하지 않는다).
@path 로그아웃·쓰기 경로의 CSRF 토큰(DEV-691).
@b 7차의 후보(AUTH_ENABLED=TRUE 엄격화, 역할 관리 화면)는 그대로다.
@h3 자원 정리 (사용자 결정)
@path 작업 트리 /home/roqkf/pr-search-wt/cr092·/home/roqkf/pr-search-wt/cr092-record, 병합된 브랜치 fix/cr092-pilot7-feedback·docs/cr-092-merge-record(로컬·원격), 이미지 prs/*:cr092-final. 격리 서비스 prs-cr091-*는 이 판도 썼다. 이전 판 자원(7차·6차 절)도 그대로다.
@h2 2026-09-15 (7차) — CR-091 뒤에 남은 것
@h3 이 세션이 이어서 할 것
@path [x] 기록 PR #192(docs/cr-091-merge-record) 병합 0c26cdd, main CI run 34928073071 success(verify·integration).
@path [x] 발행됨 2026-09-15 0.1.0-pilot.7을 main 0c26cdd에서 발행했다(자산 SHA-256 2008c7864984e88ed22d9bb4c4c70bd73688ba9522dda3bbfab18d4e0b0ee168, 1,156,972,915 바이트, immutable, 원장 6.90장). 이 발행 기록 PR(docs/pilot7-published)의 병합은 git log로 확인한다.
@path [x] 사내 운영자에게 버전·읽기 토큰·SHA-256을 릴리스와 별도 채널로 전달한다(사용자). — 사내가 0.1.0-pilot.7을 반입해 피드백을 보냈으므로(0a83797) 전달된 것으로 본다.
@h3 사내 반입 뒤 (사용자, NOT RUN)
@path 버전·읽기 토큰·SHA-256을 별도 채널로 전달한다.
@path ./prsctl upgrade. 토큰이 남은 .env로 인증을 켜면 이제 교체 전에 멈춘다 — ADMIN_API_TOKENS=로 비운다.
@path 운영자가 한 번 로그인한 뒤 ./prsctl role grant <GHE 로그인> operator, 화면 새로 고침으로 운영 메뉴 확인.
@path TLS가 없으면 SESSION_COOKIE_SECURE=false와 ALLOW_INSECURE_COOKIES=true를 함께 적고, GHE OAuth App callback을 http://로 맞춘다. ./prsctl health의 「평문 HTTP 세션 허용」 줄은 실패가 아니다. 쿠키 이름이 prs_session으로 바뀐다.
@path agent-context/upstream-feedback.md의 세 항목에 사내 확인 결과를 적는다.
@h3 후속 후보 (결정자가 정한다)
@b AUTH_ENABLED의 인식하지 못하는 값(TRUE)이 조용히 인증을 끈다(검토 A 관찰) — 관리 토큰이 있으면 토큰 경로가 열린다. 엄격화 CR.
@b 역할 관리 화면(현재는 호스트 명령만).
@h3 자원 정리 (사용자 결정)
@path 격리 서비스 prs-cr091-{postgres,redis,elasticsearch}(볼륨 prs-cr091_cr091-pg, compose 파일은 세션 aeef726c scratchpad), 작업 트리 /home/roqkf/pr-search-wt/cr091-auth·/home/roqkf/pr-search-wt/cr091-record, 병합된 브랜치 fix/cr091-pilot6-auth-feedback·docs/cr-091-merge-record(로컬·원격), 이미지 prs/*:cr091-final. 이전 판 자원(6차 절)도 그대로다.
@h2 2026-09-14 (6차) — CR-090 뒤에 남은 것
@h3 후속 (병합 뒤)
@path [x] 후속 원장·handoff PR(docs/cr-090-merge-record)이 병합됐는지 실측한다(PR #189 9c7f132 병합, main CI run 34857988095 success) — 원장 3장 커밋/PR 열·6.87장 PR CI·병합·main CI, WP-080 done, CR-090 closed, agent-context 6차 절과 handoff pack을 담는다.
@path [ ] 이 판이 만든 자원의 정리를 사용자가 정한다 — 격리 서비스 prs-approval-{postgres,redis,elasticsearch}(볼륨 prs-approval_approval-*), 작업 트리 /home/roqkf/pr-search-wt/approval·/home/roqkf/pr-search-wt/cr090-record, 병합된 브랜치 feature/rel007-registry-approval·docs/cr-090-merge-record(로컬·원격), 이미지 prs/*:cr090-verify·prs/*:cr090-final. 실측: git worktree list·docker ps -a·docker images 'prs/*'·git ls-remote --heads origin.
@path [ ] CR-089 자원(작업 트리 /home/roqkf/pr-search-wt/contracts, 브랜치 feature/rel007-result-contracts·docs/cr-089-merge-record)은 2026-09-14 정리 시도가 권한 시스템에 거절돼 남아 있다. 내용은 squash 병합(99d53d3·26e2627)과 트리가 같고 bundle을 떠 두었다(/tmp, 휘발). 사용자가 직접 정리할지 정한다 — 에이전트는 다시 시도하거나 우회하지 않는다.
@h3 사내 반입 전 (사용자)
@path [발행됨] 2026-09-15 0.1.0-pilot.6을 main 9c7f132에서 발행했다(자산 SHA-256 54abbe156130c8e69ef6bdd5dd032e1e03770c7dcdad8253fda6d44e6ab2b3e0, 원장 6.88장). 사내 운영자에게 버전·읽기 토큰·SHA-256을 릴리스와 별도 채널로 전달하고, 번들을 받아 sha256sum으로 대조한다.
@path ./prsctl upgrade로 마이그레이션 030을 적용하고, DB에서 select has_schema_privilege('prs_app', 'public', 'CREATE')가 f인지 본다.
@path Operations를 켠 배포는 ./prsctl health에서 실행기 기동 검사 통과를 본 뒤 operator로 A-006에서 근거를 확인하고 최초 운영 승인을 한다. 승인 전까지 실행은 GH_ADMIN_ACTION_REQUIRED다.
@b 한 사용자·한 저장소로 pr.list 실행 → 차단 → 거절 확인 → 재개 → 새 실행을 본다(런북 7.C 4단계).
@b 이전 앱으로 되돌릴 때는 먼저 GH_OPERATIONS_ENABLED=false로 끈다. 030을 내리는 것은 반드시 끈 뒤다.
@h3 REL-007 완료를 막는 것 (결정자가 순서를 정함)
@path GATE-GH-01e 조합 안전 — Recipe 저장이 생겨야 판정 대상이 있다(WP-058).
@path GATE-GH-06 쓰기 실행 감사·승인(R1 흐름), GATE-GH-08 성능 측정. CR-090의 내부 정책 감사 시험은 06의 증거가 아니다.
@path 사내 GHES 지원 확인(DEV-674) — 사내에서만.
@h3 WP-059의 남은 부분 (CR-090은 A-005 최소·A-006 운영 승인만 당겼다)
@cmd 완료 기준 둘: 설치 gh 버전과 manifest 버전이 다르면 새 command 실행 차단, 확장 허용 목록에 추가한 확장만 실행.
@path A-005 나머지: 위험도 재정의, 승인 필요 지정, gh api 엔드포인트 정책, 확장 허용 목록.
@h3 고치지 않기로 한 note (독립 검토)
@b search-api가 정책 변경의 잠금 대기 초과 503을 로그·지표로 남기지 않는다.
@b 실행기 지표 gh_execution_total{result="policy_unavailable"}가 정책 읽기 실패와 잠금 대기 초과를 합친다(로그 reason으로 가른다). claim 행 잠금 경합의 55P03도 같은 이름을 받을 수 있다.
@b 기존 스위퍼의 머리 막힘 — 계속 던지는 오류가 가장 오래된 대기 행에 있으면 그 회차가 멈춘다(이 판이 만든 것이 아니다).
@b checked_at(트랜잭션 시작 시각) 정렬의 동률 역전 — 실행을 여는 쪽으로 뒤집히지 않는다.
@b action이 없거나 알 수 없는 정책 변경 거절은 감사하지 않는다(정본 어휘 밖 액션을 만들지 않는다).
@h3 열린 편차
@path DEV-691 — CSRF 토큰 체계가 없다(모든 쓰기 API). 새 정책 변경 경로만 JSON 본문 전용으로 보완했다.
@h3 사내에서만 할 수 있는 것
@path 실제 GHES에서 실행기 기동 검사·A-006 근거·운영 승인·pr.list 실행·차단·재개, 대상 GHES 지원 확인(DEV-674), 운영 DB 권한 확인.
@h2 2026-09-14 (5차) — CR-089 뒤에 남은 것
@h3 후속 (병합 뒤)
@path [x] 후속 원장·handoff PR(docs/cr-089-merge-record)이 병합됐는지 실측한다(6차: #187 26e2627 병합 확인) — 원장 3장 커밋/PR 열·6.86장 PR CI·병합·main CI, WP-079 done, CR-089 closed, agent-context 5차 절과 handoff pack을 담는다
@path [ ] 이 판이 만든 자원의 정리 여부를 실측한다(6차 실측: 격리 서비스·볼륨은 없고, 작업 트리와 브랜치 둘은 정리 시도가 권한 시스템에 거절돼 남았다 — 6차 절) — 격리 서비스 prs-contracts-*(볼륨 contracts-*), 작업 트리 /home/roqkf/pr-search-wt/contracts, 병합된 브랜치 feature/rel007-result-contracts·docs/cr-089-merge-record(git worktree list·docker ps -a·git ls-remote --heads origin)
@h3 REL-007 완료를 막는 것 (결정자가 순서를 정함)
@path GATE-GH-01e 조합 안전 — Recipe 저장이 생겨야 판정 대상이 있다(WP-058).
@path GATE-GH-06 쓰기 실행 감사·승인(R1 흐름), GATE-GH-08 성능 측정.
@path 사내 GHES 지원 확인(DEV-674) — 사내에서만.
@path 스냅숏 활성화 절차와 조건에 결과 계약 차원 반영(DEV-685, WP-059) — 6차: CR-090(PR #188)이 운영 승인으로 종결했다.
@h3 WP-066의 남은 부분
@path Recipe 저장 시 호환 거부(WP-058), 아티팩트 ID 표현(FR-GH-007), GhResultEnvelope 통일, gh_api_structured adapter(WP-064).
@h3 고치지 않기로 한 nit (검토 A)
@b resource-ref.ts URL 번호 정규식이 16자리를 받은 뒤 안전 정수 검사에서 grammar로 거절 — 도달 불가라 표지만 어색하다.
@b pr status의 current_branch 정적 간선 — 실행기에서는 늘 null(workspace_required로 문서화).
@h3 사내에서만 할 수 있는 것
@path 대상 GHES에서 결과 모양·비TTY 출력·URL 문법이 2.97.0 소스와 같은지, 사내 저장소 pr list 결과로 만든 참조, 운영 DB의 검증 기록 r2, 사내 계정으로 A-006 결과 계약 표시.
@h2 후속 (병합 뒤): 4차에서 마감함
@path [x] 원장 3장 WP-045·WP-059·WP-078 행의 커밋/PR 열에 #184 · a996540, 6.85장 「병합 뒤 main CI」에 run 34788054624 두 잡 success·PR CI run 34787734188·병합된 main 재검증(typecheck 0 · 회귀 477/477 · 검증기 incomplete 그대로) — 후속 docs 브랜치(agent-context 4차 절·handoff pack 포함).
@b [x] 최종 보고(지시서 17장) — session-summary.md 4차 절이 재료.
@b [x] handoff pack 재생성.
@path [x] PR #185(head 0705d8d) check 두 개 success(verify 3m22s · integration 5m01s) → squash 병합 824eb57 → main CI run 34789146287 success(verify 3m51s · integration 5m17s). post 워크트리를 제거하고 docs/cr088-post-merge를 로컬·원격에서 지웠다.
@b [x] 메모리 previous-session-scratchpad-survives-clear.md를 더하고 Obsidian worklog를 썼다(check 오류 0·경고 0, 색인 126건).
@h2 REL-007 다음 판 후보 (결정자가 순서를 정한다)
@path GATE-GH-01d — bindability·입출력 port·자원 타입을 leaf 196에(DEV-675, WP-066). 지금은 정의 1건에만 있어 1/196·0/0. 이것과 GATE-GH-06(쓰기 승인)·08(성능)이 REL-007 완료를 막는다.
@path 호스트 지원 판정(FR-GH-011 AC-4·AC-5, DEV-674) — 사내 GHES 정보가 원천. 외부에서는 설계만 가능.
@path 스냅숏 활성화 절차와 admin_action_required·A-005 정책 편집(WP-059 나머지).
@path JOB-GH-004 주기 갱신 · A-007 · DEV-651 출력 스트리밍 · DEV-652 KMS · DEV-656 TTL · R1 승인 흐름(FR-GH-009).
@path SRS NFR-009 실측 기준 열의 --json command 41 → 40 정정 여부(DEV-676).
@h2 열어 둔 편차 (건드리지 않는다)
@path DEV-673·674·675·676 + 기존 DEV-651·652·655·656·666·581·588·603·618·642·647·649. flow-003.spec.ts:176 간헐(DEV-377)은 이번에도 배터리에서 1회 났고 단독 3/3 통과 — PR CI를 막으면 DEV-662처럼 대기 조건만 넓힌다.
@h2 사내가 나중에 할 것 (NOT RUN)
@path 실제 GHES에서 command별 지원 확인(hostSupport → verified) · 실제 실행기 컨테이너의 기동 검사 시간과 /healthz의 registry 전이 · 사내 CA·프록시 아래에서 gh --help 순회 · 운영 DB 029 적용과 prs_app 권한 · A-006을 사내 operator·security_officer로 열어 보기 · (기존) Operations App 등록·gh pr list 실행·028·이미지 반입·DEV-561 재적용·CR-083 GHE 로그인.
@p ---
@path 최신 기준 (2026-09-13 3차 · REL-007 R0 완주 — PR #181 병합, CR-086 closed) — 아래는 3차 기준이며 4차가 대체했다.
@h2 후속 (병합 뒤)
@risk 원장 3장 WP-077 행의 커밋/PR 열에 #181 · c5c8aea — docs/cr086-post-merge 브랜치의 docs 커밋(agent-context 갱신 포함). 병합된 main에서 재검증(최소 typecheck·lint·test·test:regression·a11y).
@b 최종 보고(지시서 18장) — session-summary.md 3차 절이 재료.
@h2 REL-007 다음 판 후보 (결정자가 순서를 정한다)
@path DEV-657 capability 분류 195건 → NFR-009 게이트·parity 검증기·gh_capability_snapshot·A-006 (REL-007 완료 조건).
@path DEV-651 출력 청크 스트리밍(EVT-GH-003 미영속 규칙과 함께). DEV-652 KMS 어댑터. DEV-656 멱등 표 TTL. JOB-GH-004 주기 갱신. A-007.
@path R1 capability(쓰기) 전에 승인·확인 흐름(FR-GH-009)·상충 잠금.
@h2 열어 둔 편차 (건드리지 않는다)
@path DEV-651·652·655·656·657·666 + 기존 DEV-581·588·603·618·642·647·649.
@p ---
@path 최신 기준 (2026-09-13 2차 · REL-007 R0 진행 중 — 세션 상한으로 중단. 워크트리에 미커밋 코드 44개 경로)
@path main = 5128c1c(불변). 작업은 /tmp/pr-search-rel007 브랜치 feature/rel007-r0-pr-list(origin/main에서 분기, push 안 함). 이번 지시서(결정자, 2026-09-13)가 이전 handoff의 「지시를 기다린다」를 대체했다 — 아래 순서대로 끝까지 간다.
@h2 시작하기 전에 실측할 것
@code lang=bash sha=4860690c7bda lines=7 kept=7
|cd /home/roqkf/pr-search && git fetch origin --quiet && git log --oneline -3 origin/main   # 5128c1c이어야 한다
|git worktree list                                  # /tmp/pr-search-rel007 [feature/rel007-r0-pr-list] 가 있어야 한다. 없으면 재부팅으로 사라진 것 — 아래 「워크트리가 없을 때」
|cd /tmp/pr-search-rel007 && git status --short | wc -l      # 44
|docker ps --format '{{.Names}}' | grep prs-rel007  # postgres·redis·elasticsearch 셋. 없으면 compose.rel007.yml로 다시 띄운다 (commands.md)
|ls /tmp/claude-1000/-home-roqkf-pr-search/f864b845-0c6f-4ba6-9c0b-b1a332623dcf/scratchpad/gh/gh_2.97.0_linux_amd64/bin/gh   # 없으면 ensurePinnedGh가 내려받는다 (해시 대조)
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1   # CR-085 → 이 판은 CR-086
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -1  # DEV-649 → 이 판은 DEV-650부터
@p 워크트리가 없을 때: /tmp는 휘발이다. 미커밋 코드가 사라졌으면 이 문서의 files.md 절과 decisions.md로 다시 만든다 — 그래서 첫 일은 커밋이다(아래 0번).
@h2 가장 먼저 — 미커밋 코드를 브랜치에 커밋해 /tmp 휘발에서 구한다
@p git add는 경로를 명시한다(공유 체크아웃 규율). 개행 경고(LF→CRLF)는 core.autocrlf 때문이며 HEAD는 LF다 — git diff --cached --shortstat으로 규모를 본 뒤 커밋한다.
@code lang=bash sha=65d1c82ce3fe lines=10 kept=10
|cd /tmp/pr-search-rel007
|git add Dockerfile package.json pnpm-lock.yaml tsconfig.json tsconfig.tests.json vitest.config.ts vitest.integration.config.ts vitest.regression.config.ts scripts/gh-manifest.mjs \
|  packages/gh-cli apps/gh-executor packages/db packages/bus packages/contracts apps/search-api apps/web deploy/single-host/compose.yml docs/30_technical_architecture/pr_search_api_contracts.md
|git diff --cached --stat | tail -1     # 파일 ~90개 안팎이어야 한다 (manifest JSON 518KB 포함)
|git commit -m "feat(WP-077): REL-007 R0 — gh pr list 수직 구현 진행 중 (CR-086)
|Refs: WP-077 FR-GH-001 FR-GH-002 FR-GH-003 FR-GH-006 FR-GH-008 FR-GH-011 FR-GH-012
|Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
|git push -u origin feature/rel007-r0-pr-list
@path agent-context/는 메인 체크아웃(/home/roqkf/pr-search)에 최신본이 있다(이 파일). 워크트리의 agent-context/는 지난 판 그대로이므로, 커밋 전에 cp -r /home/roqkf/pr-search/agent-context/* /tmp/pr-search-rel007/agent-context/로 맞춘다.
@h2 남은 실패 하나를 고친다 (실행기 통합 시험 10건 중 1건)
@path apps/gh-executor/integration/executor.test.ts의 const ESC = ''가 빈 문자열이다 — Write 도구가 리터럴 0x1B를 지웠다. '\x1b'로 바꾼다. 같은 이유로 packages/gh-cli/testing/mock-ghe-tls.ts·packages/gh-cli/src/safe-output.test.ts·src/result.test.ts의 ESC도 이스케이프 표기로 바꿔 둔다(지금은 바이트가 살아 있어 통과하지만 다음 편집에서 사라진다). 그다음:
@code lang=bash sha=2212d90b6816 lines=4 kept=4
|export PATH="$HOME/.nvm/versions/node/v22.23.2/bin:$PATH"
|export POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=55434 POSTGRES_USER=prs POSTGRES_PASSWORD=prs POSTGRES_TEST_DB=prs_test REDIS_URL=redis://localhost:56380 ELASTICSEARCH_NODE=http://localhost:59201
|export GH_PINNED_BIN=/tmp/claude-1000/-home-roqkf-pr-search/f864b845-0c6f-4ba6-9c0b-b1a332623dcf/scratchpad/gh/gh_2.97.0_linux_amd64/bin/gh
|pnpm vitest run --config vitest.integration.config.ts apps/gh-executor/integration/executor.test.ts   # 10/10이어야 한다
@h2 웹 화면을 끝낸다 (W-010 최소 + W-021 최소) — 컴포넌트는 있고 페이지가 없다
@path 만들 것 (기존 apps/web/app/ops/jobs/page.tsx + components/OpsJobsView.tsx 모양을 따른다):
@path apps/web/app/gh/page.tsx — GuardedPage title="GitHub 작업" returnTo="/gh" + <GhCommandCenterView />
@path apps/web/components/GhCommandCenterView.tsx ('use client') — 순서: GET /api/gh/identity·/api/gh/capabilities·/api/gh/contexts/repositories 병렬 → GhIdentityBanner(연결: POST /api/gh/identity {return_to:'/gh'} → authorize_url로 window.location.assign; 해제: DELETE) → GhCapabilityList(선택 pr.list) → GhPrListForm(즉시 검증 validateForm) → 폼이 유효하면 debounce로 POST /api/gh/executions/preview → GhExecutionPreview → 실행 버튼은 canExecute(violations, preview)일 때만 → POST /api/gh/executions (헤더 Idempotency-Key: newIdempotencyKey(), 결과 오면 키 재생성) → GET /api/gh/executions/{id}/stream(EventSource, state·done 이벤트; 실패 시 1초 폴링 GET /api/gh/executions/{id}로 대체) → GhExecutionPanel(취소 POST …/cancel). 404(GH_* 경로 없음)는 「이 배포에서는 GitHub 작업이 열리지 않았다」로. 401 GH_IDENTITY_REQUIRED는 연결 안내.
@path apps/web/app/gh/history/page.tsx + components/GhHistoryView.tsx — GET /api/gh/executions?limit=50[&before=], 표(실행 ID·capability·저장소·상태·요청·종료), 행 선택 시 GhExecutionPanel, 「같은 구성으로 다시 실행」은 invocation을 /gh?prefill=로 넘겨 새 미리보기·새 실행(과거 승인 승계 없음, FR-GH-012 AC-4).
@path apps/web/app/gh/identity/callback/route.ts — GHE가 보낸 ?code&state를 받아, lib/proxy.ts의 resolveProxyAuth·buildProxyHeaders로 세션 쿠키를 붙여 search-api의 POST /api/v1/gh/identity/callback에 넘기고, 응답의 return_to로 redirect. 실패면 /gh?identity=failed로. code·state를 로그에 남기지 않는다. web이 IdP 왕복을 소유한다는 인프라 규칙과 같은 자리(app/auth/callback/route.ts 참고).
@path components/LeftNavPanel.tsx의 ICONS에 'gh-command-center': 'branch', 'gh-history': 'jobs' 추가(없으면 repository 기본 아이콘).
@cmd lib/nav.test.ts에 github 섹션이 모든 역할에 보인다는 시험, lib/gh.test.ts(validateForm이 서버와 같은 위반을 내는지·resultKind 표·describeError), a11y/gh.test.tsx(폼 라벨·live region·표 caption·axe 0건), e2e/gh.spec.ts(API 대역: 연결 없음 → 연결 버튼 → 폼 → 미리보기 argv 표시 → 실행 202 → 스트림 → 결과 표; 위반 시 실행 버튼 비활성; 404 안내). architecture.test.ts가 dangerouslySetInnerHTML 0건을 이미 거는지 확인하고 없으면 gh 경로에 건다 (QA-GH-24).
@h2 배포·회귀를 잇는다
@path deploy/single-host/build-bundle.sh: APP_TARGETS에 gh-executor, IMAGE_NAME[gh-executor]="prs/gh-executor"; prsctl의 적재 확인 목록에 prs/gh-executor; smoke-images.sh에 executor 기동 검사(꺼진 상태로 /healthz 200 + execution: disabled).
@path deploy/single-host/.env.example에 「GitHub Operations Plane」절: GH_OPERATIONS_ENABLED=false, GHE_OPS_CLIENT_ID/SECRET/REDIRECT_URI, GH_IDENTITY_VAULT_KEY(openssl rand -hex 32), GH_EXECUTOR_MAX_CONCURRENT. 자격 분리 설명(Data App·로그인 OAuth App·표기 App·Operations App 넷).
@path docker build --target gh-executor -t prs/gh-executor:dev .을 실제로 돌려 본다 — 내려받기·해시·USER node·/healthz. 그다음 compose의 read_only·tmpfs 조합으로 기동해 execution: disabled를 본다.
@risk regression/runtime-reachability.test.ts: (a) spawn(이 저장소에서 apps/gh-executor/src/spawn.ts 한 곳뿐이고 shell: true·exec(·sh -c가 0건, (b) buildArgv( 정의가 ... per(·구독을 세우고 종료에서 닫음. CAPABILITIES 표 형식을 쓸지는 decisions.md 「아직 정하지 않은 것」.
@path deploy/k8s/: 지시는 K8s를 주력으로 만들지 말라고 했다. 최소 gh-executor.yaml(replicas 1, GH_OPERATIONS_ENABLED configmap, 전용 시크릿 prs-gh-ops-secrets)과 README 한 줄을 둘지 결정 — 인프라 3.1 표가 「Profile A 미포함」이었던 것을 이번에 뒤집으므로 표를 고친다.
@h2 전체 배터리 (순차, 격리 서비스에서)
@risk typecheck → lint → lint:deps → test → test:regression → test:integration → build → test:a11y → test:contrast → web build → test:e2e. 지금까지 돌린 것은 ... sanitizeText 무력화 · 이력 소유자 검사 제거 · 만료 검사 제거. skip은 통과가 아니다 — 통과 건수를 파싱한다.
@h2 문서 cascade (CR-086) — 순서는 CLAUDE.md의 Update Cascade
@path docs/00_governance/change_control.md: CR-086 행(scope, 트리거 「결정자 REL-007 착수 승인 2026-09-13」) + ### CR-086 cascade 절. CR-085 cascade 절(1663행~) 모양을 따른다.
@b srs_final.md: FR 문장 변경 없음을 확인해 적는다. v2.27로 올릴지는 감사 표에 gh.execution.*가 없으므로 「실행 감사는 gh_execution이 축」이라는 한 줄만 필요할 수 있다 — 필요 없으면 「바꿀 것이 없다는 사실을 확인한 뒤 적는다」.
@path 데이터 모델 3.5장: 마이그레이션 번호 006/007/009 → 028로 정정, gh_identity_secret·gh_execution_idempotency 추가, gh_execution_idem_uk가 성립하지 않는 이유, 만들지 않은 표와 이유. 비동기 9.1: prs:gh:executions 파티션 4·소비자 gh-executor·JOB-GH-007 구현 상태. 백엔드 12.1: 모듈이 apps/search-api/src/gh/*와 apps/gh-executor에 있음. 인프라 3.1: gh-executor Profile A 포함(1/1), 12장 값 확정(상한·workspace tmpfs 256m). 보안 10.2: 봉인 최소 구현과 대가; 6장 시크릿 표에 GH_IDENTITY_VAULT_KEY·Operations App client secret 행. API 계약: API-GH-002 미리보기·Idempotency-Key 헤더, API-GH-007 callback, API-GH-010 상세·before 키셋, API-GH-005 「상태만 스트리밍」, API-GH-003 경로가 /gh/contexts/repositories. 프런트엔드 9.1 라우트 표는 /gh·/gh/history만 실재.
@path 로드맵: REL-007 in_progress, 4.2장 끝에 「2026-09-13 착수」. 작업 패키지: ### WP-077 REL-007 R0 PR 목록 조회 첫 수직 구현 신설(WP-076 모양, DoD는 QA-GH-01·02·03·04·05·10·11·12·14·17·18·21·22·23·24·25 인용), 3장 표에 WP-077 in_progress, WP-045/046/047/048/061/062/066을 in_progress로 부분임을 적는다(각각 무엇이 들어왔는지).
@path 원장: 3장 행 갱신, 4장 FR-GH-001/002/003/006/008/011/012·NFR-009/010/011 partial, 5장 DEV-650~(파티션 유니크 불가 · SSE 출력 미스트리밍 · 봉인이 비밀 저장소 대체 · workflow run의 --json 필드 절 부재 · extension exec --help 인증 요구 · --state closed→CLOSED+MERGED · 앱→앱 시험 import · 멱등 표 TTL 없음 · 분류 195건 unknown), 6.83장 검증 기록(외부/사내 표, 배터리, 변이, CI run·attempt).
@path RUNBOOK.md 7.C 「GitHub 작업(Operations App) 켜기」: Operations App 등록(권한 Pull requests: read, callback <web>/gh/identity/callback, User-to-server token expiration 옵트인), .env 채우기, prsctl upgrade, 한 사용자로 연결·실행 확인, 끄는 법(GH_OPERATIONS_ENABLED=false)과 028 down은 별개라는 것.
@path validator: python3 /home/roqkf/.claude/skills/build-srs-prd-env/scripts/validate_srs_prd_env.py --root /tmp/pr-search-rel007 --strict — 기준선은 main에서 6건(오류 4·경고 2): FR-CSS-005 미정의, D-002 미정의, risks.md 경로 2건, placeholder(change_control 12·원장 8). 신규 0건이 목표이며 strict 전체 통과라고 적지 않는다.
@h2 독립 검토 → 수정 재검토 → PR → CI → 병합
@path 검토 둘: (가) 권한·자격·입력·출력·공개 CI 경계, (나) 실행 수명주기·취소·중복·결과·회귀. 검토자의 「미해소」는 코드로 확인한다. PR 본문에 Refs: WP-077 FR-GH-…와 attribution. CI는 필수 check green이 원칙(이번에는 예외 병합 승인이 없다). 병합 뒤 origin/main에서 재검증하고 원장 3장·agent-context 갱신.
@h2 최종 보고 형식 (지시서 18장)
@path 앞머리 다섯 답: ① CI가 실제로 돌았는가 → 예, run 34705991448 attempt 2, 두 잡 success, runs-on 불변, 코드 변경 0 ② 고친 것 → 없음 ③ 새로 할 수 있는 일 ④ 기존 검색과의 차이(실시간 CLI 조회 vs 수집 색인) ⑤ 사내 미확인(실제 GHE·Operations App·GHES rate limit·SSL_CERT_FILE 사내 CA). 그다음 기술 근거(SHA·run·argv·manifest 해시 ad00027d…·gh 해시·migration·이미지·리뷰·PR), 별도 장 「능동 자가 연구 결과」(session-summary.md 2절), 별도 장 「Agent-Initiated Decisions」(decisions.md의 C 표 전부, 항목마다 근거·대안·영향·되돌리기·관련 CR/DEV/시험).
@h2 열린 편차 (건드리지 않는다 — 지시 15장)
@path DEV-581·588·603·618·642·647·649. 이번 흐름을 막지 않는 한 손대지 않는다. 원장 6.82장의 변이 16/18 불일치도 실행 로그 없이는 고치지 않는다.
@h2 사내가 나중에 할 것 (NOT RUN)
@p 실제 GHE에 Operations App 등록·인가·gh pr list 실행, GHES의 user-to-server 토큰 만료 설정, 사내 CA가 SSL_CERT_FILE로 gh에 닿는지, 028 운영 적용, gh-executor 이미지 반입.
@p ---
@path 최신 기준 (2026-09-13 · WP-075 안전성 보강 — CR-085 closed, 병합 완료)
@path main = 65caf0c. 실측하라. PR #177(코드·문서)과 #178(번들 검증 기록)이 squash 병합된 결과다.
@path 브랜치 fix/wp075-annotate-safety·docs/cr085-pilot6-candidate는 원격에 남아 있다.
@h2 시작하기 전에 실측할 것
@code lang=bash sha=0537ce8ae168 lines=6 kept=6
|cd /home/roqkf/pr-search && git fetch origin --quiet && git log --oneline -3 origin/main
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 CR (CR-086으로 보인다)
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 DEV (DEV-650으로 보인다)
|gh release list --limit 3                                 # pilot.5가 여전히 Latest인가
|git worktree list                                         # 하나만 있어야 한다
|gh pr checks 178                                          # CI가 여전히 결제로 막혀 있는가
@p 원격 브랜치 전체로 재실측하라. main에서만 재면 열린 PR이나 병합되지 않은 브랜치가 선점한 번호를 놓친다. 병렬 세션이 있으면 채번 현황을 먼저 묻는다.
@h2 가장 앞에 있는 것 — CI가 코드를 실행하지 못한다 (원인은 둘이다)
@p steps=0으로 두 잡이 2초 만에 끝난다. 시험이 돌아서 실패한 것이 아니다.
@p 「결제만 풀면 된다」가 아니다. 이 저장소에는 self-hosted 러너 DESKTOP-HMAIKJ2가 이미 등록돼 있고 online인데(self-hosted,Linux,X64, busy=false), 워크플로 두 잡이 모두 runs-on: ubuntu-l ... system이 멀쩡했던 것은 그 저장소가 public이라 호스팅 러너가 무료이기 때문이며, pr-search는 private이다.
@p 선택지는 둘이고 결정자가 「지금은 바꾸지 않는다」를 골랐다(2026-09-13).
@b 결제·한도를 푼다 → 12189b1에서 다시 돌려 확인한다.
@b runs-on을 [self-hosted, Linux, X64]로 바꾼다. 저장소가 private이라 fork PR 위험이
@p 없고 러너가 docker 그룹에 있어 services:도 뜬다. 바꾸면 포트를 먼저 본다 — integration의 서비스 컨테이너가 5432·9200·6379를 호스트에 바인딩하므로 개발용 prs-postgres 등이 떠 있으면 그 잡이 실패한다. 러너가 한 대라 두 잡은 순차로 돈다.
@b 그 전까지 0.1.0-pilot.6을 발행하지 않는다. 후보 번들은 만들어 검증까지 마쳤다.
@path 후보는 /tmp/pr-search-bundle-pilot6/에 있으나 임시 경로라 재부팅에 사라진다.
@p 발행할 때는 그때의 main에서 다시 만든다.
@h2 사내가 표기를 켜려면 (런북 7.B가 정본 — 0단계가 새로 생겼다)
@b 먼저 읽기 전용으로 본다. docker compose run --rm --no-deps worker-annotate
@path node dist/annotate-preview-cli.js --repository <org>/<저장소>.
@p 「다음 회차 대상」이 켰을 때 제목이 바뀔 건수이고, 「확인하지 못한 것」도 함께 읽는다.
@b 표기 전용 App 등록 → .env 채움 → prsctl upgrade → 한 저장소에서 먼저.
@b 차단은 조회 성공으로 풀리지 않는다 — 실제 쓰기 성공이나 annotate_resume이다.
@b 제목이 기대와 다르게 저장되면 body_changed로 멈춘다. 확인한 뒤 annotate_resume으로 연다.
@h2 열린 편차 (표기 관련)
@p | 편차 | 무엇 | 왜 열려 있나 | | --- | --- | --- |
@path | DEV-618 | 제목 read-then-write 경합 | 공식 API가 비안전 메서드의 조건부 요청을 지원하지 않는다고 명시한다 |
@path | DEV-647 | 지속 실패 시 이벤트 무한 유예와 파티션 head-of-line | 유예 상한은 DEV-627의 판단을 뒤집는 일이라 별도 설계가 필요하다. 일일 스윕이 backstop한다 |
@path | DEV-649 | 토큰 발급이 중단 신호를 안 받음 | @prs/github의 계약을 넓혀야 한다. 영향은 비변경 요청의 지연뿐 |
@path | DEV-581 | 직접 푸시의 영구 부재 확정 근거 | 결정자의 판단이 필요하다 |
@path | DEV-603 | planner의 멱등 갈래가 도달 불가 | 어느 쪽이 정본인지 정해야 한다 |
@path | DEV-588 | 조정 스캔 취소 시험의 경합 | FR-ADMIN-002를 소유한 WP의 몫 |
@path | DEV-642 | 사내 GHES의 실제 한도 설정 미확인 | 사내가 확인해 주면 런북에 적는다 |
@h2 확인할 사항
@b [ ] CI 결제가 풀렸는가. 풀리면 65caf0c의 체크를 돌려 green을 확인한다.
@b [ ] 0.1.0-pilot.6을 발행할 것인가 — 발행하려면 그때의 main에서 번들을 다시 만든다.
@b [ ] 원격 브랜치 정리 여부 — 병합된 것이 계속 쌓이고 있다.
@b [ ] 병합된 PR의 미해결 리뷰 스레드는 GraphQL isResolved로 직접 세라.
@path PR #177·#178은 0건이지만 저장소 전체로는 이전 라운드의 것이 남아 있다.
@b [ ] 개발 백킹 서비스(prs-postgres 등)는 이 세션 시작 시점에 이미 종료돼 있었고
@p 내가 올리지 않았다. 시험은 prs-wp075safety 프로젝트로 따로 띄웠다가 내렸다.
@h2 그다음 후보
@path M 넘버 계열(WP-074·WP-075)이 안전성까지 끝났다. 로드맵 4.2장은 여기서 끝나고
@path 그 뒤는 REL-007 이후 GitHub Operations Plane이다. 자동으로 시작하지 않는다 —
@p 결정자의 지시를 기다린다.
@p ---
@path 최신 기준 (2026-09-12 · WP-075 PR 제목 표기 — CR-084 closed, 병합 완료)
@path main = 918a37b. 실측하라. PR #176이 squash 병합된 결과이며 그 아래가 8237ff5다.
@path 브랜치 feature/wp075-pr-title-annotate는 원격·로컬 모두 남아 있다(삭제하지 않았다).
@h2 시작하기 전에 실측할 것
@code lang=bash sha=0d21f78e567f lines=6 kept=6
|cd /home/roqkf/pr-search && git fetch origin --quiet && git log --oneline -3 origin/main
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 CR 번호 (CR-085로 보인다)
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 DEV 번호 (DEV-632로 보인다)
|gh release list --limit 3                                 # pilot.5가 여전히 Latest인가
|git worktree list                                         # 하나만 있어야 한다
|gh pr checks 176                                          # CI가 여전히 결제로 막혀 있는가
@p 병렬 세션이 있으면 채번 현황을 물어라. main에서 잰 다음 번호는 열린 PR이 선점한 CR·DEV를 못 본다.
@h2 가장 앞에 있는 것 — CI 결제 문제
@p 마지막 커밋에서 GitHub Actions 잡이 시작조차 되지 않았다. 사유는 The job was not started because recent account payments have failed or your spending limit needs to ... 재실행도 같았다. 결정자가 로컬 전 계층 검증을 근거로 병합을 승인해 병합은 끝났으나, CI로 확인된 것은 8ee26cb까지다.
@b 결정자가 Billing & plans에서 한도를 풀면 gh run rerun으로 다시 돌려 확인한다.
@b 그 전까지 이 저장소의 모든 PR이 같은 이유로 막힌다 — 다음 작업의 CI도 못 돈다.
@h2 사내가 표기를 켜려면 (런북 7.B가 정본)
@b GHE에 표기 전용 GitHub App을 새로 등록한다. 수집용 App(GHE_APP_ID)과 자격을 공유하지
@p 않는다. 권한은 Pull requests: write 하나이며 공식 문서가 그것만 요구한다.
@b .env에 MNUMBER_ANNOTATE_ENABLED=true와 GHE_ANNOTATE_APP_ID·GHE_ANNOTATE_PRIVATE_KEY·
@p GHE_ANNOTATE_INSTALLATIONS를 채운다. 켜 놓고 비어 있으면 worker-annotate가 기동을 거부한다 (의도된 거부).
@b prsctl upgrade. .env만 고치면 반영되지 않는다.
@b 한 저장소에서 먼저 켠다. 넓게 열기 전에 제목이 기대대로 바뀌는지 확인한다.
@b 켜기 전에 제목 변경이 GHE 목록·알림·빌드 로그로 나간다는 것을 관련자에게 알린다. 되돌리는
@todo 자동 경로는 없다 (ADR-022 Follow-up).
@p 사내 반입에서 확인할 것: 실제 GHE가 제목 갱신을 받아들이는가 · 설치 권한이 하나로 충분한가 · 마이그레이션 026이 적용되는가 · 병합된 PR·아카이브 저장소에서 어떤 상태 코드가 오는가(문서가 보장하지 않아 실측이 필요하다).
@h2 열린 편차 다섯
@p | 편차 | 무엇 | 왜 열려 있나 | | --- | --- | --- |
@path | DEV-618 | 제목 read-then-write 경합 | 공식 API가 비안전 메서드의 조건부 요청을 지원하지 않는다. 창을 좁히는 것 외에 할 수 있는 것이 없다. 계약이 넓어지면 재검토 |
@path | DEV-629 | 회차 겹침 방지가 프로세스 국소 | annotate 역할을 늘리려면 분산 claim이 먼저다. 지금은 두 프로파일이 인스턴스 하나로 못 박혀 있고 회귀가 강제한다 |
@path | DEV-581 | 직접 푸시의 영구 부재 확정 근거 | 공식 GHE 계약에 없다. 결정자의 판단이 필요하다 |
@path | DEV-603 | planner의 멱등 갈래가 도달 불가 | 그 갈래와 mapping_conflict 중 어느 쪽이 정본인지 정해야 한다 |
@path | DEV-588 | 조정 스캔 취소 시험의 경합 | FR-ADMIN-002를 소유한 WP의 몫이다 |
@h2 고치지 않기로 한 것 (근거는 원장 6.81장)
@b 코드 미해결 행의 재선택 순환 — 굶주림은 정렬로 풀었으나 순환 자체는 남는다. GHE 호출이 한
@p 번도 없고 지표 mnumber_annotate_total{result="code_unavailable"}가 계속 드러낸다.
@b 부 한도 403의 오분류 가능성 — 대기 신호 없는 403은 권한으로 보므로 멀쩡한 저장소가
@p 쿨다운만큼 멈출 수 있다. 런북에 쿨다운을 짧게 두는 안내를 적었다.
@b 정본 표시와 감사 사이의 crash 창 — 원자성이 불가능한 데서 오는 한계다. 감사를 앞에 두어
@p 「감사가 빠지는」 쪽을 피했다.
@h2 확인할 사항
@path [ ] CI 결제가 풀렸는가. 풀리면 PR #176의 체크를 다시 돌려 green을 확인한다.
@path [ ] 원격 브랜치 정리 여부 — feature/wp075-pr-title-annotate가 남아 있다. 이전 라운드의
@path 넷(fix/dev-561-git-ca-trust·feature/ghe-oauth-login·fix/smoke-gate-cr083·
@path docs/pilot5-published)도 병합됐으나 그대로다.
@b [x] 0.1.0-pilot.6 발행 여부 — 2026-09-15 발행했다(원장 6.88장). 당시 기록: 이번 세션은 발행하지 않았다(승인 범위 밖). 사내가
@p 표기 기능을 받으려면 새 번들이 필요하다.
@path [ ] 병합된 PR의 미해결 리뷰 스레드는 GraphQL isResolved로 직접 세라. PR #176은 0건이지만
@p 저장소 전체로는 26건이 남아 있다(전부 이전 라운드).
@b [ ] 개발 PC의 백킹 서비스(prs-postgres·prs-redis·prs-elasticsearch)를 이 세션이
@p 띄웠고 그대로 두었다. 볼륨에 prs_test 데이터가 있다.
@h2 그다음 후보
@path WP-075가 끝나 M 넘버 계열(WP-074·WP-075)이 모두 done이다. 로드맵 4.2장의 순서는 여기서
@path 끝나고 그 뒤는 REL-007 이후 GitHub Operations Plane이다. 자동으로 시작하지 않는다 —
@p 결정자의 지시를 기다린다.
@p 최신 기준 (2026-09-11 (4차) · 사내 요청 두 건 반영 — 0.1.0-pilot.5 발행 완료)
@p main = 523edae. 실측하라. 이 세션이 만든 커밋 넷이 그 위에 있다 — a3cb046(CA) · 1bd7b9a(GHE 로그인) · 2795666(게이트) · 523edae(발행 기록). 그 사이의 e7593cc·c01f926은 결정자가 직접 푸시한 agent-context 갱신이다.
@h2 시작하기 전에 실측할 것
@code lang=bash sha=258528d969ae lines=5 kept=5
|cd /home/roqkf/pr-search && git fetch origin --quiet && git log --oneline -5 origin/main
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 CR 번호 (CR-084로 보인다)
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 DEV 번호 (DEV-616으로 보인다)
|gh release list --limit 3                                 # pilot.5가 Latest인가
|git worktree list                                         # 하나만 있어야 한다
@p 병렬 세션이 있으면 채번 현황을 물어라. main에서 잰 다음 번호는 열린 PR이 선점한 CR·DEV를 못 본다.
@h2 가장 앞에 있는 것 — 사내 반입 결과를 기다린다
@p 외부에서 할 수 있는 일이 아니다. 0.1.0-pilot.5가 발행됐고 사내가 그것을 반입해야 이번 수정이 실제로 검증된다. 자동으로 다음 기능 개발을 시작하지 않는다.
@path 결정자가 사내에 전달할 것 셋 (DEV-530): 버전 0.1.0-pilot.5 · 읽기 토큰 · 자산 SHA-256
@p aa83abf91fea7f0c2511cb412c4df843564f5a7dc1b035eecd0849de4b6a6afd. 토큰 값은 어디에도 적지 않는다.
@h3 사내가 먼저 해야 하는 것
@b GHE에 로그인 전용 OAuth App 등록. 수집용 App(GHE_APP_ID)과 자격을 공유하지 않는다.
@path callback URL은 <서비스 주소>/auth/callback.
@b .env에 AUTH_PROVIDER=github과 자격 셋, 사설 CA를 쓴다면 GIT_SSL_CAINFO.
@b prsctl upgrade. .env만 고치면 반영되지 않는다.
@h3 사내 반입에서 확인해야 할 것 (원장 6.80장)
@path 임시 CA 조치(.env·compose.yml 직접 수정)를 되돌려도 미러 초기화가 성립하는가 (DEV-561)
@path 실제 GHE OAuth App 등록과 callback URL 일치, 거절 응답의 실제 상태 코드 (CR-083)
@path /user/teams가 돌려주는 팀 슬러그가 GHE_TEAM_ROLE_MAP 표기와 맞는가
@path 로그인 후 첫 조회가 503이 아닌가 (DEV-613), 세션 쿠키가 브라우저에 남는가 (DEV-614)
@b 0.1.0-pilot.4 재시험은 이 판으로 대체된다 — pilot.5가 그 수정을 전부 포함한다
@h2 열린 편차 셋 (앞 라운드에서 이어진다)
@p | 편차 | 무엇 | 왜 열려 있나 | | --- | --- | --- |
@path | DEV-581 | 직접 푸시의 영구 부재 확정 근거 | 공식 GHE 계약에 없다. 결정자의 판단이 필요하다 |
@path | DEV-603 | planner의 멱등 갈래가 도달 불가 | 그 갈래와 mapping_conflict 중 어느 쪽이 정본인지 정해야 한다 |
@path | DEV-588 | 조정 스캔 취소 시험의 경합 | FR-ADMIN-002를 소유한 WP의 몫이다 |
@h2 고치지 않기로 한 minor (근거는 원장 6.78·6.79장)
@b AUTH_ENABLED='False' 오탈자는 fail-closed이나 메시지가 원인을 가린다 — 고치려면 계약을 넓혀야 한다
@b login UNIQUE 충돌은 영구 실패인데 백오프 없이 재시도 — 실패를 캐시하면 일시 오류가 영구 장애로 굳는다
@b teamsTruncated는 로그로만 남는다 — 팀 2,000개는 한 사람 멤버십으로 비현실적이다
@path IDP_GROUP_ROLE_MAP 사문 필드(DEV-578)는 그대로다 — 인증 표면을 건드리므로 사내가 OIDC를 켜는 작업과 함께 판단한다
@h2 확인할 사항
@path [ ] WP-075(PR 제목 M 넘버 표기)는 자동으로 시작하지 않는다. GHE 제목 쓰기는 이 제품이 GHE에 쓰는 최초 경로이며 별도 승인이 필요하다.
@path [ ] 병합된 PR의 미해결 리뷰 스레드는 GraphQL isResolved로 직접 세라. 후속 PR에서 고쳤다는 사실이 스레드 상태를 바꾸지 않는다 (DEV-414).
@b [ ] 개발 PC의 파일럿 스택은 컨테이너를 제거했고 볼륨 여섯 개는 보존했다. .env의 PRS_VERSION은 0.1.0-pilot.5로 바뀌어 있으나 ADMIN_DATABASE_URL이 비어 prsctl upgrade가 멈춘 상태다. 다시 쓰려면 그 값을 채운다 (형식은 .env.example 참고).
@path [ ] 원격 브랜치 정리 여부 — fix/dev-561-git-ca-trust, feature/ghe-oauth-login, fix/smoke-gate-cr083, docs/pilot5-published가 남아 있다.
@path 최신 기준 (2026-09-11 (3차) · WP-074 M 번호 — CR-080·081 closed, 0.1.0-pilot.5 미발행)
@p main = e01bce1. 실측하라. 이 세션이 만든 커밋 넷이 그 위에 있다 — 4466fa1(구현) · 7c46543·e01bce1(리뷰 결함) · 7dcf06d(번들 기록).
@h2 시작하기 전에 실측할 것
@code lang=bash sha=9fe0b597c565 lines=4 kept=4
|cd /home/roqkf/pr-search && git fetch origin --quiet && git log --oneline -5 origin/main
|grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 CR 번호
|grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -1      # 다음 DEV 번호
|gh release list --limit 3                                 # pilot.5가 있는가
@p 병렬 세션이 있으면 채번 현황을 물어라. main에서 잰 다음 번호는 열린 PR이 선점한 CR·DEV를 못 본다.
@h2 0.1.0-pilot.5 발행 — 가장 앞에 있다
@p 기술 작업은 끝나 있다. 번들을 만들고 적재까지 검증했다. 남은 것은 승인이다.
@b 결정자가 「발행하고」로 지시했으나 자동 승인 분류기가 Create Public Surface로 거부했다. 우회하지 않았다.
@b 발행하려면 Bash 권한 규칙을 추가하거나 결정자가 직접 실행해야 한다.
@path 번들을 다시 만들어야 한다. 기존 산출물(/tmp/pr-search-bundle-out)은 4466fa1 기준이라 리뷰 결함 셋(DEV-609~611)의 수정이 빠져 있다. 현재 main은 e01bce1이다.
@code lang=bash sha=2f87e038d5f8 lines=4 kept=4
|# 깨끗한 워크트리에서. 실행 중 deploy/single-host/* 를 편집하지 마라 (실행이 무효가 된다)
|git worktree add /tmp/pr-search-bundle-main origin/main --detach
|cd /tmp/pr-search-bundle-main
|./deploy/single-host/build-bundle.sh 0.1.0-pilot.5 /tmp/pr-search-bundle-release --release
@p 발행 뒤 자산 digest는 gh api로 확인한다 — gh release view --json에는 digest가 없다.
@h2 열린 편차 셋
@p | 편차 | 무엇 | 왜 열려 있나 | | --- | --- | --- |
@path | DEV-581 | 직접 푸시의 영구 부재 확정 근거 | 공식 GHE 계약에 없다. 결정자의 판단이 필요하다 — 시간 기반 확정·초기 커밋 자동 제외·번호 재배치는 승인 없이 추가하지 않는다 |
@path | DEV-603 | planner의 멱등 갈래가 도달 불가 | 그 갈래와 mapping_conflict 중 어느 쪽이 정본인지 정해야 한다. 계약 판단이다 |
@path | DEV-588 | 조정 스캔 취소 시험의 경합 | FR-ADMIN-002를 소유한 WP의 몫이다. 이 판의 변경과 무관하다 |
@h2 고치지 않기로 한 minor (근거는 원장 6.76장)
@b covered 판정이 앱 시계와 DB created_at을 비교 — 재현되지 않은 추측이고 영향 범위가 넓다
@b proof의 resume_page 키 — 구현이 아니라 계약 문구를 고칠 일이다
@b mnumber_blocked_reason의 DB CHECK가 길이뿐 — 읽는 쪽이 이미 enum으로 거른다
@cmd 증거 수집의 git 읽기가 미러 락 밖 — 실패해도 profile_unverified로 안전하게 퇴화한다
@b requestWorkBatch가 availableAt을 무시하고 중복 키를 방어하지 않음 — 현재 호출부가 둘 다 만들 수 없다
@h2 사내 환경의 몫 (이 판에서 하지 않았다)
@b 마이그레이션 025 적용과 기동
@b MNUMBER_ENABLED=true로 켜는 것 — search-api·worker-sequence·worker-batch 셋 다 켜야 한다 (web에는 그 값이 없다)
@b 측정 CLI로 지연 재기 (런북 7.A)
@b 0.1.0-pilot.4 사내 재시험 — 여전히 NOT RUN
@h2 확인할 사항
@path WP-075는 자동으로 시작하지 않는다. GHE 제목 쓰기는 이 제품이 GHE에 쓰는 최초 경로이며 별도 승인이 필요하다.
@path 병합된 PR의 미해결 리뷰 스레드는 GraphQL isResolved로 직접 세라. 후속 PR에서 고쳤다는 사실이 스레드 상태를 바꾸지 않는다 (DEV-414가 가르친 것).
@path 최신 기준 (2026-09-11 (2차) · CR-078 기동 실패-빠름 — 0.1.0-pilot.4 발행 완료)
@path main = a198e12. PR #165가 머지됐고 작업 트리는 깨끗했다. 실측하라.
@path 그 앞의 2cd7c1c·c04208f는 같은 날 다른 세션의 CR-077 라운드다.
@h2 시작하기 전에 실측할 것
@code lang=bash sha=7c5cac5b446a lines=6 kept=6
|git log --oneline -4 origin/main            # a198e12가 맨 위여야 한다
|git status --porcelain                       # 0건. 다른 세션이 이 디렉터리를 쓰는지 먼저 확인하라
|gh release list --limit 3                    # 0.1.0-pilot.4가 Latest인가
|grep -n "^| WP-074 " docs/40_delivery/pr_search_work_packages.md   # todo — 다음 착수
|grep -rn "syncByTarget" apps/ packages/ --include=*.ts | grep -v dist   # 호출부가 여전히 0건인가
|grep -rohE 'CR-[0-9]{3}|DEV-[0-9]{3}' docs/ | sort -u | tail -2      # CR-078·DEV-579가 보여야 한다
@p 동시 세션을 먼저 확인하라. 이 작업 디렉터리는 다른 Claude 세션과 공유된다. ListAgents와 ls -la /proc/*/cwd | grep pr-search로 재고, 브랜치 작업은 git worktree로 격리한다.
@h2 2026-09-11 (2차) — CR-078 이후 남은 것
@p 사내 재시험이 먼저다. 0.1.0-pilot.4가 발행돼 있고, 사내가 그것을 반입해야 이번 수정이 실제로 검증된다. 아래는 사내 결과가 도착한 뒤에 판단할 것들이다.
@path DEV-578 (open) — resolveSessionReaderConfig가 IDP_GROUP_ROLE_MAP을 읽는데 배포 정의는
@path OIDC_GROUP_ROLE_MAP만 준다. 로그인 시 실제 역할을 부여하는 경로(apps/web/app/auth/callback/route.ts:109)는
@p 후자를 바르게 읽으므로 현재 동작은 옳다. 어긋난 쪽이 만드는 SessionReaderConfig.groupRoleMap은 어디에서도 소비되지 않는 사문 필드다. 사문을 지우는 것도 이름을 맞추는 것도 인증 표면을 건드리므로 사내가 OIDC를 실제로 켜는 작업과 함께 판단한다. 그 전까지 이 필드를 새로 읽는 코드를 더하면 언제나 빈 맵을 받는다.
@b AUTH_ENABLED=false에서 조회가 401인 것은 기존 설계다. 이번 수정이 바꾼 것은 「화면이 500 대신
@p 열린다」뿐이다. 사용자가 실제로 검색하려면 OIDC가 필요하고 OIDC를 켜려면 TLS가 필요하다. 런북 7장이 이미 「AUTH_ENABLED=false 형상에서 /search는 비활성」이라고 적고 있다.
@b .env.example 기본값이 AUTH_ENABLED=true다. OIDC 값을 채우지 않고 그대로 쓰면 이제 최초 기동이
@p 실패한다(이전에는 초록으로 뜨고 로그인만 500이었다). 의도된 트레이드오프이고 문구로 안내했으나, 사내에서 첫인상 문제가 보고되면 런북 2.B 절차에 명시적 단계로 넣는 것을 검토한다.
@b 릴리스의 prerelease 플래그. pilot.2~4가 모두 false다. 버전 문자열은 프리릴리스처럼 읽히므로
@p 정식 버전 체계를 정비할 때 함께 다룬다. 지금 바꾸면 이전 릴리스들과 형태가 갈라지고 Latest에서 빠진다.
@h3 사내 재시험에서 확인해야 할 것
@b 0.1.0-pilot.4 업그레이드와 실제 화면 접속 (npm install pg 없이)
@cmd worker-mirror에서 spawn git ENOENT 재발 여부 (0건이어야 한다)
@b prs-releases 색인이 채워지는지
@b prsctl smoke의 「웹 진입 화면」 항목이 통과하는지
@b 실제 사내 OIDC 연결 — OIDC_REDIRECT_URI는 로컬에서 IdP authorize URL까지만 확인했다
@p 사내 .env에 SESSION_COOKIE_SECURE=false가 남아 있으면 prsctl load 전에 true로 되돌려야 한다. 그러지 않으면 web이 기동하지 않고 upgrade가 health에서 멈춘다. 의도된 거부이며 docker logs web에 이유가 남는다.
@h2 다음 작업 — WP-074 (M 넘버 채번과 조회)
@path 착수 조건은 이미 충족됐다. 선행 WP(WP-021·WP-020·WP-008)가 전부 done이고 오픈 결정이 차단하지 않는다.
@path 상세 범위·DoD는 docs/40_delivery/pr_search_work_packages.md의 ### WP-074 절이 정본이다.
@path DEV-576을 먼저 닫는다 (이 WP의 첫 범위). apps/pipeline-worker/src/mirror-runner.ts:126의
@p syncByTarget을 push 경로에 잇고, 미러 fetch가 끝난 뒤에 채번이 돌도록 순서를 보장한다. 두 잡이 같은 push를 각각 받아 경주하면 채번이 옛 head를 읽고 조용히 "새 커밋 없음"으로 끝난다.
@b 마이그레이션 025 (merge_number·annotate_state·annotated_at / mnumber_head_seq·mnumber_head).
@path 채번 계산을 순수 함수로 분리한다 — apps/pipeline-worker/src/sequence-plan.ts의 numberCommits 옆자리.
@path JOB-SEQ-004 워커를 sequence 역할 안에 둔다(공간 락을 JOB-SEQ-001과 공유).
@path EVT-SEQ-004 발행 → ES 투영(merge_number) → API-SEQ-007 → W-001·W-002·W-004 병기.
@h2 그다음 — WP-075 (PR 제목 M 넘버 표기)
@b 착수 전 확인: 전용 GitHub App(pull_requests:write) 발급 절차. 조회용 Data App과 자격 증명을 공유하지 않는다.
@path 쓰기 반경은 제목 한 필드다. 본문·레이블·상태로 넓히지 마라 (ADR-022 결정 2).
@h2 확인이 필요한 것
@path [ ] DEV-576의 운영 실측. 코드 경로 판정만 했다. 아래로 확정한다.
@code lang=sql sha=e367796e54d6 lines=55 kept=55
|      SELECT percentile_disc(0.5)  WITHIN GROUP (ORDER BY assigned_at - committed_at) AS p50,
|             percentile_disc(0.95) WITHIN GROUP (ORDER BY assigned_at - committed_at) AS p95,
|             max(assigned_at - committed_at) AS worst
|      FROM merge_sequence WHERE assigned_at > now() - interval '7 days';
|      ```
|      초 단위면 놓친 경로가 있다는 뜻이고, 시간 단위면 판정이 맞다.
|- [ ] **조직이 rebase merge를 허용하는가.** 허용한다면 `WP-074`의 머지 방식 판정을 우선 검증한다 —
|      rebase는 base에 커밋 N개를 넣는데 PR은 하나만 지목하므로 나머지가 직접 푸시로 오판될 소지가 있다.
|- [ ] 원격 브랜치 `docs/cr077-m-number` 정리 여부.
|- [ ] `agent-context/` 변경분이 커밋되지 않은 채 인계된다 — 코드 작업 전에 커밋할지 정할 것. `git add -A`는 쓰지 마라.
|최신 기준 (2026-09-08 2차 · **`0.1.0-pilot.3` 발행 — CR-068 · CR-069 / DEV-555~558**)
|**main = 139e818. `0.1.0-pilot.3`이 발행됐고 잠겼다(`immutable: true`).** 실측하라.
|- 원장 review **v6.54** · 파생 토큰 v0.6 · SRS baseline v2.20(변경 없음)
|- **다음 빈 ID: CR-070 · DEV-559 · WP-074 · 마이그레이션 024** (측정값. 쓰기 전 다시 잰다)
|- 릴리스: 태그 `0.1.0-pilot.3` → `139e818`, digest `sha256:b5889459a14aa0a68df4365ba490ce78ede533444007b888a3a1aa910f220893`
|### A. 지금 당장 — 결정자 조치
|1. **읽기 토큰을 발급하고 셋을 별도 채널로 전달한다** — 버전 `0.1.0-pilot.3` · 토큰 · 위 SHA-256 (`DEV-530`). 토큰 값은 어디에도 적지 않는다
|2. ~~`PR #156` 병합~~ · ~~리뷰 재계수~~ — 했다. **미해결 9건**이고 그중 **`#154`의 P1 둘이 발행본 안에 있다**
|3. **발행본의 P1 둘을 어떻게 처리할지 정한다** — (a) 사내에 `ADMIN_DATABASE_URL` 작성 규칙을 명시해 전달 (b) 고쳐서 `0.1.0-pilot.4` 발행. `immutable`이라 pilot.3은 고칠 수 없다. **아직 토큰이 발급되지 않아 반입 전이므로 지금이 가장 싸다**
|   - `prs_app`을 거부하지 않는다 — `NOLOGIN` 그룹 롤을 `LOGIN`으로 바꾸고 `prs_admin`을 준다(감사 경계 우회)
|   - 엔드포인트(host/db)를 검증하지 않는다 — 오타여도 프로비저닝은 성공하고 `worker-batch`만 조용히 실패한다(`DEV-553` 재현)
|4. `#155`·`#156`의 리뷰 셋 — 원장 6.72.4/6.72.5의 후보 신원 불일치 · 낡은 `prs_retention` 수동 지시 · `download_count` 근거
|### B. 사내에서 — `upgrade` 절차
|- 이 번들도 `compose.yml`을 덮으므로 **CA 마운트 여섯 자리를 다시 얹는다**(런북 6장). `prs_retention` 롤은 이제 불필요하다(`DEV-556`)
|- **`DEV-551`이 web 손 스텁을 없앤다** — `upgrade` 뒤 500이 재발하지 않는지 확인한다
|- 마이그레이션 `023`으로 그대로이고 환경 변수 키·compose 서비스도 같다
|- 반입 결과를 원장에 기록하는 것이 다음 문서 작업이다
|### C. 열려 있는 것
|- **`PR #150`의 태그 소유 판정 리뷰** — "삭제 lease가 한 번 실패한 뒤라면 커밋 일치는 소유의 증거가 되지 못한다". 이 라운드 범위 밖이라 손대지 않았다
|- `DEV-377` — 병렬 부하에서 흔들리는 e2e 하나. 이번에도 재현됐다(단독 3회는 통과)
|- **Gate 4·5·6 그대로.** `/search`·사내 OIDC·성능·롤백·`ACC-06`은 `NOT RUN`
|- 증분 수집이 사내 경유 호스트 하나에 의존한다(`DEV-550`)
|### D. 이 라운드가 깔아 둔 자리 — 다시 만들지 말 것
|- `require_env`의 `ADMIN_DATABASE_URL`을 빼지 마라 — 그것이 `DEV-556`의 강제다
|- `provision_retention_role` 호출은 **셋**이며 마이그레이션 **뒤**여야 한다. 회귀가 잰다
|- 퍼센트 인코딩·`prs_admin` 직접 접속 거부를 없애지 마라
|- 제품 CSS 검사는 **재귀**다. 직계 자식만 세는 형태로 되돌리지 마라
|- 파생 토큰 문서의 절 번호 13·14를 지우지 마라 — 참조가 그 번호를 가리킨다
|- 이전 라운드 것 그대로: `Dockerfile`의 실체화 호출 · 실체화 스크립트의 "못 찾으면 종료" · 런북 7장의 남은 `NOT RUN`
|---
|## 시작하기 전에 — 이 인계의 값을 실측하라
@p bash
@cmd git -C . rev-parse --short HEAD && git status --porcelain
@cmd gh pr list --state open --json number --jq 'length'
@p grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
@path python3 agent-context/count-unresolved-reviews.py
@cmd gh release list -R 89sooner/pr-search
@cmd gh api repos/89sooner/pr-search/releases/tags/0.1.0-pilot.3 --jq '{draft,immutable,target_commitish,assets:[.assets[]|{name,size,digest,download_count}]}'
@cmd docker ps --format '{{.Names}}' | grep -E '^prs-(postgres|redis|elasticsearch)$' # 없으면 docker compose up -d
@code lang=txt sha=27fba8c32ce1 lines=47 kept=47
|---
|## (2026-09-08 1차 종료 시점의 기록)
|최신 기준 (2026-09-08 · **첫 사내 반입의 결과를 저장소에 반영 — CR-066 / DEV-548~553**)
|**브랜치 `claude/first-internal-import-findings` (main = 268efa9에서 갈라짐). PR은 아직 열지 않았다.** 실측하라.
|- SRS baseline v2.20(변경 없음) · 원장 review **v6.48** · 인프라 **v0.13** · 런북 헤더에 첫 반입 날짜 · 작업 패키지 v2.21(변경 없음)
|- **다음 빈 ID: CR-067 · DEV-554 · WP-073 · 마이그레이션 024** (측정값. 쓰기 전 다시 잰다)
|- open DEV 16건 — 이전과 같다. 이 라운드가 연 여섯은 전부 resolved
|- **인계 pack이 하루 뒤처져 있었다.** 2026-09-04 라운드(`CR-065`·`DEV-544`~`547`, PR #145~#150)가 `agent-context/`에 없었다. 이번에 함께 적었다
|### A. 지금 당장 — 이 라운드를 닫는다
|1. **PR을 열고 CI를 통과시킨다.** 커밋 둘이 브랜치에 있다 — 코드·문서·회귀(`ac2067d`)와 `agent-context`. CI는 `verify`(약 2분 50초)와 `integration`(약 3분)이며 **둘 다** 봐야 한다
|2. ~~이미지 빌드 확인~~ — **했다.** `docker build --target web` 통과, 기동해 세 경로 200·외부 모듈 오류 0건 (원장 6.70.5)
|3. **머지 뒤 리뷰를 반드시 다시 센다.** 이 저장소에서 그것이 다음 결함을 잡은 경우가 압도적이다 (`python3 agent-context/count-unresolved-reviews.py`)
|### B. 사내에서 해야 할 것 — 다음 번들을 받을 때
|- **`company/main`에 얹었는지 확인한다** (런북 5장 새 표): `.env` 여섯 값 · `compose.yml`의 CA 마운트 **여섯 자리** · `prs_retention` 롤 · 웹훅 경유 경로
|- **web 컨테이너 안의 손 스텁은 이제 필요 없다.** 이미지가 그 일을 한다 — 다음 번들부터
|- 반입 결과의 정본은 원장 6.70장이다. 사내에서 새로 재는 값이 생기면 그 장에 덧붙인다
|### C. 아직 성립하지 않은 것 — 반입 성공과 게이트 통과는 다르다
|- `/search` 실제 검색 — `AUTH_ENABLED=false` 형상이라 비활성. OIDC와 토큰은 함께 구성할 수 없다 (`DEV-048`)
|- 사내 OIDC · 그룹 클레임 · 성능 목표 · 롤백 10분 · `ACC-06` — 전부 `NOT RUN` 그대로
|- **Gate 4·5·6 그대로. 릴리스는 승인되지 않았다**
|### D. 미뤄 둔 것 — 이전과 같다
|`DEV-536`의 소유 WP 판단 · `DEV-492` · `DEV-494`(프록시 — 첫 반입 호스트가 프록시를 강제했으나 서비스 아웃바운드는 GHE뿐이라 막히지 않았다) · `DEV-447` · `DEV-433` · `DEV-427` · `DEV-304`·`305` · `DEV-058`. 여기에 **`prs_retention`을 마이그레이션이 만들 것인지**가 추가됐다 (원장 7장, 소유 WP 판단).
|### E. 이 라운드가 깔아 둔 자리 — 다시 만들지 말 것
|- `Dockerfile`의 실체화 호출은 **`pnpm deploy` 뒤**여야 한다. 앞이면 지워질 트리에 만든다
|- 실체화 스크립트의 "못 찾으면 종료 코드 1"을 없애지 마라 — 조용히 넘기면 결함이 그대로 돌아온다. 이름을 박지도 마라
|- `serverExternalPackages`에 `pg`를 넣지 마라 — **효과가 없다**(실측). 넷을 재고 넷 다 버렸다
|- 런북 7장의 남은 `NOT RUN`을 통과로 바꾸지 마라 — 회귀가 잰다
|- 회귀는 **코드에서 값을 읽어 문서와 대조하는 형태**다. 문자열을 박는 형태로 되돌리지 마라
|- 이전 라운드 것 그대로: `build-bundle.sh`의 원자적 태그 생성과 lease 삭제 · 릴리스 워크플로의 스냅숏 단계 · 파생 토큰 문서 §9 · `plans/`는 `.gitignore`
|---
|## 시작하기 전에 — 이 인계의 값을 실측하라
@p bash
@cmd git -C . rev-parse --short HEAD && git status --porcelain # 커밋되지 않은 변경이 있다
@cmd gh pr list --state open --json number --jq 'length'
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
@path python3 agent-context/count-unresolved-reviews.py
@cmd docker ps --format '{{.Names}}' | grep -E '^prs-(postgres|redis|elasticsearch)$' # 없으면 docker compose up -d
@code lang=txt sha=34388634e027 lines=56 kept=56
|---
|## (2026-09-03 2차 종료 시점의 기록)
|최신 기준 (2026-09-03 2차 · **리뷰 부채 종결 라운드 — Conductor 0.3.1 발행, unresolved review 0**)
|`main` = `c81f363` — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
|SRS baseline **v2.20**(변경 없음) · 원장 review **v6.40** · 파생 토큰 문서 **v0.3**(CR-064) · 그 밖 변경 없음.
|Conductor(`/home/roqkf/design-system`) `main` = `49675ea` — SRS baseline **v1.6**, 원장 **v0.32**, `CR-040`·`DEV-044`까지. 패키지는 **css 0.3.1 · react 0.3.1 · tokens 0.3.0**이고 릴리스 태그 셋이 원격 검증을 통과한다.
|**미해결 리뷰 0건** — 양쪽 저장소 전량 순회로 확인했다(`python3 agent-context/count-unresolved-reviews.py`). **이 값도 실측하라.** 이 라운드는 시작 시점 아홉 건으로 출발해 그 수정에 대한 리뷰가 열 라운드 넘게 이어졌고, 처리 건수를 문서에 고정값으로 적으려다 네 번 틀렸다 — 진행 중인 라운드의 건수는 문서를 쓰는 동안에도 늘어난다. 처리 내역의 정본은 각 저장소의 원장과 `change_control.md`다.
|이 라운드가 연 것 (pr-search): `CR-064` · `DEV-541`(태그 소유권) · `DEV-542`(0.3.1 반영) · `DEV-543`(plans/). 셋 다 resolved.
|이 라운드가 연 것 (Conductor): `CR-037`~`CR-040` 넷 · `DEV-035`~`DEV-044` 열. 전부 closed. **Conductor의 open DEV는 0건이다.**
|**릴리스 게이트가 네 번 고쳐졌다** — `DEV-040`(버전이 오르지 않은 패키지를 막음) → `DEV-041`(manifest 동일성이 발행 여부를 답하지 못함) → `DEV-042`(게이트가 발행 뒤에 있어 늦음, 조회 오류를 미발행으로 셈) → `DEV-043`(가벼운 태그가 발행 전 게이트를 통과). 한 정정이 다음 구멍을 여는 것을 네 번 겪었고 매번 머지 후 리뷰가 찾았다.
|**open DEV 16건 — 이전과 같다** (DEV-006·010·016·026·058·061·304·305·395·399·427·433·447·492·494·536). 이 라운드가 새로 연 open은 없다.
|다음 빈 ID: `CR-065` · `DEV-544` · `WP-073` · 마이그레이션 024 (측정값. 쓰기 전 다시 잰다)
|### A. 지금 당장 — 실제 사내 반입 (0.1.0-pilot.2)
|**외부 저장소에서 더 만들 것이 없다.** 리뷰 부채가 0이고 릴리스 자동화의 P1이 닫혔다.
|- 결정자가 fine-grained PAT를 발급한다 — 대상 `89sooner/pr-search` 하나, 권한 Contents Read-only 하나, 만료 90일 이내. 값은 어디에도 적지 않는다
|- 결정자가 사내 운영자에게 릴리스와 **별도 채널**로 셋을 전달한다 — 버전 `0.1.0-pilot.2` · 읽기 토큰 · 자산 SHA-256 `4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3`
|- 사내에서 런북 2.B 1단계부터 위에서 아래로 실행한다. GHE App 자격은 3단계(install 전)에 넣는다
|- **반입 결과를 원장에 기록하는 것이 다음 세션의 첫 문서 작업이다.** 발행 사실 자체는 이미 6.66.6장에 있다
|**주의**: 앱 CSS가 0.3.1로 바뀌었으므로 다음 번들은 새 버전을 담는다. 그리고 `build-bundle.sh`의 되돌리기가 이 라운드에서 바뀌었다 — 태그 소유를 원자적 생성으로 증명한다(`DEV-541`). 발행 흐름은 같다.
|### B. 미뤄 둔 것 — 이전과 같다
|`DEV-536`의 소유 WP 판단(전용 성능 레인 / 러너 기준선 상대 판정 / 통합 레인에서 제외) · `DEV-492` · `DEV-494` · `DEV-447` · `DEV-433` · `DEV-427` · `DEV-304`·`305` · `DEV-058`. 압축 방식 변경(zstd)·백킹 제외 업그레이드 번들·immutable releases 켜기는 필요가 실증될 때 별도 CR.
|`find-animation-opportunities`가 고른 Drawer·Tooltip·Menu 진입 모션은 Conductor에 들어갔으나 pr-search가 그 컴포넌트를 아직 쓰지 않는다. 채택 시 확인한다.
|### C. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
|Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(Profile A에서는 롤백 10분을 실측해 판정한다). ACC-06 NOT RUN → W-007·WP-043 NOT ACTIVATED.
|### D. 이 라운드가 깔아 둔 자리 — 다시 만들지 말 것
|- **`build-bundle.sh`의 태그 소유권**: 발행 직전 `gh api repos/<slug>/git/refs`로 원자적 생성 → 성공했을 때만 `TAG_CREATED_BY_THIS_RUN=1`. 되돌리기는 `--force-with-lease="refs/tags/<v>:<UPSTREAM_COMMIT>"`. **빈 기대값 lease로 되돌리지 마라** — 원격이 이미 같은 커밋이면 git이 `Everything up-to-date`로 통과한다(실측)
|- **`regression/release-tag-ownership.test.ts`**: `git`은 진짜, 원격은 로컬 bare, `gh`·`docker`만 대역. 대역 `gh`의 ref 생성이 원격 상태를 보고 422를 낸다. 대역 둘은 `.gitattributes`가 LF로 고정한다 — 확장자가 없어 `*.sh`에 안 걸린다
|- **`plans/`는 `.gitignore`에 있다.** 되돌리면 `build-bundle.sh`의 계보 검사가 미추적 파일로 보아 번들 빌드를 막는다
|- **파생 토큰 문서 §9는 "Conductor 컴포넌트가 쓰는 토큰"만 적는다.** 섹션 확장·탭 전환을 다시 넣지 마라 — 앱에 CSS 파일이 0건이라 제품이 자체 전환을 만들 수단이 없다(CR-064)
|- Conductor 쪽: 릴리스 게이트가 **발행 전 레지스트리 스냅숏**으로 소유를 가른다(`CR-038`~`CR-040`). `changeset publish` **앞**의 `--snapshot` 단계를 지우지 마라 — 그 단계가 낡은 태그(가벼운 태그·HEAD 아닌 커밋)를 게시 전에 막는다. `npm view`가 404 아닌 이유로 실패하면 미발행으로 세지 않고 중단한다
|- Conductor 쪽: `--snapshot`은 레지스트리에 이미 있는 버전의 태그를 **보지 않고**, `--published-before`는 세 패키지 전부를 보되 "태그 대상 == 릴리스 HEAD" 요구만 면제한다. 두 모드를 뭉뚱그리면 스냅숏 실패를 진단할 때 오진한다(`DEV-044`)
|- 이전 세션 것 그대로: 아카이브 생성 위치·`-C "$OUT_ROOT"`·`sed -i 's/\r$//'`·`.gitattributes`·`cmd_load`의 `require_env` 위치·런북 2.B 3단계의 GHE App 자격·`provision_app_role` 위치·`fixtureMonths`·`audit-grants.test.ts` 전수 검사·`compose.yml`의 `127.0.0.1`
|---
|### 시작하기 전에 — 이 인계의 값을 실측하라
@p bash
@cmd git -C . rev-parse --short HEAD
@cmd gh pr list --state open --json number --jq 'length'
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
@path python3 agent-context/count-unresolved-reviews.py
@cmd gh release list -R 89sooner/pr-search; git ls-remote --tags origin
@cmd npm view @conductor-by-89soone/css version # 0.3.1이어야 한다
@code lang=txt sha=5a806e3f706d lines=54 kept=54
|---
|최신 기준 (2026-09-03 · **애니메이션·컴포넌트 폴리시 라운드 완료 — Conductor 0.3.0 발행·반영**)
|`main` = `08250a8` — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
|SRS baseline **v2.20**(변경 없음) · 원장 review **v6.37** · 그 밖 문서 변경 없음. Conductor(`/home/roqkf/design-system`) `main` = `2549675`, 패키지 **0.3.0** 발행 완료.
|- 이 라운드는 **WP가 아니다.** `improve-animations`·`emil-design-eng` 스킬로 디자인 품질을 훑고 고친 것이며 `REL-006`은 그대로 2/4다.
|- pr-search에서 연 것: `DEV-538`(축소 모션 무효) · `DEV-539`(맨 button·code) · `DEV-540`(Conductor 0.3.0 반영). **셋 다 resolved**, PR #126·#127·#128 병합.
|- Conductor에서 연 것: `DEV-028`~`DEV-034` 일곱. **전부 closed**, PR #14·#18·#19 병합 후 0.3.0 발행.
|- open DEV **16건** — 이전과 같다(`DEV-006`·`010`·`016`·`026`·`058`·`061`·`304`·`305`·`395`·`399`·`427`·`433`·`447`·`492`·`494`·`536`). 이 라운드가 새로 연 open은 없다.
|- 다음 빈 ID: `CR-064` · `DEV-541` · `WP-073` · 마이그레이션 024 (측정값. 쓰기 전 다시 잰다)
|**A. 지금 당장 — Conductor PR의 머지 후 리뷰 8건**
|`python3 agent-context/count-unresolved-reviews.py`가 design-system에서 8건을 센다: #14(2) · #15(4) · #16(1) · #18(1). 이 저장소의 관행대로 **머지 뒤에 오는 자동 리뷰**이며, 이번 세션에서만 그런 리뷰가 여러 번 실결함을 잡았다. pr-search는 #125(1건)뿐이고 그것은 결정자 지시로 남겨 둔 것이다.
|**B. 사내 반입은 그대로 남아 있다**
|`0.1.0-pilot.2` 발행 상태 그대로다(태그 → `0a73065`, SHA-256 `4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3`). 이 라운드는 반입에 아무 영향이 없다. **다만 앱 CSS가 0.3.0으로 바뀌었으므로 다음 번들은 새 버전을 담는다.**
|**C. 미뤄 둔 것**
|- `find-animation-opportunities`가 고른 셋 중 Drawer·Tooltip·Menu 진입 모션은 넣었으나 **pr-search가 그 컴포넌트를 아직 쓰지 않는다.** 채택 시 확인한다.
|- `<datalist>`(최근 질의) — 저장 위치 미정(`DEV-079`)이라 목록이 비어 있어 손대지 않았다.
|- 파생 스펙 `pr_search_design_system_tokens.md` §9(180행)가 "섹션 확장·탭 전환 → `motion.standard`"를 배정하지만 구현은 `hidden` 토글로 즉시다. **고빈도 탭 전환은 즉시가 옳으므로 스펙을 CR로 현행화하는 편이 맞다** — 결정자 판단 필요.
|- `flow-003`의 뒤로가기 e2e가 6워커 병렬에서 두 번 흔들렸다(단독·워커1·재실행은 통과). 반복되면 별도로 다룬다.
|최신 기준 (2026-09-02 2차 · **CR-063·WP-072 완료 — 운반 경로가 GitHub Release로 정해졌고 `0.1.0-pilot.2`가 발행됐다**)
|`main` = `0a73065` — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
|SRS baseline **v2.20**(변경 없음) · 원장 review **v6.34** · 작업 패키지 v2.21 · 인프라 v0.12 · 보안 v1.3 · 로드맵 v0.14 · ADR v0.7 · 데이터 모델 v0.17(변경 없음)
|- **`WP-072` done — 배포 마일스톤 후속이다.** `REL`이 아니므로 `REL-006`은 그대로 2/4
|- **`0.1.0-pilot.2`가 발행됐다** — 태그 → `0a73065`, 자산 `pr-search-0.1.0-pilot.2-offline.tar.gz`(1,132,734,876바이트), SHA-256 `4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3`. 원격 태그·릴리스는 이것 하나. immutable releases는 꺼져 있다(결정)
|- 릴리스는 승인되지 않았다 — Gate 4·5·6 그대로
|- open DEV **16건** — 어제의 15(`DEV-006`·`010`·`016`·`026`·`058`·`061`·`304`·`305`·`395`·`399`·`427`·`433`·`447`·`492`·`494`) + `DEV-536`(CI 통합 p95 시험의 러너 의존). 이 세션이 연 `DEV-528`~`DEV-537` 중 나머지 아홉은 resolved
|- **미해결 리뷰 1건 — PR #125(P1).** 결정자 지시("1~3 마친 뒤 내가 지시")로 손대지 않았다. 아래 B절
|- 다음 빈 ID: `CR-064` · `DEV-538` · `WP-073` · 마이그레이션 `024` · `C-072` (측정값. 쓰기 전 다시 잰다)
|- CI 정상 — `verify`·`integration` 둘 다 판정 근거 (PR #119~#125). 통합에 시간 의존 시험이 둘 있다(`DEV-427`·`DEV-536`) — 빨간 통합은 재실행하되 병합하지 않고 원장에 적는다
|- Codex는 호출하지 않았다. 자동 리뷰는 머지 뒤에 온다 — 이 세션에서 여덟 건, 일곱이 실결함
|이 세션이 배운 것 다섯.
|1. **릴리스는 저절로 불변이 아니다** (`DEV-530`). 정본은 담당자가 별도 채널로 전달하는 자산 SHA-256이고, 사내는 그 값·자산 digest·`sha256sum` 셋을 대조한다.
|2. **되돌리기는 "무엇을 되돌렸는가"를 사실대로 말해야 한다** (`DEV-531`·`533`·`534`·`535`·`537`). 다섯 경계마다 "되돌렸다"가 거짓이 되는 자리가 있었다.
|3. **실행 중인 스크립트와 `deploy/single-host/*`를 편집하면 실행이 무효가 된다.** 백그라운드 실행 전에 커밋하고, 문서는 실행이 끝난 뒤 쓴다.
|4. **가짜 도구는 전제 검사의 호출까지 설계한다.** 첫 조회는 비우고 그다음부터 답한다.
|5. **문서의 전제를 사실로 적을 때는 누가 언제 확인했는지를 함께 적는다** (`DEV-528`). 오프라인 전제 하나가 물리 반입 채널 전체를 만들고 있었다.
|## 이번 세션이 세운 것 — GitHub Release 운반 경로
@p text
@path 외부망: 깨끗한 main → ./deploy/single-host/build-bundle.sh <version> --release
@path → 번들 + 아카이브 → 초안 → 자산 → 발행 → 자산 재독 대조 → 성공 출력(버전·SHA-256·사내 명령)
@path ──────── github.com (HTTPS · 이 저장소 한정 읽기 토큰) · 버전·토큰·SHA-256은 별도 채널 ────────
@cmd 사내망: gh release download → gh api …/releases/tags/<v> --jq '.assets[].digest' → sha256sum(셋 일치)
@path → tar -xzf → cd deploy/single-host → prsctl verify → .env → load → install → smoke → lineage → 2.C → 2.D
@code lang=txt sha=c9ddc9780efd lines=51 kept=51
|## A. 지금 당장 — 실제 사내 반입 (`0.1.0-pilot.2`)
|외부 저장소에서 더 만들 것이 없다. **이 작업이 끝났다고 다음 외부 WP를 자동으로 시작하지 않는다.**
|1. 결정자가 fine-grained PAT를 발급한다 — 대상 저장소 `89sooner/pr-search` 하나, 권한 Contents Read-only 하나, 만료 90일 이내. 값은 어디에도 적지 않는다
|2. 결정자가 사내 운영자에게 릴리스와 **별도 채널**로 셋을 전달한다 — 버전 `0.1.0-pilot.2` · 읽기 토큰 · 자산 SHA-256 `4b07415682ec3c2bce3e020d8e10657886e12f6b386ecb6026554c544f5fb2a3`
|3. 사내에서 런북 2.B 1단계부터 위에서 아래로 실행한다. GHE App 자격은 3단계(`install` 전)에 넣는다
|4. 사내에서만 검증 가능한 것을 그때 실행하고 기록한다 — 사내 위치에서 github.com 도달, 실제 GHE App·웹훅·OIDC·CA·프록시·DNS·실서버 성능·실제 롤백 소요·`ACC-06`. 런북 7장 표의 `NOT RUN` 행이 목록이다
|5. 반입 결과(성공·실패·사내 실측값)를 원장에 기록하는 것이 다음 세션의 첫 문서 작업이다
|## B. 결정 대기 — PR #125 머지 후 리뷰 (P1)
|같은 버전을 같은 커밋으로 두 작업이 겹쳐 발행하면, 다른 작업이 만든 태그의 OID가 `UPSTREAM_COMMIT`과 같아 이 실행의 되돌리기가 그 태그를 지운다는 지적. 커밋 일치는 대상을 증명하지 소유를 증명하지 않는다. 첫 반입에는 영향이 없다(발행 주체가 하나다). 선택지 셋 — 결정자가 고른다.
|- `git push --force-with-lease=refs/tags/<v>:<UPSTREAM_COMMIT> origin :refs/tags/<v>`로 기대 값이 같을 때만 지우게 고친다(`DEV-538` 후보, 가짜 git 시나리오 하나 추가, 약 20분)
|- `DEV-538`로 등록만 하고 "한 버전은 한 담당자가 한 번 발행한다"는 운영 규칙으로 갈음, 스레드에 근거를 답하고 해소
|- 그대로 두고 다음 세션으로
|## C. 원장에 아직 없는 것
|- `0.1.0-pilot.2` 발행 사실(태그·커밋·크기·SHA-256·발행 시각 2026-09-02T08:51:55Z). 6.66장 계열에 한 절로 적는다 — 이 세션은 발행 뒤 문서 PR을 더 열지 않았다
|- `DEV-536`의 소유 WP 판단(전용 성능 레인 / 러너 기준선 상대 판정 / 통합 레인에서 제외). 기준값을 근거 없이 올리지 않는다
|## D. 배포에서 해야 할 것 — 어제와 같다
|마이그레이션 023까지 · `POSTGRES_APP_USER`에 `prs_app` 금지(`DEV-503`) · `clearUnknownSizes` 소급(`pnpm es:apply-mappings`) · `ADMIN_DATABASE_URL` 없으면 `JOB-AUD-001`만 서지 않는다 · Prometheus 경보 규칙 NOT RUN · authz 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름. 사내 호스트 디스크는 첫 설치 시점에 6GB 이상(`DEV-529`).
|## E. 미뤄 둔 항목
|`DEV-536`(위) · `DEV-492`(Profile B `web` 매니페스트) · `DEV-494`(프록시 — 취득 단계의 `gh`·curl은 `HTTPS_PROXY`를 읽으므로 영향 없음, 앱은 별개) · `DEV-447` · `DEV-433` · `DEV-427`(CI 통합 link-rebuild 되먹임 — 이 세션에서 #124의 재실행 실패로 다시 관찰됐다) · `DEV-304`·`305` · `DEV-058`. 압축 방식 변경(zstd, 약 30% 절감)·백킹 제외 업그레이드 번들(약 180MB)·immutable releases 켜기는 필요가 실증될 때 별도 CR.
|## F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
|Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(Profile A에서는 롤백 10분을 실측해 판정한다). `ACC-06` NOT RUN → `W-007`·`WP-043` NOT ACTIVATED.
|## G. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
|- `build-bundle.sh` 발행 전제 검사의 순서: 초안 잔재(`LEFTOVER_DRAFT`) → `gh release view` → 태그(`TAG_TARGET`·`TAG_EXISTED`) → `IMMUTABLE`. **이미지 빌드 앞**이다. 뒤집으면 초안 잔재에 틀린 처방이 나오거나 몇 분짜리 빌드 뒤에 멈춘다
|- 같은 파일의 발행 순서: `tar -tzf` 재독 → `gh release create --draft` → `lookup_draft_id` 3회(실패 시 `undo_release` → `die … $(undo_note)`) → `PATCH draft=false` → `releases/tags/<v>` 재독으로 이름·크기·digest 대조. `--draft`를 빼거나 대조를 빼지 마라
|- `undo_release()`: id를 모르면 `lookup_draft_id`로 재조회 · 태그는 `TAG_EXISTED=0`이고 `git ls-remote --tags origin "refs/tags/<v>" "refs/tags/<v>^{}"`가 있고 그 대상이 `UPSTREAM_COMMIT`과 같을 때만 push로 지운다 · 결과 플래그 `UNDONE`·`RELEASE_DELETED`·`TAG_LEFT`·`TAG_DELETED`·`TAG_FOREIGN` · `PUBLISHED` 플래그를 되살리지 마라(`DEV-535`) · `|| true`로 태그 push를 무시하지 마라(`DEV-534`)
|- `undo_note()`가 모든 되돌리기 뒤의 `die`에 붙는다. `die "…초안을 되돌렸다"` 같은 단정 문구는 회귀가 금지한다(`DEV-533`)
|- 성공 출력의 "별도 채널로 전달할 것 셋"과 immutable 상태 줄 — 회귀가 잰다(`DEV-530`)
|- 런북 2.A "전달할 것은 셋이다" · 2.B 1단계 순서(`gh release download` → `--jq '.assets[].digest'` → `sha256sum` → `tar -xzf`)와 단계 번호(`# 3) 구성 작성`, `# 5) 설치`) · 2.B의 "담당자가 별도 채널로 전달한 SHA-256" — 회귀가 잰다
|- 런북 curl 경로의 `python3 -c 'import json,sys; …["assets"]…'` — 옛 `grep`으로 되돌리지 마라(`DEV-532`)
|- `.env.example`에 `GH_TOKEN`·`GITHUB_TOKEN`을 넣지 마라
|- 회귀 `describe('사내 반입 운반이 GitHub Release와 같은 말을 한다 (WP-072 / CR-063)')` 일곱 — 문자열 검사이며 실행 검증(6.66~6.66.5장)과 짝이다
|- 이전 세션 것 그대로: 아카이브 생성 위치·`-C "$OUT_ROOT"`·`sed -i 's/\r$//'`·`.gitattributes`·`cmd_load`의 `require_env` 위치·런북 2.B 3단계의 GHE App 자격·`provision_app_role` 위치·`|| true` 넷(prsctl)·재색인 직렬화·`git status --porcelain`·`fixtureMonths`·`audit-grants.test.ts` 전수 검사·`compose.yml`의 `127.0.0.1`
|---
|## 시작하기 전에 — 이 인계의 값을 실측하라
@p bash
@cmd git -C . rev-parse --short HEAD
@cmd gh pr list --state open --json number --jq 'length'
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
@path python3 agent-context/count-unresolved-reviews.py
@cmd gh release list -R 89sooner/pr-search; git ls-remote --tags origin
@cmd gh api repos/89sooner/pr-search/releases/tags/0.1.0-pilot.2 --jq '{draft, immutable, target_commitish, assets: [.assets[] | {name, size, digest}]}'
@code lang=txt sha=8d9323164c5d lines=27 kept=27
|---
|## (2026-09-02 · CR-062·WP-071 종료 시점의 기록)
|최신 기준 (2026-09-02 · **CR-062·WP-071 완료 — 사내에서 그대로 실행 가능한 반입 지침이 섰다**)
|`main` = `7a58fd9` — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
|SRS baseline **v2.20**(변경 없음) · 원장 review **v6.26** · 작업 패키지 v2.19 · 인프라 v0.11 · 로드맵 v0.13 · 데이터 모델 v0.17 · 보안 v1.2 · ADR v0.6(변경 없음)
|- **`WP-071` done — 배포 마일스톤 후속이다.** `REL`이 아니므로 `REL-006`은 그대로 2/4
|- 릴리스는 승인되지 않았다 — Gate 4·5·6 그대로
|- open DEV **15건** — 목록은 어제와 같다(`DEV-006`·`010`·`016`·`026`·`058`·`061`·`304`·`305`·`395`·`399`·`427`·`433`·`447`·`492`·`494`). 이 세션이 연 `DEV-523`~`527` 다섯은 전부 resolved
|- 미해결 리뷰 **0건 — pr-search 118개·design-system 12개 전량 순회 (PR #117·#118의 마지막 리뷰까지 해소)**
|- **다음 빈 ID: `CR-063` · `DEV-527` · `WP-072` · 마이그레이션 `024` · `C-072`** (측정값. 쓰기 전 다시 잰다)
|- CI 정상 — `verify`·`integration` 둘 다 판정 근거로 썼다 (PR #116·#117·#118)
|- **Codex는 호출하지 않았다.** 자동 리뷰가 머지 뒤에 오면 소스로 검증해 정정한다 — PR #116의 P1(`DEV-525`)과 PR #117의 P1(`DEV-527`)이 그랬고 둘 다 실결함이었다
|이 세션이 배운 것 다섯.
|1. **번들 내용이 빌더의 checkout 상태에 따라 달라졌다** (`DEV-526`). `.gitattributes`가 고정하지 않은 텍스트는 `autocrlf` 트리에서 CRLF로 복사되고, 어제의 번들이 LF였던 것은 우연이었다. 산출물이 커밋을 가리키면서 내용이 커밋과 다른 자리다 — 빌드가 정규화하고 속성이 고정한다.
|2. **운반 아카이브의 정합은 풀어 나온 사본에서만 증명된다.** 원래 디렉터리에서 verify하면 아카이브가 아예 없어도 통과한다.
|3. **fail-fast는 검사의 존재가 아니라 위치다** (`DEV-524`). `require_env`는 있었다 — `docker load` 뒤에.
|4. **커밋 전 파일에 `git checkout --`을 쓰지 마라.** 변이 원복에 썼다가 파일 전체가 정정 전으로 돌아갔고, 이후 변이 셋의 결과에 그 실수가 섞였다. 원복은 역방향 치환으로 한다.
|5. **검증에서 돌리지 않은 단계가 결함이 숨은 자리였다** (`DEV-527`). `install`을 돌리지 않았고, 런북의 GHE App 자격 순서 결함은 정확히 `install`에서 드러나는 것이었다. 머지 후 리뷰가 찾았다. 건너뛴 단계는 "바꾸지 않았다"가 아니라 "검증하지 않았다"로 적는다.
|## 이번 세션이 세운 것 — 사내에서 그대로 실행 가능한 반입 지침
@p text
@path 외부망: 깨끗한 checkout → ./deploy/single-host/build-bundle.sh <version>
@p → pr-search-<version>-offline/ + pr-search-<version>-offline.tar.gz (사내 반입 파일은 둘째)
@path 사내망: tar -xzf → cd deploy/single-host → prsctl verify → cp .env.example .env → prsctl load
@cmd → prsctl install → prsctl smoke → prsctl lineage → git fetch <bundle> HEAD:vendor/upstream → company/main
@code lang=txt sha=c44560f90e81 lines=42 kept=42
|풀어 나온 사본에서 24/24로 검증했다 (원장 6.64장). `deploy/single-host/RUNBOOK.md`가 정본이며 번들 안에 함께 들어간다. `install`·`smoke`는 이 세션이 바꾸지 않았고 `WP-070`이 관통한 것이다.
|## A. 지금 당장 — 실제 사내 반입
|외부 저장소에서 더 만들 것이 없다. **이 작업이 끝났다고 다음 외부 WP를 자동으로 시작하지 않는다** — 반입이 바로 진행되지 못하는 상황에서만 결정자가 `WP-042`·`WP-044`를 고른다. `WP-043`은 `ACC-06` NOT RUN이라 여전히 착수 금지다.
|1. 깨끗한 트리에서 `./deploy/single-host/build-bundle.sh <version>` → `deploy/single-host/bundle/pr-search-<version>-offline.tar.gz`
|2. 그 파일 **하나**를 사내 호스트로 옮긴다
|3. 런북 2.B → 2.C → 2.D를 위에서 아래로 실행한다
|4. 사내에서만 검증 가능한 것을 그때 실행한다 — 실제 GHE App·웹훅·OIDC·CA·프록시·DNS·실서버 성능·실제 롤백 소요·`ACC-06`
|## B. 사내 반입을 막는 것 — 어제와 같다
|기술적 blocker는 없다. 사내 GHE App·OIDC 클라이언트 등록은 사내 조치이고, 사설 CA는 `NODE_EXTRA_CA_CERTS`로 코드 변경 없이 성립하며, 프록시는 `DEV-494` open(사내망이 강제한다는 증거가 나오면 `GitHubTransport`의 `fetchImpl` 주입 지점으로 최소 수정)이다. RPO 0은 WAL 아카이빙 구성이 있어야 성립한다(`DEV-504`).
|## C. 배포에서 해야 할 것 — 어제와 같다
|마이그레이션 023까지 · `POSTGRES_APP_USER`에 `prs_app` 금지(`DEV-503`) · `clearUnknownSizes` 소급(`pnpm es:apply-mappings`) · `ADMIN_DATABASE_URL` 없으면 `JOB-AUD-001`만 서지 않는다 · Prometheus 경보 규칙 NOT RUN · authz 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름.
|## D. 미뤄 둔 항목 — 어제와 같다
|`DEV-492`(Profile B `web` 매니페스트) · `DEV-494`(프록시) · `DEV-447`(조정 스캔 30초 강제 종료 미증명) · `DEV-433` · `DEV-427`(이제 CI가 도니 확인 가능) · `DEV-304`·`305`(Profile B) · `DEV-058`(합성 데이터셋 부재).
|## E. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
|Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(Profile A에서는 롤백 10분을 실측해 판정한다). `ACC-06` NOT RUN → `W-007`·`WP-043` NOT ACTIVATED.
|## F. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
|- `build-bundle.sh`의 `ARCHIVE="${BUNDLE}.tar.gz"`와 `tar -czf "$ARCHIVE" -C "$OUT_ROOT" "$(basename "$BUNDLE")"` — checksum·시크릿 검사 **뒤**다. 앞으로 옮기면 검사 전 내용이 담긴다. `-C "$OUT_ROOT"`를 빼면 아카이브가 절대 경로를 담는다. 뒤의 `tar -tzf`는 "만든 것을 다시 읽어 본다"이며 빼지 않는다
|- 같은 파일의 복사 루프 안 `sed -i 's/\r$//'` — 빼면 빌더의 checkout이 CRLF일 때 `.env.example`이 CRLF로 나가 `load`가 멀쩡한 값을 거부한다 (`DEV-526`). checksum은 그 뒤에 계산된다
|- `.gitattributes`의 `deploy/single-host/.env.example`·`RUNBOOK.md` `eol=lf`
|- `prsctl` `cmd_load`의 첫 줄 `require_env` — `docker load` 뒤로 옮기면 fail-fast가 아니다 (`DEV-524`). `require_env`의 파서(`env_value`)는 `\r`을 지우지 않는다 — 의도적으로 건드리지 않았고 런북 8장이 안내한다
|- 런북 2.B의 명령 순서와, 2.A 표에서 `prsctl load`를 `./` 없이 쓴 것 — 회귀 시험이 2.B 안에서 `./prsctl` 형태의 순서를 잰다
|- 회귀 `describe('사내 반입 절차가 실행 도구와 같은 말을 한다')` 다섯 — 문자열 검사이며 실행 검증(6.64장)과 짝이다. 하나만 있으면 변이가 살아남는다
|- 바깥 checksum sidecar를 만들지 마라 — 정본이 둘이 된다. 사내 정책이 요구한다는 증거가 나올 때만 별도로 연다
|- 런북 2.B 3단계의 **GHE App 자격은 `install` 전**이다 — 2.C로 되돌리면 `enrich`·`reconcile`이 기동을 거부해 `install`의 health에서 깨진다 (`DEV-527`). `restart`는 `.env`를 다시 읽지 않는다
|- 이전 세션 것 그대로: `prsctl`의 `provision_app_role` 위치·`|| true` 넷·재색인 직렬화, `build-bundle.sh`의 `git status --porcelain`, 통합 헬퍼의 `fixtureMonths`, `audit-grants.test.ts`의 전수 검사 둘, `compose.yml`의 `127.0.0.1`
|---
|## 시작하기 전에 — 이 인계의 값을 실측하라
@p bash
@cmd git -C . rev-parse --short HEAD
@cmd gh pr list --state open --json number --jq 'length'
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
@path python3 agent-context/count-unresolved-reviews.py
@code lang=txt sha=a4bb9c10c373 lines=29 kept=29
|---
|## (2026-09-01 · CR-059·WP-070·CR-060·CR-061 종료 시점의 기록)
|최신 기준 (2026-09-01 · **CR-059·WP-070·CR-060·CR-061 완료 — 첫 사내 반입 가능선이 섰다**)
|`main` = `e1603b2` — 실측하라. 이 인계 커밋이 한 번 더 옮긴다.
|SRS baseline **v2.20** · 원장 review **v6.24** · 작업 패키지 v2.17 · 인프라 v0.10 · 데이터 모델 v0.17 · 보안 v1.2 · 로드맵 v0.11 · 릴리스 검증 v0.6 · 추적 매트릭스 v1.3 · **ADR v0.6 (ADR-021 신설)**
|- **`WP-070` done — 배포 마일스톤이다.** `REL`이 아니므로 `REL-006`의 진행률(2/4)은 그대로다
|- `REL-006`: `WP-041`·`WP-069` done · `WP-042`·`WP-044` todo · `WP-043` 착수 금지(`ACC-06` NOT RUN)
|- 릴리스는 승인되지 않았다 — Gate 4·5·6 그대로
|- open DEV **15건** — `DEV-006`·`010`·`016`·`026`·`058`·`061`·`304`·`305`·`395`·`399`·`427`·`433`·`447`·`492`·`494`
|- **계수는 상태 칸의 머리를 읽는다** — 판정을 덧붙이기만 하면 사람과 도구가 갈린다 (`DEV-522`). 그리고 **상태를 세는 표의 칸에 그 표를 세는 패턴을 리터럴로 쓰지 마라** — 그 인용문이 패턴에 걸린다 (실제로 겪었다)
|- 미해결 리뷰 **0건** — pr-search 115개·design-system 12개 전량 순회 (PR #114의 마지막 리뷰까지 해소)
|- **다음 빈 ID: `CR-062` · `DEV-523` · `WP-071` · 마이그레이션 `024` · `C-072`** (측정값. 쓰기 전 다시 잰다)
|- **CI가 실제로 돈다.** 인계에 적혀 있던 `BILLING_BLOCKED`는 해소됐다 — `verify`·`integration` 둘 다 판정 근거다
|- 오픈 결정 0건
|**이 세션이 배운 것 다섯.**
|- **실제로 세워 보지 않으면 배포 문서는 검증되지 않는다.** `WP-070`이 단일 호스트에서 실제 기동을 시도하자 넷이 드러났다 — 접속 주체 미정의(`DEV-503`), Next.js standalone 기동 실패, `pg_restore --clean`의 파티션 부분 실패, alpine의 `localhost` IPv6 우선 해석. **문서를 읽거나 시험을 돌리는 것으로는 하나도 나오지 않았을 것들이다.**
|- **정정이 시한폭탄을 옮길 수 있다.** `DEV-500`은 롤링 과거 3개월 창으로 고쳤는데 2026-12-01에 같은 실패를 되살리고 **그때도 회귀는 초록**이었다(`DEV-509`). 롤링 창을 없애고 픽스처가 자기 달을 선언하게 하자 **실패가 즉시** 오게 됐다.
|- **전수 검사를 만들 때 "무엇의 전수인가"를 먼저 물어라.** `022`가 표의 권한 공백을 메우며 **시퀀스를 세지 않아** 한 겹 아래에 같은 결함이 남았다(`DEV-517` → `DEV-518`).
|- **실패를 출력만 하고 종료 코드 0으로 끝내지 마라.** 복구가 색인 넷을 지우고 **하나만** 다시 만들면서 성공으로 보고했다(`DEV-519`). 그 증상("재색인 예약 2/4")을 이미 보고 있었으면서 **왜 실패했는지 쫓지 않은 것**이 원인이다.
|- **`set -e`는 진단 앞에서 스크립트를 죽인다.** 스모크 변이가 exit 1을 내면서 **어느 검사가 실패했는지 말하지 않았다** — 실패를 말하지 않는 실패는 성공으로 접는 것과 같다.
|## 이번 세션이 세운 것 — 첫 사내 반입 가능선
@p text 외부망: main의 커밋 → 이미지 6종 → 오프라인 번들 2.6GB → checksum 검증 → 이미지 적재(pull 없음) → migration → 접속 주체 → ES mapping → compose up(18 서비스) → health → 조회 왕복 스모크
@path → 계보 확인(vendor/upstream fetch → manifest의 커밋과 일치)
@code lang=txt sha=83f498f553ed lines=65 kept=65
|**전부 실제 실행으로 검증했다.** `deploy/single-host/RUNBOOK.md`가 정본이며 번들 안에 함께 들어간다.
|## A. 지금 당장 — 실제 사내 반입
|**외부 저장소에서 더 만들 것이 없다.** 다음 단계는 **사내 환경에서만** 실행할 수 있다.
|1. `./deploy/single-host/build-bundle.sh <version>` 으로 번들을 만든다 (작업 트리가 깨끗해야 한다 — 미추적 파일도 검사한다)
|2. 사내 호스트로 옮긴다
|3. `RUNBOOK.md`의 반입 절차를 따른다
|4. **사내에서만 검증 가능한 것**을 그때 실행한다: 실제 GHE App·웹훅·저장소 권한, 사내 OIDC와 그룹 클레임, 사내 CA·프록시·DNS, 실서버 성능, 실제 롤백 소요
|**`WP-042`·`WP-044`는 그 뒤다.** 둘의 요구사항은 `Could` 등급이고, 지금 우선순위는 **지금까지 만든 것을 사내에서 실제로 쓰는 것**이다 (로드맵 4.1장).
|## B. 사내 반입을 막는 것 — 정확히 무엇인가
|**기술적 blocker는 없다.** 남은 것은 조직의 결정과 사내 환경 접근이다.
|| 항목 | 상태 |
|| --- | --- |
|| 번들 생성·반입·기동·복구 | **VERIFIED (external)** |
|| 사내 GHE App 등록·웹훅 | 사내 조치 필요 |
|| 사내 OIDC 클라이언트 등록 | 사내 조치 필요 |
|| 사내 CA (있다면) | `NODE_EXTRA_CA_CERTS`로 **코드 변경 없이** 성립한다 |
|| **사내 프록시 (있다면)** | **`DEV-494` open — Node 22의 `fetch`가 프록시 환경 변수를 보지 않는다.** `git`은 받는다. 사내망이 프록시를 강제하면 `GitHubTransport`의 `fetchImpl` 주입 지점으로 최소 수정이 필요하다 |
|| 호스트 사양 | RAM 8GB 이상 · 디스크 이미지 2GB + 데이터 |
|| **`RPO 0`을 지키려면** | 연속 WAL 아카이빙을 **독립 저장소로** 보내는 구성이 필요하다 (`DEV-504`) — 없으면 실효 RPO는 마지막 백업 이후 경과 시간이다 |
|## C. 배포에서 해야 할 것
|- 마이그레이션 **023**(CR-061)·**022**(CR-060)·021(WP-069)·020(CR-055)·018·019(WP-039)·017(WP-037)
|- **`POSTGRES_APP_USER`에 `prs_app`을 주지 마라** — `NOLOGIN` 그룹 롤이다. `prsctl install`이 다른 이름의 주체를 만든다 (`DEV-503`)
|- `clearUnknownSizes` 소급 (CR-056 / DEV-456) — `pnpm es:apply-mappings`
|- `ADMIN_DATABASE_URL` (WP-039) — 없으면 `JOB-AUD-001`만 서지 않는다
|- Prometheus 경보 규칙 적용은 여전히 NOT RUN
|- **`authz` 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름이다**
|## D. 미뤄 둔 항목
|- `DEV-492` open — **Profile B의 `web` 매니페스트가 없다.** 클러스터가 없어 검증할 수 없는 매니페스트를 지금 만들지 않는다. Profile B 승격 시
|- `DEV-494` open — 프록시 (위 B절)
|- `DEV-502` resolved지만 `DEV-447` open — 조정 스캔의 30초 강제 종료는 여전히 증명되지 않았다. `Promise.race`로 감싸 "강제 종료했다"고 보고하지 마라
|- `DEV-433` open — `API-ADM-001`·`API-ADM-003`의 오프셋
|- `DEV-427` open — CI 통합의 link-rebuild 되먹임. **이제 CI가 도니 확인할 수 있다**
|- `DEV-304`·`DEV-305` — Profile A에서 해소, **Profile B는 그대로 열려 있다**
|- `DEV-058`의 머리글을 그대로 믿지 마라 — 스크립트도 설정도 실재한다. open인 이유는 1,000만 문서 합성 데이터셋 부재다
|## E. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
|Gate 4 보안(일부 가능) · Gate 5 성능(불가) · Gate 6 운영(**Profile A에서는 롤백 10분을 실측해 판정한다** — `CR-060`이 그 면제를 되돌렸다). `ACC-06`은 NOT RUN이고 `W-007`·`WP-043`은 NOT ACTIVATED다.
|## F. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
|- **`deploy/single-host/prsctl`의 `provision_app_role`** — 마이그레이션 **뒤에** 부른다. 앞으로 옮기면 `prs_app`이 아직 없어 `GRANT`가 실패한다 (`DEV-510`)
|- **같은 파일의 `|| true`** — 명령 치환 넷에 붙어 있다. 빼면 `set -e`가 **진단 앞에서** 스크립트를 죽여 무엇이 실패했는지 말하지 않는다
|- **같은 파일의 재색인 직렬화** — 동시 실행 상한이 1이라 연달아 예약하면 셋이 거절된다 (`DEV-519`)
|- **`build-bundle.sh`의 `git status --porcelain`** — `git diff --quiet`로 되돌리지 마라. 미추적 파일을 보지 못해 계보 증명이 거짓이 된다 (`DEV-512`)
|- **통합 헬퍼 넷의 `fixtureMonths`** — 롤링 과거 창으로 되돌리지 마라. 실패가 미래로 미뤄진다 (`DEV-509`)
|- **`audit-grants.test.ts`의 전수 검사 둘** — 표와 **시퀀스** 양쪽이다 (`DEV-517`·`DEV-518`)
|- **`compose.yml`의 `127.0.0.1` healthcheck** — `localhost`로 되돌리면 alpine이 `::1`로 풀어 서비스가 멀쩡한데 unhealthy로 보인다
|- **`.gitattributes`** — 셸 스크립트가 CRLF로 커밋되면 리눅스에서 `bad interpreter`로 죽는다. 사내 반입 뒤에는 고칠 방법이 그 자리에 없다
|- 이전 세션 것 그대로: `team-membership.ts`의 `upsertTeam` 위치, `author-teams.ts`의 잠금 뒤 재확인·루프 순서·`stop()`의 `await`, `documents.ts`의 `pr?.author == null` 판정, `reindex.ts`의 명시적 `delete`
|---
|## 시작하기 전에 — 이 인계의 값을 실측하라
@p bash
@cmd git -C . rev-parse --short HEAD
@cmd gh pr list --state open --json number --jq 'length'
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
@path python3 agent-context/count-unresolved-reviews.py
@code lang=txt sha=94359ca306af lines=105 kept=80
|미해결 리뷰는 **스크립트가 정본이다** (DEV-468·469). 한 줄 명령으로 되돌아가지 마라 — 창이 둘이고 둘 다 넘칠 수 있으며, 그 스크립트는 순회한 수를 각 `totalCount`와 대조해 "전량 순회=아니다"면 종료 코드 1을 낸다.
|## A. 지금 당장 — WP-042 착수 전 API-SEQ-005 계약 감사 (CR-059)
|`WP-069`이 끝났고 `REL-006`의 다음은 `WP-042`(이분 탐색 보조)다.
|**`API-SEQ-005`도 계약 4장에 상세 절이 없다.** `CR-057`이 `API-SEQ-004`를 채우면서 그 사실을 확인했고 **의도적으로 함께 채우지 않았다** — 그것은 이 WP의 감사 대상이며, "SEQ API 전부 정리"로 만들면 검증하지 않을 계약을 함께 쓰게 된다. 착수 전 감사가 이 저장소의 관행이며 `CR-058`이 열여덟 번째였다.
|**감사 후보 (이전 세션이 실측으로 모아 둔 것. 확정 결함이 아니라 판정 대상이다).**
|| 후보 | 무엇이 비어 있는가 | 근거 |
|| --- | --- | --- |
|| 상세 절 전체 | 요청·응답·오류 정의가 없다 | 4장에 `### API-SEQ-005` 0건, 3장 카탈로그 36행 한 줄뿐 |
|| 권한 구분 | 카탈로그가 `인증`만 말하고 접근 범위를 요구하지 않는다 | 이분 탐색은 저장소의 시퀀스 공간을 읽는다. `DEV-461`과 같은 모양 |
|| 초기 구간의 출처 | `good_seq`·`bad_seq`가 nullable인데 null의 뜻과 초기 구간이 정의되지 않았다 | `bisect_session`에 조사 구간 경계 열이 없고 W-004의 구간은 URL 파라미터에만 있다 |
|| 후보 구간의 경계 | 열린 구간인지 닫힌 구간인지 없다 | AC-4의 "올림 log2"가 이 정의에 따라 값이 달라진다 |
|| 중앙값 동점 처리 | 같은 거리에 커밋이 둘이면 무엇을 고르는지 없다 | AC-2가 "가장 가까운"만 말한다 |
|| 에폭 무효화의 응답 모양 | 읽기에서 에폭이 바뀌었을 때 200 봉투인지 409인지 없다 | `SEQUENCE_EPOCH_STALE`은 **쓰기 요청**으로 정의되어 있다 |
|| DELETE의 멱등성 | 세션이 없을 때 무엇을 반환하는지 없다 | 카탈로그가 `GET/POST/DELETE`만 적는다 |
|| 응답 필드 대응 | `C-029`의 `remaining`·`estimatedSteps`·`nextSeq`에 대응하는 정의가 없다 | `pr_search_ui_component_spec.md:295` |
|**이미 서 있는 것 (다시 만들지 마라).**
|- `bisect_session` 표가 **마이그레이션 002에 이미 있다.** `005_roles.up.sql`에 권한 부여도 있다
|- `BISECT_CONTRADICTION`(409)이 `packages/contracts/src/error-codes.ts`와 계약 6장에 **이미 등재돼 있다**
|- 백엔드 6.2가 이분 탐색을 **감사 미기록**으로 정했고 멱등 키를 `(user, repo, branch)`로 정했다
|- 재채번은 이분 탐색 세션에 **아무것도 쓰지 않는다** (CR-026, DEV-126). 조회가 현재 에폭과 비교해 `epoch_stale`을 계산한다
|`CR-058`이 세운 것을 그대로 쓸 자리가 많지만 **복사하지 말고 `FR-SEQ-007`이 요구하는 것을 먼저 읽어라** — 탐색 상태는 사용자별이라 겨루는 것이 다르다.
|## B. 그 뒤 — WP-044
|`WP-044` 검색 결과 내보내기 — 감사 액션 `export.create`를 활성으로 옮긴다. **`safe_marker.set`이 그 전환의 선례다** (SRS v2.18): 상태 칸은 "계약이 승인했으나 기능이 아직 없다"를 뜻하므로 기능이 서는 순간 이전 값이 거짓이 된다. **남은 미활성은 `export.create` 하나뿐이다.**
|`WP-043`은 착수하지 않는다 — `ACC-06`이 NOT RUN이라 활성화 조건이 성립하지 않는다.
|## C. 배포에서 해야 할 것
|**`WP-069`이 마이그레이션 021을 더했다.**
|- 마이그레이션 **021**(WP-069), 020(CR-055), 018·019(WP-039), 017(WP-037) — `pnpm run db:migrate`
|- `clearUnknownSizes` 소급 (CR-056 / DEV-456) — `pnpm es:apply-mappings`
|- `ADMIN_DATABASE_URL` (WP-039) — 없으면 `JOB-AUD-001`만 서지 않는다
|- `SEARCH_CURSOR_HMAC_KEY`, `pnpm es:reindex`
|- Prometheus 경보 규칙 적용은 여전히 NOT RUN
|- **`authz` 역할에 GHE 자격이 없으면 작성자 팀이 언제나 모름이다.** 스윕이 돌지 않고 기동 로그가 그 사실을 경고한다
|## D. GitHub Actions — pr-search만 결제 차단이다
|`verify`·`integration` 모두 `steps=0`이고 주석이 `The job was not started because recent account payments have failed`를 말한다. **잡이 뜨지도 않은 것이지 시험이 실패한 것이 아니다.**
|- 재시도를 누르지 않는다 — 이 세션이 PR 둘에서 같은 상태를 확인했다
|- design-system은 다르다. 그 저장소에서는 CI가 판정 근거다
|- pr-search에서는 로컬 배터리가 유일한 판정이며 그 사실을 원장·PR·인계에 적는다
|## E. 미뤄 둔 항목
|- `DEV-447` open — 조정 스캔의 30초 강제 종료가 증명되지 않았다. `Promise.race`로 감싸 "강제 종료했다"고 보고하지 마라
|- `DEV-433` open — `API-ADM-001`·`API-ADM-003`의 오프셋
|- `DEV-427` open — CI 통합의 link-rebuild 되먹임. CI가 돌지 않아 확인할 길이 없다
|- `DEV-395`·`DEV-399`·`DEV-058`·`DEV-061` — 변화 없음
|- **`DEV-058`의 머리글을 그대로 믿지 마라.** "`pnpm test:perf` 스크립트가 저장소에 없다"고 적혀 있으나 스크립트도 `vitest.perf.config.ts`도 `perf/analytics.perf.test.ts`도 **실재한다.** 여전히 open인 이유는 1,000만 문서 합성 데이터셋이 없어 실측 p95가 NOT RUN이기 때문이다
|## F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
|Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 1,000만 문서 합성 데이터셋 부재) · Gate 6 운영(불가 — 실제 K8s 없음). `ACC-06`은 NOT RUN이고 `W-007`·`WP-043`은 NOT ACTIVATED다.
|## G. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
|- **`packages/db/src/repositories/team-membership.ts`의 `upsertTeam(pool, ...)`** — 트랜잭션 **밖**이다. 안으로 옮기면 23505 재시도가 25P02로 죽는다 (DEV-489)
|- **같은 파일의 `org_team_sync` 갱신** — 교체와 **같은 트랜잭션 안**이다. 밖으로 빼면 실패한 조직이 신선해 보이고 모름이 빈 배열로 바뀐다
|- **`apps/pipeline-worker/src/author-teams.ts`의 잠금 뒤 신선도 재확인** — 지우면 동시 요청이 조직 팀 전체를 두 번 훑는다 (M10)
|- **같은 파일의 루프 순서 (`sweep` → `sleep`)** — 뒤집으면 냉시작 후 한 주기 내내 모든 문서가 모름을 답한다 (M19)
|- **같은 파일의 `stop()`의 `await loop`** — 빼면 종료가 진행 중인 동기화 위로 풀을 닫는다 (M18)
|- **`apps/pipeline-worker/src/documents.ts`의 `pr?.author == null` 판정** — 호출부의 값보다 **먼저**다. 작성자를 모르면 소속도 모름이다 (DEV-487)
|- **같은 파일의 `removed` 배열** — 변경 규모 넷과 작성자 팀 하나가 **함께** 쓴다. 규모를 보는 시험은 `sizeRemovals`로 자기 몫만 거른다
|- **`apps/pipeline-worker/src/reindex.ts`의 `delete doc['author_team_ids']`** — `undefined` 스프레드로 되돌리지 마라. `JSON.stringify`가 키를 버리는 것에 기대는 암묵적 표현이다
|- **`regression/runtime-reachability.test.ts`의 계약 정합 시험** — 계약 두 자리가 같은 사실을 말하는지 본다. 한쪽만 갱신하면 이것이 먼저 깨진다 (DEV-488)
|---
|...cut 25 lines
@p bash
@cmd git -C . rev-parse --short HEAD
@cmd gh pr list --state open --json number --jq 'length'
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@p grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1
@path python3 agent-context/count-unresolved-reviews.py
@code lang=txt sha=f8012f3f06e2 lines=84 kept=80
|**미해결 리뷰는 스크립트가 정본이다** (DEV-468·469). 한 줄 명령으로 되돌아가지 마라 — 창이 둘이고 둘 다 넘칠 수 있으며, 그 스크립트는 순회한 수를 각 `totalCount`와 대조해 **"전량 순회=아니다"면 종료 코드 1**을 낸다.
|## A. 지금 당장 — WP-042 착수 전 `API-SEQ-005` 계약 감사
|`WP-041`이 끝났고 `REL-006`의 다음은 `WP-042`(이분 탐색 보조)다.
|**`API-SEQ-005`도 계약 4장에 상세 절이 없다.** `CR-057`이 `API-SEQ-004`를 채우면서 그 사실을 확인했고 **의도적으로 함께 채우지 않았다** — 그것은 이 WP의 감사 대상이며, "SEQ API 전부 정리"로 만들면 검증하지 않을 계약을 함께 쓰게 된다. 착수 전 감사가 이 저장소의 관행이며 `CR-057`이 열일곱 번째였다.
|**`CR-057`이 세운 것을 그대로 쓸 자리가 많다.** 이분 탐색 세션도 시퀀스 공간에 붙고 에폭을 저장하므로, `SEQUENCE_EPOCH_STALE`·`FOR SHARE` 재검증·`expected_*` 조건부 갱신이 같은 모양으로 필요할 수 있다. **다만 그대로 복사하지 말고 `FR-SEQ-007`이 요구하는 것을 먼저 읽어라** — 탐색 상태는 사용자별이라 겨루는 것이 다르다.
|## B. 그 뒤 — WP-044 · WP-069
|- `WP-044` 검색 결과 내보내기 — 감사 액션 `export.create`를 활성으로 옮긴다. **`safe_marker.set`이 그 전환의 선례다**(SRS v2.18): 상태 칸은 "계약이 승인했으나 기능이 아직 없다"를 뜻하므로 기능이 서는 순간 이전 값이 거짓이 된다.
|- `WP-069` 작성자 소속 팀 채우기 — `author_team_ids`가 비어 있어 `group_by=team`과 `author_team:`이 언제나 빈 결과를 낸다.
|- **`WP-043`은 착수하지 않는다** — `ACC-06`이 NOT RUN이라 활성화 조건이 성립하지 않는다.
|## C. 배포에서 해야 할 것
|**`WP-041`이 더한 항목은 없다** — 신규 마이그레이션이 없다.
|- `clearUnknownSizes` 소급 (CR-056 / DEV-456) — `pnpm es:apply-mappings`
|- 마이그레이션 020 (CR-055), 018·019 (WP-039), 017 (WP-037) — `pnpm run db:migrate`
|- `ADMIN_DATABASE_URL` (WP-039) — 없으면 JOB-AUD-001만 서지 않는다
|- `SEARCH_CURSOR_HMAC_KEY`, `pnpm es:reindex`
|- Prometheus 경보 규칙 적용은 여전히 NOT RUN
|## D. GitHub Actions — pr-search만 결제 차단이다
|`verify`·`integration` 모두 `steps=0`이고 주석이 `The job was not started because recent account payments have failed`를 말한다. **잡이 뜨지도 않은 것이지 시험이 실패한 것이 아니다.**
|- 재시도를 누르지 않는다 — 이 세션이 세 PR에서 같은 상태를 확인했다
|- design-system은 다르다. 그 저장소에서는 CI가 판정 근거다
|- pr-search에서는 로컬 배터리가 유일한 판정이며 그 사실을 원장·PR·인계에 적는다
|## E. 미뤄 둔 항목
|- `DEV-447` open — 조정 스캔의 30초 강제 종료가 증명되지 않았다. `Promise.race`로 감싸 "강제 종료했다"고 보고하지 마라
|- `DEV-433` open — `API-ADM-001`·`API-ADM-003`의 오프셋
|- `DEV-427` open — CI 통합의 link-rebuild 되먹임. CI가 돌지 않아 확인할 길이 없다
|- `DEV-395`·`DEV-399`·`DEV-058`·`DEV-061` — 변화 없음
|## F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
|Gate 4 보안(일부 가능) · Gate 5 성능(불가 — 1,000만 문서 합성 데이터셋 부재) · Gate 6 운영(불가 — 실제 K8s 없음). `ACC-06`은 NOT RUN이고 `W-007`·`WP-043`은 NOT ACTIVATED다.
|## G. 이 세션이 깔아 둔 자리 — 다시 만들지 말 것
|- `packages/db/src/repositories/safe-marker.ts`의 **`FOR SHARE` 에폭 재검증** — 대체 트랜잭션 안에서 다시 읽는다. 라우트의 검사만 믿으면 재채번이 그 사이에 커밋한다 (DEV-471)
|- 같은 파일의 **검사 순서 7 → 8** — 완전 일치가 `expected` 대조보다 **먼저**다. 뒤집으면 정직한 재시도가 자기가 만든 상태로 409를 받는다 (DEV-464)
|- 같은 파일의 **`safeMarkerLockKey`** — 채번 락(`sequenceLockKey`)을 함께 쓰지 마라. 사람이 누르는 요청이 파이프라인을 밀어낸다
|- `apps/web/components/RangesView.tsx`의 **공간 변경 effect** — 비우는 것은 공간이 바뀔 때다. `loadMarker` 안으로 옮기면 **등록 결과를 보여 줄 카드가 사라진다** (DEV-472)
|- `apps/web/components/SafeMarkerCard.tsx`의 **제출 앞 `setResult(null)`** — 표식 변화를 보는 effect로 되돌리지 마라 (DEV-473)
|- `regression/runtime-reachability.test.ts`의 **`expectOrder`** — 순서 단언은 존재 확인을 먼저 한다 (DEV-474)
|- `agent-context/count-unresolved-reviews.py` — **창이 둘이다.** 한 줄 명령으로 되돌아가지 마라 (DEV-468·469)
|---
|# 이전 세션 기록 (보존)
|## (2026-08-30 2차 · WP-040 병합 + 머지 후 리뷰 정정 시점)
|> **최신 기준 (2026-08-30 2차 · WP-040 병합 + 머지 후 리뷰 정정)**
|> main `1df0191` (PR #89·#90·#91·#92 병합) — **이 인계 커밋이 한 번 더 옮긴다. HEAD를 실측하라**
|> **main CI는 판정에 쓰지 못했다** — 아래 D절
|> SRS **`baseline v2.15`** · 원장 `review v6.3` · 작업 패키지 `v2.8`
|> **REL-005 4/4 DONE.** WP-037·038·039·040 전부 done
|> **릴리스는 승인되지 않았다** — Gate 4·5·6은 그대로다 (F절)
|> open DEV **14건** — `DEV-001·006·010·016·026·058·061·304·305·395·399·414·427·433`
|> 다음 빈 ID: **CR-056 · DEV-443 · 마이그레이션 021 · C-072** (측정값. 쓰기 전 다시 잰다)
|> 미해결 리뷰 **31건** (pr-search 22 · design-system 9, `DEV-414`). **PR #89의 넷은 이 세션이 고쳤다**
|**PR #89의 머지 후 리뷰 넷이 전부 실결함이었다** (`DEV-439`~`442`). 병합 직전에는 스레드가 0건이었고 **병합 뒤에 도착했다** — 병합 후 재확인이 그것을 잡았다. 가장 값이 큰 것은 P1이다: 이 세션은 "취소가 완료로 덮이지 않는다"를 유형 다섯 전부에 걸었는데, 그것은 **잡 행의 라벨**에 관한 성질이었고 **일이 실제로 멈추는가**는 묻지 않았다.
|**이 세션이 배운 것 셋.**
|1. **돌리지 않은 계층이 남기는 것은 통과가 아니라 미실행이다.** 이전 세션이 백엔드를 만들고 `test:integration`을 돌리지 않았다. 이 세션은 화면보다 **백엔드 수직 증명을 먼저** 했고, 통합 전량 첫 실행에서 실패한 둘 중 하나(`DEV-437`)는 **기존 시험이 이미 잡아 두었으나 아무도 돌리지 않은** 실결함이었다. 화면을 먼저 세웠다면 그것을 화면 결함으로 오인했을 것이다.
|2. **변이가 살아남았다고 곧바로 시험 구멍이라 부르지 마라.** `M6`(주기 갈래를 `else if`에서 `if`로)은 살아남았지만 **등가**였다 — 둘이 `await`로 순차 실행되어 겹침이 생기지 않는다. 실제 겹침을 만드는 변이(`M6b`: 주기 스윕을 `await`하지 않음)를 따로 걸어 시험이 그것을 잡는지 확인했다. 반대로 `M8`은 진짜 구멍이었다.
|3. **구현이 옳아도 그것을 거는 시험이 없으면 다음 사람이 지운다.** `FR-ING-009` AC-12(브랜치 추가가 채번을 요청한다)는 구현이 정확했는데 **거는 시험이 하나도 없었다.** 인자를 빈 배열로 바꿔도 통합 열아홉이 전부 초록이었다.
|...cut 4 lines
@p bash
@cmd git -C . rev-parse --short HEAD # main HEAD (이 인계 커밋이 옮긴다)
@cmd gh pr list --state open --json number --jq 'length' # 열린 PR
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@todo grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -1 # next-free는 이 값 + 1
@code lang=txt sha=6670f092190f lines=394 kept=80
|미해결 리뷰 계수 전문은 `commands.md`의 「미해결 리뷰를 세는 법 (두 저장소)」에 있다.
|## A. 지금 당장 — DEV-414 미해결 리뷰 31건
|`REL-005`가 끝났으므로 다음은 이 위생 작업이다. **pr-search 22건**(`#65`(5) `#66`(3) `#67`(3) `#68`(1) `#69`(2) `#70`(2) `#72`(1) `#75`(5))과 **design-system 9건**(`#1`(1) `#3`(1) `#6`(1) `#8`(6))을 `HEAD`와 하나씩 대조한다.
|- 이미 정정된 것은 **근거와 함께 답변하고** resolve한다
|- 아직 고쳐지지 않은 지적은 **새 DEV로 등재**한다
|- **일괄 resolve하지 않는다** — 그러면 남은 결함이 기록 없이 사라진다
|계수 명령 전문은 `commands.md`의 「미해결 리뷰를 세는 법」에 있다. 저장소 이름은 `89sooner/pr-search`와 `89sooner/design-system`이다.
|## B. 그 뒤 — REL-006 (WP-041 → WP-042 → WP-044)
|`WP-041`(안전 구간 표식)과 `WP-044`(내보내기)가 감사 액션 `safe_marker.set`·`export.create`를 각자 활성으로 옮긴다. **`WP-040`이 미리 활성화하지 않았다.** `WP-043`은 `ACC-06`이 NOT RUN이라 착수하지 않는다.
|## C. 배포에서 해야 할 것
|- **마이그레이션 `020`** (신규, CR-055) — 등록 검토 요청의 처리 결과 열. `pnpm run db:migrate`
|- `ADMIN_DATABASE_URL` (WP-039) — 없으면 `JOB-AUD-001`만 서지 않는다
|- 마이그레이션 `018`·`019` (WP-039), `017` (WP-037)
|- `pnpm es:apply-mappings` (`changed_lines` 백필), `SEARCH_CURSOR_HMAC_KEY`, `pnpm es:reindex`
|- Prometheus 경보 규칙 적용은 여전히 **NOT RUN**
|- **새 환경 변수는 없다** — `API-ADM-009`의 커서 서명자가 기존 `SEARCH_CURSOR_HMAC_KEY`를 그대로 쓴다
|## D. GitHub Actions가 돌지 않는다 — 사용자 조치가 필요하다
|**CI 잡이 시작조차 하지 않는다.** 주석이 그대로 말한다: `The job was not started because recent account payments have failed or your spending limit needs to be increased.` 재시도 1회도 같은 이유로 끝났고 **단계가 0개**다.
|- 이것은 **코드 실패가 아니다.** `WP-040`은 로컬 전 계층(typecheck·lint·lint:deps·단위 1856·통합 1389·회귀 284·a11y 346·대비 232쌍·e2e 147·빌드)으로 판정했다
|- **실제 시험 실패를 인프라 실패로 부르지 않는다** — 이것은 잡이 뜨지도 않은 경우이며 그 둘은 다르다
|- **다음 세션이 CI를 판정 근거로 쓰려면 결제 문제가 먼저 풀려야 한다.** 그전까지는 로컬 배터리가 유일한 판정이며, 그 사실을 적고 진행한다
|## E. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
|| DEV | 역할 | 왜 아직 | 소관 |
|| --- | --- | --- | --- |
|| 305 | release (`JOB-REL-007`) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
|| 304 | backfill (`JOB-ING-004`) | enrich 파드 역할 구성 판단 | WP-019 |
|## F. 릴리스 게이트 4·5·6 — 릴리스는 승인되지 않았다
|`REL-005` 4/4는 **작업 패키지가 끝났다는 뜻이지 배포해도 된다는 뜻이 아니다.**
|- `Gate 4` 보안 (일부 가능)
|- `Gate 5` 성능 (**불가** — 1,000만 문서 합성 데이터셋 부재)
|- `Gate 6` 운영 (**불가** — 실제 K8s 없음)
|- `ACC-06`은 NOT RUN이고 `W-007`·`WP-043`은 NOT ACTIVATED다
|## G. 미뤄 둔 항목
|- `DEV-433` open — `API-ADM-001`·`API-ADM-003` 목록의 오프셋이 공통 원칙 7·ADR-010과 어긋난다. `CR-055`가 걷어 내지 않고 등재만 했다 — 두 API의 소비자를 전부 다시 세는 별도 작업이다. 신설한 `API-ADM-009`에는 처음부터 오프셋이 없다
|- `DEV-427` open — CI 통합의 `link-rebuild` 되먹임 시험. **이번 세션의 통합 전량 두 번에서 재현되지 않았으나 그것을 해소로 적지 않는다.** 그리고 CI가 돌지 않아 CI 쪽 재현은 확인할 길이 없었다
|- `DEV-395` 자기 리뷰 소급 · `DEV-399` 원장 5장 파이프 이스케이프 회귀 · `DEV-058` `test:perf`(Gate 5) · `DEV-061` `direct_push` — 변화 없음
|- 문서 검증기 `--strict` 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다. **판정은 언제나 main 대비 diff**이며 이번 세션의 새 경고는 0건이었다
|## H. WP-040이 깔아 둔 자리 — 다시 만들지 말 것
|- `apps/web/lib/{ops-jobs,ops-repositories,ops-pipeline}.ts` — **화면 판정의 전부.** 컴포넌트 안에 판정을 흩뿌리지 않는다
|- `apps/web/components/JobStatusBadge.tsx` — 잡 상태 여섯을 Conductor `Status` 일곱으로 옮기는 유일한 자리. `A-002`의 채번 안내도 이것을 쓴다
|- **파괴적 조작의 방어선은 핸들러 안의 조건이지 `disabled`가 아니다.** 네 곳(재채번·해제·요청 종료·일괄 재처리)이 같은 모양이며 e2e가 네트워크 호출 수로 센다 — 버튼 비활성 스냅숏으로 바꾸지 마라
|- `page-guard.tsx`의 `GuardedPageContext` — 관문이 역할과 인증 구성 여부를 본문에 넘긴다. **라우트가 세션을 직접 읽지 않는다**는 규율은 그대로이며 `architecture.test.ts`가 센다
|- `resolveJobTarget`의 **두 갈래가 모두** 슬러그 형식을 검사한다 (DEV-437). 회귀가 그 대칭을 센다
|- `apps/search-api/integration/admin/repositories.test.ts`의 채번 예약 시험 셋 — **차분으로 센다**(등록이 이미 예약한 것과 이 조작이 더한 것을 가른다). 절대값으로 바꾸면 변이 M8이 되살아난다
|- `apps/web/a11y/setup.ts`의 `ResizeObserver` 대역 — Radix `Switch`가 요구한다. **크기를 재는 동작을 흉내 내지 않는다**
|---
|---
|## (2026-08-30 · CR-055 병합 · WP-040 구현 중 시점)
|> **최신 기준 (2026-08-30 · CR-055 병합 · WP-040 구현 중)**
|> main `5475249` (PR #88 병합) · main CI `verify`·`integration` 통과
|> **구현 브랜치 `claude/wp-040-operations-console`이 로컬에만 있다** — 커밋 셋(`d877b1b`·`1c41e11`·`35b9ff0`), **push 안 됨, PR 없음**
|> SRS **`baseline v2.15`** · 원장 `review v6.1` · 작업 패키지 `v2.8`
|> **REL-005 3/4.** WP-040은 **진행 중**이며 완료가 아니다 — 백엔드와 판정 계층은 섰고 화면 셋이 남았다
|> open DEV **15건** — 기존 13 + `DEV-433`·`DEV-436`
|> 다음 빈 ID: **CR-056 · DEV-437 · 마이그레이션 021 · C-072** (측정값. 쓰기 전 다시 잰다)
|> 미해결 리뷰 **33건** (pr-search 24 · design-system 9, `DEV-414`). 실측한 수이며 잘림 없음
|...cut 314 lines
@p bash grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
@path ls packages/db/migrations/*.up.sql | tail -1
@code lang=txt sha=616b57c896dd lines=71 kept=71
|**착수 전에 실측한 선행 자산 (이 세션에서 확인했다)**
|| 있는 것 | 없는 것 |
|| --- | --- |
|| `apps/ingest-gateway/src/archive.ts` — NDJSON 아카이브 기록부 (WP-004 산출물) | `packages/es/src/`에 `prs-raw-events-*` 인덱스 정의 |
|| 문서 언급 — ADR·시스템 아키텍처·인프라 운영·데이터 모델·관측 다섯 | Filebeat 배포 manifest |
|| `deploy/k8s/README.md`의 적용 순서 표 | ES의 아카이브 인덱스 (현재 `prs-{commits,pull-requests}-v2`·`{links,releases}-v1` 넷뿐) |
|**감사에서 물을 것.** `archive.ts`가 실제로 기록하는 NDJSON 형식과 오프셋 처리가 Filebeat 설정의
|전제를 만족하는가. ILM 보존 기간이 어디에 정해져 있는가. `operator`·`security_officer` 한정
|조회를 **어느 API가 제공하는가** — 없으면 그것이 계약 공백이다.
|**새 역할을 만들면 셋이 함께 간다**: ① 코드 갈래 ② manifest ③ 인프라 3장 배포 단위 표.
|표에 먼저 올리는 길은 시험이 막고(DEV-312), 표에 중복 행을 만들지 않으며(DEV-321), 표 아래
|산문도 함께 고친다(DEV-322).
|### CR-051이 깔아 둔 자리 — 다시 만들지 말 것
|- `packages/query/src/sequence-binding.ts` — **`seq:`가 어느 공간을 뜻하는지 판정하는 유일한 자리다.**
|  순수 함수이며 DB도 ES도 모른다(브라우저가 같은 코드를 쓴다). 라우트마다 다시 만들면 규칙이
|  두 곳에 살고, 한쪽만 넓어지는 날 아무 오류도 안 난다
|- `packages/es/src/query-builder.ts`의 `buildQuery`는 `seq:` 범위가 있는데 에폭이 없으면 **던진다**.
|  선택 인자로 되돌리지 않는다 — 빠지면 오류 없이 **모든 세대를 함께** 돌려주고, 그 실패는
|  결과를 재는 시험에 잡히지 않는다 (fail-open이라 DEV-353보다 나쁘다)
|- `apps/search-api/src/search/sequence-context.ts` — 검색의 **4-1단계**. 접근 범위 산출 **뒤**에 온다
|  (공간 해석이 접근 통제를 지나야 하고, 미등록과 범위 밖이 같은 404여야 한다). 새 해석 경로를
|  만들지 않고 `resolveSpace`를 재사용한다
|- `apps/search-api/src/saved-search/sequence-reference.ts` — 목록 판정은 **페이지 단위 일괄**이다.
|  `toResource()` 안에서 DB를 부르지 않는다. `needsSequenceReference`로 **인용이 있을 때만** 접근
|  범위를 산출한다 — 모든 조회에 그 의존을 지우면 저장된 검색이 접근 통제 경로에 참여한다
|  (CR-049가 `/run`에서 피한 자리이며, 전 계층 통합이 외래 키 연쇄로 실제로 잡았다)
|- `readEpochParam`은 세 상태(`absent`/`value`/`invalid`)를 구분한다. **`absent`는 파라미터가 정말로
|  없을 때뿐이다** — 빈 값·비문자열은 `invalid`다. 옛 `parseEpochParam`은 지웠다
|- `apps/web/lib/query-url.ts`의 `QueryState.seqEpoch`은 **문자열 원문**이다. 화면이 형식을 판정하지
|  않는다 — 접으면 서버가 거절할 기회를 잃는다
|- 커서 계열 넷(W-001 PIT·W-004 정본 서수·W-008·W-009 키셋)은 **여전히 합치지 않는다**
|## B. 배포에서 해야 할 것
|1. **마이그레이션 017을 적용한다** — `pnpm run db:migrate`
|2. WP-032가 남긴 둘은 변화 없다: `SEARCH_CURSOR_HMAC_KEY`(32자 이상, 복제본 공통)와
|   `pnpm es:reindex --alias prs-pull-requests`·`--alias prs-commits`
|## C. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
|| DEV | 역할 | 왜 아직 | 소관 |
|| --- | --- | --- | --- |
|| 305 | `release` (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
|| 304 | `backfill` (JOB-ING-004) | `enrich` 파드 역할 구성 판단 | WP-019 |
|## D. 릴리스 게이트 4·5·6 (변화 없음)
|Gate 4 보안(일부 가능) · Gate 5 성능(**불가** — 합성 데이터셋·`test:perf` 없음) ·
|Gate 6 운영(**불가** — 실제 K8s 없음).
|## E. 미뤄 둔 항목
|- **`DEV-364` 스칼라 `seq:1234`** — 파서를 통과하지만 `query-builder`의 `TERM_FIELDS`에 `seq`가 없어
|  `MATCH_NONE`이 되고 **조용히 0건**이 된다. 고치려면 제품 결정이 필요하다: 파서가 문법 오류로
|  거절할 것인가, `merge_seq` 단일값 조회로 승인할 것인가. **DEV-349와 같은 축이 아니다** —
|  스칼라는 언제나 0건이라 "다른 커밋을 가리키는" 실패가 아니다
|- `flow-001`·`flow-003` e2e 간헐 실패 — WP-016 소관. **재시도 통과를 해소로 적지 않는다**
|- 문서 검증기 `--strict` 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다
|- PIT의 클러스터 자원 비용 · 패싯·커서의 운영 규모 성능 (DEV-058) — NOT RUN
|- `prs-links` 재색인 커버리지 판정
|- **저장된 검색 목록의 시퀀스 판정 비용** — 항목 수에 비례하지 않는다는 것은 통합이 질의 수를
|  세어 걸었으나, 그 비용을 운영 규모로 재지는 않았다
|## F. 확인할 사항
|- **open DEV는 8건이다.** 세는 법:
@p bash
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@code lang=txt sha=50cd36ed209f lines=38 kept=38
|  주의: `**open**`처럼 굵게 쓰면 이 grep에 안 잡힌다. **새 행은 `| open ...` 형식으로 쓴다** —
|  이번 세션에서도 DEV-364를 굵게 써서 한 번 놓쳤고 실측으로 잡았다
|- **호출부를 grep으로 훑을 때 glob을 믿지 마라.** `apps/search-api/src/**/*.ts`는 bash의 globstar가
|  꺼져 있으면 **한 단계만** 매칭한다. 이번에 `sequence/range.ts`가 목록에서 빠져 그 경로가
|  500이 됐고 리뷰가 잡았다. `grep -rn '패턴' apps packages`처럼 **디렉터리를 통째로** 준다
|- 남은 오픈 결정은 **OD-008** 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
|- **문서 본문을 고치면 같은 편집에서 상태 헤더를 함께 만진다** (DEV-323)
|- `agent-context/`는 main에 tracked다. **`git add -A`·`git add .`는 쓰지 않는다**
|- **CI 확인은 커밋 SHA의 check-runs로 한다** — `gh pr checks`는 옛 실행 결과를 보여 줄 수 있다
|- **전사는 `exports/`에 둔다.** `/export`에 상대 경로를 주면 저장소 루트에 떨어진다
|---
|# 이전 세션 기록 (보존)
|> **A절은 CR-051 계약과 구현으로 해소됐다** — PR #63(`7a2d0e2`)·PR #64(`2e6194c`)가
|> 들어왔고 **DEV-349가 닫혔다**. REL-004 수는 그대로 7/8이다 — DEV-349는 기존 WP의
|> 교차 결함 종결이지 새 작업 패키지가 아니다. 아래는 그때의 판단 근거를 남긴 것이다.
|## (2026-08-28 CR-050 · WP-034 종료 시점의 기록)
|> **최신 기준 (2026-08-28 CR-050 · WP-034 종료 시점)**
|> main `ea08c57` (문서 병합 `1f7ccf2`, 구현 병합 `ea08c57`) · main CI `verify`·`integration` 통과
|> SRS **`baseline v2.10`** · PRD `v1.5` · 용어집 `v0.3` · 원장 `review v4.6` · 작업 패키지 `v1.8` ·
|> API 계약 `v0.10` · 데이터 모델 `v0.7` · 보안 `v0.6` · 와이어프레임 `v0.6` · 상태 매트릭스 `v0.7` ·
|> 컴포넌트 `v0.6` · QA 체크리스트 `v0.7` · 추적 매트릭스 `v0.7`
|> **CR-050 · DEV-358까지** · **마이그레이션 016까지**(WP-034가 만들었다)
|> **REL-004 구현 7/8** (WP-029·030·031·035·032·033·**034** done). 남은 것은 **WP-036 하나**
|> **REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.**
|> 미해결 리뷰 **0건** (PR #61·#62 전부 resolve). 열린 PR 0건. 로컬 브랜치는 `main` 하나. 미추적 0건
|> **open DEV 8건** — 기존 5(`DEV-001·006·010·016·026`) + `DEV-304·305` + `DEV-349`
|> (CR-050이 등록한 DEV-350~358 아홉은 **전부 resolved**)
|## A. 지금 당장 — DEV-349 좁은 계약 종결 (WP-036보다 먼저)
|**이것은 구현이 아니라 계약을 여는 일이다.** FR-SEQ-005 AC-4가 저장된 검색의 `seq:` 조건을
|명시적으로 지목하는데 그것을 성립시킬 재료가 **어디에도 없다** — 지시서 §0의 "정본이 실제로
|없음"에 해당하므로 별도 CR이 옳다. next-free는 실측할 것(`CR-051` / `DEV-359` 예상):
@p bash grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
@path ls packages/db/migrations/*.up.sql | tail -1
@code lang=txt sha=d53f469fc9ce lines=100 kept=80
|**정해야 할 것 셋.** (1) 어느 시퀀스 공간의 에폭을 기록하는가 — 질의에 `repo:`가 없으면 접근
|범위의 모든 저장소가 대상이고, 여럿이면 공간도 여럿이다. (2) 공간이 여럿일 때 하나만 낡아도
|전체를 무효로 보는가. (3) `/search` 응답이 `epoch_stale`을 어떻게 싣는가 — 지금 그 필드는
|W-004 계열에만 있고 검색 응답 계약에 없다.
|**해법이 저장 계층이 아닐 수 있다.** W-001의 `seq:` 질의는 URL에 담기는 순간 이미 같은 성질을
|갖고, 저장된 검색은 그것을 더 오래 보관할 뿐이다 — **조회 계층에서 푸는 길**을 먼저 검토한다.
|그 판단이 서기 전에는 열 하나로 임의 해법을 만들지 않는다.
|### WP-034가 깔아 둔 자리 — 다시 만들지 말 것
|- `apps/search-api/src/repositories/overview.ts` — **접근 범위 판정이 SQL이 아니라 여기 있다.**
|  규칙을 SQL에 복제하면 두 곳이 갈라지고 한쪽만 넓어지는 날 아무 오류도 안 난다. 대신 한 요청이
|  정본을 다 훑지 못할 수 있으므로 **스캔 상한에서 커서를 반드시 남긴다**(DEV-357). 빈 페이지 +
|  유효한 커서는 정상 상태다
|- `isRepositoryInScope`의 `allowedTeamIds`는 **필수 인자다**(DEV-353). 선택으로 되돌리지 않는다 —
|  빠지면 오류 없이 조용히 좁게 답하고, 그 실패는 다섯 WP 동안 아무도 못 봤다
|- ES 문서 수 집계는 PG가 이미 걸렀어도 **`applyMandatoryScopeFilter`를 다시 지난다.** 생략을 한 번
|  허용하면 그 자리가 곧 우회 경로가 된다
|- `apps/search-api/src/repositories/cursor.ts` — PostgreSQL 키셋. 지문에 접근 범위 **해시**를 담고
|  원본 식별자는 싣지 않는다 (봉투는 서명될 뿐 암호화되지 않는다)
|- `repository_registration_request`에 **승인·반려 열을 더하지 않는다** — 그 수명주기는 A-002를
|  세우는 WP-040이 검증한다. 지금 만들면 `saved_search.team_id`가 겪은 "뜻이 정해지지 않은 열"이 된다
|- `apps/web/lib/repository-overview.ts` — 화면 판정은 전부 여기 있다. **`null`·`0`·`unavailable`
|  셋을 구분**하는 것이 이 화면의 전부다
|- 웹은 `/api/repositories`를 부른다. `/api/v1`을 적으면 프록시가 `/api/v1/v1/...`을 만든다
|## B. 그 다음 — WP-036 원본 아카이브 레인 (REL-004 마지막)
|WP-036이 들어오면 REL-004 구현이 **8/8**이 된다. 다만 **패키지 8/8과 릴리스 승인은 다른 것이다** —
|Gate 4·5·6은 별개이며 REL-003조차 아직 미통과다.
|---
|## (그 이전 기록)
|> **A절은 WP-034 구현으로 해소됐다** — PR #61(계약)·#62(구현)가 들어왔고 REL-004가 7/8이 됐다.
|> 아래는 그때의 판단 근거를 남긴 것이다.
|### WP-033이 깔아 둔 자리 — 다시 만들지 말 것
|- `packages/db/src/repositories/saved-search.ts` — **접근 규칙이 질의 안에 있다.** 라우트가
|  "소유자인가"를 판정해 넘기는 구조로 만들지 않는다. 새 조회 경로도 `visibleTo()`를 지난다
|- **두 개의 잠금이 이 자원의 무게중심이다** — 상한은 소유자 행 `FOR UPDATE`, 구성원 자격은
|  `team_member` 행 `FOR SHARE`. 지우면 각각 101건과 이탈자 공유가 된다. **회귀가 건다**
|- `apps/search-api/src/saved-search/cursor.ts` — PostgreSQL 키셋. **PIT도 `search_after`도 없다.**
|  지문에 팀 소속이 들어가고 **접근 범위 버전은 들어가지 않는다**(무관한 권한 변경이 순회를 끊는다)
|- `SavedSearchRow.created_at`은 **ISO 마이크로초 문자열**이다. `Date`로 바꾸지 않는다 —
|  밀리초로 잘리면 키셋이 같은 밀리초의 행을 건너뛴다
|- `apps/web/components/SaveSearchDialog.tsx`는 **`create`/`edit` 두 모드**다. 편집 전용 대화상자를
|  새로 만들지 않는다 — 받는 것이 같으므로 갈라 두면 한쪽에만 고쳐지는 규칙이 생긴다
|- `apps/web/lib/saved-search.ts` — 화면 판정은 전부 여기 있다. 컴포넌트에서 판정하지 않는다
|- **웹은 `/api/saved-searches`를 부른다.** `/api/v1`을 적으면 프록시가 `/api/v1/v1/...`을 만든다
|## B. 남은 큰 것 — DEV-349 (별도 CR)
|**FR-SEQ-005 AC-4가 저장된 검색을 명시적으로 지목하는데 구현되지 않았다.** 원장 4장이
|"`saved_search`가 없어 도달 불가"라고 예고해 둔 자리이고, WP-033이 표를 만들면서 도달
|가능해졌다. 재채번으로 에폭이 오르면 저장된 `seq:1280..1342`가 **조용히 다른 커밋을 가리킨다**.
|고치려면 계약의 재료가 필요하고 그것이 없다:
|1. 어느 시퀀스 공간의 에폭을 기록하는가 (질의에 `repo:`가 없으면 접근 범위 전체가 대상)
|2. 여럿이면 하나만 낡아도 전체를 무효로 보는가
|3. `/search` 응답이 `epoch_stale`을 어떻게 싣는가 (지금 그 필드는 W-004 계열에만 있다)
|**이 문제는 저장된 검색이 만든 것이 아니다** — W-001의 `seq:` 질의는 URL에 담는 순간 같은
|성질을 갖는다. 해법이 **조회 계층**일 수 있으므로 그 판단이 먼저다. 원장 7장에 등재돼 있다.
|## C. 배포에서 해야 할 것
|- **마이그레이션 015를 적용한다.** `pnpm run db:migrate`
|- WP-032가 남긴 것 (변화 없음): `SEARCH_CURSOR_HMAC_KEY`(32자 이상, 복제본 공통)와
|  `pnpm es:reindex --alias prs-pull-requests`·`--alias prs-commits`
|- **WP-033은 새 배포 전제를 만들지 않았다** — 커서 서명 키를 검색·구간과 같은 값으로 쓴다
|## D. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
|| DEV | 역할 | 왜 아직 | 소관 |
|| --- | --- | --- | --- |
|| 305 | release (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
|...cut 20 lines
@p bash
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@code lang=txt sha=49b6155977d5 lines=31 kept=31
|  주의: `**open**`처럼 굵게 쓰면 이 grep에 안 잡힌다. **새 행은 `| open ...` 형식으로 쓴다** —
|  이번 세션에서 그 함정을 그대로 밟았고 실측으로 잡았다
|- 남은 오픈 결정은 **OD-008** 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
|- **문서 본문을 고치면 같은 편집에서 상태 헤더를 함께 만진다** (DEV-323)
|- `agent-context/`는 main에 tracked다. **`git add -A`·`git add .`는 쓰지 않는다**
|- **CI 확인은 커밋 SHA의 check-runs로 한다** — `gh pr checks`는 옛 실행 결과를 보여 줄 수 있다
|- **전사는 `exports/`에 둔다.** `/export`에 상대 경로를 주면 저장소 루트에 떨어진다
|---
|# 이전 세션 기록 (보존)
|> **A절은 WP-033 구현으로 해소됐다** — PR #60(`700d825`)이 본체를 넣었고 REL-004가 6/8이 됐다.
|> 아래는 그때의 판단 근거를 남긴 것이다.
|> **최신 기준 (2026-08-27 WP-032 종료 시점)**
|> main `2d28074` (WP-032 병합은 `3237b6c`, 그 뒤 인계 기록 커밋 하나) · main CI `verify`·`integration` 통과
|> SRS **`baseline v2.8`**(변경 없음) · PRD `v1.3` · 원장 `review v4.1` · 작업 패키지 `v1.4` ·
|> 로드맵 `v0.6` · 인프라 `v0.5` · API 계약 `v0.8` · 추적 매트릭스 `v0.5`
|> **CR-048(변화 없음) · DEV-332까지** · **마이그레이션 014까지**(WP-032는 만들지 않았다)
|> **REL-004 구현 5/8** (WP-029·030·031·035·**032** done)
|> **REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.**
|> 미해결 리뷰 **0건** (PR #55·#56·#57 전부 resolve). 열린 PR 0건. 로컬 브랜치는 `main` 하나. 미추적 0건
|> **open DEV 7건** — 기존 5(`DEV-001·006·010·016·026`) + `DEV-304·305`
|## A. 지금 당장 — WP-033 저장된 검색
|**착수 전 감사를 다시 할지부터 정한다.** WP-032는 계약이 CR-043·044에서 이미 닫혀 있어 감사 없이 구현했고 그것이 옳았다. WP-033은 계약이 닫힌 적이 없다 — `saved_search` 표도 API도 SRS 밖에서 검증된 적이 없으므로 **착수 전 감사가 필요할 가능성이 높다.**
|next-free는 **실측할 것** — `CR-049` / `DEV-333` / 마이그레이션 `015`가 예상이지만 추측해 쓰지 않는다:
@p bash grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
@path ls packages/db/migrations/*.up.sql | tail -1
@code lang=txt sha=8e271c6cb17f lines=55 kept=55
|**WP-032가 깔아 둔 자리 — 다시 만들지 말 것**
|- `apps/search-api/src/cursor/envelope.ts` — HMAC 봉인·두 오류 타입. **순회의 뜻은 여기 없다.** 새 커서 계열이 생기면 봉인만 물려받고 순회는 자기 모듈에 둔다
|- `apps/search-api/src/search/cursor.ts`(PIT+search_after) · `sequence/range-cursor.ts`(정본 서수) — **합치지 않는다.** 합치면 W-004 멤버십이 ES로 넘어가 DEV-130이 되돌아온다
|- `apps/search-api/src/search/facets.ts` — `computeFacets`는 `ScopedQuery`만 받는다. 새 축은 `FacetAxis`에 더한다. **표시값 해석이 필요한 축은 `display`를 붙이고 일괄 해석**한다(N+1 금지)
|- `packages/es/src/highlight.ts` — 표식은 사설 사용 영역 문자다. **응답에 마크업을 내보내지 않는다**
|- `packages/es/src/sort.ts`의 `MISSING_SENTINEL` — `_last`로 되돌리지 않는다 (DEV-329). 새 정렬 키를 더하면 그 타입의 센티널이 있어야 한다
|- `packages/es/src/indices.ts` — **PR·커밋은 `-v2`다.** 매핑을 또 바꾸면 버전을 올리고 재색인으로 배포한다. `applyMappings`는 별칭이 옛 버전을 들고 있으면 물러난다
|## B. 배포에서 해야 할 것 (WP-032가 만든 전제)
|1. `SEARCH_CURSOR_HMAC_KEY`(32자 이상, **복제본 공통**)를 시크릿에 넣는다. 없으면 `search-api`가 운영에서 기동하지 않는다
|2. `pnpm es:reindex --alias prs-pull-requests` · `--alias prs-commits`로 v2에 올린다. **부트스트랩으로 먼저 해결하려 하지 않는다** — 빈 인덱스를 만들거나 별칭을 둘로 건다
|3. 그 전까지 전문 검색은 과거 데이터에 **조용히 적게** 답한다
|## C. 별도 CR 둘 — 남은 미배포 역할 (변화 없음)
|| DEV | 역할 | 왜 아직 | 소관 |
|| --- | --- | --- | --- |
|| 305 | `release` (JOB-REL-007) | 미러 PVC 배치 필요 (DEV-143) | WP-024 |
|| 304 | `backfill` (JOB-ING-004) | `enrich` 파드 역할 구성 판단 | WP-019 |
|## D. 릴리스 게이트 4·5·6 (변화 없음)
|Gate 4 보안(일부 가능) · Gate 5 성능(**불가** — 합성 데이터셋·`test:perf` 없음) · Gate 6 운영(**불가** — 실제 K8s 없음).
|## E. 미뤄 둔 항목
|- `flow-001`·`flow-003` e2e 간헐 실패 — WP-016 소관. **재시도 통과를 해소로 적지 않는다**
|- 문서 검증기 `--strict` 자기참조 오탐 — 문서를 고쳐 게이트를 통과시키지 않는다
|- **PIT의 클러스터 자원 비용** — 조회마다 PIT을 여는 대가를 운영 규모로 재지 않았다 (NOT RUN)
|- **패싯·커서의 운영 규모 성능** — 예산 1.5초가 어디서 걸리는지 (DEV-058)
|- `prs-links` 재색인 커버리지 판정
|---
|> **최신 기준 (2026-08-27 WP-035 · CR-048 종료 시점)**
|> main `df9f569` · SRS **`baseline v2.8`**(변경 없음) · PRD `v1.3` · 원장 `review v3.7` · 작업 패키지 `v1.2` ·
|> 인프라 `v0.5` · API 계약 `v0.8` · 로드맵 `v0.5` · 추적 매트릭스 `v0.5`
|> **CR-048 · DEV-323까지** · **마이그레이션 014까지**(이 세션은 만들지 않았다)
|> **REL-004 구현 4/8** (WP-029·030·031·**035** done). **WP-032 차단 해제**
|> **REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.**
|> 미해결 리뷰 **0건** (PR #52·#53·#54 전부 resolve). 열린 PR 0건. 로컬 브랜치는 `main` 하나
|> **open DEV 7건** — 기존 5(`DEV-001·006·010·016·026`) + `DEV-304·305` (**DEV-306은 CR-048로 resolved**)
|> ⚠️ **미추적 1건** — `202608271346.md`(전사, 99KB)가 **저장소 루트**에 있다. 아래 E절 참조
|## A. 지금 당장 — WP-032 (패싯 · 커서 페이지네이션 · 전문 검색)
|**착수 전 감사를 다시 하지 않는다.** WP-032의 계약은 CR-043(SRS v2.7)·CR-044(SRS v2.8)에서 이미 닫혔다 — 커서 계약(재료·봉인 값·불안정성 처분·동률), 패싯 계약(bucket 단위 검증·별도 요청 1.5초·`facets_status` 넷), 전문 검색 계약(강조 평문·커밋 축 first-parent·`query_too_short` 코드 포인트), ADR-010 Amendment(**모든 순회에 PIT**)가 전부 서 있다.
|**WP-035가 차단을 풀었다.** `edge_ngram`은 `index.analysis`의 비동적 설정이라 열린 인덱스에 못 넣고 `putMapping`으로 더한 서브필드는 기존 문서에서 비어 있는데, 이제 그것을 배포하는 기계가 있다. 시험용 v2로 실제 증명까지 했다(통합 `WP-032 선행 조건 증명`).
|**CR은 지시서 §0 기준으로만 연다** — 승인된 계약으로 구현이 불가능 / 정본이 실제로 없음 / 새 제품 결정 필요, 셋 중 하나가 **실물로 증명**될 때만. 단순 구현·리팩터·파일 배치·시험 편의로는 열지 않는다.
|next-free는 **실측할 것** — `CR-049` / `DEV-324` / 마이그레이션 `015`가 예상이지만 추측해 쓰지 않는다:
@p bash grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'API-SRCH-[0-9]{3}' docs/ | sort -u # 새 API ID는 충돌부터
@path ls packages/db/migrations/*.up.sql | tail -1
@code lang=txt sha=5e3a8a7f3160 lines=39 kept=39
|**WP-035가 깔아 둔 자리 — 다시 만들지 말 것**
|- `packages/es/src/versioned-index.ts` — 버전 인덱스 생성·원자 전환. **WP-032의 실제 `edge_ngram` 필드는 여기 없다** — 통합 시험이 `wp035_probe`라는 임시 이름으로 능력만 증명했고 이름을 선점하지 않았다. WP-032가 `ENTITY_INDICES`의 매핑을 바꾸면 이 기계가 그것을 배포한다
|- `packages/es/src/write-targets.ts` — 새 쓰기 경로를 만들면 **`WriteTargets`를 인자로 받아야** 하고 `packages/es/src/dual-write.test.ts`의 `DUAL_WRITE_PATHS`(현재 17)에 등재해야 한다. 등재하지 않으면 아키텍처 시험이 잡는다
|- `packages/db/src/reindex-fence.ts` — 운영 쓰기는 전부 `withReindexWrite`를 지난다. 새 호출부는 `DUAL_WRITE_CALLERS`에도 올린다
|- `apps/pipeline-worker/src/documents.ts` — `registryOwnedFields`(레지스트리 소유 필드)와 `buildProjectedCommitDocument`(PR 유래 커밋 문서). **투영과 재구축이 같은 함수를 쓴다** — 사본을 만들지 않는다
|- `apps/search-api/src/ops/jobs.ts` — `OPERATOR_JOB_TYPES`("러너가 있다")와 `CREATABLE_GENERIC_JOB_TYPES`("API-ADM-002로 만들 수 있다")가 갈려 있다. 새 잡 유형은 **어느 쪽인지 정해서** 넣는다
|## B. 별도 CR 둘 — 남은 미배포 역할
|`authz`는 CR-048로 닫혔다. 둘 남았고 회귀 예외 목록(`regression/runtime-reachability.test.ts`의 `UNDEPLOYED_ROLE_ALLOWLIST`)에 사유·DEV와 함께 있다.
|| DEV | 역할 | 영향 | 왜 아직 안 고쳤나 |
|| --- | --- | --- | --- |
|| 305 | `release` (JOB-REL-007) | 릴리스 수집이 안 돌아 `prs-releases`가 비고 W-005·API-REL-002·005가 빈 상태 — WP-024·WP-026이 만든 기능 전부 | 이 역할은 **미러 볼륨을 요구한다**(DEV-143). PVC 배치가 함께 정해져야 해 다른 manifest 복사로 못 만든다. WP-024 소관 |
|| 304 | `backfill` (JOB-ING-004) | 백필 잡을 만들면 아무도 집지 않고 `job_active_uk`가 이후 요청을 막는다 — DEV-178과 같은 모양 | `enrich` 파드의 역할 구성을 바꾸는 일이라 그 워커의 확장 축(실시간 유량) 판단이 필요하다. WP-019 소관 |
|**정상 경로 (CR-048이 처음으로 밟았다)**: manifest를 만들고 → 예외 목록에서 지우고 → 인프라 3장 배포 단위 표에 올린다. 표에 먼저 올리는 길은 시험이 막는다(DEV-312). 그리고 **표에 중복 행을 만들지 않는다**(DEV-321) — 검사가 생겼다.
|## C. 릴리스 게이트 4·5·6 (REL-003 미통과 사유 — 변화 없음)
|| 게이트 | 남은 일 | 이 환경에서 가능? |
|| --- | --- | --- |
|| Gate 4 보안 | 권한 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능 |
|| Gate 5 성능 | 성능 목표 7종 (DEV-058) | **불가** — 합성 데이터셋도 `test:perf`도 없다 |
|| Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | **불가** — 실제 Kubernetes 없음 |
|## D. 미뤄 둔 항목
|- **`flow-001`·`flow-003` e2e 간헐 실패** — WP-016 소관, 원장 §7. CI에서도 재현된다는 것이 확인돼 있다(PR #49). **재시도 통과를 해소로 적지 않는다**
|- 문서 검증기 `--strict`의 자기참조 오탐 — 원장 §7 기술 부채. **문서를 고쳐 게이트를 통과시키지 않는다**
|- 백필(JOB-ING-004)의 `updated` 정렬 열거, JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
|- **`prs-links` 재색인의 커버리지 판정** — 지금은 하지 않는다(`expectedDocuments = null`). 재구축 포트가 처리한 source 수만 돌려주기 때문이며, 나머지 검증 6항으로만 건다. 간선 문서 수를 셀 방법이 생기면 그때 판정한다
|- **실제 운영 규모 재색인** — NOT RUN. 성능 harness가 서면 함께 잰다
|## E. 확인할 사항
|- ⚠️ **전사 파일이 `.gitignore` 밖에 있다.** `pr-search/202608271346.md`(99KB). `/export`에 상대 경로를 줘서 저장소 루트에 떨어졌고, `.gitignore` 24행은 `exports/`만 덮는다 — `git check-ignore`로 실측했다. **`git add -A`를 쓰면 커밋에 딸려 간다.** `exports/`로 옮기거나 지운다
|- **open DEV는 7건이다.** 세는 법:
@p bash
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@code lang=txt sha=9736402295e6 lines=29 kept=29
|  주의: `**open**`처럼 굵게 쓰면 이 grep에 안 잡힌다. **새 행은 `| open ...` 형식으로 쓴다**
|- 남은 오픈 결정은 **OD-008** 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
|- **새 역할을 추가하면 ① 코드 갈래 ② manifest ③ 인프라 3장 표 셋이 함께** 가야 한다. 그리고 표에 **중복 행**을 만들지 않고, 표 아래 **산문도 함께** 고친다 (DEV-321·322)
|- **문서 본문을 고치면 같은 편집 안에서 상태 헤더(`버전`·`갱신일`)를 함께 만진다** (DEV-323)
|- `agent-context/`는 main에 tracked다. `.gitignore`에 넣지 않는다. **`git add -A`·`git add .`는 쓰지 않는다** — 경로를 지목해 stage 한다
|- **CI 확인은 커밋 SHA의 check-runs로 한다** — `gh pr checks`는 옛 실행 결과를 그대로 보여 줄 수 있다
|---
|# 이전 세션 기록 (보존)
|> **A절은 WP-035 구현으로 해소됐다** — PR #52(`efea3c0`)가 본체를 넣었고 REL-004가 4/8이 됐다.
|> B절의 `authz`도 CR-048(PR #53, `f4e8ab6`)로 닫혔다.
|> 아래는 그때의 판단 근거를 남긴 것이다.
|## (2026-08-26 CR-043~047 종료 시점의 기록)
|> **최신 기준 (2026-08-26 CR-043~047 종료 시점)**
|> main `99e2532` · SRS **`baseline v2.8`** · PRD `v1.3` · 원장 `review v3.4` · 작업 패키지 `v1.1` · 추적 매트릭스 `v0.5`
|> CR-047 · DEV-312까지 · **마이그레이션 014까지**(이 세션은 만들지 않았다). **REL-004 구현 3/8** (WP-029·030·031 done)
|> **REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.**
|> 미해결 리뷰 **0건** (PR #1~**#51** 전수 실측). 열린 PR 0건. 로컬 브랜치는 `main` 하나. 미추적 0건
|> **open DEV 8건** — 기존 5(`DEV-001·006·010·016·026`) + **신규 3(`DEV-304·305·306` 미배포 역할)**
|## A. 지금 당장 — WP-035 본체 구현
|**감사와 계약은 끝났다.** 원장 6.38장과 비동기 계약 3.5장에 계약이 서 있고, 러너가 살 자리(`deploy/k8s/pipeline-worker-batch.yaml`)도 생겼다. **CR을 다시 열 필요가 없다** — CR-045·046·047이 이미 닫혔고 구현이 그 계약을 따르면 된다.
|next-free는 실측할 것 — `CR-048` / `DEV-313` / 마이그레이션 `015`가 예상이지만 추측해 쓰지 않는다:
@p bash grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2
@path ls packages/db/migrations/*.up.sql | tail -1
@code lang=txt sha=65a3c5291bf5 lines=50 kept=50
|**남은 구현 (WP-035 계약 순서 그대로)**
|| # | 항목 | 자리 |
|| --- | --- | --- |
|| 1 | **버전 인덱스 생성 · 원자 별칭 전환** | `packages/es/` 신규 (`bootstrap.ts`는 `ensureIndex`+`putAlias`뿐이고 `ENTITY_INDICES.index`가 상수 `-v1`이다) |
|| 2 | **이중 쓰기 seam — 열일곱 경로** | 대상 해석은 **앱 계층**(의존 방향). `@prs/es` 원시체는 대상을 인자로 받는다 |
|| 3 | **활성화·전환 울타리** | `packages/db/src/advisory-lock.ts`에 세션·트랜잭션 범위가 이미 있다. **울타리는 두 인덱스 쓰기와 실패 기록까지 쥔다**(DEV-308) |
|| 4 | **정본 재구축** | PR=`pull_request_snapshot`, 커밋=`commit_snapshot`, 릴리스=`release` 표, 간선=JOB-REL-006 재파생. **운영 빌더를 재사용**하고 두 번째 생성 경로를 만들지 않는다 |
|| 5 | **JOB-ING-006 러너** | `apps/pipeline-worker/src/index.ts`의 `roles.includes('batch')` 블록. `claimNextJob('reindex')` |
|| 6 | **API-ADM-004 라우트** | `apps/search-api/src/ops/`. `operator` 전용, `alias`만 받는다 |
|| 7 | **CLI `pnpm es:reindex --alias`** | API와 **같은 enqueue seam**. `packages/es/src/cli.ts`는 `apply-mappings`만 받는다 |
|| 8 | **보관 7일 스윕** | 정본은 완료 잡의 `progress`(`source_index`·`switched_at`). **새 표 없음** |
|| 9 | `OPERATOR_JOB_TYPES`에 `reindex` | `apps/search-api/src/ops/jobs.ts`. **러너와 같은 커밋에서** |
|| 10 | 적대적 변이 ~20종 | 지시서 §65의 R1~R20 목록이 그대로 쓸 만하다 |
|**DoD에서 특히 주의할 것 셋**
|- **대상 버전은 "아직 쓰이지 않은 다음 번호"다** (DEV-309) — `v1 + 1`을 그대로 쓰면 실패로 남은 shadow가 재시도를 막는다
|- **shadow의 item 단위 부분 실패가 전환을 막아야 한다** — HTTP 200이 완료가 아니다
|- **WP-032 선행 조건 증명**: v1에 없는 분석기·다중 필드를 가진 **시험용** v2를 이 기계로 세워 별칭을 옮긴다. **WP-032의 실제 필드 이름을 선점하지 않는다**
|## B. 별도 CR 셋 — 미배포 역할 (감사가 드러냈다)
|**셋 다 각자 다른 WP가 만든 기능이 배포에서 죽어 있는 것이다.** 회귀 예외 목록(`regression/runtime-reachability.test.ts`의 `UNDEPLOYED_ROLE_ALLOWLIST`)에 사유·DEV와 함께 등재돼 있다.
|| DEV | 역할 | 영향 | 왜 이 세션이 안 고쳤나 |
|| --- | --- | --- | --- |
|| **306** | `authz` (JOB-AUTH-001) | **권한 회수가 캐시에 반영되지 않는다.** `EVT-AUTH-001`을 소비하는 곳이 없어 회수된 사용자가 TTL 만료까지 그 범위로 조회한다 (FR-AUTH-003 AC-2) | **접근 통제 축이다.** CR-035·036이 같은 축에서 유출을 고쳤고 이것은 그 결정이 반영되는 경로다 — **위협 모델과 함께 별도 CR이 옳다.** WP-012 소관 |
|| 305 | `release` (JOB-REL-007) | 릴리스 수집이 안 돌아 `prs-releases`가 비고 W-005·API-REL-002·005가 빈 상태 — WP-024·WP-026이 만든 기능 전부 | 이 역할은 **미러 볼륨을 요구한다**(DEV-143). PVC 배치가 함께 정해져야 해 다른 manifest 복사로 못 만든다. WP-024 소관 |
|| 304 | `backfill` (JOB-ING-004) | 백필 잡을 만들면 아무도 집지 않고 `job_active_uk`가 이후 요청을 막는다 — **DEV-178과 같은 모양** | `enrich` 파드의 역할 구성을 바꾸는 일이라 그 워커의 확장 축(실시간 유량) 판단이 필요하다. WP-019 소관 |
|**고칠 때 정상 경로**: manifest를 만들고 → 예외 목록에서 지우고 → 인프라 3장 배포 단위 표에 올린다. **표에 먼저 올리는 길은 시험이 막는다**(DEV-312).
|## C. 릴리스 게이트 4·5·6 (REL-003 미통과 사유 — 변화 없음)
|| 게이트 | 남은 일 | 이 환경에서 가능? |
|| --- | --- | --- |
|| Gate 4 보안 | 권한 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능 |
|| Gate 5 성능 | 성능 목표 7종 (DEV-058) | **불가** — 합성 데이터셋도 `test:perf`도 없다 |
|| Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | **불가** — 실제 Kubernetes 없음 |
|## D. 미뤄 둔 항목 (별도 CR)
|- **`flow-001`·`flow-003` e2e 간헐 실패** — WP-016 소관, 원장 §7. **이 세션이 기록된 믿음 하나를 반증했다: CI에서도 재현된다.** 문서만 바꾼 PR #49의 `verify`가 `flow-003.spec.ts:176`에서 실패했다(run 32983119303). §7의 "CI에서는 통과한다 → 로컬 환경 문제"라는 근거가 무너졌다. **재시도 통과를 해소로 적지 않는다**
|- 문서 검증기 `--strict`의 자기참조 오탐 — 원장 §7 기술 부채. **문서를 고쳐 게이트를 통과시키지 않는다**
|- 백필(JOB-ING-004)의 `updated` 정렬 열거, JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
|## E. 확인할 사항
|- **open DEV는 8건이다** (기존 5 + DEV-304·305·306). 세는 법:
@p bash
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@code lang=txt sha=50df9f91f692 lines=28 kept=28
|  주의: `**open**`처럼 굵게 쓰면 이 grep에 안 잡힌다. **새 행은 `| open ...` 형식으로 쓴다**
|- 남은 오픈 결정은 **OD-008** 하나다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전)
|- **회귀가 이제 배포 단위를 양방향으로 검사한다.** 새 역할을 추가하면 ① 코드 갈래 ② manifest ③ 인프라 3장 표 **셋이 함께** 가야 한다
|- `agent-context/`는 main에 tracked다. `.gitignore`에 넣지 않는다. **`git add -A`·`git add .`는 쓰지 않는다** — 경로를 지목해 stage 한다
|- **전사는 `exports/`에 있다** (`exports/202608270742.md`). `/export` 출력은 저장소 루트를 가리키지만 실제 위치는 `exports/`이고 `.gitignore` 24행이 덮는다 — **이전 세션들이 두 번 틀리게 적은 자리다**
|---
|> **A절은 CR-043으로 해소됐다** — WP-032 착수 전 감사가 스물을 찾았고 `3248559`로 머지됐다.
|> 아래는 그때의 판단 근거를 남긴 것이다.
|## (2026-08-26 CR-042 · WP-031 종료 시점의 기록)
|> **최신 기준 (2026-08-26 CR-042 · WP-031 종료 시점)**
|> main `5d287b1` · SRS **`baseline v2.6`**(변경 없음) · PRD `v1.2` · 원장 `review v2.8` · 작업 패키지 `v0.8` ·
|> CR-042 · DEV-265까지 · **마이그레이션 014까지**(CR-042는 만들지 않았다). **REL-004 구현 3/8** (WP-029·WP-030·WP-031 done).
|> **REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.**
|> 미해결 리뷰 **0건** (PR #1~#47 전수 실측). 열린 PR 0건. 로컬 브랜치는 `main` 하나. 미추적 0건.
|> `API-REL-006` 신설 — 기존은 001~005였다.
|## A. 지금 당장 — WP-032 착수 전 계약 감사 (REL-004)
|**구현부터 시작하지 않는다.** "착수 전 감사에서 계약 공백이 나왔다"가 **아홉 번 연속**이다
|(CR-029·030·031·033·035·038·039·041·042). 같은 순서를 지킨다: **감사 → CR → 구현**.
|대상: **WP-032 패싯·커서 페이지네이션·전문 검색** (`docs/40_delivery/pr_search_work_packages.md`)
|next-free는 **실측할 것** — `CR-043` / `DEV-266`이 예상이지만 추측해 쓰지 않는다:
@p bash grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'API-SRCH-[0-9]{3}' docs/ | sort -u # 새 API ID는 충돌부터
@path ls packages/db/migrations/*.up.sql | tail -1 # 다음은 015
@code lang=txt sha=171ea0b6b070 lines=45 kept=45
|### 감사에서 물을 것 넷 (원장 §8에도 남겼다)
|| # | 물음 | 왜 |
|| --- | --- | --- |
|| 1 | **OD-005가 정한 대체 경로가 계약에 실제로 반영돼 있는가** | CR-040이 `nori`를 초기 의존성에서 제외하고 `standard` + `edge_ngram`으로 정했다. 매핑·분석기·질의 빌더가 그 결정을 아는지 본다 — 문서만 바꾸고 코드가 모르는 상태일 수 있다 |
|| 2 | **커서 계약이 실재하는가** (가장 무거울 것) | ADR-010은 "커서 전용, 오프셋 없음"만 정한다. **커서의 재료·불안정성 처분(QA-W001-15의 "질의 변경 후 이전 커서")·정렬 동률 처리**가 어디에도 없다. WP-031은 관계 목록에 커서를 세우지 않았고 그 이유를 계약에 남겼다 — WP-032는 세워야 한다 |
|| 3 | **패싯이 목록과 같은 질의 조건으로 계산되는가**(QA-W001-16) | "건수 일치"를 무엇으로 확인하는지가 정해져 있지 않으면 시험이 그것을 걸 수 없다 |
|| 4 | **이 조회의 답을 바꾸는 것이 무엇이고 그것이 바뀔 때 누가 깨우는가** | WP-030(후보 변화)·WP-031(조회 축)이 **연속으로** 그 자리에서 공백을 찾았다 |
|### 감사에서 함께 볼 것
|- **새 파일이 다른 경로와 같은 검사를 지나는가** — PR #47 P1이 정확히 그 자리였다(`assertNoShardFailures`). ADR-008 가드레일 면제가 **파일 단위**라는 것도 함께 본다 (risks 38·40)
|- **화면이 그릴 상태 중 데이터가 없는 것이 있는가** (DEV-077·083·094·257 계열)
|- **계약이 요구하는 QA 항목을 이 WP로 실제로 통과할 수 있는가** (DEV-236 계열)
|- **실패하면 누가 다시 하는가** — 완결 표식·성공 카운트·페이지 상한 셋을 의심하라
|### WP-031이 깔아 둔 자리 — 다시 만들지 말 것
|- `packages/es/src/relations-read.ts` — **`links.ts` 밖에 있는 것이 설계다**. 사용자 대면 조회는 이쪽 계열에 둔다 (DEV-265)
|- ADR-008 가드레일이 이제 `mget`·`es.get`도 검사한다. `client.get`은 받지 않는다(Redis 핸들과 이름이 같다)
|- 커서를 세우지 않은 이유가 `API-REL-006` 상세 절에 적혀 있다 — WP-032가 커서를 세울 때 그 문단을 갱신한다
|- `apps/web/lib/relations.ts`의 `LinkSummaryView`가 웹의 유일한 요약 타입이다. `Record<string, unknown>` 복사를 퍼뜨리지 않는다
|- `apps/web/components/RelationSection.tsx`·`CoChangeSection.tsx`가 **지연 섹션 선례**다 — 접힘 기본 + 펼칠 때 1회 + 실패 시 재시도 버튼
|## B. 릴리스 게이트 4·5·6 (REL-003 미통과 사유)
|원장 6.31.1장과 `change_control.md` 4장에 게이트별로 적혀 있다. **변화 없음.**
|| 게이트 | 남은 일 | 이 환경에서 가능? |
|| --- | --- | --- |
|| Gate 4 보안 | 권한 78셀 중 **화면 13종 축**, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능. 화면 축은 화면이 더 서야 함 — **WP-031로 W-001·W-002·W-003의 관계 축은 증거가 생겼다** |
|| Gate 5 성능 | 성능 목표 7종 (DEV-058) | 불가 — 합성 데이터셋도 `test:perf`도 없다 |
|| Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | 불가 — 실제 Kubernetes 없음 |
|## C. 미뤄 둔 항목 (별도 CR)
|- **`flow-001`·`flow-003` e2e 간헐 실패** — WP-016 소관, 원장 §7. **이번 세션이 되돌려서 쟀다**: 화면 코드를 `main`과 같게 만든 상태에서도 3회 중 1회가 같은 시험·단언·오류로 실패한다. 단독은 4/4. 둘 다 **뒤로가기 복원을 5초 안에 확인하는 형태**이고 이것이 원인 규명의 실마리다. 재시도로 가리지 않는다
|- 문서 검증기 `--strict`의 자기참조 오탐 — 원장 §7에 기술 부채로 등록돼 있다. 문서를 고쳐 게이트를 통과시키지 않는다
|- 백필(JOB-ING-004)의 `updated` 정렬 열거 (원장 §7)
|- JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
|- **`script_score` 동시 변경의 운영 규모 비용** — 이번에 NOT RUN이다. 성능 harness가 서면 함께 잰다
|## D. 확인할 사항
|- **open DEV는 5건이다** (DEV-001·006·010·016·026). 전부 기존 환경 제약·범위 공백이다. 세는 법:
@p bash
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@code lang=txt sha=3df391067668 lines=24 kept=24
|- **남은 오픈 결정은 OD-008 하나다** (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전). OD-005는 CR-040으로 닫혔다
|- **`link` 역할에 `GHE_BASE_URL`이 필요하다** — 없으면 URL 참조를 아예 만들지 않는다(THR-036, fail closed). 조용히 적게 만드는 것이라 눈치채기 어렵다
|- **`apps/web`을 바꾸면 e2e 전에 `pnpm --filter @prs/web run build`가 필수다.** 안 그러면 새 화면이 404가 되고 그 실패를 플레이크로 오해한다
|- **CI 확인은 커밋 SHA의 check-runs로 한다** — `gh pr checks`는 옛 실행 결과를 그대로 보여 줄 수 있다 (이번에 겪었다)
|- `agent-context/`는 main에 tracked다. `.gitignore`에 넣지 않는다. `git add -A`·`git add .`는 쓰지 않는다 — 경로를 지목해 stage 한다
|> **A절은 CR-042로 해소됐다** — WP-031 착수 전 감사가 열여덟을 찾았고 `9084033`로 머지됐다.
|> 아래는 그때의 판단 근거를 남긴 것이다.
|## (2026-08-26 CR-040 / CR-041 · WP-030 종료 시점의 기록)
|> **최신 기준 (2026-08-26 CR-040 / CR-041 · WP-030 종료 시점)**
|> main `c4f8a39` · SRS **`baseline v2.6`** · PRD `v1.2` · 원장 `review v2.6` · 작업 패키지 `v0.6` ·
|> CR-041 · DEV-247까지 · **마이그레이션 014까지**. **REL-004 구현 2/8** (WP-029·WP-030 done).
|> **REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.**
|> 미해결 리뷰 **0건** (PR #1~#46 전수 실측). 열린 PR 0건. 로컬 브랜치는 `main` 하나.
|## A. 지금 당장 — WP-031 착수 전 계약 감사 (REL-004)
|**구현부터 시작하지 않는다.** "착수 전 감사에서 계약 공백이 나왔다"가 **여덟 번 연속**이다
|(CR-029·030·031·033·035·038·039·041). 같은 순서를 지킨다: **감사 → CR → 구현**.
|- 대상: **WP-031 관계 조회 API와 상세 화면 관계 섹션** (`docs/40_delivery/pr_search_work_packages.md`)
|- next-free는 **실측할 것** — CR-042 / DEV-248이 예상이지만 추측해 쓰지 않는다:
@p bash grep -rohE 'CR-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'API-REL-[0-9]{3}' docs/ | sort -u # 새 API ID는 충돌부터
@code lang=txt sha=a036991ee686 lines=44 kept=44
|### 이번 세션이 **이미 찾아 둔 다섯** (감사만 했고 CR은 열지 않았다)
|| # | 발견 | 근거 (실측) |
|| --- | --- | --- |
|| 1 | **관계 조회 API가 존재하지 않는다** (가장 무거움) | API-REL-001=`/sequence-neighbors`(FR-REL-001, WP-027), 002=`/containments`(WP-024), 003=`/co-changes`(FR-REL-007), 004=`/relation-graphs`(FR-REL-008, WP-043), 005=`/releases`(WP-026). **`prs-links` 간선을 조회할 경로가 없다.** WP-031 구현 범위 첫 줄이 "관계 조회(정방향·역방향)"인데 그것을 줄 API ID가 없다 — CR-030 DEV-155("릴리스 목록을 줄 경로가 없었다" → API-REL-005 신설)와 같은 모양 |
|| 2 | `### API-REL-003` **상세 절이 아예 없다** | `pr_search_api_contracts.md`에 001·002·004·005만 있다. WP-031 범위인 `/co-changes`의 요청·응답 형식이 정의된 곳이 없다 |
|| 3 | **THR-034가 WP-031 DoD에 없다** | CR-041이 선행 조건 문단에만 적었다. 그리고 대상 저장소 범위를 **어떻게** 교집합하는지가 미정이다 (대상 문서 조회에 `applyMandatoryScopeFilter`를 다시 태우는가, 간선만 주고 내용은 별도 조회로 미루는가) |
|| 4 | **`detached` 표시 규칙이 없다** | C-021 상태 목록은 `collapsed`/`loading`/`ready`/`links_pending`/`unresolved`뿐이고 **UI 문서 전체에 `detached`가 0회 등장**한다. WP-030이 방금 그 필드를 채우기 시작했다. 해제된 의존을 "관계 없음"으로 그리면 FR-REL-006 AC-3이 보존하려는 사실이 화면에서 사라진다 |
|| 5 | **FLOW-006 관련 요구사항에 FR-REL-006(스택)이 없다** | 003·004·005·008뿐이다. 화면이 스택을 그릴지가 흐름 명세에 없다 |
|### 감사에서 **탈락한 후보 둘** (가짜 발견을 남기지 않으려 기록한다)
|- `is:reverted`는 **이미 전 구간 배선돼 있다** — 파서(`packages/query/src/keys.ts:70`) → `packages/es/src/query-builder.ts:73` → ES term 필터, 시험까지. WP-013이 만들었고 **WP-030이 `is_reverted`에 실제 값을 채우면서 FR-REL-004 AC-4가 이번 머지로 실제로 성립했다**
|- `LinkSummary.has_stack`은 **CR-039에서 이미 선택 필드**였다 (`packages/domain/src/entities.ts:48`)
|### 감사에서 물을 것 (WP-030이 같은 계열로 열둘을 찾았다)
|1. **이 파생/조회의 답을 바꾸는 것이 source 말고 또 무엇인가, 그것이 바뀔 때 누가 깨우는가** — 이것이 WP-030의 뿌리였다
|2. 조회가 **대상의 내용**을 반환하는가 — 그렇다면 THR-034 교집합이 필수 수용 기준이다
|3. 화면이 그릴 상태 중 **데이터가 없는 것**이 있는가 (DEV-077·083·094 계열)
|4. 계약이 요구하는 QA 항목을 **이 WP로 실제로 통과할 수 있는가** (DEV-236 계열)
|5. 실패하면 누가 다시 하는가 — **완결 표식·성공 카운트·페이지 상한** 셋을 의심하라
|## B. 릴리스 게이트 4·5·6 (REL-003 미통과 사유)
|원장 6.31.1장과 `change_control.md` 4장에 게이트별로 적혀 있다. 변화 없음.
|| 게이트 | 남은 일 | 이 환경에서 가능? |
|| --- | --- | --- |
|| Gate 4 보안 | 권한 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능 |
|| Gate 5 성능 | 성능 목표 7종 (DEV-058) | 불가 — 합성 데이터셋도 `test:perf`도 없다 |
|| Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 | 불가 — 실제 Kubernetes 없음 |
|## C. 미뤄 둔 항목 (별도 CR)
|- **`flow-001` e2e 간헐 실패** — 이 세션에서도 귀속 배제를 실측했다(`apps/web` diff 0, 모든 `/api/**` 가로챔, 단독 4/4, 전량 3회 중 1회 실패). WP-016 소관, 원장 §7. **재시도로 가리지 않는다**
|- **문서 검증기 `--strict`의 자기참조 오탐** — 원장 §7에 기술 부채로 등록했다. 검사 범위를 코드 펜스 밖으로 좁히거나 무시 표식을 정한다. **문서를 고쳐 게이트를 통과시키지 않는다**
|- 백필(JOB-ING-004)의 `updated` 정렬 열거 (원장 §7)
|- JOB-MIR-002 스윕의 배치 공정성 (원장 §7)
|## D. 확인할 사항
|- **통합 전량 5회 중 1회 실패했고 그 회차의 실패 시험을 기록하지 못했다.** 출력을 요약 줄만 남기고 버린 명령 실수다. 이후 **4회 연속 900/900 통과**. 원인 미규명이며 원장 6.35장에 **해소하지 않은 채** 남겼다. 공유 `prs_test`·공유 ES의 파일 간 오염이 유력한 후보다 — **다음 세션이 같은 것을 보면 그때 귀속한다.** 전량 실행 시 출력을 통째로 파일에 남겨라
|- open DEV는 **5건**이다 (`DEV-001`·`006`·`010`·`016`·`026`). 전부 기존 환경 제약·범위 공백이다. 세는 법:
@p bash
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@code lang=txt sha=2153f9e75555 lines=23 kept=23
|- **남은 오픈 결정은 OD-008 하나**다 (안전 구간 표식 권한 범위, REL-006 착수 전 — 기한 전). OD-005는 CR-040으로 닫혔다
|- **`link` 역할에 `GHE_BASE_URL`이 필요하다** — 없으면 URL 참조를 아예 만들지 않는다(THR-036, fail closed). 조용히 적게 만드는 것이라 눈치채기 어렵다. WP-030의 세 계열은 GHE를 부르지 않으므로 이 제약과 무관하다
|- 전사는 `exports/202608262010.md`에 있다 — **실측 확인**. `/export` 출력은 저장소 루트를 가리켰으나 실제 파일은 `exports/`에 있고 `.gitignore` 24행이 덮는다. 미추적 0건. 그래도 `git add -A`·`git add .`는 쓰지 않는다 — 경로를 지목해 stage 한다
|- `agent-context/`는 main에 tracked다. `.gitignore`에 넣지 않는다
|> **A절은 CR-041로 해소됐다** — WP-030 착수 전 감사가 열여덟을 찾았고 `926b888`로 머지됐다.
|> 아래는 그때의 판단 근거를 남긴 것이다.
|## (2026-08-26 CR-039 종료 시점의 기록)
|> **최신 기준 (2026-08-26 CR-039 / WP-029 종료 시점)**
|> main `ea917b8` · SRS `baseline v2.5` · 원장 `review v2.3` · 작업 패키지 `v0.4` ·
|> CR-039 · DEV-229까지. **REL-004 구현 1/8** (WP-029 done).
|> **REL-003 릴리스 게이트는 여전히 미통과 — 베타 공개 승인 안 됨.**
|> 미해결 리뷰 **0건** (PR #1~#44 전수 실측). 브랜치 없음.
|## A. 지금 당장 — WP-030 착수 전 계약 감사 (REL-004)
|**구현부터 시작하지 않는다.** "착수 전 감사에서 계약 공백이 나왔다"가 **일곱 번 연속**이다
|(CR-029·030·031·033·035·038·039). 같은 순서를 지킨다: **감사 → CR → 구현**.
|- 대상: **WP-030 되돌림·체리픽·스택 관계 파생** (`docs/40_delivery/pr_search_work_packages.md`)
|- next-free는 **실측할 것** — CR-040 / DEV-230이 예상이지만 추측해 쓰지 않는다:
@p bash
@path grep -ohE 'CR-[0-9]{3}' docs/00_governance/change_control.md | sort -u | tail -2
@p grep -rohE 'DEV-[0-9]{3}' docs/ | sort -u | tail -2 grep -rohE 'JOB-[A-Z]+-[0-9]{3}' docs/ | sort -u # 새 ID는 충돌부터
@code lang=txt sha=f381326f6f57 lines=42 kept=42
|- **WP-029가 깔아 둔 자리** — 다시 만들지 말 것:
|  - `link` 역할·`pipeline-worker-link.yaml`·README 적용 순서가 **이미 있다**
|  - `link_summary`의 네 leaf(`has_revert`·`is_reverted`·`has_cherry_pick`·`has_stack`)와
|    `detached`가 **WP-030 소유로 명시**돼 있다 (DEV-222·223). 객체 통째 대입 금지 —
|    leaf 단위 스크립트(`LINK_SUMMARY_SCRIPT`)가 선례다
|  - `commit_snapshot.patch_id`가 체리픽 판정 근거로 **보존**된다 (CR-038, DEV-208)
|  - `STORED_LINK_TYPES`에 `reverts`·`cherry_picks`·`stacks_on`이 이미 어휘로 있다
|- **감사에서 물을 것** (WP-029가 같은 계열로 다섯을 찾았다):
|  1. 방아쇠가 **문서 존재를 전제하지 않는가** — JOB-REL-002·003·004도 `EVT-ING-003`만 적혀 있다
|  2. 되돌림·체리픽 간선의 **`link_id` 재료가 안정적인가** — 대상이 나중에 밝혀지는 경로가 있나
|  3. **본문이 바뀌면 간선이 사라지는가** — WP-029의 완전 파생 집합 규율을 이어받는가
|  4. **과거 데이터 재파생 경로**가 JOB-REL-006에 확장되는가
|  5. FR-REL-005 AC-2가 **조건부**임을 계약이 반영하는가 (CR-024, DEV-111 — blob 인출 기본 차단)
|## B. 릴리스 게이트 4·5·6 (REL-003 미통과 사유)
|원장 6.31.1장과 `change_control.md` 4장에 게이트별로 적혀 있다. **이 환경에서 돌지
|않는 항목이 섞여 있으므로 무엇을 지금 할 수 있는지부터 가른다.**
|| 게이트 | 남은 일 | 이 환경에서 가능? |
|| --- | --- | --- |
|| Gate 4 보안 | 권한 매트릭스 78셀 중 화면 13종 축, 위협 모델 재검토, 시크릿 스캔 | 시크릿 스캔·위협 모델은 가능. 화면 축은 화면이 더 서야 함 |
|| Gate 5 성능 | 성능 목표 7종 (DEV-058) | **불가** — 1000만 문서 합성 데이터셋도 `test:perf` 스크립트도 없다 |
|| Gate 6 운영 | 런북 실행, 롤백 10분 실측, 대시보드·알림 구성 | **불가** — 실제 Kubernetes 없음 |
|## C. 미뤄 둔 항목 (별도 CR)
|- **`flow-001` e2e 간헐 실패** — 로컬 전체 실행 5회 중 4회 실패, 단독 4/4 통과,
|  **CI는 통과**. 이 세션이 `apps/web`을 한 파일도 안 바꿨고 그 시험은 모든
|  `/api/**`를 가로채므로 이번 변경이 닿을 수 없다. WP-016 소관, 원장 §7.
|  **재시도로 가리지 않는다**
|- **백필(JOB-ING-004)의 `updated` 정렬 열거** — 부트스트랩만 `created`로 고쳤다.
|  백필은 조정 스캔이 보정하므로 미뤘다. DEV-098의 `direction: asc` 의미와 커서
|  재개 규칙을 함께 봐야 하는 변경이라 별도 CR (원장 §7)
|- **JOB-MIR-002 스윕의 배치 공정성** — `repository_id` 순 500건이라 영구 실패
|  커밋이 매 회차 같은 자리를 차지한다. 정확성은 안 깨진다 (원장 §7)
|## D. 확인할 사항
|- **open DEV는 5건이다** — DEV-001·006·010·016·026. 이전 인계가 "1건(DEV-010)"이라
|  적었으나 실측은 다섯이다. 전부 기존 환경 제약·범위 공백이며 이번 작업이 만든 것이
|  아니다. 세는 법:
@p bash
@path grep -cE '^\| DEV-[0-9]{3} .*\| open' docs/40_delivery/pr_search_implementation_traceability.md
@code lang=txt sha=427abb620e7c lines=221 kept=80
|- **`OD-005`(nori 플러그인)의 기한이 도래했다.** "REL-004 착수 전"인데 WP-029가
|  REL-004의 첫 WP였다. 전문 검색(WP-032) 소관이라 WP-029·030을 막지 않으므로
|  결정하지 않았다 — **사용자 결정 항목으로 열려 있다** (`change_control.md` 6장)
|- **`link` 역할이 배포에 추가됐다.** 운영 적용 시 `pipeline-worker-link.yaml`이
|  적용 순서에 들어 있는지 확인할 것 — 회귀가 그것을 검사한다
|- `GHE_BASE_URL`이 `link` 역할에 **반드시** 필요하다. 없으면 URL 참조를 아예
|  만들지 않는다 (THR-036, fail closed) — 조용히 적게 만드는 것이라 눈치채기 어렵다
|- **전사는 `exports/202608261008.md`에 있다** (2026-08-26 실측 정정 — 이전 기록의
|  "저장소 루트" 주장은 **틀렸다**). `exports/`는 `.gitignore` 대상이라 커밋에 딸려
|  갈 위험이 없고, 미추적 파일도 현재 0건이다. 앞선 전사 둘(`202608251453.md`·
|  `202608260047.md`)도 같은 자리다.
|  **그래도 `git add -A`·`git add .`는 쓰지 않는다** — 경로를 지목해 stage 한다
|- **로컬 브랜치 4개가 병합된 채 남아 있다** (`claude/cr-031-wp-027`,
|  `claude/cr-037-post-merge-correctness`, `claude/cr-038-wp-067-commit-metadata`,
|  `claude/rel-003-closure`). `git branch --no-merged main`은 비어 있다 — 삭제만
|  안 된 상태다. "브랜치 없음"을 근거로 판단하지 않는다
|- `agent-context/`는 main에 tracked다. `.gitignore`에 넣지 않는다
|- open DEV는 **1건**(DEV-010, 기존 스트림 동시성 표기)이다. 이 세션이 만든 것 아님
|> **0번은 해소됐다** — CR-037이 미해결 리뷰 10건을 전부 닫고 `7060414`로 머지됐다.
|> 1번(WP-067 계약 감사)도 CR-038로 해소됐다. 아래는 그때의 판단 근거를 남긴 것이다.
|> 상태 기준: main `5e18e00`, SRS `baseline v2.5`, 원장 `review v1.8`,
|> CR-036·DEV-190까지. REL-003 **10/11** (WP-067만 `todo`).
|> 브랜치 없음 — 전부 병합·삭제했다.
|## 0. 지금 당장 — CR-037로 미해결 리뷰 9건 정정 (블로킹)
|**전부 코드로 재현 확인했다.** WP-028·WP-068의 `done` 판정을 실질적으로
|약화시키므로 WP-067보다 먼저다. 다음 free 번호는 **CR-037 / DEV-191**이지만
|착수 시 반드시 실측할 것.
|### PR #37 (CR-034) — P1 4건
|| # | 결함 | 재현 근거 |
|| --- | --- | --- |
|| 1 | `repairSequence`가 락 획득 후 **`head_sha`를 재확인하지 않는다** (`seq_epoch`만 검사) | `apps/pipeline-worker/src/sequence.ts` 818행 부근. 그래프 walk 중 정상 채번이 head를 전진시키면 **head를 뒤로 되돌리고 이미 채번된 커밋을 누락**한다 |
|| 2 | 마이그레이션 010이 **기존 문서를 백필하지 않는다** | 업그레이드 시 기존 PR 문서는 ES에만 남는다. 투영은 바뀐 PR만 쓰고 조정 스캔은 이미 색인된 것을 건너뛰므로, 손대지 않은 운영 데이터는 스냅숏을 영영 못 얻고 `extra_in_es`로 보고된다 (DEV-190과 같은 계열) |
|| 3 | 지문에 **`org_id`·`visibility` 누락** | `apps/pipeline-worker/src/consistency.ts`의 `CANONICAL_FIELDS`. `packages/es/src/scoped-query.ts`가 `org_team` 필터에 쓰는 바로 그 필드인데 대조에서 빠져, **접근 통제 데이터가 불일치해도 일치로 보고**한다 |
|| 4 | 실행 중 잡 **취소가 무시된다** | `apps/pipeline-worker/src/sequence-repair-runner.ts` 96행 부근. 운영자가 `cancelled`로 바꿔도 무조건 `finishJob`이 `completed`/`failed`로 덮어쓰고, 비가역 복구도 계속 진행된다 |
|### PR #37 — P2 3건
|- 수동 복구가 `sequence.reassigned`(EVT-SEQ-002)를 **발행하지 않는다** —
|  `reassignSequence`와 달리 알림 소비자가 수동 복구를 놓친다
|- `reassigning`이 **트랜잭션 밖에서 관측되지 않는다** — `bumpEpoch`가 같은
|  트랜잭션 안에서 설정하고 `advanceHead`가 즉시 `ok`로 되돌린다. 긴 재구축 중
|  조회가 "정상"으로 옛 에폭을 낸다. `reassignSequence`처럼 `markReassigning`을
|  **먼저 따로 커밋**해야 한다
|- 그래프를 읽지 못한 실패가 **`stale`로 영속화되지 않는다** — `repairSequence`가
|  `stale` outcome을 반환만 하고 `markStale`을 부르지 않아, 잠재적으로 손상된
|  공간이 `ok`로 광고된다
|### PR #36 · #38 — P2 2건
|- **PR #36**: WP-027 원장 행에 `화면 커밋 / PR #33` 플레이스홀더가 남아 있고
|  실제 화면 커밋 `594fa94`와 CR-032 정정이 빠졌다
|- **PR #38**: **`docs/40_delivery/pr_search_work_packages.md`의 순서표가
|  WP-028·WP-068을 아직 `todo`로 표시한다** (48·50행). 원장은 `done`이고 DoD
|  체크박스도 미체크다. **이 세션이 만든 문서 모순이다** — 후속 에이전트가 작업
|  패키지 문서를 기준으로 판단하면 재작업하게 된다
|## 1. WP-067 — 착수 전 계약 감사 필요 (CR 먼저)
|**현재 WP-067 계약을 그대로 구현하면 안 된다.** 다음 다섯을 먼저 정리할 것.
|1. `JOB-MIR-002`가 `EVT-ING-003`를 소비할 때 `prs:projected`의 기존 `link`
|   consumer group과 **같은 group을 쓰면 안 된다** — 독립 처리라 별도 group 필요
|2. 현재 계약은 "기존 commit document **부분 갱신만**"이라 **direct-push commit
|   document가 아예 없는 문제**를 해결하지 못한다
|3. sequence projection도 `update_by_query`뿐이라 **없는 문서를 만들지 않는다**
|4. PR 상세의 `source_commits`는 SHA 객체만 만들고 **commit document를 join하지
|   않는다** — 메타데이터를 채워도 화면에는 계속 SHA만 나온다
|5. Neighbor API의 direct-push 행도 **PR document만 표시 소스로 읽는다** — 같은 이유
|**우선순위 근거**: WP-027이 직접 푸시 커밋을 선행·후행에 실제 노출하기 시작해
|이제 사용자에게 보이는 조사 품질이다(원장 §8).
|...cut 141 lines
