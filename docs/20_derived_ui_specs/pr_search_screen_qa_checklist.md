# PR Search 화면 QA 체크리스트

> 상태: review | 버전: v0.11 | 갱신일: 2026-08-29

## 1. 사용 방법

구현 후 화면별로 체크하고, 실패 항목은 요구사항 또는 화면 ID에 연결한다. 작업 패키지(WP)의 완료 기준은 이 문서의 항목 번호를 인용한다(예: `QA-W001-03`). WP DoD에 QA 항목을 다시 서술하지 않고 인용만 한다.

항목 ID 규칙: `QA-<화면ID 또는 COMMON>-<번호>`

## 2. 공통 검수 항목 (모든 화면)

| ID | 항목 | 관련 |
| --- | --- | --- |
| QA-COMMON-01 | 화면 간 이동이 IA의 진입 경로표대로 동작한다 | IA 5장 |
| QA-COMMON-02 | `loading_initial`, `empty_*`, `error_*`, `no_permission` 상태가 모두 렌더링된다 | 상태 매트릭스 3장 |
| QA-COMMON-03 | "결과 없음", "권한 없음", "미수집"이 서로 다른 문구와 액션으로 구분된다 | 상태 매트릭스 1장 |
| QA-COMMON-04 | 권한·정책으로 비활성화된 액션이 `blockedReason`으로 필요 역할명을 표시한다 | FR-AUTH-002 |
| QA-COMMON-05 | 파괴적·비가역 액션에 확인 다이얼로그와 영향 범위 표시가 있다 | 플로우 1.1장 |
| QA-COMMON-06 | 모든 인터랙티브 요소에 키보드로 도달하고 포커스 표시가 보인다 | NFR-007 |
| QA-COMMON-07 | 오버레이(Dialog/Drawer)가 포커스를 트랩하고 닫힘 시 트리거로 복귀한다 | 플로우 1.1장 |
| QA-COMMON-08 | ESC와 브라우저 뒤로가기가 오버레이 유형 규칙대로 동작한다 | 플로우 1.1장 |
| QA-COMMON-09 | URL만으로 화면 상태가 재현된다(질의, 필터, 정렬, 앵커) | 플로우 2장 |
| QA-COMMON-10 | 상태를 색상만으로 전달하지 않는다(배지에 라벨 텍스트 포함) | 토큰 1장 |
| QA-COMMON-11 | axe 자동 검사 위반이 0건이다 | NFR-007 |
| QA-COMMON-12 | 라이트·다크 두 테마에서 대비 기준을 만족한다 | 토큰 11장 |
| QA-COMMON-13 | 로딩 중 레이아웃 이동(shift)이 없다. skeleton이 실제 콘텐츠와 같은 높이다 | 토큰 7장 |
| QA-COMMON-14 | 식별자(SHA, 시퀀스, 브랜치)가 고정폭 서체로 표시된다 | 토큰 6장 |
| QA-COMMON-15 | 2초 이상 걸리는 작업에 진행 상태가 표시된다 | NFR-007 |
| QA-COMMON-16 | 제품 코드에 리터럴 색상값(`#rrggbb`)이 없다 | 토큰 11장 |
| QA-COMMON-17 | Conductor 외 UI 라이브러리 의존성이 없다 | 토큰 2장 |
| QA-COMMON-18 | 세션 만료 시 현재 경로가 보존되고 재인증 후 복귀한다 | FR-AUTH-001 |

## 3. 화면별 체크리스트

### W-001 통합 검색

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W001-01 | 40자 SHA를 붙여넣으면 커밋 상세로 이동한다 | FR-SRCH-001 AC-1 |
| QA-W001-02 | `#1234`, `owner/repo#1234`, GHE PR URL이 모두 PR로 해석된다 | FR-SRCH-001 AC-2, AC-3 |
| QA-W001-03 | 7자 축약 SHA가 접두 검색으로 해석된다 | FR-SRCH-004 AC-1 |
| QA-W001-04 | 6자 이하 hex 입력이 서버 호출 없이 즉시 거부되고 사유가 표시된다 | FR-SRCH-004 AC-2 |
| QA-W001-05 | 해석 후보 2건 이상이면 자동 이동하지 않고 후보 목록을 표시한다 | FR-SRCH-001 AC-5 |
| QA-W001-06 | 접두 결과 50건 초과 시 절삭 표시와 조건 추가 안내가 나온다 | FR-SRCH-004 AC-3 |
| QA-W001-07 | 지원하지 않는 질의 키 입력 시 오류 위치와 지원 키 목록이 표시된다 | FR-SRCH-005 AC-4 |
| QA-W001-08 | `seq:1200..1350`, `merged:2026-08-10..2026-08-19`가 범위 필터로 변환된다 | FR-SRCH-005 AC-2, AC-3 |
| QA-W001-09 | `-author:kim` 부정 조건이 동작한다 | FR-SRCH-005 AC-6 |
| QA-W001-10 | 같은 필드 다중 선택은 OR, 다른 필드는 AND로 결합된다 | FR-SRCH-006 AC-2 |
| QA-W001-11 | 결과 0건일 때 제거하면 결과가 생기는 필터 후보가 제시된다 | FR-SRCH-006 AC-3 |
| QA-W001-12 | 정렬 기본값이 머지 시퀀스 내림차순이고 시퀀스 없는 문서가 뒤에 온다 | FR-SRCH-007 AC-2 |
| QA-W001-13 | 동일 정렬 조건으로 두 번 조회하면 순서가 동일하다 | FR-SRCH-007 AC-4 |
| QA-W001-14 | 더 보기가 커서 기반으로 동작하고 페이지 번호 UI가 없다 | FR-SRCH-008 AC-1, AC-4 |
| QA-W001-15 | 질의 변경 후 이전 커서 사용 시 `CURSOR_QUERY_MISMATCH`가 반환되고 첫 페이지로 복귀한다. **훼손·만료 커서는 `CURSOR_INVALID`이며 안내 문구가 다르다** — 둘을 같은 원인으로 그리지 않고 자동 재시도 루프도 만들지 않는다 (CR-043, DEV-273). 실제 네트워크 흐름으로 검증한다 | FR-SRCH-008 AC-3, 예외 처리 |
| QA-W001-16 | 패싯이 목록과 동일한 질의 조건으로 계산된다. **검증은 bucket 단위다** (CR-043, DEV-276): 반환된 각 bucket `(field=value, count=N)`에 대해 같은 질의·정렬·접근 범위에 그 bucket 필터만 더한 독립 조회의 건수가 `N`과 같아야 한다. **`sum(buckets) == total`로 검증하지 않는다** — 상위 20 절삭, 다중 값 필드(`label`·`allowed_team_ids`), `track_total_hits` 근사 때문에 그 식은 틀리다 | FR-SRCH-009 AC-3 |
| QA-W001-17 | 패싯 실패 시 목록은 정상 표시되고 레일에만 실패가 표시된다. **예산 초과 생략(`budget_omitted`)과 계산 실패(`failed`)를 서로 다른 상태로 그린다** (CR-043, DEV-277) | FR-SRCH-009 예외 처리 |
| QA-W001-18 | 전문 검색 시 제목 일치가 본문 일치보다 상위에 온다 | FR-SRCH-011 AC-2 |
| QA-W001-19 | 강조 구간이 항목당 최대 3개, 각 160자 이내로 표시된다 | FR-SRCH-011 AC-5 |
| QA-W001-26 | **강조 응답에 raw HTML이 없다.** 제목·본문에 `<img onerror=...>` 같은 마크업이 들어 있어도 API는 평문 조각과 일치 구간으로 답하고, 화면은 그것을 텍스트로 렌더링한다 (CR-043, DEV-282) | THR-018, THR-020 |
| QA-W001-27 | 이어 보기(2페이지 이후)가 패싯을 다시 요청하지 않는다. `q`·필터·정렬이 바뀌면 커서와 패싯 상태를 함께 버리고 첫 페이지부터 다시 연다 (CR-043, DEV-280) | FR-SRCH-008, FR-SRCH-009 |
| QA-W001-28 | 한 글자 검색어가 거절된다 — **코드 포인트 기준**이다. `𠮷`·이모지 한 글자도 `QUERY_TOO_SHORT`다 (CR-043, DEV-284). 구조화 필터만 있고 자유 텍스트가 없으면 거절하지 않는다 | FR-SRCH-011 예외 처리 |
| QA-W001-29 | **정렬 값이 움직여도 페이지 사이에 항목이 중복·누락되지 않는다.** `sort=updated_at`으로 1페이지를 받은 뒤 그 페이지 밖 문서의 `updated_at`을 앞으로 옮겨도, 2페이지가 이미 본 문서를 다시 주거나 못 본 문서를 건너뛰지 않는다 — PIT이 색인 뷰를 고정한다 (CR-044, DEV-286) | FR-SRCH-008, ADR-010 Amendment |
| QA-W001-30 | **`seq:` 범위 조건이 시퀀스 공간 없이 거절된다.** `seq:1200..1350` 단독, `repo:` 없음, `base:` 없음, 서로 다른 `repo:` 둘, 서로 다른 `base:` 둘 다섯 경우가 전부 HTTP 400이고 무엇이 부족한지를 함께 답한다. 같은 값을 두 번 적은 `repo:a/x repo:a/x base:main seq:1..5`는 통과한다. **부정된 `-seq:` 범위에도 같은 규칙이 적용된다** | FR-SRCH-005 AC-7 (CR-051) |
| QA-W001-31 | **첫 조회가 URL에 에폭을 새긴다.** `seq_epoch` 없이 `repo:acme/payments base:main seq:1..5`를 제출하면 응답이 현재 에폭을 실어 주고 주소창의 URL에 `seq_epoch`이 나타난다. **히스토리에 항목이 쌓이지 않는다**(`replace`이지 `push`가 아니다) — 뒤로 가기 한 번이 에폭 없는 같은 조회로 돌아가지 않는다 | FR-SEQ-005 AC-4, ADR-007 규칙 5 (CR-051) |
| QA-W001-32 | **에폭이 오른 뒤 옛 URL이 결과를 그리지 않는다.** 에폭 3으로 조회해 URL을 얻고 재채번으로 에폭이 4가 된 뒤 그 URL을 다시 열면 `epoch_stale: true`와 두 에폭 값이 나오며 **목록·총계·패싯이 응답에 없고 화면에도 없다.** 빈 목록이나 0건으로 그리지 않는다 | FR-SEQ-005 AC-4 (CR-051) |
| QA-W001-33 | **현재 에폭으로 옮기는 것은 클릭뿐이다.** `epoch_stale` 상태에서 아무 것도 누르지 않고 기다려도 새 조회가 나가지 않는다. 「현재 에폭으로 다시 조회」를 눌렀을 때에만 URL의 `seq_epoch`이 현재 값으로 바뀌고 결과가 나온다 | FR-SEQ-005 AC-4 (CR-051) |
| QA-W001-34 | **에폭이 결과 집합을 실제로 가른다.** 같은 저장소·브랜치·서수에 에폭 1 문서와 에폭 2 문서가 함께 색인돼 있을 때 에폭 1 조회는 앞의 것만, 에폭 2 조회는 뒤의 것만 돌려준다. **목록뿐 아니라 패싯 버킷도 같은 에폭만 센다** | FR-SEQ-005 AC-4, FR-SRCH-009 (CR-051) |
| QA-W001-35 | **에폭이 커서 정체성의 일부다.** 에폭 3에서 받은 `next_cursor`를 에폭 4 조회에 쓰면 `CURSOR_QUERY_MISMATCH`다. 그리고 에폭 3을 고정한 옛 URL로 2페이지를 이어 가려 하면 커서 처리보다 **먼저** 무효 사실이 나온다 — 옛 서수 의미로 순회가 계속되지 않는다 | FR-SRCH-008 AC-3 (CR-051) |
| QA-W001-36 | **질의에서 `seq:`를 지우면 `seq_epoch`도 사라진다.** 에폭이 고정된 상태에서 `seq:` 토큰을 제거하면 URL에 그 파라미터가 남지 않고, 다음 조회가 뜻 없는 값 때문에 거절당하지 않는다 | FR-SRCH-005 AC-7 (CR-051) |
| QA-W001-37 | **`seq:`가 없는데 `seq_epoch`만 있으면 거절된다.** 조용히 무시하지 않는다. 그리고 `seq_epoch=abc`·`seq_epoch=0`·`seq_epoch=-1`은 "지정하지 않음"이 아니라 `INVALID_PARAMETER`다 — 오타가 현재 에폭 조회로 조용히 흘러가지 않는다 | FR-SEQ-005 AC-4 (CR-051, DEV-363) |
| QA-W001-38 | **저장 시점의 에폭이 지금 보고 있는 것과 같다.** 에폭 3 결과를 보는 중에 저장을 누르면 저장 요청이 3을 싣는다. 그 사이에 에폭이 4가 되었으면 **저장이 거절되고 3이 4로 바뀌어 저장되지 않는다** | FR-SRCH-010 AC-8 (CR-051) |
| QA-W001-20 | 내보내기 1000건 초과 시 비동기 잡이 생성되고 잡 ID가 표시된다 | FR-SRCH-012 AC-2 |
| QA-W001-21 | 내보내기 실행 전 대상 건수 확인 다이얼로그가 표시된다 | FR-SRCH-012 |
| QA-W001-22 | 서로 다른 시퀀스 공간의 결과가 섞이면 시퀀스 배지 tone이 구분된다 | 와이어프레임 W-001 구현 메모 |
| QA-W001-23 | 접근 범위 밖 저장소의 결과가 목록·패싯·건수 어디에도 나타나지 않는다 | FR-AUTH-002 AC-5 |
| QA-W001-24 | 결과 행의 관계 배지가 **"요약 없음" · "관계 없음" · "관계 있음"** 셋을 구분한다. 요약이 없는 행에 "관계 없음"을 그리지 않는다 (CR-042, DEV-264) | FR-REL-004, FR-REL-005, ADR-009 |
| QA-W001-25 | 관계 배지를 그리기 위해 행마다 별도 조회를 보내지 않는다 — 목록 응답의 `link_summary`만 쓴다 | ADR-009 |

### W-002 PR 상세

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W002-01 | 머지 커밋 1건과 원본 커밋 목록이 시각적으로 구분된다 | FR-SRCH-003 AC-1 |
| QA-W002-02 | 미머지 PR은 머지 커밋이 null이고 원본 커밋만 표시된다 | FR-SRCH-003 AC-2 |
| QA-W002-03 | 원본 커밋 250건 초과 시 절삭 표시와 전체 건수가 나온다 | FR-SRCH-003 AC-4 |
| QA-W002-04 | 선행·후행이 **앞뒤 각각** 기본 10건, 최대 50건까지 조절된다 | FR-REL-001 AC-1 |
| QA-W002-05 | 기준 PR이 결과에 포함되고 강조 표시된다 | FR-REL-001 AC-3 |
| QA-W002-06 | 시퀀스 공간 경계에서 존재하는 만큼만 반환되고 오류가 나지 않는다 | FR-REL-001 AC-4 |
| QA-W002-19 | 직접 푸시 커밋이 이웃 목록에서 빠지지 않아 서수가 건너뛰지 않는다 (CR-031) | FR-REL-001 AC-2 |
| QA-W002-07 | 미머지 PR에서 선행·후행 섹션이 숨겨지지 않고 비활성 + 사유로 표시된다 | FR-REL-001, 상태 매트릭스 |
| QA-W002-08 | 포함 릴리스가 시각 오름차순으로 표시된다 | FR-REL-002 AC-3 |
| QA-W002-09 | 미배포 PR에 `미배포` 표시와 대기 PR 수가 나온다 | FR-REL-002 AC-4 |
| QA-W002-10 | 신뢰도 `heuristic` 관계 항목에 근거 문자열이 반드시 함께 표시된다 | FR-REL-003 AC-2 |
| QA-W002-11 | 되돌림 관계가 방향(주체→대상)과 역방향 모두 조회된다 | FR-REL-004 AC-3 |
| QA-W002-12 | 체리픽 관계에 `exact`/`derived` 신뢰도가 구분 표시된다 | FR-REL-005 AC-1, AC-2 |
| QA-W002-13 | 동시 변경 상관이 상위 20건, 겹치는 경로 상위 10개와 함께 표시된다 | FR-REL-007 AC-3, AC-5 |
| QA-W002-14 | 변경 파일 200개 초과 PR은 동시 변경 대상에서 제외되고 사유가 표시된다 | FR-REL-007 AC-4 |
| QA-W002-15 | 보강 미완료 시 머지 커밋은 표시되고 원본 커밋 영역만 수집 중 상태가 된다 | FR-SRCH-003, FR-ING-004 AC-3 |
| QA-W002-16 | 에폭 변경 시 경고 배너가 표시되고 자동 재조회는 하지 않는다 | FR-SEQ-005 AC-4 |
| QA-W002-17 | 관계·동시 변경·포함 릴리스·선행·후행 섹션이 확장 시에만 조회된다 — 진입 시 요청은 PR 문서 하나뿐이다 (CR-031, DEV-162) | IA 원칙 4 |
| QA-W002-18 | 접근 범위 밖 PR 직접 접근 시 존재 여부가 노출되지 않는다 | FR-AUTH-002 AC-4 |
| QA-W002-23 | 해제된 스택 의존(`detached`)이 **숨겨지지도 active로 표시되지도 않고** "해제됨"으로 표시된다 (CR-042, DEV-257) | FR-REL-006 AC-3 |
| QA-W002-24 | 되돌림 제목 대조 후보가 2건 이상이면 **모든 후보가 표시되고** 다중 후보임이 밝혀진다 (CR-042, DEV-261) | FR-REL-004 예외 처리 |
| QA-W002-25 | 대상 저장소가 접근 범위 밖인 관계 항목은 간선·식별자·근거만 보이고 제목·작성자·이동 링크가 없으며, **문구가 사유를 구분하지 않는다** (CR-042, DEV-253) | FR-AUTH-002, THR-034 |
| QA-W002-26 | `links_pending`이 참조 그룹에만 적용되고 되돌림·체리픽·스택은 그대로 표시된다 (CR-042, DEV-258) | FR-REL-003 예외 처리 |
| QA-W002-27 | 관계와 동시 변경 중 한쪽이 실패해도 다른 쪽과 상세 본체가 유지되고, 실패한 섹션에만 "다시 시도"가 있다 | 상태 매트릭스 W-002 `partial_failure` |
| QA-W002-28 | 미머지 PR의 동시 변경이 오류가 아니라 `not_merged` 사유와 함께 표시되고, **0건 결과와 다르게 그려진다** (CR-042, DEV-254) | FR-REL-007 AC-2 |
| QA-W002-20 | 선행·후행 조회가 **대상 브랜치를 함께 보내고**, 응답의 시퀀스 공간이 그 브랜치와 같다. 브랜치를 모르면 조회하지 않는다 — 서버가 공간을 고르게 두거나 `main`으로 지어내지 않는다 (CR-032, DEV-168) | FR-REL-001, ADR-007 |
| QA-W002-21 | 선행·후행 조회가 실패하면 **재시도 수단이 화면에 있고** 그것으로 복구된다 — 상세 화면 전체를 다시 열지 않아도 된다 (CR-032, DEV-170) | 상태 매트릭스 W-002 |
| QA-W002-22 | 색인에 없는 PR 이웃의 "머지 시각"이 비어 있다 — 병합 커밋의 커밋 시각을 머지 시각으로 표시하지 않는다 (CR-032, DEV-169) | API-REL-001 |

### W-003 커밋 상세

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W003-01 | 머지 커밋 SHA 조회 시 대응 PR과 역할 `merge_commit`이 표시된다 | FR-SRCH-002 AC-1 |
| QA-W003-02 | 원본 커밋 SHA 조회 시 소속 PR과 역할 `source_commit`이 표시된다 | FR-SRCH-002 AC-2 |
| QA-W003-03 | 직접 푸시 커밋 조회 시 "PR 없음(직접 푸시)"이 표시되고 시퀀스는 정상 표시된다 | FR-SRCH-002 AC-3 |
| QA-W003-04 | 응답에 PR 번호, 제목, 작성자, 리뷰어, 머지 시각, 시퀀스, 시퀀스 공간이 모두 표시된다 | FR-SRCH-002 AC-4 |
| QA-W003-05 | 동일 SHA가 2개 이상 PR에 속하면 모든 PR이 목록으로 표시된다 | FR-SRCH-002 AC-5 |
| QA-W003-06 | `no_sequence`(원본 커밋)가 오류가 아니라 머지 커밋 링크와 설명으로 표시된다 | 상태 매트릭스 W-003 |
| QA-W003-07 | SHA 복사 버튼이 표시값이 아니라 전체 40자를 복사한다 | 컴포넌트 C-024 |
| QA-W003-08 | 변경 경로 목록에 파일 내용이 표시되지 않는다 | SRS 4.3 |
| QA-W003-09 | 시퀀스 위치의 선행·후행 조회가 **커밋 문서의 대상 브랜치를 함께 보낸다**. 모르면 조회하지 않는다 (CR-032, DEV-168) | FR-REL-001, ADR-007 |
| QA-W003-10 | 커밋 관계 섹션이 **확장 시에만** 조회되고 진입 시 관계 요청이 0건이다 (CR-042, DEV-263) | IA 원칙 4 |
| QA-W003-11 | 커밋 화면이 스택·동시 변경을 요청하지 않는다 — 커밋에는 그 개념이 없다 (CR-041) | FR-REL-006, FR-REL-007 |

### W-004 범위 조사

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W004-01 | 반개구간 `(from, to]` 규칙이 화면에 "제외"/"포함" 라벨로 상시 표시된다 | FR-SEQ-002 AC-1 |
| QA-W004-02 | 시작 앵커의 PR이 결과에 포함되지 않고 끝 앵커의 PR은 포함된다 | FR-SEQ-002 AC-1 |
| QA-W004-03 | 릴리스 태그, 커밋 SHA, PR 번호, 시각, 시퀀스 값 5종 앵커가 모두 정규화된다 | FR-SEQ-003 AC-1~AC-4 |
| QA-W004-04 | 정규화 결과에 원본 표현, 시퀀스 값, 대상 커밋 SHA, 에폭이 표시된다 | FR-SEQ-003 AC-5 |
| QA-W004-05 | first-parent 체인 밖 커밋 앵커 시 오류와 머지 커밋 대체 제안이 나온다 | FR-SEQ-003 AC-2 |
| QA-W004-06 | 미머지 PR 앵커 시 `anchor_not_merged` 사유가 표시된다 | FR-SEQ-003 AC-3 |
| QA-W004-07 | from > to 입력 시 `range_inverted` 오류와 교환 제안이 나온다 | FR-SEQ-002 AC-3 |
| QA-W004-08 | 구간 5만 건 초과 시 조회 전에 예상 건수와 축소 안내가 표시된다 | FR-SEQ-002 AC-4 |
| QA-W004-09 | 두 앵커가 다른 시퀀스 공간이면 조회 버튼이 클라이언트에서 비활성화된다 | FR-SEQ-002 AC-5 |
| QA-W004-10 | 요약에 PR 수, 커밋 수, 작성자 수, 변경 경로 상위 20, 되돌림 보유 수가 표시된다 | FR-SEQ-002 AC-2, FR-SEQ-004 AC-2 |
| QA-W004-11 | 결과가 시퀀스 오름차순으로 정렬된다 | FR-SEQ-002 |
| QA-W004-12 | 안전 구간 표식 버튼이 `release_manager`가 아니면 사유와 함께 비활성이다 | FR-SEQ-006 AC-3 |
| QA-W004-13 | 표식 등록 시 시퀀스 값, 등록자, 시각, 메모(500자 이내), 에폭이 저장된다 | FR-SEQ-006 AC-2 |
| QA-W004-14 | 에폭 변경 시 기존 표식이 무효 상태로 표시된다 | FR-SEQ-006 AC-4 |
| QA-W004-15 | 이분 탐색 정상 표시 시 후보 시작이 상향, 이상 표시 시 끝이 하향된다 | FR-SEQ-007 AC-1 |
| QA-W004-16 | 다음 검사 지점이 실재하는 커밋 시퀀스다(빈 값이 아니다) | FR-SEQ-007 AC-2 |
| QA-W004-17 | 후보 1건 도달 시 결과 PR을 표시하고 탐색이 종료된다 | FR-SEQ-007 AC-3 |
| QA-W004-18 | 남은 후보 수와 예상 잔여 검사 횟수가 표시된다 | FR-SEQ-007 AC-4 |
| QA-W004-19 | 브라우저를 닫았다 열어도 탐색 상태가 이어진다 | FR-SEQ-007 AC-5 |
| QA-W004-20 | good > bad 모순 표시 시 오류와 초기화 경로가 제공된다 | FR-SEQ-007 |
| QA-W004-21 | URL 에폭과 현재 에폭 불일치 시 무효 경고가 나오고 자동 재조회하지 않는다 | 플로우 2장 |
| QA-W004-22 | 재채번 중 조회 시 마지막 확정 값과 갱신 중 배너가 함께 표시된다 | FR-SEQ-005 |
| QA-W004-23 | **필터 일치가 첫 페이지 밖에 있어도 요약과 목록이 어긋나지 않는다.** 구간 1~60·`size=10`·일치가 서수 51 하나뿐인 `q`에서, 요약이 1건이면 그 항목에 커서로 도달할 수 있어야 한다 (CR-043, DEV-270) | FR-SEQ-002 AC-6 |
| QA-W004-24 | 구간을 커서로 끝까지 순회할 수 있고 마지막 페이지에서 `next_cursor`가 null이다. 페이지 번호 UI와 오프셋 파라미터가 없다 | FR-SEQ-002 AC-6 |
| QA-W004-25 | 에폭·구간 경계·질의가 바뀐 뒤 이전 커서를 쓰면 `CURSOR_QUERY_MISMATCH`이고 화면은 첫 페이지로 되돌아간다 | FR-SEQ-002 AC-7 |
| QA-W004-26 | 패싯 넷(작성자·팀·라벨·경로)이 **구간 전체** 기준으로 계산된다 — 현재 페이지의 항목만 세지 않는다. 저장소·대상 브랜치 패싯은 그리지 않는다 | FR-SEQ-002 AC-8, FR-SRCH-009 AC-1 |
| QA-W004-27 | 접근 범위 밖 저장소의 팀이 패싯 bucket·count 어디에도 나타나지 않는다 | THR-003, FR-AUTH-002 AC-5 |
| QA-W004-28 | 훼손·만료된 구간 커서가 `CURSOR_INVALID`를 내고 안내 문구가 `CURSOR_QUERY_MISMATCH`와 다르다 (CR-044, DEV-290) | FR-SEQ-002 AC-7 |
| QA-W004-29 | **한 chunk 안의 일치가 `size`보다 많아도 나머지가 사라지지 않는다.** chunk 100 · `size` 10 · 그 chunk에 일치 20건이면, 2페이지에 나머지 10건이 나온다 — 커서는 **실은 마지막 일치**까지만 전진한다 (CR-044, DEV-287) | FR-SEQ-002 AC-6 |

### W-005 릴리스·빌드

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W005-01 | 릴리스 2건 선택 시 구간 비교가 활성화된다 | FR-SEQ-004 AC-1 |
| QA-W005-02 | 지정 순서와 무관하게 시퀀스가 작은 쪽이 시작 앵커가 되고 방향이 명시된다 | FR-SEQ-004 AC-4 |
| QA-W005-03 | 다른 대상 브랜치 릴리스 2건 선택 시 비교가 차단되고 사유가 표시된다 | FR-SEQ-004 AC-3 |
| QA-W005-04 | 미배포 구간을 조회할 수 있다 | FR-SEQ-004 AC-5 |
| QA-W005-05 | 릴리스 미수집 저장소에서 `release_not_indexed`와 저장소 개요 경로가 제공된다 | FR-SEQ-004 |
| QA-W005-06 | "직전 릴리스 대비 PR 수"가 시퀀스 차이가 아니라 실제 PR 문서 수다 | 와이어프레임 W-005 구현 메모 |

### W-006 통계 대시보드

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W006-01 | 그룹 키 7종(저장소/조직/팀/작성자/라벨/브랜치/상태)이 모두 동작한다 | FR-STAT-001 AC-1 |
| QA-W006-02 | 그룹 500개 초과 시 상위 500개와 절삭 표시가 나온다 | FR-STAT-001 AC-3 |
| QA-W006-03 | 그룹 클릭 시 동일 조건 질의로 W-001에 도달한다 | FR-STAT-001 AC-5 |
| QA-W006-04 | 버킷 간격 4종(시간/일/주/월)이 동작하고 시간대가 반영된다 | FR-STAT-002 AC-1, AC-2 |
| QA-W006-05 | 버킷 400개 초과 시 오류와 간격 확대 제안이 나온다 | FR-STAT-002 AC-3 |
| QA-W006-06 | 데이터 없는 구간이 0값 버킷으로 채워진다 | FR-STAT-002 AC-4 |
| QA-W006-07 | 기간 미지정 시 최근 30일이 적용되고 응답에 명시된다 | FR-STAT-002 |
| QA-W006-08 | 리드타임 백분위 5종(p50/p75/p90/p95/p99)이 초 단위로 표시된다 | FR-STAT-003 AC-1, AC-3 |
| QA-W006-09 | 표본 20건 미만 시 백분위 대신 원값 목록과 `low_sample` 배지가 나온다 | FR-STAT-003 |
| QA-W006-10 | 리뷰 없는 PR이 리뷰 대기 집계에서 제외되고 제외 건수가 표시된다 | FR-STAT-004 AC-1 |
| QA-W006-11 | 작성자 본인 리뷰가 첫 리뷰 판정에서 제외된다 | FR-STAT-004 AC-3 |
| QA-W006-12 | 변경 규모 구간이 명세대로 나뉘고 비율이 함께 표시된다 | FR-STAT-005 AC-1~AC-3 |
| QA-W006-13 | 보강 미완료 문서가 `unknown` 구간으로 집계된다 | FR-STAT-005 AC-5 |
| QA-W006-14 | 집계 총 건수와 목록 총 건수가 일치한다 | FR-STAT-006 AC-2 |
| QA-W006-15 | 100만 건 초과 시 근사값 배지가 표시된다 | FR-STAT-006 AC-3 |
| QA-W006-16 | 한 패널 실패가 다른 패널을 비우지 않는다 | 상태 매트릭스 W-006 |
| QA-W006-17 | 개인 순위 배지·정렬 강조가 없다 | SRS 4.3 |
| QA-W006-18 | 모든 차트에 동일 데이터의 표 대체가 제공된다 | NFR-007 |

**항목 열여덟의 소유 계층 (CR-053, DEV-388).** `WP-037`의 DoD가 `QA-W006-01~15`를 요구했는데 항목은
**열여덟이고 그중 여럿은 API만으로 판정할 수 없다.** `WP-036`이 `QA-A001-08`에서 겪은 것과 같은
자리이므로 `WP-010`의 선례를 따라 계층을 나눈다. **제품 범위는 그대로이며 소유 WP만 정한다.**

| 계층 | 항목 | 소유 |
| --- | --- | --- |
| API만으로 판정된다 | `01`·`04`·`06`·`07`·`13`·`14` | WP-037 |
| API 몫과 화면 몫이 나뉜다 | `02`(`truncated`↔절삭 표시) · `05`(400과 사유↔간격 확대 제안) · `08`(값과 단위↔표시) · `09`(`low_sample`·`raw_values`↔배지) · `10`(`excluded_count`↔표시) · `12`(구간·비율↔표시) · `15`(`approximate`↔배지) | 각각 WP-037 / WP-038 |
| 화면이 판정한다 | `03`·`16`·`17`·`18` | WP-038 |
| **투영이 판정한다** | `11` — 작성자 본인 리뷰 제외는 색인 시점 계산이라 API도 화면도 고칠 수 없다. `WP-037`이 함께 고친다 (DEV-387) | WP-037 |

`QA-W006-14`(총 건수 일치)는 **PR 모집단 기준**이다 (CR-053, `FR-STAT-006` AC-2). 목록 화면은 PR과
커밋을 함께 보이므로 화면 총계와 집계 총계가 다를 수 있으며, 그 차이는 오류가 아니라 다른 것을
세기 때문이다.

### W-007 관계 그래프 (조건부, REL-004 ACC-06)

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W007-01 | 깊이 기본 2, 최대 3으로 제한된다 | FR-REL-008 AC-1 |
| QA-W007-02 | 노드 300개 초과 시 `exact`→`derived`→`heuristic` 순으로 포함되고 절삭 표시가 나온다 | FR-REL-008 AC-2 |
| QA-W007-03 | 각 간선에 유형, 방향, 신뢰도, 근거 문자열이 표시된다 | FR-REL-008 AC-3 |
| QA-W007-04 | 접근 범위 밖 노드가 그래프에 나타나지 않는다 | FR-REL-008 AC-4 |
| QA-W007-05 | 2초 초과 시 부분 그래프와 절삭 표시가 반환된다 | FR-REL-008 |
| QA-W007-06 | 캔버스와 동등한 노드·간선 표가 제공되고 키보드로 순회된다 | NFR-007 |

### W-008 저장된 검색

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W008-01 | 저장 시 이름, 질의, 공개 범위(`private`/`team`)가 저장된다. `team`이면 대상 팀 하나가 함께 저장된다 | FR-SRCH-010 AC-1 |
| QA-W008-02 | `team` 공개 검색이 **지정된 대상 팀의** 현재 구성원에게 노출되고 다른 팀 사용자에게는 `NOT_FOUND`다 | FR-SRCH-010 AC-2 |
| QA-W008-03 | 저장된 검색 실행 시 실행자의 접근 범위가 적용된다(저장자 권한 미승계) | FR-SRCH-010 AC-3 |
| QA-W008-04 | 100건 초과 저장 시 오류와 사유 코드가 반환된다 | FR-SRCH-010 AC-4 |
| QA-W008-05 | 파싱 실패 질의는 실행 버튼이 비활성이고 오류 구간이 표시된다 | FR-SRCH-010 |
| QA-W008-06 | 공유받은 항목에 편집·삭제 액션이 없고, 직접 호출해도 서버가 거절한다 | FR-SRCH-010 AC-2 (CR-049) |
| QA-W008-07 | 저장 상한이 동시 요청에서도 지켜진다 — 99건에서 동시 저장 둘이면 하나만 성공한다 | FR-SRCH-010 AC-4 (CR-049) |
| QA-W008-08 | 목록이 커서로 이어지고, 팀 구성이 바뀐 뒤의 이전 커서는 사유와 함께 거절된다 | FR-SRCH-010 AC-5 (CR-049) |
| QA-W008-09 | 문법 오류가 있는 질의는 저장·수정 단계에서 거절되고 오류 위치가 반환된다 | FR-SRCH-010 AC-6 (CR-049) |
| QA-W008-10 | 저장자가 대상 팀에서 이탈해도 항목이 남고, 삭제와 `private` 전환은 되며 그 팀을 대상으로 남기는 수정은 거절된다 | FR-SRCH-010 AC-7 (CR-049) |
| QA-W008-11 | 같은 slug을 가진 다른 조직의 팀에 공유가 새지 않는다 — 대상은 `team_id`로 식별된다 | FR-SRCH-010 AC-1 (CR-049) |
| QA-W008-12 | 공유 대상 선택기가 현재 사용자가 구성원인 팀만 제시하고, 임의의 `team_id`를 보내도 저장되지 않는다 | FR-SRCH-010 AC-1 (CR-049) |
| QA-W008-13 | **`seq:` 검색이 에폭과 함께 저장된다.** 에폭 3에서 저장한 항목의 정본에 3이 남아 있고, `seq:`가 없는 검색에는 에폭이 없다. `seq:` 없는 질의에 에폭을 실어 보내면 거절된다 | FR-SRCH-010 AC-8 (CR-051) |
| QA-W008-14 | **에폭이 오르면 목록이 낡음을 표시한다.** 저장된 3과 현재 4를 함께 보이며, 실행하면 **저장된 3을 실은 주소**로 W-001에 가서 그 화면이 무효를 알린다 — 목록이 4로 바꿔 보내지 않는다 | FR-SRCH-010 AC-8, FR-SEQ-005 AC-4 (CR-051) |
| QA-W008-15 | **에폭 없이 저장된 옛 항목은 실행되지 않는다.** 질의에 `seq:`가 있고 에폭이 없는 행은 `unbound`로 표시되고 실행이 막힌다. **목록을 열어도, 실행을 눌러도, 이름만 고쳐도 정본의 에폭은 계속 비어 있다** — 현재 값으로 채워지지 않는다 | FR-SRCH-010 AC-8 (CR-051) |
| QA-W008-16 | **이름이나 공개 범위만 고치면 저장된 에폭이 그대로다.** 낡은 에폭 3을 가진 항목의 이름을 바꿔도 3이 4로 옮겨지지 않는다. 저장자가 「현재 에폭으로 다시 연결」을 눌렀을 때에만 4가 된다 | FR-SRCH-010 AC-8 (CR-051) |
| QA-W008-17 | **질의의 `seq:`를 바꾸면 새 에폭을 확정해야 한다.** `seq:`를 지우는 수정은 에폭을 함께 비우고, `seq:`를 새로 넣거나 `repo:`·`base:`를 바꾸는 수정은 현재 에폭과 대조된 값으로만 저장된다 | FR-SRCH-010 AC-8 (CR-051) |
| QA-W008-18 | **공유받은 사람에게 볼 수 없는 저장소의 상태가 새지 않는다.** 같은 팀 공유 항목을 두 사람이 볼 때, 그 저장소에 접근할 수 있는 사람에게는 낡음·현재 에폭이 보이고 접근할 수 없는 사람에게는 `unavailable` 하나만 보인다. **`current_seq_epoch`·`stored_seq_epoch`·`sequence_state` 어느 것도 응답에 없다** — 저장된 에폭도 뺀다 | FR-SRCH-010 AC-8, THR-043 (CR-051) |
| QA-W008-19 | **한 페이지의 인용 상태를 판정하는 데 행마다 왕복하지 않는다.** 50건 목록을 열 때 저장소·시퀀스 공간 조회가 항목 수에 비례해 늘지 않는다 | FR-SRCH-010 AC-5 (CR-051) |

### W-009 저장소 개요

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-W009-01 | 저장소별 등록 상태, 마지막 수집 시각, 문서 수, 백필 상태가 표시된다 | FR-ING-006, FR-ING-009 |
| QA-W009-02 | 시퀀스 공간별 마지막 시퀀스, 에폭, 상태(`ok`/`stale`/`reassigning`/`unknown`)가 표시된다 | FR-SEQ-001, FR-SEQ-005 |
| QA-W009-03 | **최근 완료된** 조정 스캔의 누락 건수와 완료 시각이 표시된다. 미룬 회차의 부분 집계는 그 값을 덮지 않는다 | FR-ING-011 AC-6 |
| QA-W009-04 | 접근 범위 내 저장소만 표시된다. 범위 밖 저장소는 목록·집계·건수 어디에도 나타나지 않는다 | FR-AUTH-002, FR-ING-009 AC-6 |
| QA-W009-05 | 등록 실행은 `operator`만 가능하고 일반 사용자는 요청만 남긴다. 이 화면에 등록·해제·백필·재채번 실행 액션이 없다 | FR-ING-009 AC-8 |
| QA-W009-06 | 해제된(`archived`) 저장소도 접근 범위 안이면 등록 상태와 함께 표시된다 | FR-ING-009 AC-7 |
| QA-W009-07 | 진단 항목 하나가 조회에 실패해도 같은 카드의 나머지 항목이 유지되고, 실패한 항목만 미확인으로 표시되며 그 카드만 재시도할 수 있다 | FR-ING-009 예외 처리 |
| QA-W009-08 | `null`(기록 없음) · `0`(확인된 영) · `unavailable`(조회 실패)이 서로 다른 문구로 구분된다 | QA-COMMON-03 |
| QA-W009-09 | 커서로 다음 페이지를 이어 보며 중복·누락이 없고, 순회 중 접근 범위가 바뀌면 처음부터 다시 읽도록 안내한다 (자동 재조회 없음) | FR-ING-009 AC-6, FR-AUTH-002 |
| QA-W009-10 | `empty_no_repository` 문구가 "PR Search에서 표시할 수 있는 등록 저장소가 없다"는 뜻으로 읽히고, "GitHub에 접근 가능한 저장소가 없다"로 읽히지 않는다 | FR-ING-009 AC-10 |
| QA-W009-11 | 등록 검토 요청은 `owner/name`만 받고, 같은 사용자의 반복 요청이 실패가 아니며, 성공 문구가 "확인했다"·"등록된다"를 말하지 않는다 | FR-ING-009 AC-8, AC-9 |
| QA-W009-12 | 미등록 저장소와 접근 범위 밖 저장소의 조회 결과가 서로 구분되지 않는다 | FR-ING-009 AC-10, FR-AUTH-002 AC-4 |
| QA-W009-13 | W-001의 결과 없음 안내, W-002·W-003의 `not_indexed`, W-005의 `release_not_indexed`에서 이 화면으로 이동할 수 있다 | FR-SEQ-004, FR-SRCH-006 |

### A-001 수집 파이프라인 콘솔

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-A001-01 | 수신량, 대기열 길이, 단계별 지연 p50/p95, 실패 대기열 수, 보강 대기 수, 시퀀스 공간 요약이 표시된다 | FR-ADMIN-001 AC-1 |
| QA-A001-02 | 지표 신선도가 30초 이내이고 마지막 갱신 시각이 표시된다 | FR-ADMIN-001 AC-2 |
| QA-A001-03 | 저장소별 지연 상위 10개가 표시된다 | FR-ADMIN-001 AC-3 |
| QA-A001-04 | 실패 이벤트에 실패 사유, 마지막 오류 메시지, 재시도 횟수가 표시된다 | FR-ING-007 AC-2 |
| QA-A001-05 | 개별·일괄 재처리가 동작하고 100건 초과 시 재확인을 요구한다 | FR-ING-007 AC-3 |
| QA-A001-06 | 실패 대기열 100건 초과 시 경보 상태가 표시된다 | FR-ING-007 AC-5 |
| QA-A001-07 | 조정 스캔 결과와 발견 누락 건수가 표시되고 즉시 실행할 수 있다 | FR-ING-011 |
| QA-A001-08 | 원본 아카이브 조회가 `operator`/`security_officer`로 제한된다 | FR-ING-010 AC-5 |
| QA-A001-11 | 아카이브 조회 결과가 요청자의 접근 범위로 걸러지고, 범위 밖·미등록 저장소의 원본이 나오지 않는다 | FR-ING-010 AC-6 |
| QA-A001-12 | `payload`가 기본으로 접혀 있고, 펼침이 명시적 열람으로 감사 기록에 남는다 | THR-044 |
| QA-A001-13 | 아카이브 인덱스가 없어도 나머지 섹션이 정상 동작한다 | FR-ING-010 AC-3 |
| QA-A001-14 | `security_officer`가 A-001에 진입하면 `A-001-ARCHIVE`만 보인다 | FR-ING-010 AC-5 |
| QA-A001-09 | 사용자가 표를 조작 중이면 자동 갱신이 보류된다 | 와이어프레임 A-001 구현 메모 |
| QA-A001-10 | 운영 내비게이션은 **항목마다** 허용 역할을 본다 (CR-054, DEV-408). `operator`에게 파이프라인·저장소 등록이 보이고 **감사 기록은 보이지 않는다.** `security_officer`에게 감사 기록과 파이프라인이 보이고(후자는 `A-001-ARCHIVE`만, CR-052 DEV-375) **저장소 등록은 보이지 않는다.** 두 역할 어느 쪽도 아니면 운영 항목이 하나도 렌더링되지 않는다 | 컴포넌트 C-002 |

### A-002 저장소 등록 관리

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-A002-01 | 등록 시 저장소, 대상 브랜치, 미러 사용, 백필 여부를 지정할 수 있다 | FR-ING-009 AC-1 |
| QA-A002-02 | 대상 브랜치 10개 초과 시 오류가 표시된다 | FR-ING-009 AC-2 |
| QA-A002-03 | 해제 시 "문서는 유지됩니다"가 확인 다이얼로그에 명시된다 | FR-ING-009 AC-3 |
| QA-A002-04 | 접근 권한 없는 저장소 등록이 거부되고 필요 권한이 표시된다 | FR-ING-009 |
| QA-A002-05 | 등록·해제가 감사 기록에 남는다 | FR-ING-009 AC-5 |

### A-003 인덱스·잡 운영

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-A003-01 | 잡 상태 6종(`queued`/`running`/`paused`/`completed`/`failed`/`cancelled`)이 표시된다 | FR-ADMIN-002 AC-2 |
| QA-A003-02 | 진행률이 30초 이내 간격으로 갱신된다 | FR-ADMIN-002 AC-3 |
| QA-A003-03 | 동일 대상 동일 유형 잡 중복 요청 시 409와 실행 중 잡 ID가 반환된다 | FR-ADMIN-002 AC-4 |
| QA-A003-04 | 중단 요청 후 30초 안에 멈추지 않으면 강제 종료되고 `cancelled`로 기록된다 | FR-ADMIN-002 |
| QA-A003-05 | 백필이 실시간 수집보다 낮은 우선순위로 실행된다(실시간 지연 미영향) | FR-ING-006 AC-3 |
| QA-A003-06 | 백필 중단 후 재개 시 마지막 커서부터 이어진다 | FR-ING-006 AC-4 |
| QA-A003-07 | 재색인 중 이중 쓰기 상태가 인덱스 패널에 상시 표시된다 | FR-ING-008 AC-2 |
| QA-A003-08 | 재색인 실패 시 별칭이 전환되지 않고 기존 인덱스가 서비스를 유지한다 | FR-ING-008 AC-5 |
| QA-A003-09 | 정합성 점검이 최초 불일치 시퀀스, 저장 SHA, 실제 SHA를 보고한다 | FR-ADMIN-003 AC-3 |
| QA-A003-10 | 재채번 확인 다이얼로그가 영향 범위를 표시하고 저장소 이름 직접 입력을 요구한다 | 플로우 FLOW-008 |
| QA-A003-11 | 재채번 후 에폭이 1 증가하고 알림·감사 기록이 남는다 | FR-SEQ-005 AC-3, AC-5 |

### A-004 감사 로그

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-A004-01 | 사용자, 행위 유형, 대상, 기간, 결과 코드로 필터된다 | FR-AUTH-004 AC-2 |
| QA-A004-02 | 각 기록에 사용자 ID, 행위, 대상, 질의, 시각, 결과 코드, 상관 ID가 있다 | FR-AUTH-004 AC-2 |
| QA-A004-03 | 감사 기록 갱신·삭제 UI가 없다 | FR-AUTH-004 AC-3 |
| QA-A004-04 | `security_officer`가 아닌 역할이 접근 시 403이 반환된다 | FR-AUTH-004 AC-5 |
| QA-A004-05 | 감사 화면 진입 자체가 감사 기록으로 남는다. **기록은 응답을 확정한 뒤에 남기며 자기 자신이 그 페이지에 나타나지 않는다** | FR-AUTH-004 AC-8 |
| QA-A004-06 | `operator`에게 운영 내비게이션의 **감사 기록 항목이 렌더링되지 않는다** | CR-054, DEV-408 |
| QA-A004-07 | `security_officer`에게 **저장소 등록 항목이 렌더링되지 않는다** | CR-054, DEV-408 |
| QA-A004-08 | 정본 표에 없는 **legacy `action` 값**(`sequence_integrity.reassign`)으로도 과거 기록을 조회할 수 있다 | FR-AUTH-004 AC-7 |
| QA-A004-09 | 커서 순회에서 같은 기록이 두 번 나오거나 빠지지 않는다. 조건을 바꾸고 옛 커서를 쓰면 `CURSOR_QUERY_MISMATCH`다 | API-ADM-005 |
| QA-A004-10 | `target`·`query`가 없는 기록은 그 칸을 **비운 채로** 보인다 — `N/A`나 빈 문자열을 지어내지 않는다 | FR-AUTH-004 AC-2 |

## 4. 권한 매트릭스 검수

역할 6종 × 화면 13종을 모두 검사한다 (NFR-005).

| 역할 | 접근 가능 화면 | 차단 확인 항목 |
| --- | --- | --- |
| `developer` | W-001~W-009 | A-* 진입 시 `no_permission`, 내비게이션 미노출 |
| `release_manager` | W-001~W-009 | A-* 차단. W-004 표식 쓰기 가능 |
| `manager` | W-001~W-009 | A-* 차단 |
| `qa` | W-001~W-009 | A-* 차단. W-004 표식 쓰기 불가 |
| `operator` | W-001~W-009, A-001(전체)·A-002·A-003 | **A-004 진입 시 403.** 내비게이션에도 `A-004` 항목이 렌더링되지 않는다 (CR-054, DEV-408) |
| `security_officer` | W-001~W-009, A-004, **A-001(`A-001-ARCHIVE` 섹션만)** | **A-002·A-003 진입 시 `no_permission`.** 내비게이션에도 그 둘이 렌더링되지 않는다. `A-001`은 `CR-052`가 연 아카이브 진입점이며 나머지 섹션은 보이지 않는다 (CR-054, DEV-408) |

**역할별 운영 내비게이션 (CR-054, DEV-408).** 이전 판은 `operator`와 `security_officer`를 하나의 "운영 그룹"으로 묶어 두 역할이 서로의 화면을 보고 있었다. **항목마다 허용 역할을 명시한다** — 그룹 단위 판정은 항목이 늘 때 기본이 안전하지 않다.

| 내비게이션 항목 | 화면 | 보이는 역할 |
| --- | --- | --- |
| 운영 > 파이프라인 | A-001 | `operator`, `security_officer` (후자는 `A-001-ARCHIVE`만) |
| 운영 > 저장소 등록 | A-002 | `operator` |
| 운영 > 감사 기록 | A-004 | `security_officer` |

**새 역할이 생기면 어느 운영 항목도 보이지 않는 것이 기본이다** — 허용 목록에 명시적으로 넣어야 보인다.

각 역할에 대해 다음을 확인한다.

- [ ] 접근 범위 밖 저장소의 PR·커밋이 검색·집계·상세·건수 어디에도 나타나지 않는다.
- [ ] 접근 범위 밖 개체 직접 접근 시 404가 반환되고 존재 여부가 노출되지 않는다.
- [ ] 권한 제거 후 캐시 TTL(5분) 이내 또는 무효화 이벤트 후 첫 요청부터 차단된다.

## 5. 성능 검수

| ID | 항목 | 기준 |
| --- | --- | --- |
| QA-PERF-01 | 단건 식별자 해석 응답 | p95 200ms (커밋 1000만 건 데이터셋) |
| QA-PERF-02 | 필터 목록 조회 응답 | p95 500ms (대표 질의 20종) |
| QA-PERF-03 | 시퀀스 범위 조회 응답 | p95 400ms (구간 5000건) |
| QA-PERF-04 | 집계 대시보드 응답 | p95 1500ms |
| QA-PERF-05 | 화면 최초 콘텐츠 표시 | p75 1.5초 (사내망) |
| QA-PERF-06 | 동시 검색 세션 500 유지 시 위 지연 목표 유지 | 부하 시험 |
| QA-PERF-07 | 웹훅 수신 → 검색 반영 | p95 10초, p99 60초 |

## 6. 회귀 검수

머지 시퀀스와 관계 파생은 이 제품의 정확성 기반이다. 다음은 모든 릴리스에서 반복 검수한다.

- [ ] 알려진 저장소의 시퀀스 값이 이전 릴리스와 동일하다(에폭 변경이 없었다면).
- [ ] 표본 100건의 커밋 SHA → PR 매핑이 GHE 실제 값과 일치한다.
- [ ] 표본 50건의 PR → 머지 커밋 매핑이 GHE 실제 값과 일치한다.
- [ ] 표본 20건의 되돌림·체리픽 관계가 실제 관계와 일치하고 신뢰도 표기가 정확하다.
- [ ] 두 릴리스 구간 조회 결과가 `git log --first-parent <tagA>..<tagB>` 결과와 건수·구성이 일치한다.
- [ ] 재색인 전후 검색 결과가 동일하다.
- [ ] 원본 이벤트만으로 인덱스를 전량 재구성했을 때 결과가 동일하다.

## 7. 릴리스 게이트

- [ ] 모든 P0 화면의 화면별 체크리스트가 통과했다.
- [ ] 공통 검수 항목 18종이 전 화면에서 통과했다.
- [ ] 권한 매트릭스 검수(역할 6종 × 화면 13종)가 통과했다.
- [ ] 성능 검수 7종이 목표 수치를 만족했다.
- [ ] 회귀 검수 7종이 통과했다.
- [ ] axe 자동 검사 위반 0건, `checkContrast` 위반 0건(라이트·다크).
- [ ] SRS 요구사항과 추적 가능하다(모든 체크 항목이 FR 또는 상위 문서를 인용한다).
- [ ] 상태 매트릭스와 불일치가 없다.
- [ ] 알려진 제한이 `../40_delivery/pr_search_implementation_traceability.md`에 문서화되어 있다.

## 8. QA 기록 템플릿

```text
QA 실행 기록
- 대상 릴리스: REL-###
- 실행일:
- 실행자:
- 대상 화면:
- 통과 항목 수 / 전체:
- 실패 항목: [항목 ID] — [증상] — [연결 FR/WP] — [DEV-### 등록 여부]
- 성능 측정값: QA-PERF-01 ~ 07
- 권한 매트릭스 결과: 역할 6종 × 화면 13종 = 78셀 중 통과 수
- 결론: 게이트 통과 / 조건부 통과(조건 명시) / 미통과
```

### W-010~W-023, A-005~A-007 GitHub Operations (CR-005 신규)

| ID | 항목 | 관련 FR |
| --- | --- | --- |
| QA-GH-01 | capability 목록에 미지원·차단 항목이 사유와 함께 표시되고 숨겨지지 않는다 | FR-GH-001 AC-6, FR-GH-013 AC-5 |
| QA-GH-02 | 제약을 위반하는 flag 조합에서 실행 버튼이 활성화되지 않는다 | FR-GH-003 AC-1 |
| QA-GH-03 | 클라이언트를 우회한 요청도 서버가 같은 제약으로 거부한다 | FR-GH-003 AC-5 |
| QA-GH-04 | argv 미리보기에 토큰·비밀 값이 나타나지 않는다 | FR-GH-002 AC-3, FR-GH-008 AC-7 |
| QA-GH-05 | 미리보기 argv와 실제 실행 argv가 일치한다 | FR-GH-002 AC-4 |
| QA-GH-06 | R2 capability가 확인 없이 실행되지 않는다 | FR-GH-009 AC-4 |
| QA-GH-07 | R3 capability가 강한 확인을 요구한다 | FR-GH-009 AC-5 |
| QA-GH-08 | 실행 직전 대상 상태가 바뀌면 R2 이상 실행이 중단된다 | FR-GH-009 AC-6 |
| QA-GH-09 | 필요 권한과 보유 여부가 실행 전에 표시된다 | FR-GH-009 AC-7 |
| QA-GH-10 | 실행 중 출력이 스트리밍되고 취소 버튼이 동작한다 | FR-GH-006 AC-2, AC-3 |
| QA-GH-11 | 출력 상한 초과 시 절삭 사실이 표시된다 | FR-GH-006 AC-5 |
| QA-GH-12 | Operations App 미연결 시 연결 안내가 나오고 실행되지 않는다 | FR-GH-008 |
| QA-GH-13 | 실행 이력에서 동일 구성 재실행이 가능하고 비밀은 재입력을 요구한다 | FR-GH-012 AC-4 |
| QA-GH-14 | 같은 버튼을 두 번 눌러도 실행이 하나만 생성된다 | FR-GH-012 AC-5 |
| QA-GH-15 | 비밀 입력값이 화면에서 다시 표시되지 않는다 | NFR-010 |
| QA-GH-16 | 레지스트리 불일치 상태에서 신규 command 실행이 차단되고 사유가 표시된다 | FR-GH-011 AC-3 |
| QA-GH-17 | 모든 생성된 컨트롤이 키보드로 조작 가능하고 레이블이 연결되어 있다 | NFR-007 |
| QA-GH-18 | 실행 상태 변화가 스크린 리더에 전달된다 (live region) | NFR-007 |
| QA-GH-19 | `gh api` 화면에서 인증·라우팅 헤더를 입력할 수 없다 | FR-GH-010 AC-2 |
| QA-GH-20 | Recipe 단계에 등록되지 않은 capability나 임의 명령 문자열을 넣을 수 없다 | FR-GH-005 AC-2, AC-3 |
| QA-GH-21 | 실행 직전 유효 컨텍스트 패널이 호스트·신원·저장소·브랜치·gh 버전·manifest 버전·권한 판정·정책 판정을 보여주고, 비밀 값은 표시하지 않는다 | CR-008 / FR-GH-002 AC-7, C-062 |
| QA-GH-22 | ANSI CSI·OSC 시퀀스가 섞인 실행 출력이 화면을 조작하지 못한다 | CR-008 / NFR-010, THR-021·022 |
| QA-GH-23 | 스트리밍 청크 경계에서 잘린 escape 시퀀스도 무해화된다 | CR-008 / THR-024 |
| QA-GH-24 | GitHub 필드(PR 제목·브랜치명)에 심은 마크업이 실행 결과 화면에서 렌더링되지 않는다 | CR-008 / THR-023 |
| QA-GH-25 | 바이너리 출력이 텍스트로 렌더링되지 않고 바이너리로 표시된다 | CR-008 / NFR-010 |
| QA-GH-26 | 파일 입력 경로에 `..`·절대 경로·symlink를 넣어도 workspace 밖에 닿지 않는다 | CR-008 / FR-GH-007 AC-6, THR-025 |
| QA-GH-27 | 아티팩트가 실행기 파일시스템 경로 없이 ID로만 제공된다 | CR-008 / FR-GH-007 AC-7 |
| QA-GH-28 | `--web`·`gh browse` 실행이 서버에서 브라우저를 띄우지 않고 URL 링크로 제공된다 | CR-008 / FR-GH-013 AC-7, ADR-019 |
| QA-GH-29 | 승인되지 않은 extension이 실행되지 않으면서도 존재와 차단 사유는 확인된다 | CR-008 / FR-GH-013 AC-8, AC-9 |
| QA-GH-30 | 버전 pin이 없는 extension 실행이 거부된다 | CR-008 / FR-GH-013 AC-8 |
| QA-GH-31 | 재실행이 과거 승인을 승계하지 않고 현재 권한·정책으로 다시 검증되며 새 확인을 요구한다 | CR-008 / FR-GH-012 AC-4, THR-028 |
| QA-GH-32 | A-006이 차원별 커버리지와 core/extension 분리 수치를 보여주고, 드리프트가 있으면 command·flag diff를 표시한다 | CR-008 / FR-GH-001 AC-10, C-067 |
| QA-GH-33 | `gh api` 타입 폼과 원시 모드가 같은 위험도·확인·감사 경로를 거친다 | CR-008 / FR-GH-010 AC-8 |
| QA-GH-34 | 다음 단계 후보가 현재 출력과 호환되는 capability를 먼저 보여준다 | CR-009 / FR-GH-005 AC-11, C-068 |
| QA-GH-35 | 호환되지 않는 연결이 저장 전에 사유와 함께 거부된다 | CR-009 / FR-GH-005 AC-11 |
| QA-GH-36 | Recipe 그래프에 순환을 만들면 저장이 거부된다 | CR-009 / FR-GH-005 AC-7 |
| QA-GH-37 | 바인딩 편집기에 자유 표현식 입력창이 없다 — 선언된 field 또는 제한된 JSON Pointer만 고를 수 있다 | CR-009 / FR-GH-005 AC-8, C-069 |
| QA-GH-38 | 상한 없는 fan-out은 저장도 실행도 되지 않는다 | CR-009 / FR-GH-005 AC-9 |
| QA-GH-39 | 동적 R2/R3 대상 집합이 preflight로 확정되고 plan 해시 확인을 거친다 | CR-009 / FR-GH-005 AC-10 |
| QA-GH-40 | 확인 이후 plan이 바뀌면 기존 확인이 무효가 되고 다시 확인을 요구한다 | CR-009 / FR-GH-005 AC-10 |
| QA-GH-41 | `secret` 결과 capability가 바인딩 대상으로 제안되지 않고, 값이 화면·이력에 나타나지 않는다 | CR-009 / NFR-010 |
| QA-GH-42 | `opaque_text` capability가 바인딩 source로 선택되지 않으면서도 목록에서 숨겨지지 않는다 | CR-009 / FR-GH-001 AC-11 |
| QA-GH-43 | 앞 단계 파일이 실행기 경로가 아니라 아티팩트 ID로 다음 단계에 전달된다 | CR-009 / FR-GH-007 AC-9 |
