# PR Search 구현 로드맵

> 상태: review | 버전: v0.3 | 갱신일: 2026-08-26

## 1. 목적

승인된 요구사항과 아키텍처를 실제 구현 가능한 vertical slice와 milestone으로 분해한다. 각 REL은 `pr_search_work_packages.md`에서 WP로 다시 분해된다. 이 문서는 범위를 추가하지 않는다.

## 2. 원칙

- 각 슬라이스는 요구사항, 화면, API, 데이터, 테스트, 운영 준비를 함께 가진다.
- UI만 구현하거나 API만 구현한 상태를 완료로 보지 않는다.
- 선행 의존성을 명시하고, 위험한 결정은 ADR로 연결한다.
- **각 릴리스는 그 자체로 사용자에게 가치를 준다.** REL-002가 끝나면 조직은 이미 "SHA로 PR 찾기"를 실제로 쓸 수 있어야 한다. 전부 완성될 때까지 아무것도 못 쓰는 계획은 세우지 않는다.

## 3. 릴리스 순서의 근거

이 제품의 가치는 두 축이다: **양방향 식별자 해석**과 **머지 시퀀스**. 둘 중 어느 것도 관계 그래프나 통계 없이 단독으로 유용하다. 따라서 순서는 다음과 같이 잡는다.

1. 파이프라인이 먼저다. 데이터가 없으면 아무 기능도 검증할 수 없다 (REL-001).
2. 양방향 해석이 그다음이다. 가장 자주 쓰이고, 시퀀스 없이도 동작한다 (REL-002).
3. 시퀀스와 범위 조사가 세 번째다. Perforce 전환 문제의 직접 해답이며, 커밋 그래프 인프라를 요구한다 (REL-003).
4. 관계와 전문 검색은 조사 경로를 넓힌다 (REL-004).
5. 통계와 운영 고도화는 관리자 수요를 채운다 (REL-005).
6. 그래프 시각화와 조사 보조는 마지막이다 (REL-006).

권한 강제(FR-AUTH-002)만은 예외적으로 REL-002에 들어간다. 검색이 처음 열리는 순간부터 접근 통제가 있어야 하며, 나중에 얹는 접근 통제는 반드시 구멍을 남긴다.

## 4. Milestones

| REL ID | 목표 | 포함 요구사항 | 주요 산출물 | Exit Criteria |
| --- | --- | --- | --- | --- |
| REL-001 | 수집 파이프라인과 저장 기반 | FR-ING-001, FR-ING-002, FR-ING-003, FR-ING-004, FR-ING-005, FR-ING-007, FR-ING-009, FR-ADMIN-001, NFR-002 | 워크스페이스, `ingest-gateway`, `pipeline-worker`(enrich/project), PostgreSQL 스키마, ES 매핑, A-001 최소 콘솔 | 등록 저장소의 PR 이벤트가 수신 → 보강 → 색인되어 ES에서 조회된다. 수집 지연 p95 10초를 개발 데이터셋에서 만족한다. 실패 이벤트가 DLQ로 격리된다 |
| REL-002 | 양방향 식별자 해석과 권한 | FR-SRCH-001, FR-SRCH-002, FR-SRCH-003, FR-SRCH-004, FR-SRCH-005, FR-SRCH-006, FR-SRCH-007, FR-AUTH-001, FR-AUTH-002, FR-AUTH-003, FR-ING-006, NFR-001, NFR-005 | `search-api`, `web` 셸(Conductor), W-001, W-002, W-003, 백필 잡 | SHA로 PR을, PR로 커밋을 찾을 수 있다. 권한 매트릭스 테스트가 통과한다. 백필로 과거 데이터를 채울 수 있다. 단건 해석 p95 200ms |
| REL-003 | 머지 시퀀스와 범위 조사 | FR-SEQ-001, FR-SEQ-002, FR-SEQ-003, FR-SEQ-004, FR-SEQ-005, FR-REL-001, FR-REL-002, FR-ADMIN-003, FR-ING-011 | git 미러, `pipeline-worker:sequence`, W-004, W-005, 정합성 점검, 조정 스캔 | 시퀀스 값이 `git log --first-parent`와 일치한다. 두 릴리스 구간 비교가 동작한다. 강제 푸시 시 에폭이 증가하고 알림이 발생한다 |
| REL-004 | 관계 파생과 전문 검색 | FR-REL-003, FR-REL-004, FR-REL-005, FR-REL-006, FR-REL-007, FR-SRCH-008, FR-SRCH-009, FR-SRCH-010, FR-SRCH-011, FR-ING-008, FR-ING-010 | `pipeline-worker:link`, W-008, W-009, 패싯·커서·전문 검색, 재색인, Filebeat 원본 레인 | 되돌림·체리픽·참조 관계가 표시되고 신뢰도가 구분된다. 무중단 재색인이 검증된다. 관계 정확도 표본 200건 검수 결과가 기록된다(ACC-06 — W-007 활성화 판단 근거) |
| REL-005 | 통계와 운영 고도화 | FR-STAT-001, FR-STAT-002, FR-STAT-003, FR-STAT-004, FR-STAT-005, FR-STAT-006, FR-ADMIN-002, FR-AUTH-004 | W-006, A-002, A-003, A-004, 감사 조회 | 그룹·시계열·백분위·분포 집계가 동작하고 근거 목록으로 이동한다. 운영 콘솔에서 백필·재색인·재채번을 제어한다. 감사 조회가 `security_officer`로 제한된다 |
| REL-006 | 조사 보조와 관계 시각화 | FR-SEQ-006, FR-SEQ-007, FR-REL-008, FR-SRCH-012 | W-007(조건부), 이분 탐색, 안전 구간 표식, 내보내기 | 이분 탐색이 후보를 절반씩 줄인다. 안전 구간 표식이 저장·표시된다. W-007은 REL-004의 ACC-06(관계 간선 정확도 표본 200건 검수 95% 이상)을 충족할 때만 포함한다 |
| REL-007 | GitHub CLI 실행 기반 | FR-GH-001, FR-GH-002, FR-GH-003, FR-GH-006, FR-GH-008, FR-GH-009, FR-GH-011, FR-GH-012 | `@prs/gh-cli`, capability manifest와 검증 도구, 의미 제약 엔진(WP-061), 출력·파일 안전 경계(WP-062), 결과 계약·capability 그래프(WP-066), `gh-executor`, Operations App 연동, W-010, A-006, A-007 | 사용자가 웹에서 저장소를 고르고 R0 capability를 골라 옵션을 넣고 실행될 argv를 확인한 뒤 실행하고 결과와 이력을 볼 수 있다. capability 분류 커버리지 100%, 미분류 0. gh 출력이 무해화 경계를 통과한다 (CR-008) |
| REL-008 | PR·Issue·Discussion 작업 | FR-GH-004, FR-GH-009 | W-011, W-012, R1·R2 쓰기 capability 개방 | PR 머지가 전략·자동 머지·관리자 강제·head 일치·커밋 메시지·브랜치 삭제를 선택해 실행된다. R2 확인과 대상 재조회가 동작한다 |
| REL-009 | 저장소·Actions·Release·Project 작업 | FR-GH-004, FR-GH-006, FR-GH-007 | W-013, W-014, W-015, W-016, 임시 workspace, 아티팩트 | 워크플로 수동 실행과 실행 로그 스트리밍이 동작한다. 릴리스 자산 업로드·내려받기가 동작한다. 로컬 git이 필요한 명령이 임시 workspace에서 수행된다 |
| REL-010 | 고급·보안 민감 작업 | FR-GH-004, FR-GH-010, FR-GH-011, FR-GH-013 | W-017, W-018, W-019, W-020, W-022, A-005, 웹 등가·extension 신뢰 어댑터(WP-063), gh api 스키마 브리지(WP-064) | 비밀·키·룰셋 작업이 별도 정책과 마스킹 아래 동작한다. `gh api` 탐색기가 command와 동일한 정책을 거친다. 확장·터미널 전용 기능이 사유와 함께 분류되어 표시된다 |
| REL-011 | Recipe와 완전 parity | FR-GH-005, FR-GH-001 | W-021 확장, W-023 typed DAG 빌더, 조합 parity 검증기(WP-065) | 다단계 Recipe가 등록된 capability만으로 비순환 typed 그래프로 정의·실행된다. 호환되지 않는 연결과 상한 없는 fan-out은 저장되지 않는다 (CR-009). NFR-009의 전 차원 분류 커버리지가 100%이고 미분류가 0이며, core와 extension 수치가 분리 보고된다 (CR-008) |

## 5. Slice Definition

| Slice | FE | BE/API | Data | Infra/Ops | Security | QA |
| --- | --- | --- | --- | --- | --- | --- |
| REL-001 | 없음 (A-001 최소 지표 화면만) | `ingest-gateway`, enrich/project 워커, API-ING-001, API-ADM-006 | `raw_event`, `repository`, `dead_letter`, `job` 테이블 / `prs-pull-requests`, `prs-commits` 매핑 | k8s 매니페스트, PostgreSQL·ES·Redis 프로비저닝, CI 파이프라인 | 웹훅 HMAC 검증, 시크릿 배치 | 수집 통합 테스트, 멱등·버전 역전 테스트 |
| REL-002 | AppShell, W-001, W-002, W-003, Conductor 통합 | `search-api`, 질의 파서, 강제 필터, API-SRCH-001~004, API-AUTH-001 | 없음 (REL-001 매핑 사용) | `web`·`search-api` 배포, OIDC 클라이언트 등록 | OIDC, 접근 범위, 권한 캐시, 권한 매트릭스 테스트 | E2E FLOW-001·FLOW-002, 권한 매트릭스, 성능 스모크 |
| REL-003 | W-004, W-005, 시퀀스 배지, 선행·후행 섹션 | sequence 워커, **mirror 워커**, API-SEQ-001~003, API-REL-001~002, API-ADM-007 | `sequence_space`, `merge_sequence`, `repository.allowed_team_ids` / `prs-releases` 매핑 | 미러 PVC, git 도구 이미지 | ~~미러 접근 정책(OD-001)~~ **해소 (CR-024)** | 시퀀스 회귀 테스트(git 대조), FLOW-003 E2E |
| REL-004 | W-008, W-009, 패싯 레일, 커서 페이저, 관계 섹션 | link 워커, API-SRCH-005, API-REL-003~004, API-ADM-004 | `prs-links` 매핑, `saved_search` 테이블 | Filebeat 배포, 아카이브 ILM | 저장된 검색 권한 미승계 | 관계 정확도 표본 검수, 재색인 무중단 검증, FLOW-006 E2E |
| REL-005 | W-006, A-002, A-003, A-004, 차트 컴포넌트 | analytics 모듈, API-STAT-001~004, API-ADM-001~005 | `audit_record` 파티션 | 감사 보존 잡, 대시보드·알림 구성 | 감사 역할 제한, 집계 권한 반영 | 집계 정확성 테스트, FLOW-005·FLOW-007 E2E |
| REL-006 | W-007(조건부), 이분 탐색 패널, 표식 카드, 내보내기 | API-SEQ-004~005, API-SRCH-006 | `safe_marker`, `bisect_session` 테이블 | 내보내기 잡 | 표식 역할 제한, 내보내기 감사 | FLOW-004 E2E, 그래프 접근성 검증 |

## 6. 의존성 지도

| 영역 | 선행 조건 | 차단 대상 |
| --- | --- | --- |
| **Product** | SRS baseline 승인 | 전체 착수 |
| **Product** | ~~OD-003 (원본 보존 기간)~~ | 해소 (2026-08-19, CR-004 — 3년, `raw_event` 4TB) |
| **Product** | ~~OD-006 (ES 클러스터 형태)~~ | 해소 (2026-08-19, CR-004 — 전용 3노드 / 컨테이너 3개 / 복제본 1) |
| **Product** | ~~OD-007 (PR 규모 기준선)~~ | 해소 (2026-08-19, CR-004 — 1,000 PR/일. ADR-003 샤드 수 불변) |
| **Infra** | ES 컨테이너 3개의 Docker host 배치 확정 (마스터 자격 노드 3개 호스트 분리) | REL-001 프로비저닝 |
| **Product** | REL-001~006 end-to-end 완료 | REL-007 착수 — Operations Plane은 인증 기반(REL-002)과 웹 셸이 선행되어야 중복 구현이 없다 |
| **Security** | GitHub Operations App 발급과 권한 승인 | REL-007 실행 경로 |
| **Security** | 비밀 저장소 연동 (위임 토큰 보관) | REL-007 |
| **Infra** | `gh-executor` 런타임 프로비저닝 (CPU·메모리·임시 디스크, 동시 실행 상한) | REL-007 |
| **Platform** | gh 버전 고정과 이미지 빌드 파이프라인 | REL-007 |
| **Product** | capability 의미 오버라이드 작성 (위험도·권한·비밀 여부) | REL-007 — 자동 파싱만으로는 안전 정보가 나오지 않는다 (ADR-015) |
| **Security** | GitHub App 발급 (읽기 전용) | REL-001 보강 |
| **Security** | 웹훅 엔드포인트 등록 | REL-001 수집 |
| ~~**Security** | OD-002 (권한 판정 소스)~~ | **해소 (2026-08-22, CR-024)** — GHE 협업자/팀 API. 등록 저장소 1,000개 초과 시 조직 단위 조회로 바꾸는 CR을 연다 |
| **Security** | OIDC 클라이언트 등록 | REL-002 인증 |
| ~~**Security** | OD-001 (미러 클론 허용)~~ | **해소 (2026-08-22, CR-024)** — 허용. 단 blob 지연 인출은 기본 차단이며 켜는 것은 저장소 단위 예외다. 두 경로는 `mirror_enabled` 때문에 계속 유지한다 |
| **Design** | Conductor 패키지 접근 | REL-002 프런트엔드 |
| ~~**Design** | OD-005 (nori 플러그인)~~ | **해소 (2026-08-26, CR-040)** — 초기 REL-004 의존성으로 채택하지 않는다. WP-032는 `standard` 분석기 + `edge_ngram` 부분 일치 필드로 구현한다. 재검토 조건 셋(운영 승인 · relevance 실측 개선 · 재색인/롤백 검증)이 모두 충족되면 별도 CR |
| ~~**Release Eng** | OD-004 (릴리스 앵커 소스)~~ | **해소 (2026-08-22, CR-024)** — Git 태그와 GitHub Release만. CI 배포 이벤트는 그래프 밖이라 `git log --first-parent`로 검증할 수 없다 |
| **Infrastructure** | k8s 네임스페이스·PVC | REL-001 |
| **Infrastructure** | PostgreSQL·ES·Redis 인스턴스 | REL-001 |
| **Infrastructure** | 미러용 PVC 용량 | REL-003 |
| **Frontend** | REL-002의 AppShell | REL-003 이후 전 화면 |
| **Backend** | REL-001의 문서 색인 | REL-002 검색 |
| **Backend** | REL-003의 시퀀스 | REL-004의 관계 그래프 노드 시퀀스 표시 |
| **QA** | 합성 데이터셋 (1000만 문서) | REL-002 성능 검증 |
| **QA** | 시퀀스 회귀 픽스처 (강제 푸시·리베이스 포함 히스토리) | REL-003 검증 |

**차단 없는 오픈 결정은 모두 닫혔다.** OD-001·OD-004는 2026-08-22 CR-024로, OD-005는 2026-08-26 CR-040으로 결정됐다. 셋 다 "두 경로 모두 구현하거나 대체 경로가 있어" 착수를 막지 않았지만, **두 경로를 유지하는 비용은 결정이 날 때까지 계속 들었다** — 그것이 기한을 정해 두는 이유이고, 기한이 도래하면 닫는 이유다. 남은 오픈 결정은 OD-008(REL-006 착수 전) 하나다.

## 7. 위험과 완화

| 위험 | 관련 REL | 영향 | 완화 |
| --- | --- | --- | --- |
| 시퀀스 정확성이 틀리면 제품 신뢰가 무너진다 | REL-003 | 치명적 | git 대조 회귀 테스트를 REL-003 exit criteria로 둔다. 강제 푸시·리베이스·직접 푸시가 섞인 픽스처로 검증 |
| GHE API 한도로 백필이 예상보다 오래 걸린다 | REL-002 | 일정 지연 | 미러 우선 조회, 백필 우선순위 분리. 초기에는 핵심 저장소 10개만 백필하고 점진 확대 |
| 성능 목표를 1000만 문서에서 만족하지 못한다 | REL-002 | NFR 위반 | REL-002 착수 시점에 합성 데이터셋으로 조기 부하 시험. ADR-012의 follow-up 경로 준비 |
| 관계 파생 정확도가 낮아 오도한다 | REL-004 | 조사 오류 | 신뢰도 3단계 표기와 근거 필수 표시. 표본 200건 검수를 REL-004 exit criteria로 |
| Conductor에 없는 컴포넌트(차트·그래프)가 일정을 늘린다 | REL-005, REL-006 | 일정 지연 | REL-005 착수 전 차트 컴포넌트 스파이크. 그래프는 조건부(REL-004 ACC-06) |
| 권한 모델을 나중에 얹으면 구멍이 생긴다 | REL-002 | 보안 사고 | 권한 강제를 REL-002에 포함하고 타입 브랜딩으로 구조적 강제 (ADR-008) |
| gh 버전이 올라가며 신규 command가 조용히 누락된다 | REL-007~011 | parity 요구 위반 | 드리프트 검출을 CI 게이트로 둔다. 미분류 0을 릴리스 게이트로 (NFR-009) |
| 위임 토큰 설계를 나중에 얹으면 설치 토큰으로 먼저 구현된다 | REL-007 | 권한 승격 | 위임 신원을 REL-007의 첫 WP(WP-046)에 두고, 그전에는 실행 경로를 만들지 않는다 |
| 파괴적 작업의 오클릭 | REL-008~010 | 되돌리기 어려운 GitHub 변경 | 위험도 게이트를 실행 경로에 구조적으로 배치. R2+ 대상 재조회 |
| 실행기 부하가 수집·검색을 막는다 | REL-007 | 제품 전체 저하 | 별도 배포 단위로 분리 (ADR-013). 동시 실행 상한 |
| capability 의미 오버라이드 유지 부담 | REL-011 이후 | parity 저하 | 신규 flag는 일반 컨트롤로 자동 분류하되 위험도가 정해지지 않은 command는 실행 차단 |
| 재채번이 운영 중 실수로 실행된다 | REL-005 | 조사 이력 무효화 | 2단계 확인 + 영향 범위 산출 + 감사 + 알림 (FLOW-008) |

## 8. 마이그레이션 계획

이 제품은 기존 시스템을 대체하지 않으므로 데이터 마이그레이션이 아니라 **초기 적재** 문제다.

| 단계 | 시점 | 내용 |
| --- | --- | --- |
| 1 | REL-001 완료 후 | 파일럿 저장소 3개 등록, 실시간 수집만 (백필 없음) |
| 2 | REL-002 완료 후 | 파일럿 저장소 백필 (과거 전체). 소요 시간과 API 소모 실측 |
| 3 | REL-003 완료 후 | 파일럿 저장소 시퀀스 전체 채번. git 대조 검증 |
| 4 | REL-003 검증 후 | 핵심 저장소 30개로 확대. 사용자 베타 시작 |
| 5 | REL-005 완료 후 | 전체 저장소 등록. 백필은 우선순위 순으로 점진 진행 |

**백필은 한 번에 하지 않는다.** 3000개 저장소를 동시에 백필하면 GHE API 한도를 소진하고 실시간 수집까지 밀린다. 저장소별로 큐에 넣고 동시 3개씩 진행한다 (FR-ING-006 AC-6).

## 9. Known Limitations

| 제한 | 영향 | 후속 slice | 승인 여부 |
| --- | --- | --- | --- |
| REL-001~002 동안 시퀀스가 없어 범위 조사가 불가하다 | 초기 사용자는 SHA↔PR 해석만 사용 | REL-003 | 승인 (릴리스 순서의 의도된 결과) |
| REL-003까지 관계 정보가 없다 | 되돌림·체리픽 추적 불가 | REL-004 | 승인 |
| 원본 커밋 250건 초과 PR은 절삭 저장 | 초대형 PR의 일부 커밋 미검색 | 후속 없음 (GHE 링크로 대체) | 승인 (FR-SRCH-003 AC-4) |
| 변경 파일 3000개 초과 PR은 절삭 저장 | 경로 필터 누락 가능 | 후속 없음 | 승인 (FR-ING-004 AC-4) |
| 저장소 간 patch-id 비교를 하지 않아 포크 간 체리픽 미탐지 | 포크 워크플로 사용 팀에 영향 | CR 필요 | 승인 (FR-REL-005 AC-3) |
| W-007 관계 그래프는 조건부다 | 관계를 목록으로만 조회 | REL-006 (REL-004 ACC-06 충족 시) | 조건부 승인 |
| 6자 이하 축약 SHA를 검색하지 않는다 | 매우 짧은 SHA 인용 시 사용 불가 | 후속 없음 | 승인 (ADR-012) |
| 실시간 알림·구독이 없다 | 저장된 검색을 수동 실행해야 한다 | 범위 밖 | 승인 (SRS 4.3) |
| 단일 리전 배포 | 리전 장애 시 전체 중단 | 범위 밖 | 승인 (인프라 11장) |
